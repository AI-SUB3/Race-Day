# 版本紀錄

版本號顯示在網站右下角。這份紀錄從 v1.0.0（第一個完整可用版本）開始；之後每次功能異動都會在這裡加一筆，並同步更新 [USAGE.md](./USAGE.md) 與 [README.md](./README.md) 對應段落。

## v2.98.0

- **再把三個還攤在頁面上的區塊收進抽屜**：氣象資料（9 個欄位）、CP／補給站明細、多媒體與連結。這三個是前幾輪轉換時沒有一起處理的——氣象當時判斷「本來就短」所以跳過，CP 明細跟多媒體連結則是分別附屬在路線、賽後兩個區塊底下的清單，前幾輪只搬了那兩個區塊自己的欄位。實際用起來才發現：CP 站一多、或多媒體連結填了幾筆，展開的長度其實不輸給已經搬走的那些。
  - **動手前確認過三個的相依關係，都是乾淨的**：氣象的自動查詢卡片（賽事當天實際天氣、海象紀錄）是唯讀顯示、留在頁面上，被搬走的只有 9 個手動欄位；CP 明細跟多媒體連結雖然分別被海拔剖面圖（圖上的虛線 CP 標記）跟相片牆用到，但那兩個功能讀的是資料陣列本身、不是畫面上的表單 DOM，所以把編輯表單搬進抽屜不影響它們。
  - **切法跟前幾輪一致**：看的東西留在頁面（歷史天氣卡片、海象紀錄、海拔剖面圖含 CP 虛線標記、相片牆），填的東西進抽屜。摘要卡片各自顯示重點：氣象顯示體感溫度／天氣／濕度，CP 顯示「N 個・M 個有關門時間」，多媒體顯示連結筆數。
  - **測試方式**：三個抽屜的欄位編輯都確認會自動儲存（含 CP、多媒體這種清單內層的欄位）、清單的新增動作正常、關閉後摘要卡片數字跟著更新。特別確認海拔剖面圖上的 CP 虛線標記在 CP 明細搬進抽屜後依然正確畫出來——這是這次唯一有風險的地方，因為那些標記畫的正是 CP 資料。三維度游標聯動、前幾輪的六張抽屜卡片、獎牌牆 3D 傾斜、手風琴收合都重新跑過回歸測試。回歸測試中有一項斷言失敗，追下去確認是測試腳本自己過時了（它斷言 CP 明細要出現在頁面上，而那正是這次刻意改掉的），已更新斷言。

## v2.97.0

- **獎牌牆的 3D 傾斜與反光特效**：滑鼠滑過獎牌卡片時，卡片會朝游標方向傾斜，表面有一道跟著游標移動的反光，卡片上的文字與 PB 標章則浮在照片前方一點點，產生視差景深。
  - **只在有滑鼠的裝置上啟用**（`@media (hover:hover) and (pointer:fine)`）：觸控裝置沒有「懸停」這個狀態，手指離開畫面後卡片會卡在某個傾斜角度轉不回來，做了反而是扣分。JS 端也用同一個條件判斷，不符合就完全不寫入任何樣式，不是先算好再讓 CSS 擋掉。
  - **尊重系統的「減少動態效果」設定**：開啟時傾斜與反光整組關掉、卡片維持平面，但邊框變色、陰影這些單純的懸停回饋不受影響——這個設定是要減少動態，不是要把所有互動回饋都拿掉。
  - **效能**：滑鼠移動事件的觸發頻率可能高於螢幕更新率，實際寫入樣式的動作包在 `requestAnimationFrame` 裡，同一個畫面更新週期內只會寫一次。旋轉角度與反光位置都透過 CSS 變數傳遞，JS 不自己組 transform 字串。
  - **兩個實作上的調整，理由都是實際套進這個畫面之後才看得出來的**：其一，原始構想建議文字圖層用 `translateZ(40px)`，但這個獎牌牆的卡片尺寸下，40px 會讓文字明顯飄出卡片邊界、跟相鄰卡片重疊，改成 18px，有立體感但不會散掉。其二，卡片原本的 `:hover` 狀態帶有 `translateY(-2px)` 的上浮效果，那個 transform 會跟 3D 傾斜互相覆蓋（同一個屬性只能有一個值），上浮已經拿掉，改由傾斜本身提供互動回饋。
  - **測試方式**：驗證傾斜角度的數學正確——游標在左上象限跟右下象限時 rotateX／rotateY 的正負號會對調、絕對值不超過設定的上限 9 度、游標移到卡片正中心時角度回到 0。游標離開卡片會歸零並套上緩慢轉回的過渡；但在卡片「內部元素之間」移動（例如從照片滑到底下的賽事名稱）不會誤判成離開而閃動一下——這是 `mouseout` 事件的常見陷阱，特別寫了測試確認。最重要的是確認卡片仍然可以點擊開啟賽事詳情頁（反光層有設 `pointer-events:none`，不會擋住點擊），以及「減少動態效果」與觸控裝置兩種情況下都完全不套用、也不會出錯。前幾輪的六張抽屜卡片、三維度游標聯動、手風琴收合都重新跑過回歸測試。

## v2.96.0

- **「地理與路線」改用抽屜面板，六個區塊的轉換到此全部完成**。這個區塊是六個裡最複雜的，所以刻意用跟「賽後紀錄」同一套切法：圖表留在頁面上、只有要填的欄位進抽屜。
  - **動手前先把這個區塊的相依關係整個盤過一次**，確認有三個東西是「畫面畫完之後還要再跑一段初始化、去 DOM 裡找元素」的：路線地圖（找 `#route-map` 建立 Leaflet 實體）、海拔剖面圖的游標聯動（找四個海拔相關元素，而且還會把分段配速表的每一列快取起來）、照片時空對位（檔案輸入框跟時差校正滑桿）。這三個只要被搬進抽屜，抽屜沒打開時初始化就會抓到空的，功能會安靜地失效。
  - **最後的切法**：海拔剖面圖、路線軌跡地圖、照片時空對位、GPS 降噪、CP／補給站明細全部留在頁面上；只有 13 個純欄位（總距離、總爬升、總下降、起點／最低／最高海拔、柏油路／土路／階梯比例、CP 數量、關門時間、ITRA 點數、賽道陡升段說明）搬進「路線數據」這張摘要卡片，卡片上顯示距離、爬升、關門時間。這 13 個欄位沒有任何初始化邏輯掛在上面，是單純的欄位輸出，跟前面幾輪搬過的那些性質一樣。
  - **這樣切還有一個額外的好處**：海拔剖面圖跟 CP 明細本來就是互相對照著看的（剖面圖上的虛線就是 CP 站位置），兩個留在同一個畫面上，比把其中一個藏進抽屜更合理。
  - **測試方式**：最重要的是確認三維度游標聯動完全沒受影響——不只測了改動後的當下，還特別測了「打開抽屜、改一個欄位、關掉抽屜」這一整圈之後（這會觸發整個詳情頁重畫）聯動是否依然正常，兩次都正確高亮對應的分段配速列。另外確認地圖容器、照片時空對位的拖放區與檔案輸入框在這一圈之後都還在。13 個欄位確認全部都出現在抽屜裡、沒有遺漏，欄位編輯正確自動儲存、關閉後摘要卡片數字跟著更新。前五輪的裝備、賽後成績、賽後心得、基本資料、時間與報名、預算與行程六張抽屜卡片，以及生命週期軌道、手風琴收合、環境光渲染、指令控制台全部重新跑過回歸測試。

## v2.95.0

- **「預算與行程規劃」改用抽屜面板**：報名費用、幣別、繳費狀態、晶片押金四個欄位，加上住宿、交通、隨行人員三份清單，全部收進一張「預算與行程」摘要卡片。卡片上顯示報名費用跟各份清單的筆數（例如「TWD 1800・住宿 1・同行者 1」），沒填的清單不會佔位。
  - **這一輪是前面四個區塊裡最單純的**：查證後確認這個區塊沒有跨區塊相依——沒有像賽後成績那樣被游標聯動牽著，也沒有像基本資訊那樣被生命週期軌道透過 DOM 元素間接依賴，四個欄位跟三份清單都是自足的，所以照既有模式直接搬，不用額外處理。
  - **順手修掉一個先前埋下、這次才會發作的樣式問題**：抽屜裡有一條「把區塊自己的標題藏起來」的規則，是做裝備清單那一輪加的——當時抽屜裡只有一份清單，標題跟抽屜標題重複，藏起來合理。但這次一個抽屜裡放了住宿／交通／隨行人員三份清單，那條規則會把三個標題全部藏掉，畫面上只剩三組長得一樣的「＋新增」按鈕，根本分不出哪個是哪個。改成只藏「跟抽屜標題重複的那一個」，清單自己的標題加上標記保留。
  - **測試方式**：驗證四個預算欄位跟三份清單都正確出現在抽屜裡、預算欄位編輯會自動儲存、清單的新增與刪除都正常、清單內層欄位（例如住宿的飯店名稱）編輯也會自動儲存、關閉後摘要卡片數字跟著更新。標題問題修正後特別回頭確認兩種情況都對：預算抽屜裡三個清單標題都看得到、裝備抽屜裡那個重複標題仍然正確藏起來。前四輪的裝備抽屜、賽後成績抽屜、基本資訊抽屜、生命週期軌道、三維度游標聯動、手風琴收合都重新跑過回歸測試。

## v2.94.0

- **「基本資訊與時間」也改用抽屜面板，22 個欄位拆成兩張卡片**：「賽事基本資料」（名稱、別名、官網、系列、狀態、運動種類、競賽形式、起終點、縣市、國家、應援衣著）跟「時間與報名」（比賽日期、起跑時間、起跑分區、號碼布、報名開始／截止、抽籤結果日、抽籤人數與中籤率、是否抽中）。跟上一輪同樣的理由：22 個欄位塞進同一個面板只是把長頁面換成長面板。
  - **動手前查證抓到一個會靜默失效的相依**：生命週期軌道（考慮中→抽籤中→已報名→已完賽 那排節點）原本的運作方式，是去畫面上找 `#f-status` 這個下拉選單、把值設好再送出 change 事件，藉此間接觸發既有的存檔邏輯。但狀態欄位搬進抽屜之後，抽屜沒打開時那個下拉選單根本不在畫面上——軌道會變成點了完全沒反應，而且不會報任何錯。
  - **修法是把共用邏輯抽出來，而不是讓軌道去開抽屜**：原本「改狀態」這件事分成兩層——資料層（鞋款門檻提醒、無感智慧預設、裝備範本自動套用、存檔）早就抽成 `quickSetRaceStatus()` 跟手機滑動完賽共用；畫面層（存檔提示、重繪、完賽時自動展開並捲到賽後紀錄）則還寫在欄位變更的處理函式裡。這次把畫面層也抽成 `setRaceStatusFromUi()`，下拉選單跟生命週期軌道兩個入口都直接呼叫它，不再透過某個 DOM 元素轉手。
  - **過程中修掉自己寫錯的一處**：摘要卡片要顯示運動種類的中文名稱，第一版寫成用陣列索引去取（`o[0]`／`o[1]`），但這個專案的運動種類選項是物件格式（`{value, label}`）；翻譯鍵也拼錯成 `sport.xxx`，正確慣例是 `opt.sport.xxx`。兩個都會安靜地顯示不出東西而不是報錯，是查了既有寫法才發現的。
  - **測試方式**：最重要的是確認生命週期軌道在「基本資訊區塊收合著」的狀態下仍然能改狀態（實測點「已報名」正確生效、點「已完賽」除了改狀態也正確自動展開賽後紀錄區塊）。另外驗證 22 個欄位完整分配到兩個抽屜、沒有任何一個遺漏或重複（11＋11、零重疊）；兩個抽屜各自的欄位編輯都正確自動儲存、關閉後摘要卡片數字跟著更新；無感智慧預設在抽屜裡照常運作（輸入「2026 第5屆 日月潭國際萬人泳渡」正確反推出系列名稱）。前三輪的裝備抽屜、賽後成績抽屜、三維度游標聯動、手風琴收合、環境光渲染、指令控制台都重新跑過回歸測試。

## v2.93.0

- **賽後紀錄區塊也改用抽屜面板，但刻意只搬「要填的欄位」，圖表留在頁面上**。查證後發現這個區塊不能照裝備清單的模式整塊搬走，原因跟一個既有功能有關，所以做了範圍上的取捨。
  - **不整塊搬的理由**：分段配速表是「三維度游標聯動」的其中一腳——滑過海拔剖面圖時，地圖標記跟分段配速表對應的那一列會同步亮起。這個聯動是在頁面畫完的當下，就去 DOM 裡把分段表的每一列快取起來的；海拔圖在「地理與路線」區塊，如果分段表被搬進抽屜，快取的當下抽屜還是空的，會抓到零列——結果是滑海拔圖時地圖標記照常移動、分段表卻默默不再高亮，而且不會報任何錯。反方向（滑分段表帶動海拔圖游標）也會一起失效。與其想辦法讓聯動跨越抽屜邊界，不如讓需要互相看見的東西留在同一個畫面上。
  - **最後的切法**：成績儀表板、分段分析、分段配速表、多媒體與相片牆這些「看的」東西全部留在頁面上；只有「填的」欄位搬進抽屜，而且拆成兩個——「成績與數據」（完賽時間、名次、心率、步頻、鞋款等 14 個欄位）跟「賽後心得」（撞牆期、路線心得、優缺點、檢討筆記 5 個文字欄位）。19 個欄位擠進同一個面板只是把「頁面很長」變成「面板很長」，沒有真的解決問題。
  - **順手把抽屜機制一般化**：原本 openDrawer() 是寫死裝備清單的，現在改成查一張「區塊登記表」，要再加區塊只要多登記一筆，不用動開關邏輯本身；詳情頁重畫時同步更新抽屜內容的那段也一併改成通用的，不會只認得裝備清單。
  - **測試方式**：最重要的一項是確認三維度游標聯動完全沒壞——實測滑海拔剖面圖時分段表正確高亮對應的那一列（5.0 km 的位置亮起 kmEnd=5.000 那列），反方向滑分段表也正確帶動海拔圖游標跟提示框（第 3 列顯示 2.5 km）。兩個新抽屜各自測過欄位編輯有正確自動儲存、關閉後摘要卡片的數字會更新（名次改成 99 之後卡片跟著變）。上一輪的裝備清單抽屜、四種關閉方式、手風琴收合行為、以及前幾輪的環境光渲染、指令控制台、刪除復原提示都重新跑過回歸測試。日文、英文介面下兩張新卡片的文字都正確代入數字。

## v2.92.0

- **賽事詳情頁改成手風琴（Accordion）：預設全部收合，一次只展開一個區塊**。原本六個區塊會依賽事狀態自動展開好幾個（例如賽前狀態會同時打開路線、預算、裝備三個），整頁很長、要一直捲；現在一進到賽事就是六行標題的總覽，點哪個才展開哪個，展開新的會自動把前一個收起來。
  - **用瀏覽器原生的 toggle 事件，而不是攔截標題的點擊**：`<details>` 的展開收合本來就是瀏覽器內建行為，攔截點擊等於要自己重寫一遍（還得自己處理鍵盤操作和無障礙焦點）。改成讓原生行為照常發生、事後再把其他區塊收起來，比較單純，而且鍵盤使用者按 Enter 展開也一樣會觸發，不會漏掉。toggle 事件不會冒泡，所以監聽器掛在 document 的捕獲階段，這樣每次重畫詳情頁之後都不用再重新掛一次。
  - **兩個既有的「自動展開」行為改成配合手風琴，而不是跟它打架**：上方分頁列（基本／路線／氣象…）點下去本來就會展開對應區塊，現在會一併收合其他區塊；把賽事狀態改成「已完賽」時會自動展開並捲到賽後紀錄區塊，現在同樣只留那一個展開。
  - **順手修掉一個被這次改動暴露出來的潛在問題**：路線地圖是在頁面畫完的當下就初始化的，而現在那個區塊預設是收合的——Leaflet 在隱藏容器裡初始化時，算出來的圖磚排版不一定正確（經典症狀是圖磚只鋪半邊、或整塊灰白）。展開路線區塊時會叫地圖重新量一次尺寸。這次測試環境連不到地圖的 CDN，所以是用模擬物件驗證「展開路線區塊時確實會呼叫、展開其他區塊不會誤呼叫、沒有地圖時也不會出錯」這三件事。
  - **測試方式**：驗證了賽前與已完賽兩種狀態進來時六個區塊都是收合的；連續展開三個不同區塊，每次都確認只有當下那一個是開的；手動把唯一展開的區塊關掉之後，不會有區塊被強制彈開。另外確認海拔剖面圖的滑鼠游標聯動在「區塊稍後才展開」的情況下仍然正常運作（游標、提示框都正確）。上一輪的裝備清單抽屜、以及前幾輪的環境光渲染、指令控制台、刪除復原提示都重新跑過回歸測試。

## v2.91.0

- **抽屜面板（Drawer）試作：裝備清單改成「摘要卡片＋點開編輯」**。這次刻意只做「裝備清單」這一個區塊，不是一次把六個區塊全部改掉——動手前查證發現這個轉換的風險比表面上看起來大，先用一個區塊把接線方式驗證到可靠，再決定要不要推廣。
  - **原始構想裡「事件完全不用改、直接無痛接軌」這個前提，在這個專案並不成立**：構想的說法是因為有事件代理，所以自動儲存邏輯搬到抽屜裡也會照常運作。但這個 App 的自動儲存（change）跟大部分按鈕（click）事件並不是掛在 document 上，而是掛在詳情頁那個容器元素上；抽屜在畫面結構上不在那個容器底下，事件不會冒泡過去。如果照字面直接搬，結果會是「抽屜裡的欄位改了完全沒反應、而且不會跳任何錯誤」——畫面看起來一切正常、資料卻悄悄沒存進去，是最難察覺、也最傷的那種故障。實際做法是把原本兩個匿名處理函式抽成具名函式，詳情頁跟抽屜掛的是同一份函式（不是各自複製一份，避免日後改了一邊忘了另一邊）。
  - **另外兩件原本擔心會出問題、查證後確認不用動的事**：拖曳排序跟裝備佈署（Kanban）檢視的拖放，監聽器本來就是掛在 document 上，搬進抽屜完全不受影響；抽屜也刻意避開了會牽動地圖、海拔圖、天氣抓取這些「畫面畫完之後還要再跑一段初始化」的區塊，所以這次不用處理那部分的複雜度。
  - **開發過程抓到兩個真的會影響使用的 bug**：其一，在抽屜裡套用範本、切換檢視、新增刪除項目之後，資料確實存進去了、但抽屜內容不會跟著更新，看起來像按了沒反應——原因是這些操作完成後呼叫的是「重畫詳情頁」，而詳情頁不包含抽屜。修在「重畫詳情頁」這個函式裡統一處理（開著抽屜就順便重畫抽屜內容），不是去改那二十幾個各自呼叫它的地方，避免漏改。其二，在抽屜裡按下「套用範本」會導致整個抽屜莫名關閉——原因是判斷「有沒有點到背景」的寫法是「往上找不到面板就算背景」，但按鈕按下去之後抽屜內容被重畫，原本被點的那個節點已經從畫面上移除了，往上當然找不到面板，就被誤判成點到背景。改成只認「直接點在遮罩本身」才算背景。
  - **UX 細節**：桌機從右側滑入、手機從底部滑上來（頂端有小灰橫條），關閉方式有 ✕／點背景／Esc／手機返回鍵四種。手機返回鍵那項是用瀏覽器的歷史狀態做的，按返回會先收起面板而不是直接離開網站；關閉時也會把先前推進去的狀態退掉，不會累積成「要多按好幾次返回才有反應」。背景加了模糊，讓注意力集中在編輯中的內容上。
  - **測試方式**：最優先驗證的就是前面說的那個無聲故障——確認在抽屜裡編輯欄位真的有存進資料。另外測過抽屜裡的新增／刪除項目、套用內建範本、切換 Kanban 檢視都正常；四種關閉方式各測一次；關閉後摘要卡片的數字正確更新（「已打包 0」→「已打包 1」）。回歸測試確認其他五個區塊維持原本直接展開的形式沒有被動到、尤其是跟裝備清單位在同一個大區塊裡的「補給時程規劃」完全不受影響；前幾輪的環境光渲染、指令控制台、刪除復原提示也都重新驗證過。日文、英文介面下摘要卡片的文字都正確代入數字。桌機與手機兩種版型各自截圖確認過。

## v2.90.0

- **流體漸層與微光呼吸背景**：備戰釘選卡片沒有封面照片時，改用金色與森林綠兩塊模糊光暈緩慢交融的流體漸層；全站所有空狀態區塊加上極淡的微光呼吸背景。
  - **效能做法**：兩個效果都只動 `transform` 跟 `opacity` 這兩個由 GPU 處理的屬性，不去動背景顏色或背景位置（那會讓瀏覽器每一影格都要重新計算版面、重繪整塊畫面）。這不只是照著理論寫，而是實際量測驗證過的——在同一個環境下分別量測「動畫開著」跟「動畫關掉」的影格時間，兩者平均 27.4ms 對 26.83ms、最差影格 50.0ms 對 50.1ms，幾乎完全一樣，代表這兩層動畫實際增加的成本大約只有 0.6ms；那個 27ms 的基準值是這個測試環境（無頭瀏覽器）本身的渲染負擔，不是動畫造成的。如果沒有做這組對照量測，光看「最差影格 50ms」這個數字很容易誤判成動畫造成了嚴重卡頓。
  - **尊重系統的「減少動態效果」設定**：這個 App 其他動畫本來就都有處理這個無障礙設定，這兩個新效果也一併處理——開啟該設定時漂移／呼吸動畫會停止，但斑塊本身的漸層顏色完整保留，畫面不會突然退回一片平板的純色，只是靜止不動。
  - **順手修掉一個視覺上的瑕疵**：備戰釘選卡片裡的三張小卡片（裝備進度、配速策略、天氣預報）原本是不透明的實心底色，加上流體漸層之後會變成「漂亮的背景被三個方塊整齊地蓋住」，漸層只在卡片之間的縫隙露出來，看起來像沒做完。改成半透明之後漸層會從卡片底下透上來，整個面板才是一塊完整流動的畫面。這個調整只套用在有流體背景的面板上，其他地方用到同一個卡片樣式的維持原本的實心底色。
  - **空狀態只改了一個地方，全站 7 處以上同時生效**：這個 App 的空狀態早就已經統一由同一個函式產生，所以這次不用逐一去改每個空白區塊，單純在共用的樣式上加一層背景就全部涵蓋到了——實際測試確認同一個畫面上就有 7 個空狀態同時套用到效果。
  - **測試方式**：驗證了有封面照片時流體漸層完全不會出現（照片本身就是主視覺，不會兩層背景打架）、沒有照片時才出現且模糊與動畫都正確套用；空狀態驗證了動畫確實掛在共用樣式上、內容（圖示與文字）正確疊在微光層之上不被蓋住；開啟「減少動態效果」的環境下驗證了動畫停止但顏色保留；效能做了前面說的對照量測；最後跑了一次全站回歸，確認上一輪的環境光渲染、指令控制台、空狀態的範本按鈕都沒有被這次改動影響。淺色與深色模式各自截圖比對過。

## v2.89.0

- **環境光渲染**：打開一場有封面照片的賽事，從照片抓出的主色調會以極淡的比例暈染整個頁面背景，畫面上方浮現呼應這個顏色的柔光，離開時平順淡回原本的底色。
  - **動手前先查證，才發現這個 App 早就已經把最難的一步做完了**：抓照片主色調這件事，其實從很早的版本就已經存在——`extractDominantColor()` 這個函式、跟儲存在每場賽事底下的 `coverImageAccentColor` 欄位都已經在正常運作，只是原本唯一的用途是套用在倒數進度條的顏色上。這次完全沒有重新寫一套顏色分析邏輯，單純是把這個早就算好、一直躺在資料裡沒有被充分利用的顏色，多接上一個新的應用場景。
  - **選了 CSS 原生的 `color-mix()` 來做混色，而不是在 JS 裡把顏色兜成一堆 `rgba()` 字串**：JS 端只需要把抓出來的顏色寫進一個 `--ambient-color` 的 CSS 變數，混色的比例、跟深色/淺色主題怎麼搭配，全部交給 CSS 處理，不用為了計算混合後的顏色值另外寫一套色彩運算。
  - **設計上特別避開了一個容易忽略、會直接讓效果整個失效的陷阱**：如果直接把 `--paper`／`--surface` 這兩個既有變數改寫成參照它們自己的 `color-mix()`（例如 `--paper: color-mix(in srgb, var(--paper) 96%, ...)`），這在 CSS 裡是無效的循環參照，套用到同一個元素上會讓這個變數整個變成「無效值」，不是優雅地退回原本的顏色——這樣輕則沒有效果，重則讓整個頁面背景消失或穿透。改成另外新增兩個乾淨的「原始未混色」變數（`--paper-base`／`--surface-base`）當作 `color-mix()` 的參照起點，兩者完全獨立，不會互相牽扯。
  - **整段效果包在 `@supports` 的條件判斷裡**：不支援 `color-mix()` 的瀏覽器會完全跳過這層覆蓋，介面退回原本純粹的底色，不會因為看不懂這個語法就讓背景失效或變透明——這也是前面提到的陷阱如果沒處理好，最容易在舊瀏覽器上出問題的地方。
  - **淺色、深色模式分別用不同的混合模式**：柔光那層特效原本設計是用 `screen`（讓疊加處變亮、發光），這個模式在深色背景上效果很好，但淺色模式的底色本來就接近純白，`screen` 疊加什麼顏色上去都會被稀釋到幾乎看不見——改成淺色模式用 `multiply`（讓疊加處變暗、更飽和），深色模式維持 `screen`，兩種主題下才都看得出這個效果，不是只有深色模式才有用。
  - **混色比例刻意壓得很低（約 4%）**：實際驗證過，就算故意用最極端的主色調（螢光黃）去測試，文字跟背景的對比度還是遠遠超過無障礙閱讀（WCAG AA）的基本標準——因為文字顏色本身完全不受這個效果影響，只有背景色被極輕微地暈染，不會因為換了氛圍就犧牲了可讀性。
  - **測試方式**：驗證了套用氛圍後 `--paper` 實際解析出來的顏色（不是只看 CSS 變數字面上的字串）真的照 96%／4% 的比例混合、算出來的數字精確對得上；測過離開賽事詳情頁、切到沒有封面照片的賽事、重新上傳新封面照片這幾種情境，氛圍色都會正確切換或關閉；用最極端的主色調驗證過文字對比度遠超標準；也測過深色模式切換、指令控制台、刪除復原提示這些前幾輪剛做的功能都沒有被這次改動連帶影響。淺色跟深色模式各自截圖比對過視覺效果。

## v2.88.0

- **刪除賽事改成「先刪除、給 5 秒可以反悔」，取代原本「先確認、才刪除」**：詳情頁「刪除賽事」按鈕跟手機滑動刪除，都從「需要兩步驟才會真的刪除」改成「一個動作立刻刪除，跳出可以復原的提示」。動手前先盤點了這個 App 既有的刪除基礎設施，發現一個原始構想沒有考慮到、但很關鍵的既有優勢，做了對應的設計調整。
  - **查證後發現這個 App 的賽事刪除，早就已經是軟刪除（`deletedAt` 欄位＋30 天保留期＋「資料復原」畫面），比原始構想建議的做法本身更穩固**：原始構想建議用一個記憶體變數暫存要刪除的資料，5 秒後才真的寫進儲存空間——如果照字面實作，反而是一種倒退：瀏覽器分頁在那 5 秒倒數期間如果被關掉、當機、或重新整理，只存在記憶體裡、還沒真的持久化的那份資料就會不見。改成完全沿用既有、已經在正常運作的軟刪除機制——按下刪除的當下，資料就已經安全寫進本機儲存空間（只是標記成已刪除），這次新加的 5 秒倒數提示純粹是疊加在既有安全網上面的一層「立即、不用特地跑去開資料復原畫面」的反悔捷徑，不是取代那層安全網，兩者是疊加關係，不是二選一。
  - **移除的是「動作前的確認」，不是「動作後的安全網」**：詳情頁的刪除按鈕原本要連按兩次（第一次進入「確定刪除？再按一次」的待確認狀態，4 秒沒有點第二次會自動取消）；手機滑動刪除原本滑到底也要再點一次確認（5 秒沒點會自動收回）。這兩層「動作前先攔一下」的機制都拿掉了，但刪除本身走的還是同一套軟刪除＋30 天保留期，資料的安全程度沒有變差，只是把「確認」這件事的時間點，從「刪除之前」搬到「刪除之後」。
  - **連續刪除好幾筆的情況，做成疊加追蹤而不是逐一蓋台**：5 秒視窗內如果連續刪了好幾場賽事，不會是「刪一場、跳一次提示、下一場刪除馬上蓋掉上一則提示逼它立刻定案」——而是同一個提示會持續追蹤這個視窗內刪除的所有賽事，文字從「已刪除：{名稱}」變成「已刪除 N 場賽事」，倒數也會跟著最新一次刪除重新算滿 5 秒，按一次「復原」會把這個視窗內刪除的全部救回來，不會顧此失彼。
  - **測試方式**：詳情頁刪除按鈕測過單次點擊立刻軟刪除（不用第二次點擊）、跳出的提示文字正確、點擊「復原」正確清掉 `deletedAt`。手機滑動刪除測過滑到門檻直接軟刪除、不再需要「再次點擊確認」這個中間步驟。連續刪除測過三筆賽事在同一個 5 秒視窗內，提示正確從單筆文字變成「已刪除 3 場賽事」、且維持只有一則提示（不是疊出三則）、按一次復原正確把三筆全部救回來。特別測過提示倒數超過 5 秒沒有點擊的情況：提示正確自動消失，但賽事本身依然安全留在「已刪除」狀態，`recoveryModalHtml()` 產出的畫面裡依然找得到它、可以手動還原——這一項是整個設計能不能成立的關鍵測試，確認拿掉即時提示之後，既有的 30 天安全網完全沒有被削弱。最後跑了一次全站回歸，確認「資料復原」畫面裡既有的手動還原／永久刪除功能、滑動完賽（向右滑）這個沒有改動的方向，都沒有被這次改動連帶影響；三語言版本的提示文字（含單筆／多筆兩種）都重新測過。

## v2.87.0

- **兩個原生手感手勢**：彈出視窗改成手機版 Bottom Sheet、可下拉關閉；月曆標題列邊緣滑動切換月份。動手前先盤點了這個 App 既有的觸控手勢，發現原始構想裡「整塊賽事清單都能滑動切月份」的範圍設計，會直接跟已經上線的功能衝突，做了必要的範圍調整。
  - **月曆滑動切換月份的範圍，刻意收斂在標題列，不是整個賽事清單區塊**：查證後發現手機版（寬度 ≤640px）的月曆本體，本來就已經是「整排都能左右滑動來標記完賽／刪除」的清單（v2.80.0 做的功能）——如果邊緣滑動月份也綁在同一塊區域，兩種手勢會搶奪同一個觸控起點，變成「滑一下賽事列，到底是要切月份還是要標記完賽」這種沒辦法分辨的衝突，怎麼調整判斷邏輯都沒辦法兩全。改成把這個手勢收斂在月曆標題列（「‹ 年 月 ›」那排導覽按鈕所在的地方）——這塊區域不管在哪種檢視模式下都存在、不會被賽事列佔用，而且緊鄰既有的 ‹/› 按鈕，滑這裡跟按鈕本身要做的事完全一致，不是另外發明一個要使用者自己摸索的隱藏熱區。
  - **Bottom Sheet 的下拉關閉手勢，只認面板最上方的把手區，不是整個面板**：這個 App 有些彈出視窗內容很長（例如鞋款一多的鞋款管理、內建使用說明），如果整個面板都能觸發下拉關閉，使用者想在面板裡往下捲動瀏覽內容時會被誤判成要關閉——這正是原始構想裡把手柄稱為「最佳觸控熱區」而不是裝飾用途的原因。實作上，把手本身是用 CSS 偽元素畫出來的（不是真正的 DOM 節點，因為這個 App 裡開關彈窗的函式散落在十幾個地方，沒有一個統一入口可以順手掛上一個真正的把手元素），偽元素沒辦法單獨接收觸控事件，瀏覽器回報的 e.target 一律是面板本體，所以改用觸控起點的 Y 座標離面板頂端夠不夠近來判斷「這次摸的是不是把手」，不是比對元素本身。
  - **關閉彈窗的方式，是去找、去點這個彈窗自己既有的「關閉」按鈕，不是自己動手重設顯示狀態**：這個 App 裡「焦點位置編輯」這類少數彈窗，關閉時除了收起視窗本身，還會額外清掉一份暫存的草稿資料，不是每個彈窗都只有「隱藏＋清空內容」這麼單純。這次新增的下拉關閉手勢複用既有 Esc 鍵那段邏輯已經在用的做法——去找該彈窗自己的 `[data-action^="close-"]` 按鈕、模擬點擊它——這樣不管哪個彈窗被下拉關閉，原本該執行的收尾動作都會正常跑，不會因為多了這個手勢入口而被繞過去。
  - **兩個手勢都只在手機版生效**（跟月曆切清單檢視用同一個寬度斷點），桌機（滑鼠操作）維持原本的置中彈窗跟純點擊月曆導覽，不會為了模擬觸控手感而畫蛇添足。
  - **測試方式**：Bottom Sheet 測過從把手區下拉超過門檻正確觸發關閉（且會去點該彈窗自己的關閉按鈕，不是直接改顯示狀態）、拉不到門檻正確彈回原位、從把手區以外的地方（模擬在長列表中間往下捲動）完全不會觸發這個手勢、寬螢幕（桌機）底下這個手勢完全不生效、原本的置中彈窗樣式維持不變。月曆邊緣滑動測過往左滑正確切到下個月、往右滑正確切回上個月、滑動距離不夠門檻正確不切換、垂直方向為主的移動正確整個放棄手勢；特別測過標題列上的既有按鈕（‹、›）單純點擊仍然正常運作、以及切到有賽事的那一天、對賽事列做滑動完賽的既有手勢，確認完全不會被新加的月份切換手勢誤判或搶走——兩者互不干擾。最後跑了一次全站回歸，確認前幾輪做的指令控制台、Esc 鍵關閉彈窗、鞋款管理 Enter 鍵新增這些既有功能都沒有被這次改動連帶影響。

## v2.86.0

- **鞋款「訓練里程」欄位＋批次匯入＋介面重新設計**：這個 App 本身只記錄比賽當天的距離，不追蹤日常訓練——新增「訓練里程」欄位讓使用者可以自己填入從 Strava 之類運動錶 App 看到的訓練里程，計入鞋款總里程；同時把整個鞋款管理畫面重新設計得更清楚。
  - **總里程計算**：`computeShoeStats()` 現在回傳比賽里程、訓練里程、兩者相加的總里程三個數字，既有只取總里程的呼叫端（判斷鞋子是否達到壽命上限等）完全不用改，自動把訓練里程也算進去；需要分開顯示兩個來源的地方（鞋款管理畫面）另外取用分開的兩個數字。
  - **編輯表單整合**：預期壽命、訓練里程、鞋底厚度、足跟差四個欄位原本分成兩個各自獨立的編輯狀態（分別要點兩個不同的 ✎ 圖示），這次合併成一個統一的編輯表單、一組儲存／取消——兩個獨立狀態變數也合併成一個，不只是介面上看起來統一，程式碼裡的狀態管理也單純了一份。
  - **整體版面重新設計**：鞋款一多（測試時用真實情境模擬了 39 雙鞋），舊版每一雙鞋要占好幾行、資訊擠在一起不容易掃過去。新版把最重要的數字（總里程／目標）用較大字體放在明顯位置，賽事里程／訓練里程／場次這些細節退到次要、較淡的一行，規格摘要再退一行，操作按鈕（戰力分析、退役、刪除）整排調淡、平常不搶視覺焦點，滑過去才轉深色——這個順序是刻意設計的資訊層級，不是把原本的東西全部塞進去而已。
  - **批次匯入訓練里程**：延伸上一輪剛做好的「全域貼上匯入」——貼上一段 `[{"name":"鞋款名稱","trainingKm":123.4}, ...]` 格式的 JSON 陣列會自動識別成「鞋款訓練里程更新」（不是賽事陣列，靠陣列裡每一項是不是同時有 `name`／`trainingKm`、卻沒有 `schedule`／`sportType` 這種賽事才有的欄位來判斷），依名稱比對更新對應鞋款。比對刻意做成寬鬆比對（雙方字串誰包含誰都算）——這個 App 的鞋款名稱是自由輸入的文字，跟外部運動記錄軟體匯出的名稱不會完全一致，要求打字要一模一樣只會讓大部分資料配不上；但同一個關鍵字如果同時比對到兩雙以上的鞋，代表沒辦法判斷該歸給哪一雙，這種情況一律跳過、算進「找不到對應」，不會隨便挑一雙硬套——這個功能只會更新 `trainingKm` 這一個欄位，就算真的配錯了，也只是某雙鞋的數字不準，透過編輯表單就能修正，不是無法復原的操作。
  - **修正一句連帶變得不準確的既有說明文字**：鞋款管理的說明文字原本寫「僅計算本 app 記錄的比賽距離，不含訓練里程」——這句話在加入訓練里程欄位之後就不對了，三個語言版本都一併改成正確描述現在的計算方式。
  - **測試方式**：新增鞋款物件字面量原本漏了 `trainingKm:0` 這個初始值（既有的容錯機制讓這不會造成看得見的錯誤，但物件形狀跟「從儲存空間讀出來、經過既有 migrateShoe() 補齊欄位」的鞋款不一致），寫測試比對新建鞋款跟載入鞋款的欄位是否一致時抓到並補上。批次匯入的比對邏輯特別測過四種情境：完全相符、部分字串相符、完全找不到、兩雙鞋同時符合同一個關鍵字（這種情況第一次測試設計的兩筆測試資料其實因為一個空格的差異根本不會同時比對到，重新設計出真正會同時比對到的測試資料後才正確驗證到「兩雙都不更新、都算進找不到對應」這個安全機制）。全域貼上匯入的路由邏輯也測過兩個方向都不會誤判：鞋款更新陣列不會被誤認成賽事陣列亂建賽事、真正的賽事陣列也不會被誤認成鞋款更新。編輯表單的四個欄位、鞋款完整生命週期（新增、編輯、退役、恢復服役、刪除）、個人資訊分頁裡的鞋款子頁籤也都重新跑過一次確認沒有因為這次的版面重寫而壞掉。三語言版本的所有文字（含修正後的說明文字）都重新截圖比對過。

## v2.85.0

- **全域貼上匯入（Ctrl/Cmd+V）**：不用先點開任何按鈕，直接按貼上就依剪貼簿內容自動判斷——圖片設成封面照、JSON 陣列整批匯入賽事、JSON 物件快速填入目前開著的賽事。動手前先盤點了這個專案既有的匯入基礎設施，發現大部分需要的邏輯其實都已經存在、已經測過，這次真正新增的只有「從剪貼簿事件裡正確攔截、判斷資料類型」這一層，不是重寫一套平行的匯入系統。
  - **查證後發現一個原始構想沒注意到、但很關鍵的既有分歧**：這個專案原本就有一個「貼上資料快速填入」功能（按鈕觸發、貼進文字框），但它處理的是**單一 JSON 物件**（深度合併進目前開著的賽事，只更新貼上的欄位）；而既有的「匯入 JSON」處理的是**JSON 陣列**（整批賽事還原／合併，走完全不同的邏輯）。這兩者的資料形狀（物件 vs 陣列）本來就不一樣，直接沿用原始構想「偵測到 JSON 就用同一套邏輯處理」的想法會出錯——這次改成先判斷貼上的 JSON 是物件還是陣列，分別導向這兩套既有、已經各自測過的邏輯，不是發明第三套新的匯入規則。
  - **圖片貼上沒有做構想中的「裁切 Modal」**：查證後發現這個專案原本就沒有「上傳前先裁切」這個環節，既有的「封面照片」上傳邏輯是整張圖直接處理，賽事已經有封面照之後才有一個獨立的「焦點位置」工具可以事後調整要顯示哪個區域——貼上圖片沿用的是既有的封面照上傳邏輯（跟既有的上傳按鈕、全域拖曳匯入圖片，三個入口共用同一套函式），不是另外掛一個裁切套件重造一個新環節。
  - **重構了既有的 JSON 陣列匯入邏輯，讓它變成一個可以共用的核心函式**：原本「匯入 JSON」的驗證、合併、存檔、跳提示這幾步都直接寫在處理檔案的函式裡，沒辦法給「直接貼上一段已經是字串的 JSON」這種情境重複使用。抽出一個只接收「已經解析好的陣列」的共用函式，原本的檔案匯入跟這次新的貼上匯入都呼叫這一份，不是各自維護一份容易兜不起來的重複邏輯；抽出來的同時也把陣列裡每一筆賽事都要有 `id` 這個驗證規則放寬——直接貼上的通常是請 AI 生成的全新賽事，本來就不會有 id，缺的話直接補一個，不會因為這個欄位就整批擋下來。
  - **焦點防護（Focus Guard）是這個功能能不能安全存在的前提，不是可有可無的細節**：使用者在任何輸入框、文字框、可編輯區域打字的時候按貼上，必須完全交還給瀏覽器的預設行為——沒有這道防護，在「檢討筆記」裡貼一段普通文字會被誤判成要匯入賽事資料，這個功能非但沒有幫上忙，反而會讓一般的文字編輯體驗變得不可靠。
  - **測試方式**：焦點防護是最先確認、也是最重要的一項——模擬在文字框裡貼上一段 JSON 陣列，確認完全沒有觸發匯入、沒有跳出任何提示，瀏覽器原本的貼上行為不受干擾；接著分別測過圖片貼上（正確設成封面照）、JSON 陣列貼上（不管有沒有 id 都正確匯入，且不需要先開著任何賽事）、單一 JSON 物件貼上（有開著賽事時正確合併、只更新貼上的欄位，沒開著賽事時正確跳出提示、不會出錯中斷）、以及一般純文字貼上（完全不攔截，`event.defaultPrevented` 確認維持 false，瀏覽器原生貼上行為正常發生）。三語言版本的新提示文字也重新測過。最後跑了一次全站回歸，確認既有的「貼上資料快速填入」按鈕彈窗、全域拖曳匯入、指令控制台這幾個相關功能都沒有被這次改動連帶影響。

## v2.84.0

- **全域指令控制台（Cmd/Ctrl+K）**：不用碰滑鼠，按快捷鍵呼叫一個置中搜尋框，就能跳到任何一場賽事，或執行幾個最常用的全域動作。三種語法——沒有前綴是賽事全文檢索，`>` 開頭是系統動作，`#` 開頭是快速篩選。
  - **系統動作清單刻意只收錄少數幾個，不是把這個 App 累積至今的每個功能都塞進去**：這個 App 這幾輪下來功能非常多，指令控制台的價值在「快」，選項太多反而變得要滑很久才找得到、失去意義。目前收錄切換深色模式、新增賽事、管理鞋款、補給品資料庫、建立個人資訊、匯出 JSON、匯出 CSV、範例資料這幾個最常用的，已登入雲端同步時額外多一項同步診斷（用一個 `available` 條件在搜尋當下動態判斷要不要列出，不是寫死在清單裡）；這份清單之後有需要可以再擴充，不是這次要一次做完的範圍。
  - **賽事全文檢索的比對邏輯，直接沿用主畫面搜尋框既有的那一套**（name／alias／city 字串比對），不是另外發明一套獨立的模糊搜尋演算法——這樣同一個關鍵字在兩個入口找出來的結果會是一致的，不會出現「搜尋框找得到、指令控制台找不到」這種行為不一致的情況。
  - **`#` 開頭的快速篩選，動手前先查證了這個 App 既有的篩選機制**：`dnf`／`dns` 本來就是賽事狀態的合法值，既有的篩選函式已經支援，不用另外寫；但「個人最佳 (PB)」不是狀態值，是另一個獨立的欄位，這部分補了新的比對邏輯。選到的賽事不是套用篩選晶片、切回月曆檢視，而是直接把符合的賽事列在控制台裡當結果——這樣跟賽事全文檢索、系統動作三種語法的操作手感是一致的（都是「列出結果、選一個、按 Enter」），不會有的走「列結果」有的走「套用畫面上的篩選條件」這種不一致的分支。
  - **開發過程中抓到兩個真正的 bug，都是在測試流程裡發現、不是憑感覺猜的**：第一個一開始看起來像是不小心弄壞了既有的「按 Esc 關閉彈窗」機制——測試顯示 Esc 鍵按下去賽事鞋款彈窗沒有關閉——追下去才發現是測試腳本自己的問題（直接對著 `window` 派送鍵盤事件，跳過了瀏覽器原生按鍵事件一定會經過的 `document` 冒泡路徑，既有機制其實完全正常）；但這個烏龍也順便揪出一個真正該修的地方——這個 App 所有其他的全域快捷鍵監聽器都是掛在 `document` 上，只有這次新寫的掛在 `window`，兩者在真實按鍵事件下雖然行為一致，但風格不一致，之後如果有人（包含我自己）再讀這段程式碼容易誤判成兩者行為有差異——已經統一改成掛在 `document`，跟其他既有寫法一致。第二個是貨真價實的顯示 bug：日文、英文版的提示文字裡，把 HTML 逸出字元 `&gt;` 直接寫進 JavaScript 字串常數裡——這種字串是直接指派給 `.placeholder` 這個純文字屬性、不是當成 HTML 解析，套用下去畫面上會顯示成看得到的四個字元 `&gt;`，不會變成一個 `>` 符號。跨語言測試時直接看到畫面上的實際文字才抓到，已經改成三種語言都直接放原始的 `>` 字元（`>` 這個字元本來就不需要在 HTML 屬性值或 JS 字串裡逸出，一開始就是不必要的處理）。
  - **測試方式**：用合成的鍵盤事件模擬真實的 Cmd/Ctrl+K 開啟、Esc 關閉、再按一次快捷鍵關閉；賽事全文檢索測過打關鍵字能找到對的賽事、按 Enter 正確跳轉並關閉控制台；系統動作測過搜尋關鍵字找得到「切換深色模式」這個動作、選取執行後畫面真的切換了主題（不是只驗證函式被呼叫，是驗證實際的畫面狀態真的變了）；`#pb`／`#dnf` 快速篩選各自測過只列出真正符合條件的那一場賽事，單獨輸入 `#` 測過會列出全部四個篩選選項；方向鍵導覽測過往下、往上都會正確循環（最後一項再往下回到第一項，第一項再往上回到最後一項）、目前選取的項目樣式正確跟著換；篩選選項點下去測過會把完整語法填進輸入框、控制台不會關閉（因為這是往下鑽一層，不是最終選定）。另外特別測過這次新增的功能跟上一輪剛做的「鞋款新增按 Enter 送出」不會互相干擾——在鞋款輸入框按 Enter 不會意外打開指令控制台，兩邊的鍵盤事件各自獨立運作正常。最後跑了一次全站回歸，確認前幾輪做的生命週期軌道、全域拖曳匯入、三維度游標聯動、脈絡化空狀態引導都沒有被這次改動連帶影響，既有的「按 Esc 關閉彈窗」機制也重新確認過運作正常。

## v2.83.0

- **脈絡化的空狀態行動引導**：裝備清單空狀態的一鍵套用標配範本按鈕、鞋款新增改成按 Enter 直接送出、檢討筆記空狀態的 KPT 復盤範本按鈕。使用者這次提出的三個構想，動手前逐一查證這個專案的既有狀態，發現實際情況跟原始構想有不小落差，三個都做了相應調整：
  - **裝備清單的空狀態，原本設想的「依運動種類顯示多種不同範本按鈕（例如越野跑要分『UTMB 強制裝備包』跟『輕量單日包』兩種）」，跟這個專案實際的資料模型對不上**——內建的標配範本（`STARTER_TEMPLATES`）一個運動種類只對應一份範本，不是好幾份變體，硬做出好幾個按鈕只會是名稱不同、內容其實一樣的假選項。改成老實地依實際資料只做一個按鈕，按鈕上直接顯示真正要套用的範本名稱。另外查證時還發現一件更根本的事：新增賽事的當下，系統本來就已經會依運動種類自動套用一次標配範本了（這是更早之前就有的機制），所以這個空狀態按鈕實際上不會像構想中想像的那樣「每次新增賽事都會看到」——只有運動種類選「其他」（沒有對應範本）、或使用者自己把清單清空重來的時候才會真的用到，這是這個功能一開始就該有的自我認知，不是事後才發現的限制。
  - **鞋款「內嵌快速建檔表單」，原始構想以為需要重新設計，查證後發現這個表單其實在更早之前的版本就已經做好了**——名稱、預期壽命、鞋底厚度、足跟差都是同一列的內嵌輸入框，本來就不是跳出一個大 Modal 中斷心流。真正缺的只有「按 Enter 直接送出」這個小動作，原本這幾個輸入框沒有包在 `<form>` 裡、也沒有接 Enter 鍵的處理，敲完最後一欄按 Enter 什麼事都不會發生。這次只補上這一小塊，讓填完直接按 Enter 就能送出，跟填一般表單的直覺一致。
  - **檢討筆記的模板注入，範圍調整成只放在「檢討筆記」這一個欄位**——這個專案的賽後心得其實已經拆成撞牆期紀錄、路線心得、優點評價、缺點評價、檢討筆記五個各自獨立、標籤明確的欄位，前四個本來就已經是有聚焦主題的小範圍提示，真正符合原始構想描述的「面對一片空白不知道從何下筆」的，只有「檢討筆記」這個開放式的大欄位，所以只在這裡加了 KPT（Keep／Problem／Try）範本按鈕，不是每個欄位都塞一組模板。
  - **開發過程中抓到兩個真正的 bug，都是在測試流程裡發現、不是憑感覺猜的**：第一個是鞋款 Enter 鍵的共用事件監聽器，寫的時候把它跟「獨立彈窗」那份監聽器放在同一個地方註冊，但「個人資訊」分頁那個容器的變數宣告在檔案更後面才出現——用 `const` 宣告的變數在自己那行程式碼真正執行之前是不能存取的，太早呼叫會直接丟出例外，中斷後面所有程式碼的載入，等於整個網站打不開。移到正確的位置（那個變數宣告之後）才解決。第二個是裝備範本按鈕的文字：`STARTER_TEMPLATES` 裡的範本名稱是寫死的中文字串，從來沒有透過翻譯系統過——這個限制一直都在，只是自動套用是安靜生效、不會把名稱顯示出來，所以从來沒被注意到；這次因為空狀態按鈕要把範本名稱直接秀在畫面上，才第一次讓這個限制在跨語言測試時現形（切到日文、英文畫面，按鈕上的範本名稱卻還是中文）。範本名稱本身（不含範本內每一項裝備的名稱，那是更大範圍、這次不處理的既有限制）已經補上對應翻譯。
  - **測試方式**：裝備範本按鈕測過會出現的情境（運動種類有對應範本、清單確實是空的）、不會出現的情境（運動種類是「其他」）、點擊後清單正確套用內建範本內容；鞋款 Enter 鍵測過在獨立彈窗跟個人資訊分頁兩種容器裡、從四個欄位中任何一個按 Enter 都能正確送出，也確認了非 Enter 鍵不會誤觸發；檢討筆記範本測過空欄位時按鈕正確出現、點擊後正確套用範本文字、游標正確落在第一個待填空處，套用後按鈕正確消失（避免誤以為再按一次會疊加或覆蓋），另外也測過已經手動寫過內容的賽事不會顯示這個按鈕。三語言版本的所有新文字（含修正後的範本名稱翻譯）都重新截圖比對過；最後跑了一次全站回歸，確認前幾輪做的生命週期軌道、全域拖曳匯入、三維度游標聯動都沒有被這次改動連帶影響，既有的「套用範本」下拉選單（使用者自存範本）、點擊新增鞋款這兩個原本就有的操作方式也都正常運作。

## v2.82.0

- **三個新的網頁震動回饋場景**：陡坡阻力感（三維度游標聯動時，坡度跨過陡坡門檻會震動一下）、PB 心跳共鳴（晶片時間破了同系列最快紀錄）、步頻震動體驗（按鈕依平均步頻節奏震動 5 秒）。使用者這次提出的原始構想裡，有兩個具體觸發條件動手前查證後發現這個專案目前沒有對應的資料可以判斷，做了調整，不是照單全收：
  - **「心率區間 Z3→Z4 跨越」「坡度進入 Cat 1 等級爬坡」這兩個原本設想的觸發點，目前都沒有對應的資料模型**——這個專案目前完全沒有爬坡分級（FIETS Index 之類）的功能，海拔剖面圖游標聯動當下能拿到的也只有距離、海拔、瞬時坡度，沒有心率（心率是從有時間戳記的 GPS 軌跡點來的，跟海拔剖面是兩份不同的資料，目前沒有互相對應）。改成用游標聯動當下本來就已經算好的「瞬時坡度」，設一個陡坡門檻（±8%，山徑健行常見的陡坡定義），坡度跨過這個門檻（不管進入還是離開）就震動一下，效果上一樣是「滑過一段特別硬的坡就震一下」，只是用的是這個專案實際上已經有的資料，不是假裝有一套不存在的爬坡分級系統。
  - **「系統比對發現破了 PB」這個觸發點，動手前先查證了這個專案的既有設計**：`results.isPb`（個人最佳）這個欄位的既有設計明確是「使用者自己勾選，不是系統自動判斷」——這次新增的偵測邏輯尊重這個既有設計，只負責「比對、發現、通知」，不會越權自動幫忙把這個欄位勾起來，提示裡附一個按鈕，要不要標記還是使用者自己按下去決定。比對範圍刻意只在「同系列」賽事之間（跟既有的「系列賽事比較（跨年）」那張表用的是同一套比較基準）——距離相近但賽道完全不同（例如兩場路況天差地遠的越野 10K）放在一起比「PB」沒有意義，同系列（通常是同一條賽道年復一年的賽事）才是真的公平的比較對象。
  - **步頻震動體驗**：成績儀表板本來就有一排數據小卡（配速、名次、心率、爬升），這次補上原本沒有顯示的「平均步頻」，卡片裡附一個小小的手掌圖示按鈕，按下去手機會按照那個步頻的節奏（用 60000 除以步頻，算出兩步之間該隔幾毫秒）連續震動 5 秒。體驗進行中按鈕會鎖住、避免重複觸發，播完自動解鎖。
  - **iOS 的限制這次維持一貫的處理方式**：三個場景全部都先判斷 `navigator.vibrate` 是不是存在，不支援的裝置（iOS Safari 目前完全不支援這個 API）三維度聯動、PB 偵測都會正常運作、只是沒有震動；步頻體驗按鈕比較特別，判斷到不支援時不會假裝在震動空跑 5 秒，會直接跳出提示說明原因，比讓使用者以為按鈕壞掉更誠實。
  - **測試方式**：陡坡震動用一段刻意設計的假海拔剖面（平地→陡坡→平地）逐步模擬游標移動，確認只有在真正跨過門檻的那一刻震動、同一個陡坡區段內來回移動不會重複震動；PB 偵測完整測過會觸發的情境（同系列裡確實更快）跟三種不該觸發的情境（沒有系列、比既有最快的還慢、已經手動標記過 PB 不會重複提示）；步頻體驗測過完整的 5 秒週期，確認震動次數精準對應步頻換算出來的間隔（180 spm 測出 15 次，跟算出來的理論值一致）、重複點擊會被正確忽略、播完會自動解鎖；也測過完全不支援震動 API 的環境下，三個功能分別的降級行為都正確、不會拋出例外中斷其他功能。過程中也抓到自己寫的一個真正的 bug：PB 提示文字一開始把賽事系列名稱直接用樣板字串接進中文預設字串裡，沒有照這個專案既有的 `{n}` 佔位符慣例寫——這個專案的多語言取代函式認的是字串裡字面上的 `{n}` 這個記號，接了實際數值進去以後，字串裡已經沒有 `{n}` 可以取代，等於那個取代邏輯完全沒作用，只是中文版本剛好看不出差異（因為接進去的內容跟原本該顯示的內容一樣）。這是在寫日文、英文版本的翻譯、比對既有慣例時發現寫法不一致才抓到並修正的，不是等測試失敗才發現。

## v2.81.0

- **三維度游標聯動**：分段配速表、海拔剖面圖、路線軌跡地圖，滑鼠移到任何一邊都會同步另外兩邊。動手之前先盤點了現況：海拔圖游標連動地圖標記這件事其實已經做過了（更早之前的版本就有），使用者這次提出的構想真正補的是「第三隻腳」——分段配速表還沒接進這套聯動——不是從零開始做一套全新機制，是延伸既有的。
  - **重構既有、已經在正常運作的程式碼是這次風險最高的部分**：原本海拔圖的滑鼠移動處理函式，把「算出游標位置」「更新地圖標記」這些邏輯全部寫在同一個函式裡、直接綁著滑鼠的螢幕座標算，沒有對外暴露一個「給定一個公里數，更新這三邊」的介面可以給表格那邊呼叫。抽出一個共用的核心函式，把「滑鼠在海拔圖的哪個像素」跟「這個像素對應賽事的第幾公里」這兩件事分開——分段配速表滑進某一列的時候沒有滑鼠在 SVG 上的像素座標可用，但知道那一列對應的公里數（用那一段的中點），直接呼叫這個共用函式一樣可以正確聯動，不用假造一個不存在的滑鼠位置。
  - **效能防抖**：海拔圖的 `pointermove` 事件在部分裝置上可能一秒觸發數十次，如果每次都直接更新 DOM（地圖標記、表格列的樣式），高頻觸發下畫面會卡頓。實際更新的動作包進 `requestAnimationFrame`，同一個畫面更新週期內收到好幾次移動事件也只會真的畫一次；分段配速表的列數比對用二分搜尋（表格本身先把每一列的「累積距離終點」快取成排序好的陣列），不是每次滑動都重新掃過所有列——賽事距離長、分段多的時候（例如破百公里的超馬）這個差異看得出來。
  - 分段配速表被同步到的那一列，用跟海拔圖游標、生命週期軌道「目前」節點同一個金色（`--stamp-bg-gold`）標出來，這個顏色在整個 App 裡已經是「這是互動當下的焦點」的固定語彙，不是這次另外挑一個新顏色。
  - **測試方式**：這是一段有風險的既有功能重構，改完立刻驗證原本的行為（海拔圖游標移動、地圖標記跟著移動）完全沒有跑掉，才繼續往下做新的部分。新增的兩個方向都個別測過：在海拔圖上移動滑鼠，確認地圖標記跟著動、分段配速表對應的那一列同步亮起（測了滑到剛好整數公里邊界的情況，確認抓到的是正確的那一列，不是差一列的邊界誤判）；滑進分段配速表某一列，確認海拔圖游標移動到那一段的中點位置、地圖標記跟著移動、那一列本身正確亮起，滑出去之後三邊同時清除。效能防抖也直接測過：連續快速觸發 10 次移動事件，確認畫面最終只反映最後一個位置，不是把中間那幾個也全部畫出來又立刻被蓋掉的浪費行為。由於 Leaflet 地圖套件需要連網從 CDN 載入、這個測試環境沒有對外網路，改用模擬物件驗證程式碼呼叫地圖 API 時傳的座標是不是正確，不影響驗證的嚴謹度。

## v2.80.0

- **手機版滑動清單（Swipe-to-Action）**：清單裡每一張賽事卡片向右滑可以快速標記完賽，向左滑可以快速刪除。使用者提出的原始構想是用 Tailwind CSS 搭配原生 Touch Events，這個專案沒有 Tailwind，改用這個專案既有的 CSS 變數跟樣式慣例重新刻了一版雙層卡片結構跟阻尼手感，不是直接照搬。
  - **這次動到的不只是新增功能，還包含一段風險不低的既有程式碼重構**：狀態變更（鞋款里程門檻提醒、無感智慧預設、裝備範本自動套用）原本的邏輯整段寫死在賽事詳情頁的欄位存檔流程裡，只認得「目前開著的那場賽事」——但滑動手勢發生在清單畫面，使用者根本沒有點開任何一場賽事的詳情頁，那份邏輯原封不動搬過來用不了。抽出一個獨立的 `quickSetRaceStatus(race, newStatus)`，把核心邏輯（鞋款提醒、智慧預設、裝備範本、存檔、跳提示）改成可以對任何一場賽事呼叫，不限定是目前開著的那場；接著把賽事詳情頁原本的欄位存檔流程也改成呼叫這個共用函式，不是留一份舊的、另外寫一份新的，兩份行為之後大概率會慢慢長歪。這段重構完成後立刻做了一輪完整回歸測試（基本狀態變更、完賽時自動捲動到賽後紀錄區塊、鞋款里程 80% 門檻提醒、其他欄位正常存檔、上一輪的無感智慧預設全部逐一驗證過一遍）才繼續往下做滑動手勢本身，確保踩在一個真的還是穩的地基上。
  - **觸控物理**：`touchmove` 時比較水平／垂直位移，垂直位移比較大就整個放棄這次手勢（判斷成使用者在捲動頁面），避免滑清單變成誤觸完賽或刪除；確定是水平滑動之後才鎖住垂直捲動，卡片才會跟著手指移動。超過門檻後用阻尼公式（每多拉 1px 畫面只移動 0.28px）模擬拉到底的物理極限感，不會讓卡片無止盡跟著手指飛出畫面外。
  - **刪除是兩段式操作，不是滑到底立刻刪除**：向左滑到門檻、放開手指，卡片會定格在滑開的位置、露出「再次點擊以確認刪除」，要再點一次那個區塊才真的刪除（5 秒沒點會自動收回）——這是刻意比照賽事詳情頁「刪除賽事」原本就有的連按兩次確認機制，滑動這個新的操作入口一樣不會一個手勢就讓資料消失；刪除本身也還是原本的軟刪除，可以從「資料復原」救回來。
  - **觸覺震動回饋刻意加了防呆**：`navigator.vibrate` 這個 API iOS Safari 完全不支援，呼叫下去會直接拋例外——如果沒有先判斷 `navigator.vibrate` 是不是存在就直接呼叫，會在 iPhone 上把整個滑動手勢的後續處理打斷。加了 `if(navigator.vibrate)` 判斷之後，iOS 上滑動完賽／刪除都能正常運作，只是沒有震動感，這是平台本身的限制、不是這個功能故障，行為上跟其他有支援震動 API 的裝置（多數 Android 瀏覽器）沒有差別，只差在有沒有那個震動回饋。
  - **測試方式**：用合成的 `TouchEvent`／`Touch` 物件模擬真實觸控手勢（不是憑印象覺得寫對了）——驗證過阻尼公式算出來的位移數字跟手算完全吻合；向右滑過門檻確認賽事狀態真的改成已完賽；向左滑過門檻確認賽事還沒被刪除、只是進入確認狀態，點畫面其他地方不會誤刪，只有點那個確認區塊才會真的刪除；垂直方向的捲動手勢確認完全不會觸發卡片位移或任何狀態改變；沒有任何拖曳的單純點擊確認還是能正常開啟賽事詳情頁；滑動距離沒過門檻確認會彈回原位、不觸發任何動作。過程中也發現一個真正遺漏的地方：三個新的操作提示文字（完賽、刪除、再次點擊確認刪除）一開始漏了日文／英文翻譯，跨語言測試時直接抓到（切到日文、英文畫面卻還是顯示中文），已經補上並重新測過確認正確。最後跑了一次全站回歸，確認前幾輪做的生命週期軌道、全域拖曳匯入、裝備佈署看板都沒有被這次改動連帶影響。

## v2.79.0

- **生命週期軌道（Lifecycle Stepper）**：狀態下拉選單多了一種操作方式——標題正下方一條可以直接點擊切換的進度軌道。使用者提出的原始構想裡有幾個 Tailwind 專屬的寫法（`animate-ping`、`opacity-50`、`cursor-pointer` 這種工具類別），這個專案完全沒有用 Tailwind，動手前先確認了既有的 CSS 變數（`--trail`／`--gold`／`--flag`）跟既有的欄位存檔機制，照這個專案實際的寫法重新設計，不是把提案的程式碼直接搬過來。
  - **狀態選項本身不是一條乾淨的直線流程**：這個專案的六個狀態裡，「未起跑 (DNS)」「未完賽 (DNF)」是例外分支，不是接在「已完賽」後面的第五、第六個階段——賽事已經到了比賽日這個時間點，DNS／DNF 是這個時間點的另一種結果，不是「完賽之後才發生的事」。軌道因此只畫「考慮中→抽籤中→已報名→已完賽」這四個正常路徑的節點，DNS／DNF 發生時把最後一個節點換成橘紅色的驚嘆號樣式表示，不會多畫兩個節點造成「這是完賽之後的下一步」這種不存在的先後順序錯覺。
  - **四個節點無論目前是哪個狀態都可以直接點擊**，包含目前正處於 DNS／DNF 的時候——點擊任何一個正常節點就能切回去，不特別做一個「返回正常」的按鈕，因為「點擊你想要的狀態」本身就已經是最直接的操作，不需要多一層機制。
  - **點擊節點不是另外寫一套存檔邏輯**：找到既有的狀態下拉選單（`#f-status`），把值設好、送出一個 `change` 事件，讓事件照原本的路徑冒泡上去，交給既有的委派事件處理——原本存檔、無感智慧預設（上一輪剛做的功能）、換運動種類自動套用裝備範本這一整套邏輯完整沿用，不會因為多了一個新的操作入口就要重新兜一份存檔流程，之後這幾套邏輯不管誰改了，兩個入口都會一起生效，不用分別維護。
  - 原始構想裡「呼吸燈光暈」的效果用了 `animate-ping` 這個 Tailwind 專屬類別，這個專案沒有 Tailwind，用 CSS `@keyframes` 重新做了一個等效的脈動動畫；過程中也發現一個真正的相容性問題：一開始直接把 `--gold` 這個顏色變數的近似 RGB 數值寫死在 `box-shadow` 的透明度動畫裡，這樣寫深色模式切換顏色時動畫不會跟著變——已經改成用一個疊在節點上方的偽元素（`::after`）搭配縮放＋淡出，直接吃 `var(--gold)` 本身，淺色／深色主題會自動套用各自對應的顏色，不用另外寫深色模式的例外規則。
  - **測試方式**：先把六個狀態值各自產生一次軌道 HTML，逐一比對每個節點該是「已通過」「目前」「未來」哪一種樣式，確認全部正確（包含 DNS／DNF 讓最後一個節點正確變成例外樣式、DNS／DNF 時另外那兩個連結正確消失，不會出現「已經是 DNS 了，畫面上還在問你要不要標記成 DNS」這種矛盾）。接著透過真正的點擊（不是直接呼叫函式）測過完整互動：點一個節點確認賽事狀態真的改了、畫面重繪後軌道正確反映新狀態；點 DNS 連結確認正確切換；在 DNS 狀態下點回「已報名」節點，確認正確切回正常狀態。另外測過「基本資訊」區塊裡原本的下拉選單改狀態時，軌道會不會跟著更新（雙向同步，不管從哪邊改，兩邊都要一致）——確認正確同步。

## v2.78.0

- **無感智慧預設**：賽事名稱、日期、抽籤結果這幾個欄位存檔時，順手推論賽事系列、運動種類、狀態這幾個「大概率接下來也要填」的欄位。使用者提出的原始構想假設的是 `document.getElementById('race-name').addEventListener('blur', ...)` 這種個別欄位各自綁定監聽器的寫法，但這個專案的編輯表單其實是一套通用的 schema 驅動系統（`SECTION_BASIC` 欄位定義陣列＋單一個 `data-path` 委派事件處理），所有欄位存檔都會先經過同一個地方——先確認了這套既有架構，把推論邏輯整合進那個既有的存檔流程，不是另外接一組獨立的監聽器。
  - **原始構想也有幾處跟這個專案的實際資料模型對不上**，動手前先逐一核對：構想裡「21.0975km 自動選半程馬拉松」這類依距離切換運動種類的想法，這個專案的「運動種類」其實沒有半程馬拉松這個選項（半馬、全馬都算「路跑」，距離另外用一個獨立數字欄位記錄，不是運動種類的一部分）——距離判斷因此只保留唯一有意義的一條規則：超過 50 公里才切到「超級馬拉松」。「中籤率 100% 自動切換已報名」的想法，重新核對後改用更準確對應使用者意圖的欄位——這個專案本來就有一個「本次是否抽中」的核取方塊，比「這場賽事整體的中籤率數字」更直接對應「這次是不是真的抽中了」，用錯欄位的話，一場中籤率剛好 100%（沒有抽籤機制）但使用者根本還沒決定要不要報名的賽事，也會被誤判成已報名。
  - **每一項推論都有「只在還沒被動過」的保護條件**，用的是「這個欄位還是不是表單給的原始預設值」，不是「這個欄位是不是空的」——運動種類、狀態這兩個欄位本來就有預設值（不是空字串），一旦使用者自己選過別的，就算後來又去改了會觸發推論的欄位（名稱、日期），也不會被推論結果蓋掉。
  - 系列名稱的猜測用正則表達式剝掉名稱裡常見、但每年會變動的部分（西元年、民國年、「第X屆」，中文數字跟阿拉伯數字都認得），反覆剝、不是只套一輪，才能處理「2026年第10屆」這種年份跟屆數疊在一起的情況；關鍵字比對故意不用「水」「山」這種單一個字（這類地名本身就常帶這些字，例如「水上鄉半程馬拉松」是嘉義地名不是游泳賽事），只用「泳渡」「游泳」這種完整詞組降低誤判機率。
  - 被自動改掉的欄位會有一個短暫的綠色高亮動畫（2.2 秒淡出），讓使用者看得出「這裡剛被系統動過」，不是毫無感覺地被默默改掉；動畫播完之後不會殘留，不會讓之後跟這次無關的畫面重繪也誤觸發。
  - **測試方式**：正則表達式先獨立測過 10 種常見賽事命名模式（西元年開頭／結尾、民國年、阿拉伯數字屆數、中文數字屆數、年份跟屆數疊在一起、完全沒有年份屆數、整段都是年份這種應該回傳空值的邊界情況），全部正確；運動種類關鍵字判斷測過關鍵字優先於距離（一場只有 10K 但名稱寫明越野的賽事，正確判斷成越野跑而不是被距離判斷蓋掉）、以及刻意設計的地名誤判測試案例（「嘉義水上鄉半程馬拉松」確認不會被誤判成游泳賽事）。接著透過真正的欄位存檔流程（不是只測試獨立的猜測函式）逐一驗證：改名稱觸發系列自動填入、高亮動畫正確出現；手動填過系列之後再改名稱，確認不會被覆寫；改名稱觸發運動種類自動切換，確認既有的「自動套用裝備範本」機制正確跟著觸發；手動選過運動種類之後再改名稱，確認不會被覆寫；改成過去的日期觸發狀態自動切成已完賽；手動設過狀態（例如未起跑 DNS）之後再改日期，確認不會被覆寫回已完賽；勾選「本次是否抽中」觸發狀態自動切成已報名。最後跑過一次一般欄位（跟這次推論邏輯完全無關的欄位，例如號碼布編號）編輯，確認正常存檔不受影響。

## v2.77.1

- **全域拖曳匯入 GPX：沒有開著任何賽事時，改成直接建立新賽事**。上一輪這種情況只會跳出「請先開一場賽事」的提示，把使用者晾在原地——這次改成直接呼叫既有的「新增賽事」流程（`startCreate()`），把解析到的活動資料預先掛上去（等同於使用者自己點了「新增賽事」、又剛好用「匯入運動紀錄檔」選了同一個檔案），只需要補填賽事名稱就能完成，不用自己手動重新走一次。
  - **狀態、日期也一併預先帶好**：新增賽事表單原本狀態固定預設「考慮中」、日期固定空白，這兩個對「剛跑完丟一個 GPX 進來」的情境沒有意義——拖進來的是一份已經發生過的真實活動，不是在規劃一場還沒發生的比賽。這次改成可覆寫的預設值，這種情境下改成「已完賽」＋軌跡裡第一個時間戳記那天，一般走「新增賽事」按鈕的正常流程完全不受影響、還是原本的預設。
  - 日期是從 `summarizeTrackPoints()`（GPX/TCX/FIT 都會經過這個函式彙整）新增的 `startDate` 欄位取得，這是唯一新增的解析邏輯，其餘全部沿用既有函式——刻意用本機時區的年月日組字串、不是 `toISOString()`（那個是 UTC，半夜運動容易算錯成隔天）。
  - 為了不讓使用者卡在一個「半成品」的建立畫面裡，做法是先把檔案解析完、確定真的能解析成功，才轉進建立賽事流程；檔案本身壞掉或格式不對，會直接跳出錯誤提示、完全不會進入建立畫面，不會有「畫面看起來像在建立賽事、卻不知道剛剛那個檔案到底有沒有讀到」這種模糊狀態。
  - **測試方式**：直接還原了「沒開賽事、拖一個 GPX 進來」的情境，逐一確認：畫面正確轉成新增賽事表單、活動資料正確暫存、表單裡的狀態欄位顯示「已完賽」、日期欄位顯示軌跡當天日期（不是空白也不是今天）、既有的「✓ 檔案名稱」提示正確顯示；接著把賽事名稱填完、按下「建立賽事」，確認賽事真的建立成功、距離等數據正確套用；然後再走一次**正常**的「新增賽事」（不透過拖曳），確認狀態、日期都乖乖退回原本的預設值，沒有被上一次拖曳留下的殘值污染；也測過拖進一個內容根本不是合法 GPX 的檔案，確認正確跳出錯誤提示、完全沒有進入建立賽事畫面。

## v2.77.0

- **全域拖曳匯入**：GPX／TCX／FIT 軌跡檔、JSON 備份、JPG／PNG 照片可以直接從檔案總管拖進網站畫面裡任何位置匯入，不用先點開對應的「選擇檔案」按鈕。使用者原本提出的實作構想用的是 Tailwind CSS 跟這個專案完全不存在的架構（`state.currentView`、`handleGpxImport` 等等），拿來就用會整個對不上；改成先盤點這個專案實際既有的匯入邏輯（GPX/TCX/FIT 解析、JSON 合併、封面照處理、空間記憶迴廊的批次照片匯入都已經是獨立、可運作的功能），再設計一層「依副檔名分流、呼叫既有函式」的路由邏輯，不是把使用者的參考程式碼直接貼上去。
  - **關鍵風險：這個專案其實已經有兩套拖放功能**（補給時程規劃清單重新排序、裝備佈署看板），都是掛在 `document` 的 `dragover`／`drop` 監聽器。新增的全域監聽器如果沒有正確分辨「拖曳外部檔案」跟「拖曳頁面內部的項目」，兩套機制會互相干擾。解法是利用瀏覽器原生就有的區分方式：拖曳外部檔案時 `e.dataTransfer.types` 一定會包含 `'Files'`；頁面內部那兩套既有功能用的是 `setData('text/plain', ...)`，不會有這個類型。新的監聽器一律先檢查這個條件，不符合就完全不處理、直接讓事件正常往下走，不影響既有兩套功能。
  - 原本三個匯入入口（`json-file-input`／`gpx-file-input`／`cover-file-input` 的 `change` 監聽器）都是內嵌在各自的匿名函式裡，沒辦法從別處呼叫——先抽成 `importJsonFile()`／`importActivityFile()`／`importCoverImageFile()` 三個具名、可重複呼叫的函式，原本的「選擇檔案」入口跟這次新增的拖曳入口共用同一份邏輯，不是各自維護一份容易兜不起來的重複程式碼。
  - 圖片的「智慧分流」：只拖一張時設成封面照；拖好幾張、且這場賽事已經有帶時間戳記的 GPS 軌跡時，改走既有的「空間記憶迴廊」批次匯入（拍照時空對位，本來就是設計給一次匯入整批賽事照片用的）；沒有 GPS 軌跡可以對位的話，硬塞多張進去只會全部變成「未對位」、沒有實際用處，這種情況退回只取第一張當封面、其餘忽略，並用 toast 說明原因，不是靜靜漏掉使用者其他照片不給任何交代。
  - 拖曳提示層的毛玻璃效果直接用這個專案既有的 `--paper-glass`（本來就是半透明紙色調的設計變數）搭配 `backdrop-filter`，淺色／深色主題會自動套用對應色調，虛線邊框用 `--trail`（賽道綠）配色，跟這個 App 本身的視覺語言一致，不是套一份跟整體風格對不上的通用深色/黑色毛玻璃。
  - 過程中也建了一個原本沒有的共用小工具 `showToast()`——這次要在好幾個「沒有對應賽事可以匯入」「圖片處理失敗」的情境跳出提示，發現這幾處原本各自手刻一份幾乎一樣的 toast 建立邏輯（或者像封面照失敗那樣，原本只有 `console.error`、透過拖曳操作的使用者根本看不到瀏覽器主控台，等於完全沒有回饋），抽成一個共用函式順便補齊這個回饋缺口。
  - **測試方式**：用合成的 `DragEvent`＋`DataTransfer`（帶真正的 `File` 物件）模擬瀏覽器實際的拖曳行為，不是只看程式碼覺得應該沒問題。驗證過拖曳提示層的計數器機制正確防止「滑鼠移到子元素上又被誤判成離開」的閃爍問題；JSON、GPX（含「沒有賽事上下文時跳出提示」）、單張圖片（設成封面照）、多張圖片（分別測過有／沒有 GPS 軌跡兩種情境）全部個別測過一輪，確認匯入結果正確。**最關鍵的一項**：直接模擬既有補給時程清單重新排序、裝備佈署看板兩套功能實際會用到的 `dataTransfer`（`setData('text/plain','')`，不含 `'Files'` 類型），確認新的全域監聽器完全不會被觸發；另外也直接操作了真正的裝備看板拖放（切到看板檢視、對某個看板欄位觸發 `drop`），確認項目分類確實正確更動，這套既有功能沒有被這次新增的東西連帶影響。三語言版本的拖曳提示文字也個別測過，並用手機寬度截圖確認毛玻璃效果、虛線邊框、文字排版都正常顯示。

## v2.76.0

- **鞋款管理、補給品管理加進「個人資訊」面板**：原本這兩個管理介面只能從特定賽事的「裝備」「補給」分頁點進去，現在「個人資訊」面板從兩個頁籤擴充成四個（心率區間、身體數據、鞋款管理、補給品管理），不用先點進某一場賽事才找得到——多一個入口，不是多一份重複的資料或介面。
  - **實作上不是簡單複製貼上一份 UI**：鞋款、補給品清單各自原本已經是完整、獨立的彈窗（`shoeModalHtml()`／`nutritionDictModalHtml()`），這次先把「新增列＋清單本體」抽成共用函式（`shoeManagementBodyHtml()`／`nutritionDictManagementBodyHtml()`），獨立彈窗跟個人資訊面板的新分頁都呼叫同一份，不是各自維護一份會逐漸長歪的重複清單。
  - **這次改動裡最容易出錯、也最花時間確認的一點**：瀏覽器的事件監聽器只對自己那個 DOM 子樹裡發生的點擊有反應，原本「新增鞋款」「刪除鞋款」這些按鈕的點擊處理邏輯是綁在 `shoe-modal` 這個元素上——如果同一套清單畫面原封不動搬進 `profile-modal` 這個完全不同的元素裡，按鈕點了會完全沒反應，因為事件根本傳不到 `shoe-modal` 的監聽器。修法是把所有的新增／編輯／退役／刪除處理邏輯抽成共用函式（`handleShoeManagementClick()`／`handleNutritionDictManagementClick()`），獨立彈窗跟個人資訊面板的監聽器都呼叫同一份，兩邊的按鈕才會都有反應。
  - **另外處理了「兩個容器同時開著、只改了其中一個」的情況**：例如個人資訊的鞋款分頁開著沒關、又跑去某場賽事的裝備分頁點開獨立的鞋款彈窗做了退役動作——這種情況下兩個畫面都要跟著更新，不能只更新使用者剛剛點擊的那一個，不然關掉再打開才會看到最新狀態，中間那段時間畫面是舊的。新增了 `refreshShoeManagementUI()`／`refreshNutritionDictManagementUI()`，每次資料異動後檢查獨立彈窗跟個人資訊面板兩邊「當下是不是開著」，開著的都重新畫一次；上一輪為了雲端同步而加的 `refreshOpenGlobalListModals()`（背景同步完成後刷新畫面）也一併更新，涵蓋個人資訊面板這個新的容器。
  - **測試方式**：直接還原了「兩個容器同時開著、互相同步」的情境——在個人資訊的鞋款分頁新增一雙鞋，確認資料寫進去之後，另外開啟獨立的鞋款彈窗看到同一雙鞋（不是空的、不是兩份各自獨立的清單）；接著在獨立彈窗把這雙鞋退役，確認個人資訊面板（全程沒有關閉）畫面也跟著更新成「已退役」狀態，不用手動重新整理。補給品管理也用同樣的方式（新增、跨容器可見、刪除、另一邊同步更新）測過一輪。另外測過原本的心率區間、身體數據兩個分頁沒有被這次改動連帶影響，四個分頁切換都正常，也用手機版寬度截圖確認四個分頁標籤會正確換行、不會被撐出畫面外。

## v2.75.1

- **找到並修正真正的根源：app 啟動時「讀本機資料」跟「雲端合併」兩條流程之間，從來沒有互相等待過**。v2.75.0 上線後用同步診斷面板直接查證：手機上雲端資料完全正確（23 雙鞋、有正確的更新時間），但本機鞋款顯示是 0——代表雲端讀取本身沒問題，問題出在「讀到的雲端資料，合併進本機之後又被蓋掉」。追下去發現：`init()` 裡讀本機賽事／鞋款／補給品資料庫（`loadRaces()`／`loadShoes()`／`loadNutritionDictionary()`）跟 Firebase 判斷登入狀態、觸發 `handleAuthChange()` 是兩條完全獨立的非同步流程，過去沒有任何先後順序保證。如果 `handleAuthChange()` 剛好比本機讀取先跑完，合併函式讀到的 `shoes`／`state.races` 還是初始的空值，會把雲端資料當成唯一內容合併進去；緊接著本機讀取才跑完，用「整個蓋過去」而不是「合併」的方式覆寫這幾個變數，剛剛合併好的雲端資料就這樣被蒸發，本機又變回空的。
  - **這個問題原則上不只影響鞋款、補給品資料庫，賽事資料理論上也有同樣的風險**（`mergeRacesIntoState()` 一樣是先讀當下的 `state.races` 再合併，時序一亂就會被後面的 `loadRaces()` 蓋掉）——只是這次剛好先在鞋款上被抓到。這也是為什麼先前這個問題只在手機上重現、電腦版一直正常：本機資料庫讀取的速度（IndexedDB）在不同裝置、不同瀏覽器上快慢不同，桌機上長期剛好是「讀取先跑完、雲端合併後跑」這個安全的順序，沒有踩到過這個時序洞；手機上剛好相反，才會每次都重現。這不是手機專屬的 bug，是兩條路徑原本就沒有協調過，只是機率在不同裝置上不一樣。
  - 修法是讓 `init()` 把本機讀取那段 `Promise.all(...)` 存進一個共用變數（`localDataReady`），`handleAuthChange()` 動用 `shoes`／`state.races` 這些共用變數之前，先 `await` 這個變數，確保本機資料一定讀取完成、順序固定之後才開始合併，不會再被晚到的本機讀取蓋掉——不管兩條流程在哪一台裝置上誰先誰後，結果都會一樣。
  - **測試方式**：這種計時類的 bug 光看程式碼容易誤判「應該沒問題」，這次直接寫測試去重現真實的競速情境，不是憑印象覺得改對了——先讓 `localDataReady` 停在一個刻意不 resolve 的 Promise（模擬本機讀取還在跑），同時啟動 `handleAuthChange()`（帶著雲端有 23 雙鞋的假資料），確認合併流程確實卡住、沒有提早動用還是空的 `shoes`；50 毫秒後才放一雙「裝置本機才有」的鞋進去、resolve 那個 Promise（模擬本機讀取姍姍來遲），確認合併完成後這雙「後來才讀到」的本機鞋跟雲端 23 雙鞋都正確保留、沒有互相蓋掉。同樣的情境也對賽事資料的合併路徑重新測過一次，確認這條路徑一樣受到保護。另外測過一般情況（本機讀取正常先跑完、沒有時序問題）跟登出（沒有登入使用者）這兩種常見路徑沒有被這次改動影響。

## v2.75.0

- **新增同步診斷面板**：v2.74.5 部署後使用者回報手機端還是看不到鞋款資料，但先前已經用電腦版主控台直接查證過雲端資料本身完全正確（23 筆、時間戳記是最新的），繼續排查下去卡在「手機端沒有方便的主控台可以查」——每一步都要先確認能不能借到電腦、接不接得上遠端偵錯，來回一次要好幾個問題才問得完。這次直接把這個排查能力做進 App 本身：登入後登入資訊旁邊多一個「同步診斷」按鈕，點開會顯示 App 版本、登入帳號、本機鞋款／補給品資料庫筆數，按「立即查詢雲端資料」可以直接查 Firestore 裡實際存的筆數跟最後更新時間（或直接顯示查詢失敗的錯誤原因），手機上不用開發者工具就能拿到跟電腦版主控台指令一樣的資訊。
  - 開發過程中抓到自己寫的一個邏輯錯誤：一開始用同一個變數的 `null` 值同時代表「還沒查詢過」跟「查詢完成、雲端沒有這筆資料」兩種不同狀態，導致「雲端從來沒同步過」這種情況畫面上什麼提示都不會顯示（會誤判成「還沒查詢過」）。改用獨立的旗標分開這兩種狀態，兩種情況現在都會正確顯示對應的文字。
  - 過程中也發現面板文字裡「標籤：數值」的冒號寫死在畫面模板裡、沒有放進可翻譯的字串本身，導致英文版也顯示中日文慣用的全形冒號「：」，不符合英文排版習慣。已修正成冒號放進各語言字串內部，跟這個 App 其他地方「標籤：數值」的既有寫法一致（中日文全形冒號、英文半形冒號+空格）。
  - **測試方式**：分別測過三種雲端查詢結果——正常回傳資料（顯示筆數與時間戳記）、雲端從來沒有這筆資料（顯示「從來沒有成功推送過」）、查詢過程出錯（顯示錯誤訊息）——確認三種文字都各自正確顯示，不會互相蓋掉或顯示空白。三語言版本的標點符號也重新截圖比對過。另外測過沒有登入時開啟這個面板不會出錯、面板本身不影響既有的登入／登出／鞋款管理等功能。

## v2.74.5

- **修正真正的 bug：鞋款／補給品資料庫彈窗如果在雲端同步完成前就先打開，同步完成後畫面不會自動更新**。這次是接續 v2.74.0 那個雲端同步修正，實際排查使用者手機看不到鞋款資料的過程中找到的第二個問題——用主控台直接查證過雲端資料本身完全正確（23 雙鞋、時間戳記是最新的），推送跟拉取的邏輯都有確實執行，問題出在畫面沒有跟著更新：登入後「抓雲端資料→合併→推回雲端」這一整段是非同步的，需要一小段時間；如果使用者在這段時間跑完之前就先點開了鞋款管理（例如打開網站想確認鞋款在不在，直覺上會立刻點進去看，比這段非同步流程跑完還快），畫面當下顯示的會是「同步完成前」的內容（很可能是空的），而 `renderAll()` 並不包含鞋款、補給品資料庫這兩個彈窗——它們只有在明確呼叫 `openShoeModal()`／`openNutritionDictModal()` 時才會重新畫，資料到齊後不會自動刷新畫面，除非使用者自己關掉彈窗再重新打開一次。
  - 修法是新增 `refreshOpenGlobalListModals()`，登入合併流程跑完之後檢查這兩個彈窗當下是不是開著，開著的話直接重新畫一次，不用使用者自己關掉重開才看得到剛同步回來的資料。
  - **測試方式**：直接還原了使用者回報的確切情境——先把本機鞋款清空、打開鞋款管理彈窗（畫面顯示「尚未新增鞋款」），再模擬雲端同步帶回 23 雙鞋（跟主控台實際查到的雲端資料一致），驗證彈窗在保持開啟的狀態下正確從空清單變成顯示全部 23 雙，不用手動關閉再打開；同樣的情境也測過補給品資料庫彈窗。另外測過兩個彈窗都沒開著時，這個新函式是安全的空操作，不會誤觸發或報錯，也沒有影響到平常點「管理鞋款」正常開啟彈窗的既有行為。

## v2.74.4

- **修正瀏覽器主控台的棄用警告**：`<meta name="apple-mobile-web-app-capable">` 這個舊式標籤已被標記為棄用，Chrome 系瀏覽器每次載入頁面都會印一次警告，建議改用標準化的 `<meta name="mobile-web-app-capable">`。查證過正確做法不是直接取代——舊版 iOS Safari（以及部分 iOS 的啟動畫面行為）只認得舊的 Apple 專用標籤，拿掉它會有讓「加入主畫面」全螢幕模式在舊裝置上失效的風險，實務上多個專案都是保留舊標籤、額外加上新標籤，不是二選一。這次採用同樣的加法式做法：新標籤加進去，舊標籤原封不動保留，純粹是多一行 `<meta>`，沒有動到任何 JavaScript 邏輯。

## v2.74.3

- **空間記憶迴廊（照片時空對位）：資料不足時不再整段消失，改顯示原因說明**。使用者回報「大部分賽事都看不到這個功能，只有一場看得到」，查證後確認原因：這個功能需要 `route.timedTrackPoints`（帶時間戳記的 GPS 軌跡），這個欄位是這個功能上線（v2.68.0）之後才開始存的，在那之前匯入的 GPX／TCX／FIT 只存了經緯度、沒存時間戳記——而且這件事沒辦法回頭補，時間戳記本身沒有存在任何地方，唯一還留著的只有原始檔案。這不是 bug，是已知、無法回頭修的資料限制，但問題出在**畫面上完全沒有任何線索**：原本 `geoPhotoSectionHtml()` 一旦資料不足就直接回傳空字串，整段憑空消失，逼得使用者只能來問才知道原因。
  - 修法是分兩種情況：完全沒有匯入過 GPS 軌跡的賽事（例如手動輸入），維持原樣整段不顯示，這種賽事本來就跟這個功能無關，不需要多一段提示；**曾經匯入過 GPS 軌跡、但是是舊格式（只有經緯度）** 的賽事，現在會顯示一段說明，講清楚原因（功能上線前匯入的、當時沒存時間戳記）跟怎麼解決（重新匯入原始 GPX／TCX／FIT 檔案），順便提到開放水域賽事適用的 GPS 降噪也是靠同一份資料、會一起解鎖。
  - **測試方式**：分別驗證過三種情境——完全沒有 GPS 軌跡的賽事（確認還是整段不顯示，不會被誤判成「舊格式」而跳出不相關的提示）、有舊格式軌跡（經緯度、無時間戳記）的賽事（確認正確顯示新的說明文字）、有新格式軌跡（含時間戳記）的賽事（確認完整功能——拖曳上傳區、時間偏差校正滑桿——照常顯示，沒有被這次改動連帶影響）。三語言版本的新增說明文字也個別測過，最後跑了一次全站回歸確認沒有連帶壞掉的地方。

## v2.74.2（程式碼品質：B2，Blob 下載樣板抽出共用函式）

依需求（B2）把 10 處「`createObjectURL` → `<a download>` → `click` → `revokeObjectURL`」的重複樣板抽成共用的 `downloadBlob(blob, filename)`，另外把 5 處（其中一處在 `downloadICS()` 的呼叫端、不在原本列出的 10 處清單裡，一併處理了）重複的檔名清洗正則抽成 `safeFilename(name)`。

- **動手前先重新盤點了一輪**：因為 B1 改完之後全檔行號都跟著動了，先用 `grep` 重新定位所有 `createObjectURL` 出現的位置，確認還是 10 處，再逐一看過每一處的完整程式碼，不是照著舊的行號改。
- **發現一處原本就存在的不一致，順帶決定怎麼處理**：10 處裡有 9 處是 `<a>` 元素只建在記憶體裡、從來沒接進 DOM 就直接 `click()`；只有匯出 Notebook 那一處原本就有接進 DOM 再移除。抽成同一個共用函式勢必要選一種寫法，這裡選擇統一改成「接進 DOM → 點擊 → 移除」——這是比較保守、相容性較好的寫法（部分瀏覽器對沒接進 DOM 的元素直接 `click()` 偶爾不會觸發下載），跟需求本身要的 `revokeObjectURL` 延遲修正是同一個「讓下載更可靠」的方向，所以一併做了，但這是在需求明確要求之外我自己做的決定，跟需求本身要求的那一項（下面這條）分開列出來，不是混在一起沒有說清楚。
  - **需求明確要求的部分**：`revokeObjectURL` 從點擊完當下同步呼叫，改成 `setTimeout(...,1000)` 延遲一秒才撤銷——iOS Safari 對「blob URL 剛觸發下載就被撤銷」偶爾會直接讓下載失敗，這是已知的相容性問題，延遲撤銷不影響其他瀏覽器原本就正常的行為。
- **測試方式**：先直接測 `downloadBlob()` 本身的內部行為——用攔截 `appendChild`／`revokeObjectURL`／`setTimeout` 的方式確認：點擊當下元素真的在 DOM 裡、`revokeObjectURL` 不是同步呼叫、撤銷確實是排程在 1000ms 之後，不是憑印象覺得改對了。接著把 10 處全部個別觸發過一次真的下載，用一個刻意塞滿特殊符號（`/:*?"<>|`）的賽事名稱測那 5 個有套用 `safeFilename` 的下載，確認檔名正確去掉符號；其餘固定檔名或不需要清洗的下載也各自確認檔名輸出正確，不是只測一兩個就當作全部沒問題。最後跑了一次全站回歸（賽事讀取、EPP、榮譽櫃）確認沒有連帶壞掉的地方。

至此 B1、B2 兩項程式碼品質改善都完成。

## v2.74.1（程式碼品質：B1，存讀樣板抽出共用函式）

純內部重構，沒有改變任何使用者看得到的行為（兩個小修正除外，見下方）——依需求（B1）把 7 組 `persistXxx()`／`loadXxx()`（賽事、範本、個人資訊、鞋款、補給品資料庫、同步覆寫紀錄、徽章解鎖紀錄）裡各自重複的「依 `hasArtifactStorage` 走 `window.storage` 或 `idbSet`／`idbGet` 分支、JSON 序列化／解析」抽成共用的 `saveJson(key, value)` / `loadJson(key, fallback)`，每組 `persistXxx` 都改成一行呼叫。

- **動手前先盤點了每一組的差異**，確認抽出來的共用函式不會不小心抹掉各自的特殊行為：`loadRaces` 讀完要過一輪 `migrateRace`、`loadUserProfile` 讀完要過 `migrateUserProfile`、`persist()` 存完要開關儲存錯誤橫幅並觸發雲端同步、`persistShoes`／`persistNutritionDictionary` 存完要觸發上一輪才加的全域清單雲端同步、`persistSyncOverwrites` 存之前要先 `.slice(0,20)` 只留最近 20 筆——這些全部維持原樣，留在各自的 `persistXxx`／`loadXxx` 裡，沒有被塞進共用函式。
- **`saveJson`／`loadJson` 刻意不包自己的 `try/catch`**：原本每組 `loadXxx` 的 `try/catch` 包的是「讀取＋解析＋讀完後的轉換」一整段，不是只有讀取本身——如果共用函式自己把錯誤吞掉，轉換那段（例如 `migrateShoe`）一旦出錯就不會被外層接住，等於悄悄改變了行為。這次錯誤處理還是留給每組函式自己既有的 `try/catch`，用一個刻意讓轉換步驟拋錯的測試案例確認過：拋錯後確實還是乖乖退回空陣列，跟改之前行為一致。
- **兩個小修正**（需求裡順便要求的）：`persistSyncOverwrites`／`persistBadgeUnlocks` 原本失敗時是完全靜默的 `catch(err){}`，這次統一成跟其他 5 組一樣至少 `console.error`，方便以後真的存檔失敗時能在主控台看到，不會毫無線索。
- **測試方式**：7 組全部逐一驗證過存進去、清空記憶體變數、讀回來，資料要一致；轉換步驟（`migrateShoe` 等）留下的預設值也逐一比對過；`syncOverwrites` 的 25 筆存檔後正確裁到 20 筆；用模擬拋錯的方式確認新加的兩個 `console.error` 真的會印出來、且訊息文字正確；也確認過原本就有的 `persist()` 失敗路徑（顯示儲存錯誤橫幅）沒有跟著壞掉。最後跑了一次全站回歸（賽事讀取、EPP、榮譽櫃、檔案匯出）確認沒有連帶壞掉的地方。

B2（10 處 Blob 下載樣板、`downloadBlob`／`safeFilename`、iOS Safari 的 `revokeObjectURL` 延遲修正）這次沒有動，留到下一輪。

## v2.74.0

- **修正真正的 bug：鞋款管理、補給品資料庫從來沒有被雲端同步過**。使用者回報「換一台電腦登入，鞋款管理的資料全部消失」，查證後確認這不是同步失敗，是這兩份清單本來就完全沒有接進雲端同步機制——`persistShoes()`／`persistNutritionDictionary()` 只會寫進本機 IndexedDB，從來沒有呼叫過推上 Firestore 的邏輯；反觀賽事資料的 `persist()` 每次都會接著觸發雲端同步。這個落差存在多久，這兩份清單的資料就流失了多久，不是最近才壞掉。
  - 修法是照著賽事已經在用的「本機異動防抖 0.8 秒後才真的送出」節流寫法，補上鞋款／補給品資料庫自己的一組（`scheduleGlobalListsSync`／`cloudSyncGlobalLists`），兩份清單共用同一個 Firestore 文件（`users/{uid}/meta/globalLists`）、同一個計時器一起送出。登入時除了原本就有的賽事讀取＋合併，也一併補上這兩份清單的讀取＋合併，讀到之後立刻寫回本機、再推一次上雲端，讓兩邊在登入當下就同步完成，不用等下一次編輯才觸發。
  - 這兩份清單裡的項目沒有像賽事那樣各自記錄 `updatedAt`，沒辦法比較「雲端還是本機比較新」，合併規則因此比賽事簡單：依 id 取聯集，同一個 id 兩邊都有但內容不同時保留本機版本，不彈跳「偵測到分歧」的通知——預期這種情況只會在「修好這個 bug 之前，兩台裝置各自累積出不同鞋款」這種一次性情境發生。
  - `firestore.rules` 加了一條新規則（`users/{uid}/meta/{docId}`，比照賽事跟推播權杖已經在用的「限本人讀寫」規則），沒有這條的話這次新增的 Firestore 寫入會被安全規則擋下來，光改程式碼不會真的生效——如果目前已經部署了自己的 Firebase 專案，記得把這份更新後的 `firestore.rules` 重新部署上去（`firebase deploy --only firestore:rules`），只更新 `index.html` 不會讓這個修正真的運作。
  - **測試方式**：這次改動橫跨兩種完全不同的執行環境——一般的分類 `<script>`（用 `new Function()` 驗證語法）跟這次有改到的 Firebase `<script type="module">`（改用 `node --check` 驗證，`new Function()` 沒辦法解析 `import` 語法）——過程中確認了原本的語法檢查流程從頭到尾都沒有真的驗證過這個 module 區塊，這次一併補上這道檢查。核心同步邏輯用一組模擬的 Firestore 後端（純 JS 物件模擬雲端資料庫的讀寫行為）直接測過：情境一還原了使用者回報的確切 bug（裝置 A 本機有鞋款、雲端是空的，登入後正確推上雲端；裝置 B 本機是空的，登入後正確從雲端拉回同一雙鞋）；情境二測過聯集合併（雲端獨有、本機獨有的項目都正確保留）跟 id 衝突時保留本機版本；情境三測過雲端還沒有任何資料（`null`）時不會噴錯誤、本機資料原封不動。

## v2.73.1

- **分段配速一覽：兩處文字調整**：
  - 距離欄位原本是「0-1K」「1-2K」這種區間標示法，改成累計公里數「1K」「2K」，跟大部分跑錶、跑步 App 的分段標示習慣一致。
  - 配速欄位的粗體大字（GAP，坡度校正配速）後面加上「(GAP)」，灰色小字（實際配速）原本結尾是「實際」，改成完整的「(實際配速)」，兩個數字各自代表什麼一眼就能分辨，不用另外對照上面說明文字裡的「粗體是 GAP、小字是實際」。順手也更新了 USAGE.md 裡漏掉的一段——上一輪幫心率區間加的柱狀圖示，其實也套用在這張分段配速表的心率欄位上，但文件當時沒有一併寫到。

## v2.73.0

- **氣候與表現散佈圖：可讀性重做**：依使用者回饋「完全看不懂這張圖的涵義」重新評估過這個功能，結論是留下來、但原本的執行方式確實有兩個各自獨立的問題要修：
  - **統計可信度問題**：原本資料點只要滿 3 場就會畫圖，3 個點畫出來的趨勢線在視覺上看起來會跟資料齊全時一樣「篤定」，但統計上完全不是一回事；而且程式其實早就有算 R²（判定係數，這條線解釋了多少變異），只是從來沒有顯示給使用者看過。這次把最低門檻拉高到 4 場（跟 EPP 模型的高溫折損係數用同一個標準，同樣是「溫度 vs 表現」的迴歸沒理由套用不同門檻），R² 跟採用的賽事數這次一定會顯示在文字裡，不是藏起來；R² 低於 0.4 時，趨勢線改用更淡、更稀疏的虛線呈現，下方多一行提醒文字，讓「這條線不太可信」這件事从視覺本身就看得出來，不是只放一行小字警語、圖案本身還是畫得信心滿滿。
  - **純視覺可讀性問題**：原本兩個軸完全沒有標題，只有數字，要用猜的才知道橫軸是溫度、縱軸是配速衰退；金色點（PB）跟藍色點（其他賽事）的差別只能靠滑鼠移上去看提示文字才知道，截圖或手機上根本看不出來；綠色色塊（最佳作戰溫度帶）在圖上完全沒有標籤，要另外讀下面的文字才知道那塊是什麼意思。這次補上了橫軸標題「體感溫度」、縱軸標題「配速衰退（相對自己 PB）」、右上角圖例（PB／其他賽事）、色塊上直接標「甜蜜點」字樣，另外也在 0% 到最低值中間補了一條淡淡的參考格線，原本兩端有標數字、中間完全沒有參考線。
  - **測試方式**：分別驗證過剛好 3 筆資料時正確完全不顯示、4 筆資料時正確顯示；設計了一組乾淨的線性資料（R²≈1.0）確認趨勢線正常呈現、文字正確顯示 R² 與樣本數；另外設計了一組刻意打亂順序的資料（R²≈0.4）確認趨勢線正確變淡、警語文字正確出現。過程中也發現甜蜜點色塊剛好落在圖表最左側時，色塊上的「甜蜜點」標籤會跟縱軸標題文字重疊，已經把標籤從色塊上方移到色塊內部靠上的位置，兩者不會再互相干擾。三語言版本的新增文字（軸標題、圖例、甜蜜點標籤、R² 警語）都個別測過。

## v2.72.1

- **年度回顧長圖卡：年份選擇改成下拉選單**：原本是一排年份按鈕（一次最多顯示 5 個，超過就看不到），改成一個下拉選單＋「下載」按鈕，選好年份再按下載。順便拿掉了原本「最多顯示 5 個年份」的上限——按鈕排一排超過 5 個會擠、會換行，下拉選單沒有這個問題，累積再多年的紀錄也能選到最早那一年。測試上確認過：選單預設帶出最新一年、選別的年份後下載確實抓到選單當下選的那個年份（不是還停在預設值）、超過 5 個年份時全部都出現在選單裡、三語言版本文字正確。

## v2.72.0

- **EPP 逐公里切片預測引擎**（EPP 模型原始需求的最後一個方向，前兩個——預測引擎骨架、折損瀑布圖——先前已完成，這次把當時的簡化版換成真正逐段計算的版本）：
  - 原本 `computeRaceWaterfall()` 把全場總爬升攤平成一個平均坡度來算折損，這次新增 `computeSegmentPredictions()`，把賽事切成固定長度的區段（預設每 1 公里，超過 15 公里的賽事自動放寬到每 3 公里），依照 `route.elevationProfile`（海拔剖面）逐段內插算出每一段自己的坡度，各自套用爬升折損係數跟高溫折損係數，而不是整場均勻打折——`computeRaceWaterfall()` 現在會優先呼叫這個新引擎，只有賽事沒有海拔剖面資料時才退回舊的全場平均簡化版。
  - **下坡段刻意沒有把折損夾在「最多只會變慢」**：坡度計算用的是「這一段起訖點的淨海拔變化」（下坡是負值），跟原本用來迴歸出爬升折損係數的 `splitGradePct()` 算法完全一致——這代表下坡段套用同一個係數，算出來的折損本來就會是負值（比基準配速快），這正是原始需求想看到的「技術下坡優勢」，不是額外加的規則，是同一個係數自然延伸出來的結果。全場總計的瀑布圖版本（`computeRaceWaterfall()` 沒有海拔剖面時用的那個簡化版）維持原本「最多只會變慢」的保守夾法，因為那個版本連上坡下坡都分不出來，沒有依據可以給折扣。
  - **氣溫也是動態的**：如果賽事有填起跑時間、且日期在 7 天內抓到了逐時天氣預報，每一段的氣溫會依照「模擬跑到這一段時的預估時鐘時間」去查對應時段的預報值，用的是既有的 `formatClockTimeOffset()`／`findClosestHourIndex()`（原本給天氣自動回填用的工具函式，這次直接複用，不是重寫一套）——清晨出發、中午還在賽道上的長距離賽事，後段抓到的溫度會比前段高，符合原始需求裡「氣溫隨時間變化」的描述。查不到逐時預報、或模擬時鐘跨到隔天（逐時預報只涵蓋單一天）時，退回整場統一套用同一個溫度。
  - **UI**：預測時間下方新增「顯示逐公里預測」的展開按鈕（沒有海拔剖面資料時改顯示原因說明），展開後是一張表格，列出每一段的距離範圍、淨爬升、預測配速、預估抵達時刻，可以捲動瀏覽，不會把整個賽事詳情頁撐得很長。
  - **測試方式**：核心引擎先設計了一個對稱的爬升 100 公尺、下降 100 公尺的假想賽道，逐段手算跟程式輸出逐項核對，包含下坡段折損算出負值（比基準快）這件事——手算結果跟程式輸出完全吻合，全場加總的爬升折損淨額也剛好等於 0（一半上坡的折損被一半下坡的優勢完全抵銷），驗證了模型在這種對稱情境下的行為符合預期。動態氣溫查詢另外設計了一個橫跨多個小時、氣溫逐漸上升的假想清晨賽事，確認查到的溫度真的會隨著模擬時鐘往後推移而改變，不是整場卡在起跑當下那一個小時的溫度。過程中也抓到一個真的會讓資料悄悄失真的 bug：沒有海拔剖面資料的賽事，原本新引擎會直接用「每段坡度都是 0」算出一個爬升折損掛零的答案，而不是老實承認算不出來、讓呼叫端退回用 `elevationGainM` 這個比較粗略但至少有資料的簡化版——已修正成沒有海拔剖面就直接回傳空值，交給上層正確退回。沒有海拔剖面時的文字說明、逐公里表格的三語言版本、以及既有的瀑布圖／採用為目標按鈕沒有因為這次改動跟著壞掉，都個別測過。

## v2.71.0

- **疲勞模式：強光／色盲適配**（第三個原始方向，前兩個方向——滑動操作介面、語音與震動回饋——先前已完成）：
  - **心率區間形狀圖示**：心率區間（Z1~Z5）原本只靠顏色分辨，色盲或色弱使用者（尤其紅綠色弱，偏偏綠→金→紅這種漸層正好是最容易搞混的組合）光看顏色很難分辨區間。新增一個不依賴顏色的「柱狀強度」SVG 圖示，5 根由矮到高的直條，區間 N 就把前 N 根塗實、其餘畫空心——概念上類似訊號強度格數，掃一眼柱子填了幾根就知道區間高低，把圖示轉成灰階或黑白列印也一樣看得懂，不是只在有顏色的螢幕上才成立。這組圖示套用在所有既有的心率區間顯示：賽後紀錄的分段配速表、「個人資訊」的心率區間設定表，以及疲勞模式新增的心率區間對照卡。
  - **疲勞模式心率區間對照卡**：疲勞模式原本沒有任何心率相關顯示。這次盤點後發現 App 本身沒有跟心率錶做藍牙連線、讀不到賽事進行中的即時心率，所以新增的是一張「賽前參考卡」而不是假裝在讀取即時數據——列出這場賽事對應運動種類的心率區間表（bpm 範圍＋顏色＋上述形狀圖示），供使用者拿自己手錶顯示的心率數字來對照，卡片上也用文字清楚寫明「這是參考區間，不是即時讀數」，避免被誤會成正在讀取心率。優先用「個人資訊」設定好的個人化儲備心率區間，沒設定時退回用這場賽事填的最大心率粗估，兩者都沒有時顯示「還沒有設定安靜心率／最大心率」的提示，不會顯示錯誤或空白表格。
  - **電子紙高對比模式**：疲勞模式原本固定是純黑底配金色文字（省電、也適合暗處閱讀），但強烈陽光下螢幕自體發光常被環境光蓋過去，對比反而變差。新增一個可切換的高對比配色（暖白底、近黑粗體字，去除原本偏灰偏暗的次要文字顏色），模擬電子紙「靠反射閱讀」而非「靠自體發光」的邏輯，戶外大太陽下會比預設深色主題更容易辨識。這個選擇存在瀏覽器本機（跟深色模式偏好用同一種存法），下次開啟疲勞模式自動套用上次的選擇，不用每次重新切換。
  - 兩個新按鈕都做成大面積、方便誤觸也沒關係的大按鈕，維持疲勞模式一貫「不需要精準點擊」的設計原則，沒有為了塞進新功能而破壞既有的操作邏輯（原本的左右滑動手勢完全不受影響）。
  - **測試方式**：心率區間圖示除了正常截圖檢查，還把截圖轉成灰階圖片重新檢視，確認拿掉顏色資訊後 5 個區間依然可以只靠柱子數量分辨。電子紙模式做了正常／心率卡兩種畫面的截圖比對；過程中發現區間卡片裡「Z1」文字本身用的是既有的區間顏色（淺灰），在暖白底的高對比模式下對比度不夠，已修正成在高對比模式下強制使用近黑色，柱狀圖示才是真正負責顏色編碼的部分，文字標籤本身不需要，也不應該犧牲可讀性去兼顧著色。另外也測過：沒有任何心率資料時的空狀態、只有賽事層級最大心率（沒設定個人資訊）時的粗估退回、偏好設定跨頁面重新整理後仍然記得、三語言版本文字正確、以及既有心率顯示功能（賽後分段配速表）沒有因為新增圖示而跟著壞掉。

## v2.70.0

- **開放水域 GPS 降噪**：路線軌跡地圖下方新增「套用開放水域 GPS 降噪」按鈕（三鐵／游泳、且有含時間戳記的 GPS 軌跡才會出現），用卡爾曼濾波（等速度運動模型）平滑化開放水域游泳段的 GPS 軌跡。按下去會先算出降噪前後的總距離差異給你比較，不會直接套用；確認套用時可以選「只更新地圖軌跡」或「連同賽事距離欄位一起更新」。
  - **參數不是憑感覺選的**：查了實測資料，開放水域游泳時手腕每划一次手就沒入水中斷一次 GPS 訊號，單點誤差常見到 10 公尺、甚至到 50 公尺——濾波用的量測雜訊標準差（15 公尺）就是抓這個居中值；另一個參數（模型允許的加速度變異）則是用合成測試資料（已知真實路徑＋人工加入同樣 15 公尺標準差的雜訊，涵蓋直線跟繞浮標彎道兩種形狀、各自跑 5 組不同亂數種子）挑出在兩種情境下都穩定有效的數值，不是只在單一組測試資料上湊出來的。
  - **測試比平常更嚴格一截，因為這是矩陣運算，容易寫錯又不容易肉眼抓到**：先用 Python／numpy 寫了一份參考實作，在已知真實路徑＋人工雜訊的合成資料上驗證濾波確實有效（直線情境位置誤差穩定降低 39~53%，彎道情境的總距離誤差從六到七成降到約二成，兩者都橫跨 5 組不同亂數種子確認不是巧合），才把同一套邏輯搬進 JavaScript；搬完之後沒有假設兩邊寫得一樣，而是把同一組測試資料餵給 JS 版本，逐點比對輸出，結果跟 Python 版本的誤差在百萬分之一公尺等級（純浮點數精度雜訊）。過程中也抓到一個自己造成的 bug：改 `summarizeTrackPoints()` 附近程式碼時，中間有一次編輯不小心把這個函式自己的宣告那一行刪掉、只留下函式本體，導致整個檔案語法錯誤——用 `node --check` 定位到確切行號後修正，重新驗證過整份程式碼語法正常、GPX 匯入流程沒有跟著壞掉。
  - 功能整合測試用一段模擬的開放水域游泳雜訊軌跡（15 公尺標準差雜訊、時間間隔刻意不規則，模擬手錶每次划手斷訊的狀況）驗證過：原始距離膨脹到 1.31km（實際路徑約 0.8km），降噪後回到 0.71km，跟真實距離接近很多；「只更新地圖」「連同距離一起更新」「取消」三種操作分別測過各自的狀態變化，也確認過陸地型賽事（路跑）即使意外有 GPS 軌跡也不會出現這個按鈕。
  - 跟賽事詳情頁裡其他「模型算出來的數字」功能（EPP 環境折損預測）一樣，套用前一定會先讓你看到差異、由你自己決定要不要採用，不會有任何欄位被靜默覆寫。

## 網站地圖：CP1～CP4 底下的項目改成純文字，不再逐一超連結

- 依需求把 CP1～CP4 底下總共 20 個項目，從各自可點擊的連結改成純文字——原本這 20 個連結全部連到同一個主程式網址，逐一做成連結反而容易讓人誤以為每個項目會各自跳到不同地方。首頁介紹（START）那一項因為是真的連到功能介紹頁（`/about/`）這個不同的頁面，維持連結。
  - 對應把上方「關於這份地圖」的說明文字也改了，原本寫「點下去都會開啟同一個主程式」，現在改成「是純文字說明…不是可以個別點擊的連結」，跟畫面上的實際行為保持一致。
  - CSS 上新增一個共用的 `.item-row` 排版類別，靜態項目用 `<div class="item-row">`、首頁介紹那一項用 `<a class="item-row">`，兩者外觀排版完全一樣，但只有真的是連結的那個會有底線消失、hover 變色這些「這個可以點」的視覺提示，靜態項目滑鼠移上去游標維持一般箭頭，不會誤導。
  - 測試上，用瀏覽器實際檢查過每個項目的 DOM 結構跟滑鼠游標樣式，確認 20 個靜態項目真的都沒有 `<a>` 標籤、只有首頁介紹那一項還是可以點的連結；手機版面也重新截圖確認排版沒有跑掉。

## 網站地圖：移除「專案文件」（GOAL）區塊

- 依需求把網站地圖頁（`sitemap/index.html`）最後一段連到 GitHub 上 README／USAGE.md／CHANGELOG.md／LICENSE 的「專案文件」區塊整段拿掉，路線現在停在 CP4（資料與工具管理）。一併清掉只有這個區塊在用、拿掉後變成沒人用的 CSS（`.is-terminus` 金色終點樣式、`.is-external` 外部連結配色），跟頁面 `meta description` 裡對這個區塊的文字描述，避免留下死程式碼或講不準的說明。

## 修正：功能介紹頁資料夾改名 `showcase-page/` → `about/`（不影響網站右下角版本號）

- **這是一個部署前就該抓到的錯誤，感謝使用者在問部署方式時順帶發現**：功能介紹頁的資料夾原本叫 `showcase-page/`，但這幾輪新增的每一個連結——README.md、`sitemap.xml`、HTML 網站地圖頁自己的「功能介紹頁」項目、甚至功能介紹頁自己內嵌的 `og:url`／`og:image` 標籤——全部都是指向 `/about/`，沒有一個指向 `/showcase-page/`。如果照原本的資料夾名稱直接部署，上面這些連結會全部連到不存在的網址，社群分享時的預覽圖也會抓不到圖。
- 修正方式是把資料夾整個從 `showcase-page/` 改名成 `about/`（含底下的 `images/` 子資料夾），讓實際檔案位置跟所有既有連結一致，而不是反過來把所有連結都改成配合 `showcase-page/`——因為 `/about/` 已經是目前唯一被使用者看得到的公開連結（README、sitemap.xml），改連結而不改資料夾，等於要同時改更多地方、風險更高。
- 頁面內部用來記住深色／淺色模式選擇的 `localStorage` 鍵名（`race-day-showcase-theme`）維持不變——這只是瀏覽器儲存用的任意字串，兩個頁面（功能介紹頁、網站地圖）本來就要用同一個鍵名才能共用主題設定，跟資料夾實際叫什麼名字無關，不需要跟著改。
- 重新驗證過：資料夾改名後，`images/` 底下 8 張圖片（含 `og-cover.png`）都還在；HTML 網站地圖頁裡 `../about/` 這個相對連結、功能介紹頁自己的 `og:url`／`og:image`，都已經跟實際檔案位置一致。

## sitemap.xml／robots.txt（新增，不影響網站右下角版本號——這兩個檔案 App 本身不會載入）

- **新增 `sitemap.xml`**（部署後在根目錄 `/sitemap.xml`）：收錄兩個真正可被搜尋引擎索引的頁面——主程式首頁（`/`）跟功能介紹頁（`/about/`）。**刻意排除**上一輪新增的 HTML 網站地圖頁（`/sitemap/`）：那個頁面本身就標了 `<meta name="robots" content="noindex">`，而「不要放 noindex 頁面」正是這次需求本身列出的排除原則之一——两份「網站地圖」剛好是一個很好的一致性檢查點。
  - **只列 `<loc>` 跟 `<lastmod>`，沒有加 `<priority>`／`<changefreq>`**：查證了 Google 跟 Bing 官方文件，兩家都明講這兩個欄位「會被忽略」，不是排名或爬取頻率的訊號——協定本身沒有廢除這兩個欄位、加了也不算錯，但加了只是徒增檔案內容跟未來要手動維護的假象，拿掉是精簡，不是漏做。`lastmod` 目前設定的日期反映的是這兩個頁面實際異動的日期，之後如果又更新了首頁或功能介紹頁的內容，記得手動同步這裡的日期——查證到的資料也提醒「用不準確的 lastmod 可能反而讓 Google 對這個網站的 sitemap 失去信任」，寧可少填也不要填假資料。
  - **這次的需求描述的兩種自動產生方式（CMS 外掛、或是 Next.js 這類框架的建置期套件）都不適用這個專案**：這個網站本身是「純靜態網頁、無建置流程、無框架」（見 README.md 技術架構），沒有 CMS 也沒有 build step 可以掛外掛或套件。對只有 2、3 個固定頁面、且新增頁面本來就是低頻率、需要動到程式碼的事件（像上一輪新增 HTML 網站地圖那樣）的專案來說，手動維護這份 sitemap.xml 反而是符合實際情況的做法，不是「應該自動化但沒做到」的缺口——真的哪天新增了新的獨立頁面，直接在這份檔案裡多加一個 `<url>` 區塊即可。
- **新增 `robots.txt`**（部署後在根目錄 `/robots.txt`）：`Allow: /`（整個網站本來就是公開內容，沒有需要擋爬蟲的後台或私人頁面）＋ `Sitemap:` 指向上面這份 sitemap.xml。這是目前讓搜尋引擎找到 sitemap 的標準做法——查證到 Google 跟 Bing 的「sitemap ping」網址（過去可以直接呼叫來通知爬蟲）已經正式棄用（Google 2023 年公告、Bing 同步跟進，兩者的 ping 端點目前呼叫都回傳錯誤狀態），現在的正確流程是宣告在 robots.txt 裡，加上到 Google Search Console／Bing Webmaster Tools 手動提交一次。
- **測試方式**：`sitemap.xml` 除了驗證過是合法的 XML（可以被解析），還進一步比對了 sitemaps.org 官方發布的 0.9 版 XSD schema，做了完整的 schema 驗證（不是只檢查格式合不合法，是真的比對這份 schema 定義的每個欄位規則），確認通過。
- **附帶觀察，這次沒有動手改**：盤點主程式 `index.html` 的 `<head>` 時，發現只有 `<title>賽事紀錄</title>`，沒有 `meta description`——不影響能不能被索引，但會讓 Google 在搜尋結果裡自動從頁面內容抓一段文字當摘要，不一定會抓得漂亮。這次任務範圍是 XML 網站地圖，沒有一併動這個，先記錄下來，之後有需要再處理。

## v2.69.0

- **新增網站地圖頁面**（`sitemap/index.html`，部署後在 `/sitemap/`）：用「賽道式」版面把整個網站現有的內容分門別類列出來——起點是功能介紹頁，CP1～CP4 依序是行事曆與總覽、賽事詳情頁六大分頁、賽事層級工具（更多操作選單）、資料與工具管理，終點是 GitHub 上的專案文件（README／USAGE／CHANGELOG／LICENSE）。視覺上沿用既有功能介紹頁（`about/`）的設計系統（同一組配色 CSS 變數、LXGW WenKai TC／Noto Sans TC／IBM Plex Mono 字型組合、深色模式），但版面刻意不是常見的卡片格線，而是一條垂直路線串起每個 CP 標記，呼應這個網站本身就是在記錄賽道的產品調性。
  - **這次先盤點了一件事，才決定怎麼設計連結**：「賽事紀錄」主程式是單頁應用程式，行事曆、賽事詳情、鞋款管理等功能沒有各自獨立的頁面網址，硬要假裝每個項目都能連到不同的網址會是誤導。頁面最上方用一段文字誠實說明這件事，CP1～CP4 底下每個項目點下去都連回同一個主程式網址，文字說明的是「進去之後要找哪個分頁或按鈕」，不是不同的頁面；只有起點（功能介紹頁）跟終點（GitHub 文件）是真正各自獨立的頁面。
  - 收錄原則採「精準收錄架構頁」：只列到分頁／功能群組這個層級（例如「裝備、補給與戰略」分頁本身，不會再往下列出補給品資料庫、配速試算等分頁裡的每個子功能各自一條），避免清單長到反而讓人更找不到重點。
  - 依使用者需求，把「網站地圖」連結固定放在功能介紹頁的頁尾（原本頁尾沒有這個連結，這次補上）；主程式本身沒有傳統網頁的頁尾（是功能型 App 介面，不是內容頁），改成放進主程式右下角「使用說明」彈窗最後的「其他」小節（三個語言版本都同步加了這一條），這是主程式裡最接近「頁尾／雜項連結」性質的既有位置，不會為了硬塞一個連結而改動主程式原本精簡的工具列版面。
  - 測試上，HTML 結構驗證過沒有標籤沒閉合的問題，淺色／深色模式與手機版面都截圖檢查過；「使用說明」彈窗新增的連結，三語言版本都各自點開確認文字跟連結網址正確。

## v2.68.0

- **空間記憶迴廊（照片時空對位）**：路線軌跡地圖下方新增照片上傳區，拖照片進來會自動讀取 EXIF 拍攝時間，跟這場賽事的 GPS 軌跡做時空對位，算出拍下那一刻的座標、海拔、心率、局部配速，變成地圖上的圓形縮圖圖釘，點下去彈出照片跟這些數據。上方有 ±120 秒的「相機時間與 GPS 時間偏差校正」滑桿，拖動會即時重新對位所有照片。只讀取照片檔案的中繼資料跟內嵌縮圖，不會把整張原始照片載入網頁。
  - **開發前先盤點了一件事，直接改變了整個實作方式**：原始需求描述的「在 GPX 軌跡陣列 `[lat, lng, time, ele]` 裡二元搜尋」，實測發現這個陣列在現有資料結構裡根本不存在——既有的 `route.trackPoints` 為了效能考量，從匯入那一刻起就只保留經緯度，時間戳記在賽事摘要算完之後就丟棄了。與其假裝這個功能能對所有賽事生效，這次新增了一個獨立的 `route.timedTrackPoints`（跟既有畫地圖用的 `trackPoints` 完全分開存，互不影響），GPX/TCX/FIT（重新）匯入時才會產生。**這代表這個功能只對這次更新之後（重新）匯入的賽事有效**，已經匯入很久的舊賽事需要重新匯入一次原始檔案才會出現這個功能。
  - **EXIF 解析改用開源套件 exifr，沒有照原始需求自己手刻 DataView 解析器**：跑步錶／相機廠牌眾多，EXIF/TIFF 格式的實務怪癖很多，手刻的解析器很難涵蓋所有真實世界的邊界案例，改用這套經過大量實戰測試、免費、MIT 授權、原生支援分段讀取（瀏覽器端預設一次只讀 64KB，跟原始需求描述的做法一致）的套件，跟這次專案裡 Leaflet 地圖套件的延遲載入模式一致（只有真的要用照片功能時才會去載入，不影響一般情況下的頁面載入速度）。
  - **測試方式比平常更嚴格一截，因為這個功能牽涉到二元搜尋＋線性插值這種容易寫錯边界條件的演算法，加上外部套件的真實行為**：插值引擎（座標／海拔／心率／局部配速）先設計了 7 組手算過的測試案例（包含剛好落在軌跡起點／終點的邊界值、超出軌跡時間範圍該回傳 null 而不是亂猜外推），每一組都跟手算結果逐一核對過完全吻合。接著沒有滿足於「跟真正的 exifr 套件、真正帶 EXIF 資料的 JPEG 照片」整合測試——過程中就抓到一個只有真的用上真實套件才會發現的 bug：exifr 的 `pick`（只挑特定欄位解析）選項在目前這個版本裡本身有問題，一用就直接拋出例外，等於每張照片都解析失敗；已經改成不加這個選項、直接完整解析，用真實產生的測試照片（用 Python 的 piexif 手動嵌入 EXIF 時間戳記跟縮圖）重新跑過，確認可以正確解析出拍攝時間、抽出縮圖、算出跟手算結果完全吻合的插值數據；也測過沒有 EXIF 的照片會被安靜略過、不會中斷整批處理。時差校正滑桿的重新插值、刪除照片、地圖圖釘與 popup 的視覺呈現，也都個別測過。
  - 這是原始需求三個方向（EXIF 解析、時空對位、地圖圖釘呈現）的全部三個，隱藏徽章（時空校正大師、決定性瞬間、前端渲染暴徒、流動的風景）留到之後。

## v2.67.0

- **環境折損與等價配速預測模型（EPP）**：「賽前目標」下方新增一個區塊，從自己過去的完賽紀錄統計出「高溫折損係數」跟「爬升折損係數」，套用到某場賽事上，用折損瀑布圖拆解成「基準時間→爬升折損→高溫折損→（已完賽的話還有）其他因素→預測／實際完賽時間」，一眼看出這場比賽的最大敵人是山還是太陽。
  - **這次沒有照原始需求的方法直接寫，是先想清楚統計上的陷阱才決定怎麼調整的**：業餘跑者的比賽紀錄通常只有個位數到十幾筆，用最小平方法硬算出來的係數很容易被單一一場「那天狀況特別差」的比賽整個帶偏，卻還是會吐出一個看起來很精確的數字——精確不等於可信。所以兩個係數都加了最低樣本數門檻（沒到門檻就顯示「資料還不夠」跟目前差多少，不會硬擠一個沒根據的假精確數字出來），旁邊也都附上 R²，讓使用者自己判斷這條迴歸線值不值得信。
  - **爬升折損係數刻意不是拿不同賽事的總配速互相比較**——那樣會把「這場是全馬、那場是超馬」這種純粹因為距離不同造成的配速差異，錯誤地也算進爬升的帳上。改成只在同一場賽事「自己的分段」之間比較（同一天、同一個人、同一個體能狀態，只有坡度在變），這樣量出來的爬升效應才乾淨。
  - **「AI 預測完賽時間」刻意不會自動寫進賽前目標**——瀑布圖下方是獨立的「採用為 A／B／C 目標」按鈕，要不要拿這個估計值當目標由使用者自己主動選擇；模型算出來的數字如果剛好是錯的、又被直接當成配速計畫在比賽當天執行，後果比模型不準本身嚴重得多（例如高估耐熱能力導致前段衝太快、後段撞牆）。
  - 測試上，兩段迴歸（高溫折損、爬升折損）都拿手動設計的資料，另外用 numpy 的 `polyfit` 獨立重算一次交叉驗證，斜率跟 R² 都精確到機器精度等級完全吻合；完整的瀑布圖拆解（基準時間、爬升折損秒數、高溫折損秒數、預測總時間、跟實際成績的差距）也逐項手算核對過，數字完全對得上。過程中也抓到並修掉一個真的會影響顯示的 bug：兩處文案原本用了只會代換單一 `{n}` 佔位符的既有翻譯函式，但文案裡其實有 `{pct}`、`{r2}`、`{tier}` 好幾個不同名稱的佔位符，會被悄悄代換錯位——已經改用不做自動代換的版本、自己依序把每個佔位符換掉，並確認全專案沒有其他地方犯一樣的錯。「資料還不夠」的空狀態、已完賽／未完賽兩種瀑布圖版本、「採用為目標」按鈕寫入賽事目標的正確性，都個別測過；三語言的完整文案也核對過內容一致。
  - 這是原始需求三個方向（預測引擎、瀑布圖視覺化、隱藏徽章）裡的前兩個，而且瀑布圖用的是「全場總爬升攤平成平均坡度」的簡化版本，還不是逐公里切片預測；逐公里切片引擎、演算法先知／熱力學異常／反重力引擎／無效電阻這四個新徽章都留到之後。

## v2.66.0

- **資料科學沙盒**：「匯出 CSV」旁邊新增「下載 Python 分析套件（.ipynb）」，一鍵下載一份可以直接在 Jupyter Notebook 開啟執行的分析範本，純前端產生（.ipynb 本質就是 JSON），不需要任何後端。裡面預先寫好三段分析，直接對應「匯出 CSV」會產生的 races.csv／shoes.csv 實際欄位名稱：① K-Means 把鞋款規格（鞋底厚度、足跟差）跟平均步頻分群的 3D 散佈圖；② 只看路跑賽事、依體感溫度與濕度分桶算出相對同距離 PB 配速衰退幅度的互動式熱力圖；③ 減量天數、週跑量目標、完賽時間三者的皮爾森相關係數矩陣。三段都會先檢查資料量夠不夠，不夠時印出中文提示、不會噴 Python 錯誤卡住。
  - **鞋款新增兩個規格欄位：鞋底厚度、足跟差（mm，選填）**，這是為了讓 Module A 的分群分析真的有東西可以分析——原始需求指定的分群維度（鞋底厚度、足跟差）在這次改版之前完全不存在於資料結構裡，鞋款本身也從來沒有被匯出成 CSV 過。與其把這個模組寫成一個引用不存在欄位、打開就會 `KeyError` 的空殼，這次順便把這兩個欄位補進鞋款資料結構、加上編輯 UI，並且讓 `shoes.csv` 加入匯出清單，讓三段分析範本裡沒有一段是「看起來很厲害、實際上跑不動」的擺設。
  - **這次的驗證方式比平常更嚴格一截，因為交付物本身是給另一個語言環境（Python／Jupyter）執行的程式碼，光靠人工讀程式碼沒辦法真正確認「打開來按 Shift+Enter 會不會動」**：找了一個真的裝有 pandas／scikit-learn／seaborn／plotly／scipy 的環境，先用模擬資料把三段範本個別實際跑過一輪，過程中就抓到兩個只有真的執行過才會發現的問題——plotly 的熱力圖函式沒辦法直接吃 `pd.cut()` 產生的區間物件當座標軸標籤（要先轉成文字），以及 seaborn 的 `set_style()` 會把設定好的中文字型整組蓋掉，導致圖表標題裡的中文變成缺字方框，兩個都已修正。接著把 app 實際產生出來的 .ipynb 檔案，用 Jupyter 官方的 `nbformat` 套件跑過正式驗證（不是憑感覺覺得格式對），再把檔案裡實際嵌入的 Python 原始碼整段抽出來重新執行一次，確認經過「切成逐行陣列塞進 JSON」這個轉換步驟後，程式碼還是跟原本測試時一模一樣能跑。三段範本「資料量不足」的分支也都測過空資料集，確認不會噴例外、只會印出對應的中文提示。
  - 這是原始需求三個方向（Notebook 沙盒、分析模組、隱藏徽章）裡的前兩個；蟒蛇煉金術、黃金清洗工、大數法則這三個新徽章留到之後。

## v2.65.0

- **海象自動串接（三鐵／游泳專用）**：三鐵、游泳這兩種有開放水域游泳段的賽事，日期已過且已匯入 GPX/FIT 軌跡時，「氣象」分頁會自動在背景查一次當天的海象資料（浪高、湧浪週期、海水溫度），資料來源是 Open-Meteo 的海洋氣象資料庫，跟既有的「歷史賽事自動回填天氣」是同一種「打開賽事詳情頁時查一次、永久快取、失敗就安靜放棄」設計，只是資料來源換了、限定運動種類。海水溫度低於 18°C、完賽時間超過 2 小時時，會額外顯示「❄️ 高失溫風險環境完賽」提示。這是原始需求「軌跡降噪」與「海象數據」兩個方向裡的第二個；軌跡降噪（GPS 訊號在開放水域被遮蔽造成的鋸齒軌跡校正）跟對應的三個隱藏徽章規模又是另一輪的量，留到之後——降噪演算法的正確性直接關係到使用者的真實距離、配速數據，寫錯了可能悄悄污染既有的比賽紀錄，這種正確性風險值得獨立一輪專心處理，不想在這輪順便倉促做掉。
  - **API 的端點跟參數名稱，這次沒有只憑記憶就動手寫，是先查證過才確定的**：一開始網路搜尋到的多個第三方資料來源，對「海水溫度」這個參數該叫什麼名字有點分歧，於是直接把 Open-Meteo 官方文件頁面整頁抓下來核對，才確定實際的參數名稱是 `sea_surface_temperature`、端點是 `marine-api.open-meteo.com/v1/marine`——這個端點恰好跟既有的歷史天氣不一樣：既有的歷史天氣預報／回溯資料庫是兩個分開的網域，海象這個端點反而是同一個網址就能同時查未來預報跟過去日期（帶 `start_date`／`end_date` 參數即可），這點也是從官方文件裡確認的，不是憑經驗猜的。文件裡另外提到一個查證時才發現的真實限制：海洋氣象模型只在實際海面上的座標網格才有資料，起點座標如果落在陸地網格上會整批查無資料——這不是查詢寫錯，已經照這個前提設計成跟其他背景查詢一樣安靜放棄，不會顯示誤導性的錯誤訊息。
  - 這一輪因為沙盒環境的網路權限限制沒辦法直接對外打一次真正的 API 確認回傳格式，改用「模擬網路直接失敗」「模擬回傳空資料（對應查到陸地座標的情境）」「模擬正常回傳」三種情境個別測過整個抓取／快取／回填流程，三種情境都不會讓畫面卡住或報錯，正常回傳的情境也核對過數字精確對應。高失溫風險的判斷邏輯測了四個象限（低溫久泳、低溫短泳、溫暖久泳、溫暖短泳），確認只有「兩個條件都成立」才會顯示警示，不會有任一條件成立就誤判。三語言的顯示文字也逐一核對過內容完全正確。

## v2.64.0

- **年度回顧長圖卡新增路線拼貼**：長圖卡下方原本的空白處，現在會鋪一排當年所有完賽賽事的去背路線軌跡——每場有存軌跡點的賽事各自畫一條極簡線條，依比賽日期排成 3 欄拼貼，最多顯示 9 場、超過會註明「+N 場」，畫面空間不夠或整年都沒有軌跡資料時這段就不出現，不會硬擠成一團看不出形狀。
  - **純軌跡投影的繪圖邏輯直接沿用單一徽章分享卡既有的函式，沒有另外重寫一份**：原本那個函式（畫去背路線、經緯度餘弦修正、起終點色點標示）的線條粗細是寫死的，這次加了兩個選填參數（線寬、色點半徑），沒帶就維持原本粗細——這樣單一徽章分享卡原本呼叫這個函式的地方完全不用改，新的拼貼呼叫時另外帶較細的數值即可，兩處共用同一套投影數學，不用擔心兩份邏輯以後兜不起來。
  - 測試時特別針對「共用函式改了參數之後，原本呼叫它的地方有沒有被動到」寫了獨立的核對：用跟徽章分享卡完全相同的呼叫方式（不帶新的選填參數）重新產生一張圖，確認畫出來的線條粗細、色點大小、發光效果都跟改之前一模一樣。新功能本身測了三種情境：賽事數量剛好塞得下（5 場、3 欄 2 列）、賽事數量超過上限（12 場，確認畫面只顯示 9 場、下面正確顯示「+3 場」、不會跟最下面的浮水印文字疊在一起）、以及整年都沒有任何軌跡點資料（確認整段完全不出現，不會留下空白區塊或殘缺的標題文字）。三語言版本也都截圖比對過。

## v2.63.1

- **範例資料改版**：「範例資料」從原本 2 場（1 場路跑、1 場已完賽的越野賽）改成 5 場，時間全部落在當年度 9-12 月、狀態一律是「即將到來」（已報名／抽籤中／考慮中，不再有已完賽的範例），涵蓋路跑、越野、鐵人三項三種運動種類。5 場刻意分散在不同時間點（10 天後、1 個月後、快 2 個月後...）跟不同狀態，用意是讓剛打開這個 app 的人一眼就能在「即將到來」跟月曆檢視裡看到分散的賽事，而不是只有一場快到期、一場已經跑完的範例。
  - 越野賽的裝備清單有一項先放在 CP2 的轉換袋，鐵人三項的裝備清單則分別放了幾項在 T1、T2 轉換區——直接示範裝備佈署視圖（Kanban，v2.60.0）的動態欄位怎麼運作，不用使用者自己填資料才能看到效果。
  - 兩場範例賽事另外示範了「應援指南：當天衣著描述」（v2.57.0）、號碼布編號這類近期新增的欄位，翻開範例就能看到這些欄位實際填了之後長什麼樣子。
  - 移除了原本那場「已完賽」的越野賽範例——既然這次改版全部聚焦在賽前規劃階段，保留一場已完賽的範例只會讓賽後相關的功能（分段配速、GAP、徽章等）混進來，跟「即將到來的賽事」這個主題不一致，所以拿掉了；賽後功能不需要靠範例資料展示，使用者自己完賽第一場之後自然就會用到。
  - 測試上，確認 5 場範例都能正確通過既有的資料遷移邏輯（`migrateRace`，避免用了今年稍早新增的欄位卻漏了預設值，載入舊版存檔時噴錯）、按「範例資料」按鈕的完整流程（建立、選取、月曆自動跳到對應月份、再按一次不會重複建立）、以及裝備佈署看板在這兩場範例上確實依照設計分類到對應欄位，都各自核對過數字（越野賽 CP2 轉換袋 1 項、鐵人三項 T1 三項＋T2 四項），不是只看畫面有沒有跑版。

## v2.63.0

- **疲勞模式（賽中用）**：新增一個給賽事進行中用的全螢幕簡化介面——長距離賽事後段雙手濕滑沾泥、認知能力因疲勞下降時，捨棄所有精準點擊，改成整個畫面隨便哪裡都能「往左或往右大動作滑」：向右滑標記抵達目前 CP 站（自動切到下一站，並語音唸出下一站資訊）、向左滑標記下一項補給為已補給。純黑背景配大字級金色文字（OLED 省電＋高可讀性），開啟時嘗試鎖定螢幕常亮，每次滑動有震動回饋，滑錯了有「復原上一步」。這是原始需求三個方向（疲勞模式、多感官播報、強光/色盲適配）裡的第一個；強光適配（HR 區間改用形狀/紋理雙重編碼、電子紙高對比模式）跟對應的隱藏徽章規模又是好幾輪的量，留到之後。
  - **刻意不做自動地理柵欄播報，這是評估過瀏覽器平台限制後的技術決定，不是偷工減料**：原始需求裡設計的是「手機背景偵測 GPS 接近 CP 站，自動語音提醒」，但瀏覽器分頁只要背景化或螢幕鎖定，JavaScript（包含定位監聽）幾乎一定會被系統暫停，iOS Safari 尤其明顯；Wake Lock 只能防止畫面自動變暗，不保證背景還在執行，硬做一個「常常不會觸發」的自動功能，萬一使用者在真正比賽時依賴它、結果沒反應，會比完全沒有這個功能更危險（安全相關的錯誤信任）。改成滑動當下才觸發播報——語音本身完全可靠，只是從「自動」改成「你自己滑的當下」。
  - **震動回饋刻意做成偵測不到就安靜跳過**：iOS 上所有瀏覽器底層都是蘋果的 WebKit，而蘋果從來沒有在 WebKit 實作震動 API，這是平台政策而非某個瀏覽器的 bug，iPhone 上這個功能永遠不會有反應。Wake Lock、語音、震動三個功能都是「支援就用、不支援就安靜跳過」，缺任何一個都不影響核心的滑動記錄功能。
  - **測試特別針對「這三個進階功能缺席時，核心功能還能不能用」寫了專門的案例**：模擬瀏覽器完全沒有 Wake Lock、震動、語音合成任何一個 API 的情況下，確認畫面不會報錯、滑動手勢依然能正確標記 CP 抵達跟補給狀態；也用真正的 `TouchEvent`＋`Touch` 物件（不是滑鼠事件模擬）測過完整的滑動流程，包含滑動後畫面正確切換到下一個 CP、復原上一步能正確還原資料；Wake Lock 跟語音合成在（模擬）支援的情況下也核對過確實被正確呼叫、語音內容正確帶入下一站名稱與距離。三語言的疲勞模式畫面也截圖比對過，英文版文字較長時會正確換行，不會爆版。
  - 最後把這一輪跟之前累積的所有功能放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.62.0

- **補給品資料庫**：把補給從「吃了幾包」的自由文字，升級成可以換算「碳水攝取率（g/hr）」的結構化資料。全域（跨賽事共用，做法完全比照既有的「鞋款管理」）建一份補給品資料庫，記錄每款補給品的碳水、鈉、咖啡因含量；補給時程規劃的每一列可以選填連結資料庫裡的一項，連結後只需要再填數量，系統會自動加總這場賽事目前的總碳水、總鈉。這是原始需求裡「資料庫、賽前預測、賽後診斷」三個維度的第一個——賽前的碳水攝取率紅綠燈儀表板、賽後撞牆期交叉診斷、還有對應的隱藏徽章，規模又是好幾輪的量，留到之後。
  - **刻意不去動既有的「預計份量」自由文字欄位**：那個欄位本來就是給沒辦法用「數量 × 固定碳水量」模型描述的情況用的（例如「半包」「溫水吞」），新增的「數量」是全新、獨立的欄位，只有連結資料庫時才會拿來做乘法，兩者互不影響，舊資料完全不受影響。
  - **開發中間發現、也一併修掉一個雖然跟這次主題無關、但會讓這個新功能顯得像故障的既有小 bug**：補給時程規劃裡任何一個欄位（不只是新增的「數量」）修改完，畫面原本不會整段重新整理，只有幾個特定欄位（像賽事狀態、運動種類）會觸發整段重繪——這代表使用者填完數量之後，畫面上方的碳水／鈉加總不會馬上更新，要等做了其他會觸發整段重繪的操作才會跳出來，感覺就像「填了數量沒反應、功能故障」。修法是把「補給時程規劃」的任何欄位異動也加進會觸發整段重繪的清單，這樣填完數量，加總立刻就會反映在畫面上。
  - **測試方式**：資料庫的加總公式（數量 × 碳水量、數量 × 鈉含量）拿手算過的例子核對過，包含「自訂文字項目不計入加總」「編輯資料庫項目後，已連結的加總會自動反映新數值（不是算過一次就存死）」「刪除資料庫項目後，已連結的補給時程規劃列還在、只是不再計入加總，不會報錯或憑空消失」這幾種情境都各自測過；三語言介面（含資料庫管理面板跟補給時程表單）都截圖比對過排版正常，沒有文字溢出或跑版。最後把這次跟之前累積的所有功能放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.61.0

- **移除「分段配速」逐段編輯清單**：賽後紀錄區塊原本在「分段配速一覽」圖表下方，還有一份把每一段展開成完整表單（距離、分段時間、心率、配速、淨爬升、備註六個欄位）的可編輯清單——分段資料幾乎都是從 GPX/TCX/FIT 匯入自動產生的，這份表單平常很少用到手動編輯，卻佔掉大量畫面高度，賽事分段一多，這個區塊會拉得很長。拿掉之後，賽後紀錄區塊從彩色長條圖（含 GAP、心率）直接接到「匯入運動紀錄檔」按鈕，資料本身完全沒動——GPX/TCX/FIT 匯入照常正確帶入分段資料，CSV 匯出的分段欄位也不受影響，只是拿掉這份很少用到、佔版面的表單。

## v2.60.1（bugfix）

- **修正：GAP 坡度校正配速沒有顯示在舊賽事上**。v2.58.0 上線的 GAP 功能，靠的是分段資料裡的「淨爬升」欄位；但這個欄位在 v2.58.0 以前，不管 GPX 逐點匯入還是 TCX 圈數匯入都從來沒有真的被計算過，永遠是空的——這代表 v2.58.0 以前匯入的賽事，即使升級到有 GAP 功能的版本，分段配速表格還是只會顯示實際配速一行，GAP 完全不會出現，因為分段資料裡根本沒有坡度可以拿來算。
  - 修法是回頭利用賽事本身早就存好的海拔剖面（route.elevationProfile，只要當初是用 GPX/TCX/FIT 匯入的，就一定有這份資料）內插出每一段的淨海拔變化，把缺漏的「淨爬升」欄位補上——不需要重新匯入原始檔案，開啟更新後的版本就會在讀取資料時自動處理。只補「淨爬升」還是空值的分段，手動填過的（即使填的是 0）不會被覆蓋。
  - 用「已知海拔曲線＋預期算出來的淨爬升」這組測試資料核對過補值邏輯，數字分毫不差；也測過已經算過淨爬升的新匯入賽事，確認補值邏輯不會誤觸發、不會覆蓋掉本來就正確的資料；還模擬了整個「舊資料存進去、重新整理頁面」的完整流程（不是只呼叫函式測試，是真的重新載入頁面），確認 GAP 會在分段配速表格上正確顯示出來。

## v2.60.0

- **裝備佈署視圖（Kanban）**：裝備清單新增一個依實體放置位置分類的看板檢視，跟既有的清單檢視可以隨時切換。位置選項有部分是動態產生的——三鐵／二鐵賽事自動多出 T1／T2 轉換區，「路線」分頁裡任何一個勾選「可寄放裝備」的 CP 站，也會自動變成一個轉換袋欄位；每張卡片可以拖曳到想要的欄位，也可以用卡片自帶的下拉選單做同樣的事（觸控裝置的替代做法）；每一欄自己有打包進度條。這一輪只做核心的看板檢視跟拖放機制（原始需求裡的強制裝備誤置警示、寄物袋爆量視覺提示，留到下一輪）。
  - **拖放沒有沿用既有的清單內重新排序功能，是刻意另外做的一套獨立系統**：這個 app 原本就有「拖曳排序」（裝備清單、補給時程、分段配速三個清單都有），但那套的邏輯是「在同一個陣列裡搬位置」；這次要做的是「把項目搬到不同的位置分類去」，兩者處理方式不一樣，如果直接改既有那套風險較高，用完全獨立的一套事件監聽器（不同的 class 名稱、不同的狀態變數），兩邊不會互相干擾。
  - **測試時抓到、也修正了一個原本會讓裝備憑空消失的邊界情況**：如果一件裝備原本放在某個 CP 轉換袋，後來那個 CP 站被刪除（或賽事種類從三鐵改掉，導致原本放在 T1/T2 的裝備失去對應欄位），原本的寫法會讓這件裝備完全從看板上消失不見——裝備本身還在清單裡，只是找不到對應的看板欄位可以顯示。修正後這種「位置已經失效」的裝備會自動歸到「尚未分類」那一欄，不會憑空消失，使用者還是看得到、可以重新指定位置。
  - **拖放的自動化測試特別分兩層驗證，不是只測一次就相信結果**：先用 Playwright 內建的拖放模擬確認整體流程沒問題，這層測試中途發現既有的清單內重新排序好像失效了——沒有直接認定是真的壞了，而是改用手動組出真正的瀏覽器原生拖放事件（`DragEvent`＋`DataTransfer`）直接觸發，繞過 Playwright 那層模擬，結果證實應用程式本身的拖放邏輯完全正常，問題出在 Playwright 的模擬對這個 app 特定的小型拖曳把手（裡面包一個 SVG 圖示）支援得不夠好，不是這次新增的看板拖放程式碼造成的迴歸。這個排查過程本身也順便確認了新舊兩套拖放系統彼此獨立、不會互相干擾。
  - 最後把這一輪跟之前累積的所有功能放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.59.0

- **氣候與表現散佈圖矩陣**：生涯數據新增第三個圖表，把路跑已完賽賽事依 10K／半馬／全馬三個經典距離分別正規化（各自找出自己的 PB 配速當 100% 效率基準），橫軸體感溫度、縱軸相對 PB 的配速衰退百分比，同一張圖上比較不同距離的表現。圖上有最佳作戰溫度帶色塊、線性迴歸趨勢線、逐點 hover tooltip。這次只做資料模型跟視覺化（原始需求裡的第三項——把趨勢線接回配速試算表、依預估氣溫動態調整目標時間——規模又是另一個量級，留到下一輪）。
  - **正規化公式直接用使用者提供的例子核對過**：PB 4:30/km、某場 28°C 跑出 5:00/km，算出來的效率剛好是 90.00%、衰退 -10.00%，跟原始需求裡舉的例子分毫不差；線性迴歸也拿一組手算過斜率、截距的資料核對，數字吻合才往下做視覺呈現。
  - **只收路跑、已完賽、經典距離、有體感溫度四個條件都符合的賽事**：越野賽的爬升雜訊會蓋過氣溫對配速的影響，混進來會讓「氣溫每上升 1°C 配速掉多少 %」這個統計失去意義，所以照需求排除；沒有落在 10K／半馬／全馬距離範圍內、或沒有填體感溫度的賽事也不列入，這四個排除條件各自寫了測試確認符合條件的賽事被正確擋下。至少要有 3 場符合條件的資料才會顯示這張圖，資料不夠時不會硬畫一張看不出趨勢的圖。
  - **開發過程中不小心搞出一個原本會直接讓整個生涯數據頁面壞掉的失誤，靠語法檢查在第一時間攔下來**：改動附近程式碼時，`computeCareerStats()`（全馬 PB、當年里程等好幾個既有數字都靠這個函式算）的函式宣告那一行被整個誤刪，只剩函式內容、少了 `function computeCareerStats(){` 這行——如果沒有在每次編輯後都跑一次語法檢查，這個問題會一路帶到最後才被發現，而且會讓任何有 5 場賽事以上的使用者一打開頁面就整個壞掉。抓到後補回遺漏的那一行，並且重新拿實際資料測過這個函式的輸出數字，確認完全正常才繼續往下做。
  - **三語言截圖比對時抓到一個英文文法問題**：「最佳作戰溫度帶...1 場高效率賽事」的英文版原本寫成「1 high-efficiency race**s**」，單複數用法不對；改成「race(s)」這種在賽事數量不固定時常見的寫法，數量是 1 或多場都不會念起來怪怪的。
  - 最後把這一輪跟之前累積的所有功能放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.58.0

- **坡度校正配速（GAP）**：「分段配速一覽」表格新增坡度校正配速——粗體大字顯示 GAP（把坡度影響換算回平地等效配速），下方小字灰色顯示實際配速，長條圖的滿版程度改以 GAP 為準，陡坡段即使實際配速慢，只要體感強度夠高，長條圖還是會顯得「滿」。
  - **開工前先查了「分段」資料實際上怎麼存，發現這個功能原本缺一塊關鍵資料**：`elevationGainM`（分段的海拔資料）這個欄位雖然一直都在，但不管是 GPX 逐點匯入還是 TCX 圈數匯入，實際上從來沒有真的算過這個值，永遠是空的——這代表要做坡度校正配速，不是「把既有資料拿來套公式」，而是要先補上一段從來沒存在過的計算邏輯（在兩種匯入路徑裡，各自從軌跡點的海拔資料算出每一段的淨海拔變化），不查清楚這點直接套用公式，算出來的坡度會全部是空值。
  - **GAP 公式**：上坡每增加 1% 坡度，配速大約校正變慢 3.75%；下坡在坡度 -6% 左右校正效果最大，比這個更陡的下坡因為煞車式的肌肉收縮（離心收縮）耗能回升，效果會用同一條拋物線自然反轉，不用另外寫兩段式判斷。GAP 是算出來就顯示的衍生值，不會另外存進資料裡——這樣使用者事後手動修改某一段的「淨爬升」，GAP 會自動跟著重新計算，不會出現改了爬升、GAP 卻還是舊數字的情況。
  - **測試方式是自己手算好答案再核對，不是看輸出合不合理就算過**：搭了一組已知海拔曲線的假軌跡（例如前 2 公里固定每公里爬升 50 公尺、第 3 公里固定下降 20 公尺），確認匯入邏輯算出來的每一段海拔變化跟手算的完全一致；GAP 換算也用手算的坡度百分比、配速乘數逐項核對，包含上坡（配速變快）、下坡在 -6° 附近（校正效果最大）、超陡下坡（校正效果反轉回升）三種情境，還有零距離、極端坡度（模擬 GPS 誤差）這類邊界情況，確認都有安全的保底範圍，不會算出離譜或無限大的數字。
  - **開發過程中額外抓到兩個原本就存在、這次不修會直接影響 GAP 可讀性的問題**：
    1. 表格原本是依「這一段自己的距離」排序，不是依賽事先後順序——GPX 匯入固定每段 1 公里時剛好每段長度一樣，排序等於沒作用，歪打正著沒出事；但 TCX 圈數匯入的每圈長度通常不固定，這個排序邏輯會把分段順序整個打亂。這個問題不會在一般測試裡自己冒出來，是重新確認「距離」欄位到底存的是什麼之後才發現的。順手把距離欄位也從只顯示一小段（每列都顯示同樣的「1.0K」不太有意義）改成顯示範圍（例如「3-4K」），這樣才看得出每一列對應賽道的哪個位置。
    2. 實際截圖比對畫面時發現，GAP 粗體數字原本設定的顏色，跟長條圖底色是同一個顏色變數——數字疊在長條圖填滿的部分時會完全隱形，只看得到後面一小截露出來的文字。這種問題純粹看程式碼看不出來（兩個地方各自看都合理，只有疊在一起才會出事），是靠實際截圖才抓到，抓到後把文字顏色改成經過驗證在長條圖兩種底色（填色跟透明）下都讀得出來的顏色，重新截圖確認可讀性後才繼續。
  - 最後跟本輪之前已經上線的功能放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.57.0

- **親友應援與補給指南圖**：賽前狀態賽事的「更多操作」選單新增「下載親友應援指南圖」，跟 v2.37.0 的賽前作戰卡是分開的另一張卡片——作戰卡給跑者自己看，這張給場邊等待的親友看：號碼布、當天衣著描述（新增欄位，方便從人群裡認出你）、每個 CP 站的預計抵達**時鐘時間**（不是「開賽後幾小時幾分」，親友不用自己換算）跟該站要遞的補給清單。
  - **ETA 計算沒有重新發明配速插值公式**：把既有配速試算表（`computePacingRows`）裡的配速插值邏輯抽成一個獨立的 `pacingTimeAtDistance()`，讓配速試算表（固定間隔）跟這張卡片（CP 站的實際距離，通常不是固定間隔）共用同一套公式；抽出來之後有拿同一組輸入重新算過一次，確認跟抽出前的輸出完全一致，不是抽了之後才發現算出來的數字變了。
  - **時鐘時間換算特別處理了跨日的情況**：長距離賽事常常跑超過 24 小時或很晚起跑，換算出來的時間有可能跨過午夜——用一組刻意設計成「剛好跨過午夜」跟「剛好整點跨日」的測試案例驗證過，確認换算正確、也有標記隔天的提示，不會出現同一張卡片上兩個看起來一樣的「14:30」讓親友搞混是今天還是明天。
  - **CP 站跟補給計畫是用最接近的距離自動配對，不要求兩邊數字對到小數點**：「路線」分頁的 CP 站跟「補給」分頁的補給時程規劃本來就是分開填寫的兩個清單，距離不會剛好完全一致（例如 CP 填 10km、補給計畫填 10.2km），配對邏輯抓「最接近的那個 CP」，用故意留了小數點誤差的測試資料驗證過配對結果符合預期。
  - **測試時用真實的跨語言畫面截圖，抓到兩個只有實際看畫面才會發現的排版問題**：號碼布跟起跑時間那一行，中文版看起來沒事，但英文版因為「Bib Number 1024 · Start 05:00」比中文版的對應字串長很多，原本用置中對齊、沒有寬度限制的寫法會讓文字整個超出畫布邊界、被裁切看不到；下方的免責聲明文字同樣因為英文版比較長，原本設定的兩行上限不夠、被截斷加上刪節號。修正時加了自動換行跟寬度限制，改完立刻重新截圖比對，這次過程中又順手抓到一個新問題——換行函式在文字剛好只有一行、不需要真的換行的情況下，回傳的座標並不包含那一行本身的高度，導致下面那一行的欄位往上貼到重疊；這個問題只有在中文版（字串較短、不會換行）才會出現，也是靠實際截圖比對抓到的，不是用眼睛看程式碼推測出來的。
  - 最後把這一輪連同前三輪加起來總共七個功能（徽章篩選、年度回顧長圖卡、動態回顧輪播、裝備重量、戰靴分析、AI 提示詞生成、親友應援指南圖）放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.56.0

- **賽事復盤 AI 提示詞生成**：已完賽賽事的「更多操作」選單新增「複製賽後檢討 AI 提示詞」，把天氣、心率區間、分段配速與掉速比例、補給計畫執行狀況、撞牆期跟其他賽後反思筆記組合成一段文字複製到剪貼簿，貼給 ChatGPT / Claude 就能請 AI 寫成 IG 貼文跟檢討網誌兩種版本。這是這一批功能裡第一個「輸出文字、不是輸出圖片或畫面」的功能，思路上刻意跟這個 app 本來就有的「使用者拿 AI 把簡章轉成 JSON 貼進來」反過來對稱。
  - **每個區塊都是資料驅動的**：天氣、心率、分段配速、補給、賽後反思，哪一段沒有資料就整段不出現在 prompt 裡，不會出現「天氣：（未填寫）」這種空欄位反而要 AI 自己編造內容；用完全沒填任何進階資料的賽事測過，確認 prompt 只剩下最基本的賽事資訊那一段，不會有一串空段落。
  - **心率區間直接沿用 v2.42.0 就有的 `hrZoneInfoForRace()`，掉速比例沿用既有的 `computeSplitPercent()`**，沒有另外重新設計計算邏輯；有拿一組平均心率、儲備心率兩個端點、分段前後半程秒數手算過答案，確認 prompt 裡算出來的心率區間跟掉速百分比跟手算結果吻合。
  - **複製到剪貼簿用瀏覽器原生的 Clipboard API**，成功會讓按鈕文字短暫變成「已複製到剪貼簿！」再自動變回原本文字；特別測過 Clipboard API 失敗（例如瀏覽器拒絕權限）的情境——這種情況不會讓使用者以為複製成功但其實沒有，而是跳出一個文字框把完整內容顯示出來，讓使用者自己全選手動複製，並且確認了這個備用文字框裡的內容跟本來要複製的內容一致。
  - **英文版介面單獨處理了一個排版細節**：中日文用全形【】方框當作 prompt 裡的段落標題比較自然，但這個寫法套用在英文版会顯得突兀（英文很少用這種方框），所以英文版改用「## 」開頭的 markdown 風格標題，貼給 AI 一樣能正確辨識成段落，只是更符合英文使用者的閱讀習慣——這個調整是在做完三語言測試、實際看過三種語言的輸出結果之後才發現並修正的，不是一開始就設計好的。
  - 最後把這一輪連同前兩輪加起來總共六個功能（徽章篩選、年度回顧長圖卡、動態回顧輪播、裝備重量、戰靴分析、AI 提示詞生成）放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.55.0

- **戰靴勝率與衰退矩陣**：鞋款管理清單裡，只要那雙鞋有完賽紀錄，就會多一個「戰力分析」按鈕，展開後顯示穿這雙鞋的 PB 率、平均配速、逐場賽事列表，以及「衰退交叉比對」——用鞋子預期壽命的一半當分界，比較還算新跟已經跑得比較舊這兩組賽事的平均後半程掉速幅度，看得出累積里程增加後掉速有沒有變得更明顯。
  - **後半程掉速的計算直接沿用既有的 `computeSplitPercent()`，沒有另外發明一套邏輯**：這個 app 本來就有「正分段／負分段」的概念（`results.firstHalfSeconds`／`secondHalfSeconds` 這兩個欄位），用在單一賽事的分段配速分析上；這次只是把同一個算法套用到「這雙鞋穿過的每一場賽事」上，再依鞋子當時累積的里程分組比較，不是重新設計一套掉速判定方式。
  - **鞋子「當時累積里程」算的是跑這一場之前的里程，不含這場本身**：因為要看的是「鞋子已經多舊」對這場表現的影響，不是這場對總里程的貢獻，兩者搞混會讓比對失真。分界點沒有寫死固定的公里數，是用每雙鞋自己設定的預期壽命除以二，這樣不同定位的鞋子（例如比賽用的競速鞋壽命通常比訓練鞋短很多）才會用各自合理的分界比較，不會全部套同一個數字。
  - **測試用一組手算過答案的資料核對**：5 場賽事、已知距離與正分段秒數、2 場標 PB，PB 率、平均配速、分組後的平均掉速百分比、兩組的差距，全部拿計算機重新算過一次跟系統輸出比對，數字對得上才继续做畫面呈現；也故意測了「這雙鞋完全沒有比賽紀錄」「只有一場賽事、沒有分段時間資料」這兩種資料不足的情境，確認畫面會相應地不顯示分析按鈕、或只顯示基本統計而跳過衰退比對，不會硬擠出一個資料不足的比較結果。
  - **展開狀態做成不會被其他操作打斷**：鞋款清單本身在新增／編輯／退役任何一雙鞋時都會整個重新渲染，如果沒特別處理，正在看的某雙鞋的戰力分析會在做了不相關的操作後被意外收合。這次用同一套「記住目前展開的是哪一雙鞋、重新渲染後照這個記錄還原」的作法（跟前一輪榮譽櫃篩選用的是同一個模式），並且實際測過「展開 A 鞋的分析、去編輯 B 鞋的預期壽命」這個情境，確認 A 鞋的分析畫面沒有被意外收合。
  - 最後也把這一輪跟上一輪加起來總共五個功能（徽章篩選、年度回顧長圖卡、動態回顧輪播、裝備重量、戰靴分析）放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.54.0

- **裝備重量與推重比**：裝備清單項目新增選填的「重量（克）」與「消耗品」欄位，加上一個資料驅動的輕量化儀表板（甜甜圈圖：基礎重量 vs 消耗品重量），有填「個人資訊」體重的話還會多算「裝備佔體重比例」跟「總負重（人＋裝備）」。這一輪四個功能裡唯一動到核心資料結構（`equipmentChecklist` 陣列）的一個，所以從一開始就先確認了既有欄位的用途，才決定怎麼設計新欄位。
  - **沒有用既有「分類」欄位去猜「是不是消耗品」**：裝備原本就有一個「分類」欄位（頭燈、服裝、補給品…），但那是裝備種類，不是「這件東西會不會在賽事中被用掉」——水袋容器本身是耐用裝備、袋子裡的水才是消耗品，兩者是不同概念，用分類去猜容易猜錯，所以另外加了一個獨立的「消耗品」勾選框，讓使用者自己標記。新手模板裡明顯無爭議的消耗品（能量膠、鹽錠、自行車水壺）已經先預設勾好。
  - **重量的加總刻意排除沒填寫的項目**，不會當成 0 克計算，避免只填了一部分裝備重量時總數字看起來比實際輕很多；儀表板上方會註明目前是根據幾分之幾的項目算出來的。完全沒有任何一項填重量時，整個儀表板不會出現，不會顯示一個全部是 0 的空面板。
  - **開發過程中實際測出兩個範本系統沒接住新欄位的地方，不是憑空猜的**：直接走一次真實的「另存為範本」→「套用範本到新賽事」流程（不是只測資料層的函式），發現重量跟消耗品標記在這個路徑上會整個遺失——問題出在「套用範本」用的是另一段獨立寫死欄位清單的程式碼，跟裝備範本本身建立時用的邏輯是兩條不同路徑，新增欄位時只改了其中一條、漏了另一條。抓到問題後兩處都補上，重新走一次完整流程（建立賽事、填重量、存成範本、套用到新賽事）確認重量與消耗品標記正確保留，這樣以後同一件裝備才不用每場賽事重新輸入一次重量。
  - 最後也把這一輪做的四個功能（徽章篩選、年度回顧長圖卡、動態回顧輪播、裝備重量）放在同一個情境裡各自操作一輪，確認彼此不會互相干擾。

## v2.53.0

- **動態回顧輪播（Story Mode）**：行事曆導覽列新增「{年份} 年度回顧」按鈕，全螢幕、類似 IG 限時動態的方式播放當年度每場有封面照片的完賽賽事，結合賽事基本資訊與當天天氣。這是這一輪三個「年度回顧」相關功能裡規模最大的一個，沒有現成的東西可以直接照搬，從零開始搭了一套全螢幕輪播機制。
  - **資料篩選**：只收「已完賽 + 有上傳封面照片」的賽事——沒有照片的賽事沒辦法當成沉浸式的相片畫面。按鈕本身也是資料驅動的：完全沒有符合條件的賽事就不會出現這個按鈕，不會讓使用者點了才發現裡面是空的。
  - **播放機制**：開頭一張純文字年份標題頁，接著依日期排序播放每場賽事；每張畫面上方是分段進度條，5 秒自動播放下一張，也可以點畫面左右兩側手動切換、或用鍵盤左右鍵切換、Esc 關閉。播完最後一張直接關閉，不循環回開頭。
  - **測試特別著重在計時器正確性**：這類「自動播放 + 手動切換」混合的功能，最容易出的問題是計時器沒清乾淨、手動切換後舊的計時器還在背景跑，之後補上一次不該發生的自動跳頁。這次測試不是只測「功能有動」，而是用 Playwright 實際等待超過 5 秒鐘確認自動播放真的會觸發、快速連續點兩次手動切換確認頁碼剛好走兩步（不會因為殘留的計時器變成走三步或卡住）、關閉之後再等超過原本計時器會觸發的時間，確認沒有任何殘留計時器在背景造成非預期的狀態變化。
  - **開發中抓到一個真的會顯示錯誤文字的問題**：天氣狀況（晴/陰/雨/霧）那行原本在繁體中文介面下會直接顯示英文代碼「sunny」而不是「晴」——原因是套用翻譯函式時，找不到對應翻譯時的備用文字誤寫成原始代碼本身，而不是查表找出對應的中文標籤；繁體中文剛好是靠這個備用文字顯示（跟日文、英文不同，繁中沒有另外存一份翻譯字典），這個寫法錯誤在繁中介面下會直接被使用者看到。修正後重新測過三種語言，確認天氣狀況欄位都顯示正確的翻譯文字，不是代碼本身。

## v2.52.0

- **年度回顧長圖卡產生器**：生涯數據的「歷年完賽場次」長條圖下方，新增每個有完賽紀錄的年份按鈕，點一下直接下載那個年度的直式回顧圖（1080×1920）。
  - 沿用 v2.43.0 徽章成就圖同一套 Canvas 繪圖慣例（配色、字型、統計欄位排版、下載成 PNG 的方式），沒有另外發明一套新的視覺語言。
  - 統計數字全部實際驗證過：用一組已知答案的測試資料（6 場賽事、已知里程與爬升、2 場標記 PB、1 個當年解鎖的徽章、4 場穿同一雙鞋、2 場有溫度紀錄）算出 `computeYearInReviewStats()` 的回傳值，逐一核對總里程、總爬升、PB 次數、最常穿的鞋（含場次與里程）、最冷最熱賽事的溫度，全部跟手算答案吻合才繼續往下做視覺呈現；也另外驗證了「新解鎖徽章數」正確只算落在那個年度的解鎖記錄，故意混進一筆前一年的解鎖記錄確認沒有被誤算進去。
  - 鞋款、氣候紀錄這兩個區塊做成資料驅動：那個年度沒有鞋款紀錄、或沒有任何一場賽事填過「實際體感溫度」，就整段跳過不畫，不會出現空白區塊或「—」佔位符；用完全沒有鞋款跟氣候資料的最小情境實際產生過一張圖確認畫面正常、沒有出現半截的區塊。這張卡片在資料量少時下半部會留白（跟徽章成就圖在沒有對應賽事時一樣），這是延續現有卡片的既定版面邏輯，不是這次新增的問題。
  - 氣溫資料抓的是 `raceDayWeather.feelsLikeTempC`（賽事詳情頁「實際體感溫度」欄位的最終值），不管這個數字是歷史天氣自動回填的還是使用者自己手動填寫覆蓋過的都算數，跟這個欄位在賽事詳情頁本身呈現的意義保持一致。
  - 中日英三種語言都測過：畫在 Canvas 圖片裡的文字（標籤、單位、「賽事紀錄」品牌落款）跟頁面上的按鈕文字，個別確認翻譯正確顯示；也跟上一版剛做好的榮譽櫃篩選功能一起跑過混合情境測試（開榮譽櫃、切四個篩選鈕、再觸發年度回顧下載），確認兩個功能互不干擾、榮譽櫃的展開狀態全程沒被打斷。

## v2.51.0

- **榮譽櫃加入快速篩選**：95 個徽章累積下來已經很多，加上「全部／已解鎖／進行中／未達成」四個篩選鈕。
  - 每個徽章判定函式本來就已經會回傳一個 0~100 的 `progress` 數字（連未解鎖的徽章都有算，只是原本只用來畫進度條、沒有拿來做篩選或排序），這次直接沿用這個既有欄位，不用另外發明一套進度追蹤機制。
  - 「進行中」特別做了跟其他三個篩選不一樣的處理：其他篩選都維持原本按類別分四個區塊顯示，「進行中」則是刻意攤平成一個列表、不分類別，直接按進度高到低排序——原本設計是保留分類區塊、只在區塊內排序，但這樣真正全站最接近解鎖的徽章可能被埋在後面才展開的類別裡，跟「進行中」篩選本來要解決的「幫使用者一眼看到只差一步的目標」互相矛盾，所以改成跨類別攤平排序，每張卡片改貼一個小的類別標籤取代原本的分區塊。
  - 切換篩選鈕做成只換掉榮譽櫃「裡面」的內容，外層的展開／收合狀態刻意不去動它——如果做成整個區塊重新渲染，使用者每點一次篩選鈕榮譽櫃就會自動收合回去，等於每次都要重新展開一次，體驗會很差；測試時特別針對這點逐一點過四個篩選鈕確認展開狀態全程沒被重置。
  - 測試涵蓋：四個篩選各自顯示的數量互相驗證加總等於 95（已解鎖＋進行中＋未達成＝全部）；「進行中」確認是真的跨類別全域排序（不是只在各類別內部排序）；切到「已解鎖」在完全沒有解鎖徽章時正確顯示「這個篩選條件下沒有符合的徽章」而不是空白一片；已解鎖徽章卡片上的「下載成就圖」按鈕在重構卡片渲染邏輯之後重新測過，確認沒有連帶壞掉；日文、英文介面下四個篩選鈕的翻譯文字與切換功能都個別測過。

## v2.50.0

- **歷史天氣自動回填擴大範圍**：三項擴充都查證過 Open-Meteo 官方文件/範例確認參數名稱正確，不是憑印象套用。
  - **降雨→機率與氣候狀況**：當天資料如果查到有降雨量（大於 0），直接把「降雨機率」填 100%、「當日氣候狀況」選「雨」；這是使用者明確要求的簡化規則（已經發生過的事，用「有下雨／沒下雨」對應機率的兩個極端），跟其他欄位保守、不亂猜的原則不同，只有在使用者自己這樣要求時才套用這種簡化。
  - **風速、風向自動填入**：抓取範圍新增 `wind_speed_10m`、`wind_direction_10m` 兩個變數，風向的角度數字（0~360）轉換成「東北風」這種中文 16 方位慣用說法；轉換函式用已知角度（0/45/90/135/180/225/270/315 度等）逐一驗證過對應到正確的方位文字，包含超過 360 度、負數這種要「繞回去」的邊界值。
  - **歷年同日平均氣溫、濕度**：新增一個獨立的背景查詢，抓同一個地點、同一個月日，往前五年（不含賽事當年）的歷史資料取平均，填進「歷年平均氣溫」「歷年平均濕度」。Open-Meteo 沒有「同一天橫跨多年」的查詢方式，拆成 5 次個別查詢平行送出，用 allSettled 而不是 all，某些年份查詢失敗不影響其他年份、只要至少 1 年成功就能算出平均；實際測過 2 個年份失敗、3 個年份成功的情境，確認平均值正確只用成功的 3 年計算，也測過 5 個年份全部失敗時會安靜放棄、不會當機也不會卡住。
    - 這個功能開發過程中發現一個真實的邊界情況並修正：賽事日期如果剛好是閏年的 2/29，往前五年裡有些年份根本沒有這一天，直接把年份代進日期字串送出去，Open-Meteo 對這種「不存在的日期」會如何處理並不確定。改成送出查詢之前先驗證這個日期字串是不是真的存在（用 JS 的 Date 建構子對無效日期會自動「進位」這個特性反過來檢查），不是真正閏年的年份直接跳過、不送出查詢，平均只計算真的有這一天的年份，不會拿 2/28 去湊（兩者是不同的一天）。
  - **維持既有的防呆機制**：每擴充一次抓取的變數範圍，都同步更新了「這份快取是不是在新欄位還沒加進去之前存下來的舊資料」這個偵測邏輯（沿用上一版修正過的機制），確認在既有欄位版本、新增風速風向版本這幾種新舊混合的快取情境下都能正確判斷要不要重新抓取；也重新測過「使用者已經自己填過的欄位不會被新抓到的資料覆蓋」在所有新增欄位上都成立。三個部分整合在一起測過完整流程，確認彼此不會互相干擾、也沒有重新引入先前修過的無限重試迴圈問題。

## v2.49.2

- **修正上一版「自動填欄位」對已經查過的舊賽事沒有作用的問題**：回報的情況是卡片正確顯示「已自動帶入」，但下面「實際體感溫度」「當日濕度」卻還是空的。原因是上一版的回填邏輯寫死在「剛抓取成功的當下」才會執行；如果這場賽事在更早之前（還沒有回填功能、也還沒開始抓體感溫度／濕度這兩個欄位的版本）就已經查過歷史天氣、存成快取了，之後即使升級到有回填功能的新版本，因為快取已經在、不會觸發重新抓取，回填邏輯就永遠沒有機會執行——卡片顯示的是舊快取的溫度／降雨量／陣風沒錯，但訊息卻繼續講「已自動帶入」，變成文字說有做、實際上沒做。
  - 修法：偵測到現有快取缺少「體感溫度」「濕度」這兩個欄位（不是欄位裡的值剛好是空的，是整個欄位在資料結構裡就不存在，代表這是舊版本抓的），就當成「這份快取不完整、需要重新抓一次」處理，跟完全沒有快取過的賽事用同一套流程重新查詢；如果快取本身已經是完整的、只是還沒被回填進手動欄位，才單純用現有資料回填，不用再打一次 API。
  - 過程中另外抓到一個相關的小問題：卡片本身在資料不完整時（例如只有部分欄位）會直接當機報錯，因為讀取陣列前沒有先確認陣列本身存在；連帶把「已自動帶入」這句話也改成只有在真的有體感溫度或濕度資料可以回填時才顯示，資料不齊全時不會再出現名不副實的訊息。
  - 用完全比照回報情境重建的測試資料（快取裡只有氣溫、降雨量、陣風，缺體感溫度跟濕度）驗證過修正後正確觸發重新查詢並完整回填；也重新測過「快取已完整只是還沒回填」「使用者已經自己填過」這兩種情境沒有被這次修正影響，加總只送出必要的最少次數網路請求（不會為了已經有完整資料的賽事又重新查一次）。

## v2.49.1

- **歷史天氣查到之後，順便自動填好對應的手動欄位**：上一版做的自動查詢，查到資料後只有顯示在自己的小卡片裡，跟下面「實際體感溫度」「當日濕度」這兩個手動欄位是分開的，使用者還是得自己對照數字重打一次。這次改成查到資料後，如果那兩個欄位還是空的，直接用查到的數字（一樣挑最接近起跑時刻的那筆）幫忙填上；已經填過的欄位不會被覆蓋，尊重使用者自己輸入或修改過的值。
  - 這次額外多抓了「體感溫度」與「相對濕度」兩個變數（原本只有氣溫、降雨量、陣風），才有辦法準確對應到「實際體感溫度」跟「當日濕度」這兩個欄位，不是拿原始氣溫去填體感溫度欄位濫竽充數。
  - 刻意沒有自動填的欄位：「歷年平均氣溫／濕度」跟「降雨機率」語意上是多年平均值或機率，跟單一天的實際量測值是不同概念，拿單日資料去填會誤導；「當日氣候狀況」（晴/陰/雨/霧）是質化判斷，光靠溫度濕度降雨量沒辦法可靠推斷，猜錯了使用者又不一定會注意到去修正，比留白更糟，所以這幾欄還是需要手動填寫。
  - 測過確認：欄位原本是空的會被正確填上、欄位原本已經有使用者自己填的值（測了一個明顯不合理的數字 99.9 當標記）不會被覆蓋。
  - 順手把「找最接近起跑時刻的那筆資料」這段邏輯（即時天氣預報、歷史天氣自動回填共用同一套演算法）抽成一個共用函式，原本兩個地方（後來變三個）各自複製貼上同一段程式碼，重構後只有一份，也重新測過即時天氣預報確認沒有因為這次重構而壞掉。

## v2.49.0

- **歷史賽事自動回填當天實際天氣**：日期已經過去、有匯入 GPX/FIT 軌跡的賽事，打開詳情頁時「氣象」分頁會自動在背景查詢當天實際發生過的天氣（氣溫、降雨量、陣風），不用按任何按鈕。用的是 Open-Meteo 的「歷史氣象回溯資料庫」端點（`archive-api.open-meteo.com`），跟先前做的即時預報是不同端點——查證過官方文件確認：預報端點的「降雨機率」是只對未來才有意義的統計值，歷史端點沒有這個欄位，改用「實際降雨量」（已經發生過的量測／回算數值），不是照抄預報端點的參數名稱硬套。抓到之後永久快取（歷史資料不會變，不像即時預報有時效性）。
  - **開發過程中抓到一個嚴重的無限迴圈問題**：一開始的寫法是「抓取失敗就放棄，讓使用者下次打開這場賽事再自動重試」，但因為抓取失敗後會重設狀態並觸發重新渲染，而重新渲染又會看到「還沒抓到資料、現在也沒有在抓取中」而立刻再次觸發抓取，形成抓取失敗→重新渲染→再次觸發抓取的無限迴圈。這個環境本身連不到這個 API（會直接失敗），意外變成很適合拿來測試失敗情境的環境——沒有這樣測就不會在這麼早發現：實測抓到 3 秒內呼叫了 101 次。改成用一個「這次瀏覽階段已經嘗試過的賽事」清單擋下重複觸發，同一場賽事同一次瀏覽最多真正嘗試一次，重新整理頁面才會再給一次機會；修正後同一情境下 3 秒內只會真的發出 1 次網路請求，確認過不是巧合。
  - 用模擬的假回應測過完整成功路徑：資料正確存起來、畫面正確挑出最接近起跑時刻的那筆顯示、重新整理頁面後不會重複查詢；也測過完全沒有 GPS 軌跡的歷史賽事不會觸發查詢、已經抓過的賽事重新打開不會再查一次。中、日、英三個語言的顯示文字也都個別確認過。

## v2.48.1

- 「更多操作」選單裡單場賽事的「加入行事曆提醒」改名為「匯出行事曆提醒」，跟左下角「資料管理」選單裡全部賽事的那個按鈕用詞一致。
- 這兩個「匯出行事曆提醒」按鈕（單場、全部）後面都加上「（.ics檔）」小字說明，講清楚匯出的是什麼格式的檔案。三個語言都改過，日文、英文版下載功能重新測過一次確認沒有受影響。

## v2.48.0

- **「在 Google 日曆開啟」新增一個捷徑位置**：原本只放在「更多操作」下拉選單裡，現在賽事日期那一行（「2026-09-06・已完賽・晶片時間...」這行）正下方的空白處，也放了一個同樣功能的小連結，不用先點開選單就能直接開啟。兩個位置共用同一個動作，行為完全一致；沒有設定比賽日期的賽事，跟下拉選單裡那個按鈕一樣不會顯示。

## v2.47.1

- **空白狀態（還沒選任何賽事時）裡的「+ 新增賽事」按鈕，第一次使用時也一併有呼吸光暈效果**：上一版只做了右上角那個固定不變的「新增賽事」按鈕，這個空白狀態裡的按鈕因為每次重新渲染都會整段重寫 HTML，用事後加 CSS class 的做法會在下一次重新渲染時被覆蓋掉消失，改成產生 HTML 的當下就直接判斷要不要帶上光暈的 class。兩個按鈕現在共用同一套判斷邏輯與同一個「已經看過」的記錄，點任何一個都會同時關掉兩邊的光暈效果，不會出現點了其中一個、另一個卻還亮著的情況。

## v2.47.0

- **第一次使用時「新增賽事」按鈕會有呼吸光暈效果**：完全沒有任何賽事資料、而且這個瀏覽器從沒標記過「已經看過」時，右上角「新增賽事」按鈕會有金色的呼吸光暈動畫引導視線；點過一次（不論是真的新增還是後來取消）、或改用「範例資料」開始，都會永久關掉這個效果並記在 localStorage 裡——特別設計成「這個網站第一次用」的引導，不是「目前賽事清單是空的」的提示，所以就算之後把賽事全部刪光、清單又變回空的，也不會再亮起來。
  - 已經有賽事資料的使用者（不管是自己建立的還是匯入的）完全不會看到這個效果；用直接呼叫檢查函式的方式測過，確認有資料時這個效果不會被加上去。
  - 系統設定「減少動態效果」時動畫會關閉，改成一圈固定不閃爍的光環，不是整個效果都消失不見；兩種模式都各自截圖確認過。
  - 光暈顏色跟著淺色／深色主題走，測過切換主題後顏色會正確對應到當下主題的金色，不會出現色差。

## v2.46.0

- **賽事詳情頁與左下角資料管理區的一排按鈕改成下拉選單**：這兩個區塊過去幾輪陸續加按鈕（加入行事曆提醒、在 Google 日曆開啟、下載賽前作戰卡、貼上資料快速填入、複製賽事、刪除賽事；以及匯入 Excel、匯出 JSON、匯出 CSV、匯入 JSON、匯出行事曆提醒、已刪除賽事、清除全部資料），越疊越多，變成一整排長相一樣的文字按鈕擠在一起，沒有分組也沒有主次之分。改成賽事詳情頁收成一個「更多操作」按鈕、左下角收成一個「資料管理」按鈕，點開才是一個依邏輯分組（行事曆／資料操作、匯入、匯出、刪除還原）的下拉選單，平常畫面清爽很多。
  - 開發過程中遇到一個不算小的 CSS 堆疊順序（stacking context）問題：賽事詳情頁裡 `.dh-content` 這層容器本身設了 `position:relative` 加上自己的 `z-index:1`，導致裡面任何東西不管自己的 z-index 設多高，都會被畫面上其他層（例如捲動時會固定在上方的分頁快速導覽列）蓋過去，選單開了但看不見或按不到。修正方式是選單開啟時暫時把 `.dh-content` 的 z-index 拉高、關閉時還原，不會影響一般捲動時快速導覽列原本的遮蓋效果。
  - 中途原本試過另一種修法：選單開啟時直接把面板搬到 `<body>` 底下，脫離所有祖先容器的堆疊限制。這個方向後來放棄了，因為賽事詳情頁裡有些按鈕的點擊事件是掛在特定容器上、不是整個網頁，把面板搬離那個容器之後，點擊事件不會再往上傳到那個容器，按鈕整個失去反應。改用前面說的「暫時拉高 z-index」這個不需要搬動 DOM 位置的做法，才沒有這個副作用。
  - 「刪除賽事」「清除全部資料」這種點兩次才會真的執行的按鈕，特別測過確認選單在點第一次之後會留著、正確顯示「確定刪除／清除？再按一次」，點第二次才會真的執行並收起選單，跟原本的操作習慣一致，不會因為選單自動收合而看不到確認文字。
  - 手機窄螢幕、深色模式、繁中／日文／英文三個語言都個別截圖確認過版面正常，選單不會超出畫面或被截斷。

## v2.45.0

- **功能使用次數統計（第一批）**：新增以 Firestore 為基礎的共用計數機制，記錄各功能被點擊的次數，之後可以用來分析哪些功能最常用、哪些最少用。這是第一批，涵蓋約 20 個較具代表性的功能：新增／複製賽事、貼上快速填入、切換獎牌牆／表格檢視、Excel／GPX／JSON 三種匯入、CSV／JSON 匯出、單場與全部賽事的 `.ics` 匯出、在 Google 日曆開啟、產生分享圖、下載賽前作戰卡、下載徽章成就圖、配速試算工具／管理鞋款／個人資訊面板的開啟、Google 登入、抓取即時天氣預報。其餘功能會在後續分批補上。
  - **架構**：單一共用 Firestore 文件（`analytics/featureCounts`），每個功能一個欄位，用 `increment()` 做伺服器端原子累加，不管多少訪客同時點擊都不會互相覆蓋掉彼此的次數。寫入權限刻意開放給所有訪客（含未登入），這樣才能統計到所有人的使用狀況；讀取則限制只有登入的人（也就是網站擁有者）能看，一般訪客沒辦法直接讀出完整的使用量統計。這條新規則需要重新發布 [`firestore.rules`](./firestore.rules) 才會生效，USAGE.md 有寫怎麼做。
  - **寫入節流**：點擊當下不會馬上發送網路請求，而是先在瀏覽器本機累加，大約每 8 秒、或分頁切到背景／關閉時才整批送出一次，避免使用者連續操作時對 Firestore 送出過多次個別寫入。這個批次機制實測過：連續多次點擊同一個功能會正確合併成一個數字（不是各自送出一次請求）、切到背景分頁時會立刻搶在計時器之前送出目前累積的次數（不會因為分頁太快關閉而遺失）。
  - **登入次數判定**：只計算使用者實際完成 Google 登入的次數，特別處理過「重新整理頁面時偵測到已經是登入狀態」這種情況（不會被誤算成一次新的登入行為）——這兩種情境分別測過，確認判斷正確。
  - 沒有設定 Firebase 的部署（`firebaseConfig` 還是預設值）完全不受影響，這個統計功能會自動整個不啟用，不會出現任何錯誤或空白動作。
  - 目前還沒有在網站裡做統計數字的檢視畫面，要看目前的累積次數需要直接到 Firebase Console 查看那份 Firestore 文件；之後如果需要，可以再另外做一個簡單的儀表板頁面。

## v2.44.0

- **直接在 Google 日曆開啟**：詳情頁「加入行事曆提醒」旁邊新增「在 Google 日曆開啟」按鈕，用 Google 日曆官方支援的網址參數直接開啟新增活動頁面（不需要 API 金鑰、不需要登入授權，純粹是組一個帶參數的網址），賽事名稱、日期、地點、賽事官網先自動帶入，不用像 `.ics` 一樣先下載檔案再匯入。
  - 網址格式照 Google 官方文件查證過（`action=TEMPLATE`＋`text`／`dates`／`location`／`details` 這幾個參數），不是憑印象拼湊。
  - 日期部分沿用既有 `.ics` 的做法，一律當全天活動處理（起訖日期，不含時刻），這樣完全不用處理時區換算問題，賽事所在地跟使用者瀏覽器時區不同也不會有落差；有測過賽事名稱含中日文字元時網址編碼正確、地址跟賽事官網沒填時會正確省略對應參數、完全沒有比賽日期的賽事則跟 `.ics` 按鈕一樣不會顯示這個按鈕。
  - 這個方式有一個跟 `.ics` 檔不一樣的地方：Google 日曆的網址參數沒辦法像 `.ics` 檔案那樣內建「賽事倒數一個月／一週」的提醒鬧鐘，活動建立後預設不會有這兩個提醒，需要自己手動加；比較看重內建提醒的話，原本的 `.ics` 匯出方式還是保留著，兩個按鈕並存，看情況選用哪一個。

## v2.43.0

- **徽章成就圖新增賽事數據與路線軌跡**：下載徽章成就圖時，原本徽章資訊下方一大塊空白，現在如果這個徽章有對應到單一觸發賽事，會補上那場賽事的關鍵數據（距離、總爬升、完賽時間、平均配速，只顯示實際有填的欄位，1~4 欄視資料多寡自動排列）；如果那場賽事也有匯入 GPS 軌跡，再下面會畫出路線軌跡圖——刻意去背，不畫地圖底圖，只留軌跡線條本身，起點用綠色圓點、終點用紅色圓點標示方向。
  - 軌跡線條的經緯度換算成畫面座標時，有針對緯度做餘弦修正（同樣一度經度在不同緯度代表的實際距離不一樣），避免路線的真實形狀被拉伸變形；用一條環狀路線跟一條點到點路線分別測過，兩種常見賽道形狀畫出來的比例都正確、不會跑掉。
  - 沒有對應觸發賽事的徽章（例如累積使用滿 5 雙鞋的「千足蜈蚣」），或該賽事沒有匯入 GPS 軌跡的情況，都測過確認會正確省略對應的區塊，不會留下空白或報錯。
  - 中文、日文、英文三個語言都測過版面：英文的欄位標籤（Distance／Elevation Gain／Finish Time／Avg Pace）比中文長不少，確認四欄並排時仍然沒有互相擠壓或換行跑版。

## v2.42.1

- 沒有新功能，針對上一版個人資訊面板做的小範圍品質檢查：
  - 修正安靜心率輸入負數或 0 時，儲備心率計算沒有擋下來、算出下限是負值的區間這個小問題（正常操作不太會遇到，但直接在資料層擋掉比較保險，不用依賴輸入框的 min 屬性）。
  - 確認「清除全部資料」不會連帶清掉個人資訊（心率區間／身體數據），行為跟既有的裝備範本、鞋款一致——這些都是可重複使用的個人參考資料，不是賽事紀錄本身；程式碼裡的註解也一併更新，明確寫出這個設計決定，不只是行為剛好正確。
  - 全面搜尋過一次，確認上一版抓到的「靜態 HTML 裡誤用只有 JS 樣板字串才會解析的 `${...}` 語法」這個錯誤模式沒有在其他地方重複出現。
  - JSON 匯出匯入的往返測試（號碼布、起跑分區、即時天氣快取這幾個較新的欄位）確認資料不會遺失。

## v2.42.0

- **個人資訊面板（心率區間＋身體數據）**：右上角 Google 登入區塊下方新增「建立個人資訊」按鈕，打開兩個頁籤的面板。這是跟「人」綁定的資料，不是跟單一賽事綁定，所有賽事共用同一份，獨立存成自己的一份檔案。
  - **心率區間**分頁：採用儲備心率法（Karvonen Method，安靜心率 +（最大心率－安靜心率）× 強度百分比），跑步／騎車／游泳三種運動分開設定安靜心率與最大心率，各自算出 Z1~Z5 五個區間的範圍（含 Z1 自己的上下限）。算法用查證過的外部範例（安靜心率 50、最大心率 185）驗證過，算出來的 Z2 區間（131–145 bpm）跟外部來源完全一致，不是憑印象套公式。
  - 這個功能真正的重點在整合：賽後紀錄裡「分段配速一覽」表格的心率欄位，現在會依那場賽事的運動種類，自動套用對應運動的個人化區間與顏色，不再只是單純的「心率 ÷ 這場賽事填的最大心率」。用跑步賽事（用跑步區間）、游泳賽事（用游泳區間，數字跟跑步不同）、以及還沒設定騎車區間的自行車賽事（正確退回舊的簡易估算，不會空白或報錯）三種情境都測過。
  - **身體數據**分頁：年齡、性別、體重、身高、體脂肪、肌肉重，體重供之後推力比等計算使用，其餘選填。
  - 兩個分頁都是欄位失焦或選單變更時即時儲存並持久化，測過重新整理頁面後資料還在。
  - 開發過程中抓到一個問題：按鈕最初寫成直接在靜態 HTML 裡放 `${t(...)}` 這種只有在 JS 樣板字串裡才會被解析的語法，因為這段本來就是靜態 HTML（不是 JS 產生的），導致三個語言都直接顯示成一串沒被解析的程式碼文字，不是預期的翻譯後文字。改成跟旁邊其他按鈕一樣的 `data-i18n` 屬性寫法後，三個語言都正確顯示了。
- **InBody 圖檔／PDF 自動匯入尚未開始**：這個功能牽涉到的技術問題（圖片需要 OCR 文字辨識、PDF 如果是掃描檔也一樣需要 OCR；就算是有選取文字的 PDF，InBody 不同機型的報表版面差異也很大，需要另外做版面辨識）風險與複雜度都明顯高於這次完成的部分，這次沒有貿然嘗試，會另外找時間討論怎麼做比較務實。

## v2.41.0

- **移除同系列配速疊圖功能**：這個功能上線後連續兩輪都出現「圖表看起來壞掉」的回報，雖然兩次都各自修正了實際的成因，但拿掉這個功能本身。相關程式碼、樣式、翻譯字串、說明文件都一併清乾淨，不是只隱藏起來。
- **修正分段配速一覽（以及系列賽事比較表、媒體縮圖列）在內容其實顯示得下時，最左邊、最右邊文字看起來像被吃掉的問題**：這三個區塊原本不管內容塞不塞得下容器寬度，都固定套用「邊緣淡出」效果（用意是暗示使用者「這裡可以左右滑動看更多」），但如果內容本來就顯示得下、根本不需要橫向捲動，這個淡出效果還是會讓最左邊跟最右邊的文字看起來半透明、像被裁掉一樣，容易被誤會成排版壞掉——回報的分段配速一覽正是這種情況（只有距離／配速／心率三欄，並不需要橫向捲動）。改成只在內容真的超出容器寬度、真的需要捲動時才套用這個效果，畫面大小改變時（例如轉直向/橫向）也會重新判斷一次。用回報的資料型態重現過問題（1K／2K 兩筆分段）確認邊緣文字現在完整可見；也測過同一份程式碼在真的需要橫向捲動的情況下（系列賽事比較表欄位較多時）淡出效果依然正常出現，不是整個拿掉。

## v2.40.3

- **修正同系列配速疊圖在分段紀錄很多筆、但只有少數幾筆有填「平均配速」欄位時幾乎完全空白的問題**：上一版（v2.40.2）修的是「分段紀錄本來就集中在賽事某一段」，但這次回報的萬金石馬拉松全馬情境不一樣：很多人記分段習慣是每公里記一次「累積距離＋累積時間」（例如 Garmin 自動分圈匯出），不會另外手動算、填「這一段的平均配速」欄位——圖表原本只認 avgPaceSecPerKm 這一個欄位，這種紀錄方式下大部分分段都會被當成「沒有配速資料」整批濾掉，只剩極少數真的有手動填配速的分段留在圖上，變成只在畫面邊緣擠出一小段看起來像壞掉的線，跟上一版修的問題長得很像、但成因不同。
  - 這次改成：有填 avgPaceSecPerKm 就直接用；沒填的話，只要這筆跟距離排序後緊鄰的前一筆都有累積距離跟累積時間，就直接用兩者的差算出這一段配速，不需要使用者自己手動換算填入；連第一筆分段也一併處理（假設賽事從距離 0、時間 0 開始）。用「42 筆分段都有距離跟累積時間、但只有最後兩筆手動填了配速」這個貼近萬金石馬拉松回報情境的測試資料重現過問題，確認修正後圖表正確顯示完整的 1~42 公里配速曲線。
  - 針對這次回報裡提出的「乾脆固定 X 軸 0~100 公里、Y 軸 1:00~20:00/km」：沒有採用這個做法，因為固定這麼寬的配速範圍，全馬正常配速（通常落在 4~7 分速之間）的實際變化幅度會被壓縮成一條幾乎看不出差異的平線，反而讓圖表更難用；改成修正資料判讀的根本問題後，圖表本來就會依實際資料自動縮放到看得清楚差異的範圍，不需要犧牲精細度換取「至少看得到東西」。
  - 另外針對重複距離、時間往回跳等異常資料也測過，確認會安全跳過不會讓整段計算掛掉或算出負的配速。

## v2.40.2

- **修正同系列配速疊圖在分段紀錄集中於賽事後段時幾乎完全空白的問題**：圖表 X 軸（距離）下限原本寫死是 0（代表「起點」），如果兩場賽事的分段紀錄本來就只集中在賽事的某一段（例如只補了最後幾公里的配速資料，沒有從頭開始記錄），寫死的 0 會硬生生撐出一大段完全沒有資料的空白區域，實際的配速曲線反而被擠壓成畫面最右邊一小條看起來像「壞掉」的線——這正是回報的畫面呈現的樣子。改成用兩場賽事分段資料裡實際出現的最小距離當下限（跟海拔剖面圖用同一種做法），圖表就會確實對應到資料涵蓋的範圍，不會再硬塞一段不存在的空白。用回報畫面同樣的情境（分段只從 38km 開始到 42km）重現過問題，確認修正後圖表正確撐滿版面；也重跑了前一版測過的既有情境（分段從頭開始記錄、兩場賽事距離不同），確認畫面沒有變化，這次的修正沒有影響到原本就正常的案例。

## v2.40.1

- **修正住宿／交通區塊的日期時間欄位被裁切看不到時間的問題**：入住時間、退房時間、發車/出發時間這幾個欄位，瀏覽器原生會同時顯示日期、上午/下午、時:分跟一個選擇器圖示，需要的寬度比一般文字輸入框多不少；但這幾個欄位所在的重複列表（住宿、交通）原本跟其他一般欄位共用同一個最小寬度（130px），欄位一多、排在同一列時常常被擠到只剩 130px，時:分的部分就會被裁掉看不見。這次針對這種日期時間欄位額外給一個比較寬的最小寬度（200px），其他欄位不受影響。用跟原始回報畫面接近的版面寬度測過，確認入住/退房時間現在能完整顯示「09/26/2026, 04:00 PM」這樣的完整日期時間，不會再被裁切。
  - 過程中另外發現一個跟這次回報無關、範圍更小的既有問題：手機寬度下「裝備、補給與戰略」整個區塊本身有大約 22px 的水平溢出（不需要任何資料就會出現，不是這次修改造成的），這次沒有一併處理，先記錄下來，之後有需要再處理。

## v2.40.0

- **同系列賽事配速疊圖（Ghost Comparison）**：五個功能需求中的最後一個，全部完成（即時天氣、賽前作戰卡、海拔剖面圖地圖聯動、單一徽章分享卡、本次的配速疊圖）。本場賽事跟同一個「賽事系列」的其他賽事都有分段配速資料時，賽後紀錄區塊會自動出現一張疊圖：本場一條實線、比較對象一條虛線，本場較快的區間上色成綠色、較慢的區間上色成紅色。同系列如果有 2 場以上可以比較，圖表上方會出現下拉選單切換要跟哪一年比，預設選最近的一場。
  - 兩場賽事的分段點距離不需要完全對齊（例如一場每 5km 記錄一次、另一場每 1km 一次），用線性內插對齊到共同的距離刻度上再比較色塊；用一組模擬「前半場超前、後半場撞牆」的測試資料驗證過，確認前後兩段能正確分別上色成綠、紅，不會混成一片。
  - 兩場賽事總距離不同時（例如系列賽某年路線微調），色塊只會畫在兩者距離重疊的範圍內，超出的部分維持單純畫線、不強行上色比較；截圖驗證過不會有版面跑掉或色塊亂畫的情況。
  - 沒有系列名稱、系列裡只有這一場、或比較對象沒有分段配速資料時，這個區塊不會出現，不會是空白或報錯。

## v2.39.0

- **單一徽章分享卡**：五個功能需求中的第 5 個（前 4 個已在先前版本完成：即時天氣、賽前作戰卡、海拔剖面圖地圖聯動；同系列配速疊圖／Ghost Comparison 目前還沒動工，是這五個裡面複雜度最高的一個，會在下一輪繼續）。開箱動畫新增「下載成就圖」按鈕（跟「收下徽章」並排，下載不會關掉動畫），榮譽櫃裡每個已解鎖的徽章下方也有同樣的連結，可以事後回頭下載任何一個已解鎖徽章的圖片。產生 9:16 直式圖片，徽章圖示（原本開箱動畫用的 emoji，這次確認在 canvas 裡能正確以全彩渲染，不是缺字方框）、名稱、達成條件、達成方式（沿用既有的「達成於：某場賽事（日期）」邏輯，沒有對應單一賽事的徽章正確顯示達成日期），配上呼應開箱動畫金色慶祝感的視覺，適合直接發布到社群限時動態。
- 順手在 USAGE.md 補上整個徽章系統（榮譽櫃、開箱動畫）的說明——這個功能經過好幾輪陸續擴充，但從來沒有寫進使用說明過，這次一併補齊。

## v2.38.0

- **海拔剖面圖與地圖游標聯動**：五個功能需求中的第 3 個（第 1、2 個已完成）。滑鼠移到海拔變化圖上時，即時顯示該點的累積里程、海拔、瞬時坡度，如果賽事同時有匯入 GPS 軌跡，路線地圖上會同步出現一個游標標記跟著移動；手機上用手指觸控滑動也支援。CP 站點會在圖上標出虛線位置，並用箭頭標示這個 CP 是落在爬升後還是下坡後。
  - 這個功能最核心的技術問題：GPS 軌跡（`trackPoints`，只有經緯度）跟海拔剖面資料（`elevationProfile`，距離＋海拔）是分開匯入、各自獨立降採樣的兩份資料，彼此沒有共同索引可以對應。解法是用 haversine 公式重新算出軌跡點自己的累積距離，再依比例去對應海拔圖游標的位置，而不是假設兩個陣列的點數或間距一致。
  - 開發過程中抓到並修正了兩個實測後才發現的問題：
    1. 軌跡點的「最近點」搜尋原本無條件進位到下一個點，資料點稀疏時會抓到明顯不夠近的點；改成同時比較前後兩個點，取真正距離最近的那一個。
    2. CP 是「爬升後還是下坡後」的判斷，原本用固定公里數的窗口去抓附近的海拔點，但海拔剖面資料點的間距落差很大，窗口太小的話常常一個點都篩不到、誤判成「持平」；改成不管點的實際間距、固定抓最近點前後幾個索引，資料點數夠就一定抓得到。
    3. CP 標記本身用的顏色是深色模式的顏色，在預設的淺色主題下幾乎看不見；改成白底深色外框的樣式，不管在哪個主題、疊在海拔圖哪種顏色的背景上都維持清楚可辨識，兩個主題都截圖確認過。
  - 完全沒有海拔或路線資料的賽事不會顯示這個區塊，不會是空白或報錯。

## v2.37.0

- **賽前作戰卡 / 鎖屏桌布產生器**：這是五個功能需求中的第 2 個（第 1 個即時天氣已在上一版完成）。賽前狀態的賽事詳情頁新增「下載賽前作戰卡」按鈕，產生 9:16 直式高對比圖片（號碼布、起跑分區/時間、CP 關門時間、緊急聯絡人、補給配方），可直接設成手機鎖屏桌布，賽中不用解鎖手機就能快速確認關門點。
  - 新增兩個欄位支援這個功能：**號碼布編號**、**起跑分區 / 波次**（都在「基本」分頁、起跑時間旁邊）。
  - 圖片版面經過測試：完全沒填任何細節時顯示引導提示而不是一張空白卡片；CP 數量很多時（測過 12 個）只顯示前 8 個並標明還有幾個未顯示，不會把版面撐爆；欄位內容豐富時（測過同時有號碼布、起跑資訊、4 個 CP、緊急聯絡人、補給配方）各區塊間距與分隔線都正確，沒有重疊。
  - 過程中修正一個問題：CP 距離的括號原本不管什麼語言都用全形括號「（）」，英文版這樣不符合排版慣例，已改成依語言顯示半形或全形括號。
  - 完賽狀態的賽事不會顯示這個按鈕（賽前作戰卡只在賽前用得到）。
- 距離開賽 7 天內、有匯入 GPS 軌跡的賽事，天氣卡片仍會優先顯示即時預報，跟賽前作戰卡各自獨立、互不影響。

## v2.36.0

- **即時賽事天氣（Open-Meteo API）**：這是這次一次收到的五個功能需求中的第一個，其餘四個（賽前作戰字卡／鎖屏桌布產生器、海拔剖面圖與地圖游標聯動、同系列配速疊圖、單一徽章分享卡）都還沒開始做，會在後續分開處理，不倉促一次全部趕上。
  - 備戰釘選面板的天氣卡片，距離開賽 7 天內、且賽事已經匯入 GPX/FIT 軌跡的話，會出現「抓取即時預報」按鈕，直接在瀏覽器呼叫免費、免金鑰的 Open-Meteo API（用軌跡第一個點的座標查詢），取得當日逐時氣溫、降雨機率、陣風風速，自動挑出最接近起跑時刻的一筆顯示。
  - 抓回來的資料快取在賽事自己身上，不會每次打開面板都重新呼叫一次 API；「重新整理預報」按鈕可以手動強制更新。
  - 沒有 GPS 軌跡的賽事會顯示提示文字，不會出現抓取按鈕（沒有座標可以查）；抓取失敗（網路問題等）會顯示明確錯誤訊息，不會整個卡住或靜默失敗。
  - 這個功能本身依賴外部 API，這次開發環境本身連不到 api.open-meteo.com（限制對外連線的沙盒環境），沒辦法測試真正的網路請求成功案例；改用攔截並回傳模擬回應的方式，驗證了實際的資料解析、依起跑時刻挑選最接近時段、寫回快取、畫面顯示這幾個環節都正確無誤，網址組成也對照官方文件範例逐項核對過（`temperature_2m`、`precipitation_probability`、`wind_gusts_10m` 這幾個參數名稱都是查證過的正確值，不是憑印象猜的）。實際在你自己的瀏覽器環境應該可以正常連線，如果遇到問題請告訴我。

## v2.35.3

- **修正 Excel 匯入沒有錯誤保護的問題**：選擇檔案後的解析過程原本完全沒有 try/catch，如果 Excel 解析工具（透過 CDN 載入的外部套件）剛好沒載入成功（網路問題、被廣告攔截外掛擋到、CDN 本身出狀況等），或是選到的檔案根本不是有效的 Excel 檔，整個匯入動作會靜默失敗——畫面上什麼提示都不會有，只有瀏覽器主控台會留下一行使用者永遠不會看到的錯誤訊息。這次補上跟 GPX 匯入一樣等級的錯誤處理：解析失敗時會跳出說明視窗，並且針對「解析工具本身沒載入成功」這個特定情況給出具體建議（檢查網路、重新整理、或先停用廣告攔截外掛試試看），不是只丟一句「發生錯誤」。用「解析工具確實沒載入」這個實際情境測試過（剛好這個開發環境本身連不到那個 CDN，直接拿來當測試案例），確認會正確顯示提示，不會是靜默失敗。
- 這是接續上一版的系統性排查，這次額外檢查了：honor-retire 徽章動畫期間按鈕能不能被連續點擊（原本以為是問題，重新用不強迫的點擊方式測過後確認既有的 `pointer-events:none` 保護本身就正確擋下了，不需要額外修正）、95 個徽章判定在 200 筆賽事規模下的執行效能（21ms，沒有問題）、GPX/TCX 檔案解析對畸形輸入的容錯（確認原本就有妥善的錯誤處理）。

## v2.35.2

- **修正兩個徽章（千里老兵、千足蜈蚣）永遠不會播放開箱動畫的問題**：這兩個徽章的達成條件不是綁定單一場賽事（分別是「鞋款退役時累積里程滿壽命」與「累積使用滿 5 雙不同鞋款」），系統原本只有「使用者點進觸發徽章的那場賽事」才會補放開箱動畫，但這兩個徽章根本沒有對應的單一賽事，永遠不會被配對到，等於解鎖了也不會有任何畫面回饋，只會在榮譽櫃裡悄悄多一個沒被看到的徽章。另外「標記為退役」這個操作本身也從來沒有觸發過徽章判定，就算之後改別的地方讓判定補跑，也要等到下次完賽才會發現。這次修正：徽章判定新增一個機制，對於沒有對應單一賽事的徽章，判定出新解鎖時直接播放，不用等一個永遠不會出現的時機；同時在「退役」「標記為退役」相關的操作裡也補上判定，不再只有完賽才會觸發。用實際情境測過確認兩個徽章都能正確在對的時機播放動畫。
  - 這個 bug 是這次系統性排查找到的，過程另外測過：95 個徽章全數重新跑過判定邏輯（含空資料防呆）、事件監聽器有沒有重複註冊、徽章判定裡引用的欄位名稱與選項值逐一比對資料結構確認沒有打錯字、匯出/匯入/刪除復原流程、z-index 疊層順序——這些都沒有發現問題。

## v2.35.1

- **新增 15 個徽章**：安心羈絆、跨國連線、櫻花追跡者、迴圈地獄、志在參加、全地形制霸、游刃有餘、高速賽道、海外野獸、水岸巡航、補給站獵人、溜滑梯特攻、鏡像分段、無痛巡航。總數從 80 個增加到 95 個。
  - 「新北探旅」做了簡化：原始描述提到「包含特殊賽制（如夜間定向越野）或新店周邊」，但系統裡賽事地點只記錄到「縣市」層級，沒有「特殊賽制」這種分類欄位，也沒有細到「新店」這種行政區以下的資料，這兩個條件都沒有依據可以判定，所以只保留「新北市累積 3 場」這個能明確判定的部分。
  - 「安心羈絆」判定邏輯跟稍早的「不敗金身」同一套（連續 N 場、中途中斷就重新累計），用一組刻意在中間拿掉緊急聯絡人的測試資料驗證過，確認會正確判定為未達成而不是誤判成還連續著。

## v2.35.0

- **新增 12 個徽章**：跨海遠征、星夜迷航、親情後盾、鋸齒狂人、無氧稜線、積分入帳、跨年連鬥、不敗金身、晨星領航員、減量期大師、前線起跑、終點狙擊手。總數從 68 個增加到 80 個。
  - 「鋸齒狂人」用海拔剖面圖的資料自動偵測方向反轉次數，且落差要達到 50 公尺以上才算數，避免把資料裡的微小雜訊誤判成一個山頭；用平滑爬升的假資料測過，確認不會誤判。
  - 「不敗金身」判定的是「連續 10 場」，賽事狀態會包含 DNS／DNF（不是只看已完賽的紀錄，因為那樣就永遠不會有 DNS/DNF 出現在序列裡，條件會變得沒有意義）；用一筆特地插入的 DNF 紀錄測過，確認會正確打斷連續紀錄。
- **兩個既有徽章的門檻調整**：這次收到的清單裡「強制裝備達人」跟「碳水無底洞」用的是新的門檻數字（強制裝備 5 項、能量膠 5 包），跟兩輪前建立時的門檻（8 項／8 包）不一樣。判斷這是同一個徽章的門檻更新，不是兩個新徽章，所以直接修改原本那兩個徽章的判定條件，不是新增重複的徽章。
- **三個項目沒有新增，附上原因**：「Watts goingon」「瓦數倉鼠」這次收到的內容跟兩輪前一模一樣（瓦數倉鼠的 FTP／推重比門檻依然沒有可判定的資料來源，原因同兩輪前的說明），已經存在的不重複新增；「膝蓋終結者」的條件（階梯比例超過 15%）跟既有的「石階地獄」完全相同，只是換了名稱，所以也沒有另外新增一個條件重複的徽章。

## v2.34.3

- **手機版頂部導覽列改成完全不固定**：上一版把固定範圍縮小到只剩「月份切換＋搜尋＋篩選」，但實測後這樣還是佔掉螢幕不小的空間，尤其是有海外賽事等次要篩選標籤時會再多一行。這次手機寬度下（640px 以下）不管是行事曆列表還是賽事詳情頁，頂部導覽列整個都不固定，一律跟著內容正常捲走，需要搜尋或篩選時捲回最上面即可。桌機、平板寬度（641px 以上）維持吸頂不變。
- 過程中也修掉一個連帶發現的問題：641~860px 這個平板寬度區間，原本負責調整吸頂列左右間距的樣式規則沒有限定範圍，會在 640px 以下的手機寬度也生效，跟這次新增的「完全不固定」規則衝突。已經把該規則限定在 641~860px 之間，跟手機寬度的規則互斥，不會再有疊加的問題。

## v2.34.2

- **修正手機版行事曆列表畫面頂部固定區塊過大的問題**：隨著這幾輪陸續加入生涯數據、徽章榮譽櫃等內容，頂部導覽列（品牌列＋行事曆列＋匯入匯出列三段疊在一起）在手機上實測高度逼近 450~485px，超過一支手機螢幕的一半，不管捲到哪裡都固定佔住這麼大一塊，變成不管往下滑多少，都會有一大塊資訊「擋住」下面剛長出來的生涯數據、榮譽櫃內容。這次把固定的範圍縮小：只有「行事曆月份切換＋搜尋＋篩選」這一段維持固定（往下捲動時螢幕上方會自動再壓縮一次，隱藏副標題），品牌列跟匯入匯出列改成跟著內容正常捲走。整個捲動範圍內固定區塊的總高度從原本約 447~485px 降到約 145~313px（依捲動位置浮動，捲到底時約為螢幕高度的 37%，原本約 55%）。
  - 過程中抓到一個真正的技術問題：CSS 的 `position:sticky` 只能在「自己所屬的容器還沒整個捲出畫面」的範圍內維持固定，如果只是把某一段的樣式改成 sticky、但它還是包在原本那個三段疊起來、捲一下就整個滑走的頂部容器裡面，捲過那個容器的高度之後，這段還是會被一起帶走，看起來像是固定了、其實只是撐了一小段就失效。所以除了改樣式，也把這段的 HTML 實際搬到跟頁面內容一樣長的容器裡，才能讓它在整趟捲動過程中真的維持固定——這點在提交前已經用長頁面實際捲動測試（捲到 5000px 深）確認過不會再失效。
  - 桌機寬度、以及手機上檢視單一賽事詳情頁的行為維持不變。

## v2.34.1

- **已解鎖徽章顯示達成條件**：名稱下方改成優先顯示徽章本身的解鎖條件說明（例如「比賽當日體感溫度落在 5°C ~ 16°C 之間且完賽」），下面再用更小的字附上「達成於：某場賽事（日期）」，兩種資訊都保留。
- **四大類別各自可收折**：探索與疆域、地形與極限、速度與戰略、跨界與裝備現在各自是獨立的收折區塊，標題旁會顯示該類別自己的解鎖進度（例如「3 / 20」），不用每次都整批一起展開或收起。
- **同類型徽章排在一起**：68 個徽章在各自類別內重新排序，讓條件相近的擺在一起方便比對，例如破三殿堂緊接著破三菁英、三個連續週末的挑戰（越戰越勇／勇者中的勇者／太喜歡了吧）排在一起、三鐵潛力股到三鐵傳奇的階梯依序排列。排序透過新增的 `order` 欄位控制，沒有更動任何判定邏輯本身，68 個徽章的解鎖條件測試過一輪，確認調整順序沒有影響判定結果。

## v2.34.0

- **新增 20 個徽章**：橫跨四大類別（探索與疆域、地形與極限、速度與戰略、跨界與裝備），總數從 48 個增加到 68 個。包含前線紮營、孤狼出征、天時地利、台幣戰士、東洋征途、定向尋寶員／定向菜雞、玩泥巴、單攻王、海平面起飛、中級山神獸、高反差地貌、分組凸台、無傷減量、巡航導彈、All-in、海神波賽頓、Watts goingon、強制裝備達人、碳水無底洞。
  - 兩個項目沒有實作，原因說明：「海線越野」需要判斷賽道是否「結合海線與山徑」，系統目前沒有這種地形分類欄位，沒有依據可以判定；「瓦數倉鼠」原本兩輪前就標註過 FTP／推重比門檻無法判定（沒有 FTP 欄位），這次的說明補了「推力比 2 以上且持續 1 小時」，但這個 app 沒有記錄體重也沒有記錄逐秒功率數據，同樣無法判定，維持原本簡化版本（自行車項目有填平均功率即符合）；同時新增的「Watts goingon」條件跟這個簡化版本完全一樣，為了不做出兩個永遠同時解鎖/同時鎖住的重複徽章，只新增了「Watts goingon」，沒有把「瓦數倉鼠」的條件改寫成重複的版本。
- **已解鎖徽章顯示達成方式**：榮譽櫃裡已解鎖的徽章，名稱下方會用小字標明「達成於：某場賽事（日期）」；沒有對應到單一觸發賽事的徽章（例如需要累積使用 5 雙不同鞋款的「千足蜈蚣」），改顯示達成日期。鎖定中的徽章維持原本的剪影加解鎖條件提示，兩種狀態的文字視覺上清楚區分。

## v2.33.0

- **徽章系統（榮譽櫃）**：生涯數據列新增「榮譽櫃」，48 個徽章橫跨探索與疆域、地形與極限、速度與戰略、跨界與裝備四大類。未解鎖的徽章顯示灰色剪影加問號、解鎖條件提示文字，累積型的徽章（例如「累積完成 3 場日本賽事」）還會顯示進度條。
  - **完賽當下的開箱體驗**：賽事狀態切成「已完賽」的當下，只要剛好觸發新徽章，會立即彈出全螢幕五彩紙屑動畫，配上金色徽章卡片；點「收下徽章」後才真正收進榮譽櫃。開啟系統「減少動態效果」設定的話會跳過動畫直接顯示結果。
  - **舊資料的補放機制**：這個功能上線前就已經完賽、且早就符合某個徽章條件的賽事，不會一開網站就轟炸畫面——會先在背景安靜記錄「已解鎖但還沒看過動畫」，等你之後點進「當初觸發這個徽章的那一場賽事」，才會補放開箱動畫。
  - 依你的說明訂正了「破百俱樂部」的「半馬破百」條件為半馬完賽成績低於 100 分鐘；「越戰越勇／勇者中的勇者／太喜歡了吧」原始描述中有兩個重複的「連續三個週末」，實作成連續 2／3／4 個週末的三個遞增檔位，如果理解錯了請告訴我調整；「瓦數倉鼠」原本提到的 FTP／推重比門檻沒有明確數值、這個 app 目前也沒有 FTP 欄位，暫時只判定自行車項目是否有填平均功率數據。
  - **已知限制**：目前 48 個徽章的名稱與解鎖提示文字只有中文版本，日文／英文介面下這部分內容會維持顯示中文（其餘所有徽章系統的介面文字，例如「榮譽櫃」「徽章解鎖」「收下徽章」等，都已經正確支援三語）。

## v2.32.1

- **既有鞋款的預期壽命可以編輯了**：上一版只有新增鞋款時能設定，已經存在的鞋款沒有編輯入口；這次在每雙鞋的里程數字旁加了一個編輯（✎）按鈕，點下去會就地變成可輸入的欄位，存檔後立即反映在健康度計算上。
- **耐久度光條改成分段式 HP 長條**：從原本的平滑漸層長條，改成 5 格區塊、隨里程逐格點亮的樣式，0~50% 綠色、51~80% 黃色、超過 80% 紅色並持續閃爍提醒；旁邊的文字說明也改得更具體：「中底彈性開始衰退」、「已達退役里程，注意受傷風險」。
- **榮譽退役儀式**：鞋款累積里程達到 100% 時，原本樸素的「退役」文字連結會換成醒目的「✅ 標記為退役」按鈕，點下去會有金色「光榮退役」印章蓋上去的動畫（沿用這個 app 原本就有的蓋章視覺語言），動畫結束後才真的移到「已退役」區塊，比平時任意時候想退役一雙鞋的操作更有儀式感。開啟系統「減少動態效果」設定的話會直接顯示結果，不播放動畫。
- 這次也順手抓到一個翻譯沒對齊的地方：中文版把健康度文字說明改得更具體之後，忘記同步更新日文/英文版的翻譯字典，導致這兩個語言還停在舊的簡短版本；已經一併補上。

## v2.32.0

- **鞋款管理全面改版**：
  - 新增鞋款時可以填「預期壽命(km)」（選填，預設 600），舊資料自動補上這個欄位，無痛升級。
  - 「刪除」改成「退役」：有比賽紀錄綁定的鞋款只能退役，不能直接刪除，賽事的歷史紀錄與總里程完整保留；完全沒有綁定紀錄的鞋款才會同時提供「退役」跟「刪除」兩個選項。退役的鞋款可以再按「恢復服役」拿回來用。
  - 已退役的鞋款會跟服役中的分成兩個區塊，套用灰階＋降低透明度效果，加上金色「光榮退役」標籤。
  - 每雙鞋名稱下方新增健康度漸層長條：0~50% 綠色（`--trail`）、51~80% 平滑漸變到黃色（`--gold`）、超過 80% 轉紅色（`--flag`），旁邊搭配對應的文字狀態標籤（服役中／中底衰退／建議汰換），顏色轉換的門檻值抓到一個原本不一致的地方並修正——長條顏色原本在剛好 80% 時已經變紅，但狀態標籤要超過 80% 才切換，兩者現在統一在同一個門檻切換。
  - 「本場穿著鞋款」下拉選單預設隱藏已退役的鞋款；如果某場賽事本來就綁定了一雙後來退役的鞋，那場賽事自己的下拉選單還是會保留顯示（並標註「光榮退役」），只是不會再提供給其他新賽事選用。
  - 賽事狀態改成「已完賽」且有綁定鞋款時，背景會計算這雙鞋累積里程是否剛好跨過 80% 或 100% 的門檻，是的話立即跳出提示（沿用既有的 toast 元件），只有真的跨過門檻那一刻才會提醒，不會每次存檔都重複跳。

## v2.31.2

- **移除離線狀態提示**：拿掉頂部導覽列的「離線中，變更將於連線後自動同步」提示，連同對應的 HTML、CSS、JS（定時檢查、事件監聽、手動關閉按鈕）一併清乾淨，不是只用樣式藏起來。本機資料一律先存本機這件事本身沒有變，只是不再額外顯示這個提示。

## v2.31.1

- **修正手機版檢視賽事詳情時頂部導覽列一直佔住畫面的問題**：頂部導覽列（行事曆切換、搜尋框、篩選標籤、匯入匯出這些按鈕）原本不管在哪個畫面都會固定黏在頂端；這些控制項只有在看行事曆列表時有用，點進單一賽事詳情頁後，捲動瀏覽賽事內容時，這一大塊固定不動的區域會佔掉手機螢幕一大半空間，擋住實際想看的賽事資訊。這次改成：手機寬度下，點進賽事詳情（或新增賽事表單）後，頂部導覽列不再固定，會跟著內容正常往上捲走；區塊快速導覽列（基本／路線／氣象…那排小按鈕）改成直接貼齊螢幕頂端固定，捲動賽事內容時還是隨時點得到。回到行事曆列表畫面、或桌機寬度瀏覽時，頂部導覽列維持原本固定的行為不變。
- **修正儲存失敗提示疊在頂部導覽列上的問題**：這個提示原本固定在畫面最上方，跟頂部導覽列搶同一個位置，兩者互相干擾；改成固定在畫面最下方，不會再跟頂部導覽列衝突。
- 附註：這次錄影裡看到的紅色「本機儲存失敗」提示，版本號顯示是 v2.30.1——這正是上一版（v2.31.0）「改用 IndexedDB」要解決的問題（原本用 localStorage，78 場賽事加上封面照片容易超過瀏覽器約 5MB 的額度上限），更新到 v2.31.0 以後，這個特定的儲存失敗提示應該就不會再出現了。

## v2.31.0

- **改用 IndexedDB 儲存（原本是 localStorage）**：本機資料改存在 IndexedDB（經由開源套件 idb-keyval，CDN 免費載入），額度通常是數百 MB 到數 GB，不會再有 localStorage 那種 ~5MB 就見底的問題。CDN 連不到時（網路問題、被封鎖）自動退回原本的 localStorage，確保這個核心功能不會因為外部套件載入失敗就整個壞掉。第一次用新版開啟網站時，會自動偵測並搬移舊版留在 localStorage 裡的資料到 IndexedDB，只搬一次，不會遺失既有資料，也不會每次開啟都重跑一次搬移。因為 `persist`／`loadRaces` 這幾個核心函式先前就已經是非同步（為了支援 Claude 內建 artifact 預覽環境而設計），這次改動不需要大規模調整呼叫端程式碼；唯一原本是同步寫法的 `loadSyncOverwrites`／`persistSyncOverwrites`（同步覆蓋紀錄）已一併改成非同步。移除了原本針對 localStorage ~5MB 額度設計的「儲存空間接近上限」黃色主動預警（改用 IndexedDB 後這個假設不再成立），保留真正存檔失敗時的紅色錯誤提示。

## v2.30.1

- **加入專案授權**：新增 `LICENSE` 檔案，採用 PolyForm Noncommercial License 1.0.0（官方原文，未經改寫），個人/研究/教育/非營利等非商業用途可自由使用、修改、散布，不授權商業性使用。頂部導覽列版本號旁新增「© 2026 劉恩龍 LIU EN LUNG」版權標示；使用說明視窗底部新增「版權與授權」段落並附授權連結；README.md 新增授權段落。原本的 GNU AGPLv3 與「All Rights Reserved」都沒有採用——AGPLv3 是可以商用的（只是要求原始碼公開，不是禁止商用），跟「杜絕商業炒作」這個目標不完全吻合；「All Rights Reserved」則會連非商業的社群使用都一併擋掉，跟「與社群良性交流」互相矛盾。PolyForm Noncommercial 是專門為「非商業用途開放、商業用途禁止」這個組合設計的授權條款，比較貼近原始需求。

## v2.30.0

- **封面主色環境光暈**：有封面照片的賽事，詳情頁標題區塊後方會用已經抽取好的主色調（`coverImageAccentColor`）暈染一片大範圍模糊的柔光，沒有照片的賽事不受影響。實際截圖調過參數，原本照你給的數值（模糊 120px、透明度 0.15、範圍較大）看起來比較像一塊有軟邊的色塊而不是真正的柔光暈染，縮小範圍、提高模糊相對於色塊的比例後，邊緣過渡更自然。
- **時間降噪排版**：成績儀表板的大字完賽時間、全馬 PB 榮譽卡片、備戰釘選的配速目標時間，冒號透明度降到 0.4，數字本身保持完整對比。範圍上沒有套用到密集表格裡的小字時間欄位（系列賽事比較、分段配速表、緊湊表格檢視）——那些欄位本來就小又密集，跟其他純文字欄位混在一起，部分文字降低透明度反而增加雜訊而不是減少。
- **橫向滾動漸隱邊緣**：系列賽事比較、分段配速一覽（順便讓這個表格本身也真的能橫向捲動了，之前沒有）、多媒體相片網格，捲動容器邊緣改用 `mask-image` 做柔和淡出，兩側都會淡出（不是只有右側），不管捲到哪個位置邊緣視覺都一致。
- **獎牌牆懸浮聚焦**：滑鼠移到獎牌牆的相片卡片上，照片會有微小放大效果，底下的文字區塊背景也會稍微加深。

## v2.29.2

- **離線提示「X」按鈕徹底重做**：連續三次回報同一個「離線提示卡住、按 X 沒反應」的問題，橫跨 Chrome、Edge、iPhone 三種不同瀏覽器引擎都一樣——這種一致性代表問題不太可能是單一瀏覽器的怪癖，是先前那幾版的「聰明」自動復原邏輯本身有問題。這次整個簡化：按鈕改成直接綁定事件（不透過 document 層級的事件代理），關掉後在這次頁面瀏覽期間是絕對的，任何自動檢查（定時輪詢、瀏覽器事件、切回分頁）都不會再把它打開，除非重新整理頁面。用大量重複觸發事件（10 次 offline 事件、跨越兩輪定時輪詢、分頁切換事件）測過，確認按了就是真的關掉；也確認重新整理頁面後會照常重新判斷，不是整個功能永久失效。附註：如果離線提示本身仍然常態性出現，有可能是瀏覽器依據當下的網路環境（VPN、特殊路由等）回報的連線狀態，不完全是這個網站能單方面修正的瀏覽器限制——但不管實際連線狀態為何，這次「X」按鈕保證按了就會關掉。

## v2.29.1

- **修正離線提示的「X」按鈕按了沒反應的問題**：上一版（v2.26.1）修離線提示卡住的問題時，自己另外種下一個新 bug——只要瀏覽器觸發一次 offline 事件，就會把「使用者已經手動關掉」的狀態重置回去。問題是瀏覽器在還沒恢復連線之前，其實可能重複觸發好幾次 offline 事件，不是每一次都代表真的重新斷線一次。結果就是按 X 關掉提示，幾乎馬上被下一次 offline 事件打開，看起來像完全沒反應。這次改成只追蹤「真的從連線狀態變回離線」這種情況才會重置，已經用重複觸發 offline 事件（不夾雜 online 事件）的方式重現並確認修好；連線真的恢復、或是真的重新斷線一次，提示還是會照常運作。

## v2.29.0

- **備戰釘選英雄倒數視覺**：備戰釘選卡片裡的賽事如果有封面照片，標題區塊會改成全滿版的照片背景，疊加巨大的倒數天數數字，背景圖用 JS 依捲動位置做視差位移（沒有用 `background-attachment:fixed` 這個常見做法，因為那個在 iOS Safari 上經常失效，這個 app 有不少人會用「加入主畫面」在手機上開，所以改用可以跨瀏覽器穩定運作的位移寫法，也有做位移範圍限制避免捲到底部/頂部露出圖片邊緣空白）。裝備打包進度、配速策略、天氣預報這幾張小卡片維持在下方，功能沒有減少。沒有封面照片的賽事維持原本的樣式，不會顯示一個空的英雄區塊。有開啟系統「減少動態效果」設定的話會停用視差效果。
- **全馬 PB 榮譽卡片**：生涯數據列的「全馬 PB」從原本跟其他數字並排的小欄位，獨立成一張置中的榮譽卡片，用大字級的金色等寬字體呈現成績。

## v2.28.0

- **生涯數據數字滾動動畫**：總完賽場次、今年累計里程會用電子碼表式的滾動效果跳到最終數值；也新增了「生涯累計爬升」這個新的數據卡片。動畫只有在數值真的變動時才會播放（例如剛把某場賽事標記完賽），單純因為其他操作重新整理畫面、數字沒變的話不會重播，避免每次互動都跳動反而分心。有開啟系統「減少動態效果」設定的話會直接顯示最終數字。
- **裝備打包進度改成動態進度環**：備戰釘選卡片裡的裝備打包進度，從長條圖改成圓形進度環，開啟時會有填滿的動畫效果，中間顯示「已打包／總數」。
- **修正一個找出來的既有翻譯漏洞**：生涯數據列的場次/里程數字後面的單位文字（「場」「公里」）之前是寫死的中文，不管切到日文還是英文介面都只有數字換了、單位還是中文，變成「6 races」寫成「6 場」這種混雜。這次一併修正並加上正確的多語系單位文字。

## v2.27.0

- **毛玻璃導覽列**：頂部導覽列改成半透明背景加 `backdrop-filter: blur(12px)`，往下捲動時底下內容會隱約透出模糊的顏色，深色/淺色模式各自調過透明度與模糊程度。快速導覽列（區塊跳轉那排小按鈕）也一併套用同樣效果，避免跟頂部導覽列的視覺風格不一致。
- **漸層海拔剖面圖**：海拔變化圖的面積填色改用 SVG 遮罩做垂直漸層，從貼近稜線的地方開始淡入，往下漸漸變透明到底部完全看不見，不再是一整塊同樣深淺的色塊。之前做的「依坡度上色」（平緩綠色、陡峭紅色）維持不變，這次是疊加一層「上濃下淡」的效果在上面，兩個效果同時存在，不是二選一。

## v2.26.1

- **修正離線提示卡住的問題**：先前完全依賴瀏覽器的 `online`/`offline` 事件更新離線提示，但這兩個事件在部分瀏覽器、作業系統、網路環境組合下並不可靠，有紀錄顯示的案例是明明已經恢復連線，事件卻沒有觸發，導致提示一直卡在「離線中」。這次加了兩層修正：每 8 秒重新檢查一次目前的連線狀態（不依賴事件）、切回分頁時也會重新檢查；另外離線提示旁邊新增一個「✕」按鈕，如果提示卡住了可以直接點掉，之後只要真的偵測到連線恢復、或是又發生一次新的離線狀況，提示會自動恢復正常運作，不會被永久關掉。

## v2.26.0

- **數據表格靠右對齊**：系列賽事比較、分段配速一覽、配速試算表這三個表格的數字欄位（時間、距離、中籤率、心率）改成靠右對齊，冒號跟小數點會對齊在同一條線上，掃視比較不同列的數字時更好讀。分段配速表的「配速」欄位維持左對齊——那一欄本來就用背景長條圖顯示相對快慢，跟右對齊要解決的問題是同一件事，兩個疊在一起反而互相干擾。過程中抓到一個 CSS 優先權的問題：原本通用的靠右對齊規則跟表格自己的靠左對齊規則優先權相同，後宣告的會贏，剛好表格自己的規則排在後面，會把我要的靠右對齊蓋掉；已經改成優先權更高的寫法，不受宣告順序影響。
- **狀態標籤柔和化**：`.stamp` 狀態標籤、PB 徽章、減量期標示，從「外框+文字色」改成「10~20% 透明度背景色+文字色」，去邊框，深淺色模式都有各自調過的透明度數值（深色模式的背景色需要更高的透明度才看得清楚，不是兩個模式套用同一組數字）。原本 stamp 標籤的輕微傾斜（像蓋章）維持不變——這是既有的設計巧思，跟這次要求的邊框/背景改動是兩件事，沒有一併拿掉。

## v2.25.0

- **卡片化設計**：賽事詳情頁的六大區塊（基本、路線、氣象、預算、裝備、賽後）從原本的底線分隔，改成獨立卡片——白色（深色模式為 `--surface`）背景、8px 圓角、淡淡的陰影，卡片之間留白分開。同時把頁面底色（`--paper`）調暗一點點，讓卡片跟底色的層次更明顯。深色模式的陰影透明度另外調高（0.3 而非淺色模式的 0.05），不然陰影在深色背景上幾乎看不見。
- **表單欄位呼吸感**：欄位之間的間距加大，標籤文字顏色從 `--ink-soft` 改成更淡的 `--stone`，跟實際填寫的資料（維持原本較深的顏色）拉開對比，掃視表單時更容易分辨「這是標題」還是「這是我填的內容」。

## v2.24.0

- **柔光 Focus 狀態**：全站鍵盤 focus 樣式從實線外框改成柔和的光暈陰影（`box-shadow` 加短過場動畫），輸入框額外加上邊框顏色過場。透明度比原本建議的 0.2 略高（淺色模式 0.28、深色模式 0.35），確保在一般螢幕亮度下仍看得清楚，不會因為太淡而失去 focus 指示的實際功能（這是鍵盤操作、無障礙輔助工具都會依賴的東西，太淡等於變相拿掉這個功能）。修正一個過程中抓到的細節：一開始的寫法會讓所有 focus 狀態的元素都變成方角，圓形的懸浮按鈕、藥丸形狀的篩選標籤被 focus 到時會整個變形，已修正為讓光暈自動貼合每個元素原本的形狀。
- **行事曆「今天」實心圓底白字**：日期數字從「綠字＋外框」改成 iOS 行事曆那種實心圓底白字，視線一落到月曆上就能立刻定錨今天的位置。

## v2.23.0

- **封面照片自適應主題色**：上傳封面照片時，會在瀏覽器本機用 Canvas 分析像素（不需要外部套件、不會上傳到任何地方），取樣統計排除太暗、太亮、太灰的像素後，找出照片的主色調，套用到標題區塊的倒數進度條顏色，讓每場賽事的頁面依照片呈現不同氛圍。賽事進入「7 天內」的緊急倒數狀態時，維持既有的警示紅色，不會被主題色蓋掉（功能性警示優先於裝飾）。
- **摺疊區塊展開動畫**：賽事詳情的六大區塊（基本、路線、氣象、預算、裝備、賽後）展開時，內容會有一個輕微淡入＋位移的過場，不再是瞬間跳出。收合方向維持原生瞬間收合（這是目前瀏覽器 `<details>` 標籤的已知限制——要做到收合也平滑，需要攔截每一次點擊、延遲原生收合時機，牽動範圍太大、風險偏高，這次先做風險較低、也是使用者最常感受到的展開方向）。已加上 `prefers-reduced-motion` 判斷，開啟「減少動態效果」系統設定的使用者會停用動畫。
- **卡片懸浮與點擊回饋**：行事曆格子、手機版清單卡片、獎牌牆卡片，滑鼠移上去會有輕微上浮＋陰影，點擊時有微縮回饋，操作手感更接近原生 App。緊湊表格檢視維持原本的純色塊 hover，沒有加上浮動效果——密集資料表格習慣上不會有這種效果，加了反而干擾閱讀對齊。

## v2.22.0

- **分段配速內嵌長條圖**：賽事詳情新增一張「分段配速一覽」表格，配速欄位背景用漸層畫出長條，長度對應該分段的相對快慢（該場賽事最快的分段長條最滿），一眼就能看出哪一段掉速，不用逐行讀數字。
- **心率區間色彩標籤（Z1~Z5）**：成績儀表板的平均心率、分段配速表的心率欄位，會依「該場賽事自己填的最大心率」算出百分比，套上經典的灰／藍／綠／橘／紅五區間配色，並加上 Z1~Z5 文字標籤（不只靠顏色，避免色盲使用者看不出差異）。沒有填最大心率時維持原樣不上色。
- **參賽熱力圖**：生涯數據列新增類似 GitHub 貢獻度熱力圖的檢視，以年為列、月為欄，顏色深淺對應該月總里程，滑鼠移上去可以看到當月場次與里程明細。

## v2.21.0

- **扁平化 CSV 匯出（自動化友善）**：新增「匯出 CSV（扁平化）」按鈕，一次下載 10 份 CSV 檔——`races.csv` 主表（所有純量欄位攤平成單一資料列，例如 `route_distanceKm`、`location_country`）加上 9 份子表（`checkpoints.csv`、`splits.csv`、`equipmentChecklist.csv`、`goals.csv`、`nutritionSchedule.csv`、`accommodations.csv`、`transportation.csv`、`companions.csv`、`mediaLinks.csv`），每份子表都有 `race_id`／`race_name` 欄位對回主表，設計成關聯式結構，方便直接用 SQL JOIN、Python `pandas.merge`、Excel VBA 逐表讀取。已刪除（垃圾桶）的賽事不會出現在匯出結果。GPS 座標與海拔剖面點位資料量大且非分析用途，不列入匯出（需要完整資料仍可用既有的 JSON 匯出）。CSV 開頭加了 UTF-8 BOM，確保 Excel 開啟中文/日文內容不會變亂碼；欄位名稱固定使用英文，不會因為介面語言切換而改變，避免自動化腳本因此失效。

## v2.20.0

- **累積爬升趨勢圖**：生涯數據列新增第二個圖表，以每季為單位畫出總爬升量的趨勢線，至少要有 2 個季度的已完賽資料才會顯示（只有 1 個點畫不出趨勢）。
- **緊湊表格檢視**：行事曆區塊新增第二個檢視切換按鈕，可以把賽事清單轉換成類似 Excel 的純文字密集表格——日期、賽事名稱、狀態、距離、總爬升、完賽時間，數字欄位靠右對齊方便跨年比對歷屆表現；表格會跟著目前的篩選條件走，點列可直接開啟該場賽事。

## v2.19.0

- **備戰釘選（Focus Mode）**：賽前狀態的賽事若在 14 天內開賽，主畫面最上方會自動獨立顯示一張卡片，直接展開裝備打包進度（幾件已打包／總數）、目標配速策略、天氣預報，不用在清單裡找。同時開賽的賽事有好幾場時，只會顯示最近的一場。可以「暫時隱藏」（重新整理頁面後會再出現，不是永久關掉）。
- **減量期狀態標示**：賽前訓練計畫設定的「減量期開始日」一旦跨入，會在備戰釘選卡片與賽事詳情頁自動亮起「進入減量期」標示，不用自己算日期。

## v2.18.0

- **多媒體相片網格**：賽後紀錄的「多媒體與連結」下方新增一條橫向捲動的縮圖網格。連結本身直接指向圖片檔（.jpg/.png/.gif/.webp/.avif）時會顯示真正的縮圖；其他類型的連結（相簿首頁、社群貼文、PDF 等）因為瀏覽器基於安全性限制無法跨網域讀取對方網頁內容取得預覽圖，改顯示對應類型的圖示卡片（相簿用相機圖示、獎牌用獎盃圖示、文件類用文件圖示），一樣可以點擊開啟；圖片連結萬一載入失敗，也會自動退回顯示圖示卡片，不會留著一張破圖。
- **海外賽事篩選**：篩選列新增「海外賽事」標籤，會自動判斷出現次數明顯過半的國家當作主場（低於一半時不會顯示這個篩選，避免誤判），點開後可以再依國家細分（例如「日本 (3)」），把跨國遠征的賽事單獨拉出來看。

## v2.17.0

- **離線狀態提示**：瀏覽器偵測到沒有網路連線時，頂部導覽列會出現「離線中，變更將於連線後自動同步」的提示（連線恢復後自動消失）。這只是把既有事實顯示出來——本機資料本來就是優先存 localStorage，跟有沒有網路無關，離線時所有編輯照樣正常儲存；這個提示單純是讓使用者在山區收不到訊號時，能親眼看到「離線也沒關係」，不用自己猜。

## v2.16.2

- 路線軌跡圖的路線顏色從暗綠色改成深紅色搭配發光效果（外層較寬、半透明的亮紅色疊在深紅色實線下方），不管是一般地圖還是地形圖（綠色系為主）背景，深色或淺色模式下都清楚好辨識。起點/終點標記也一併調整成白色外框的亮綠色／金黃色，跟路線顏色明確區分開來。

## v2.16.1

- 運動種類新增「游泳」，主題色用水藍色（跟既有的三鐵色、一般狀態藍色都特意錯開，不會混淆）。新增專屬的游泳圖示（波浪+划手姿勢），也新增「游泳標配」裝備範本（泳鏡、泳帽、開放水域安全浮標等，泳鏡/泳帽/浮標預設必填），套用跟前一版其他運動種類一樣的自動套用邏輯；游泳賽事跟自行車一樣不會顯示「比賽鞋款」追蹤。

## v2.16.0

- **依運動種類動態調整表單**：新增賽事、或後續變更賽事的運動種類時，如果裝備清單還是空的，會自動帶入對應的標準範本（路跑／越野／三鐵／新增的自行車標配），已經自己填過內容的清單不會被覆蓋。越野跑、超馬、障礙賽會自動帶出頭燈、求生毯等強制裝備並標記必填；自行車賽事會隱藏「比賽鞋款」追蹤（改用清單裡的功率計欄位），三鐵、二鐵因為仍有跑步項目所以維持顯示。
- **海拔變化圖坡度上色**：圖表現在會依兩點間的坡度百分比逐段上色，從平緩的綠色到陡峭（≥25%）的深紅色，附上小圖例，不用細看數字也能一眼找出「魔王陡坡」在哪裡。
- **封面照片焦點調整**：新增「調整焦點」功能，可以在完整照片上點選或拖曳決定裁切基準點（存成百分比座標），套用到行事曆格子、清單、獎牌牆這些會把照片裁成正方形的小縮圖，避免專業相機拍的照片主角被裁掉。詳情頁標題背景本身是完整顯示不裁切，所以焦點只影響小縮圖，不影響標題背景。

## v2.15.1

- **手機清單卡片補上地形資訊**：手機版月曆清單的賽事卡片，名稱下方新增一行淡色小字顯示距離與總爬升（例如「25K · +900m」），沒有這些資料的賽事則不顯示該行，不用點進詳情頁也能一眼掌握賽事硬度。

## v2.15.0

- **運動類型專屬主題色**：8 種運動種類（路跑、越野跑、超馬、二鐵、三鐵、自行車、障礙賽、其他）各自有一個專屬的低飽和度顏色，套用在賽事詳情頁的運動圖示、標題區塊下緣的裝飾線（沒有封面照片時），以及行事曆格子／清單裡沒有封面照片賽事的小色點，讓掃視行事曆或切換賽事時能更快辨識出是哪種運動。
- 顏色刻意跟既有的狀態色系（綠色＝已報名／已完賽、藍色＝考慮中／抽籤中、橘紅＝DNS／DNF）錯開色相，避免和賽事狀態的顏色語意混淆；深色模式下也各自有對應的較亮版本。

## v2.14.0

- **清單拖曳排序**：裝備清單、補給時程規劃、分段配速這三個區塊，每筆資料左側新增拖曳把手，滑鼠可以直接拖曳調整順序。考量到原生拖放無法用鍵盤操作、觸控裝置支援度也不一致，同時提供「上移／下移」按鈕作為鍵盤與觸控都能用的替代方案（第一筆的上移、最後一筆的下移會自動停用）。
- **完賽自動導覽**：把賽事狀態從「已報名／抽籤中」等賽前狀態切換成「已完賽」時，畫面會自動展開並平滑捲動到「賽後紀錄與個人數據」區塊，不用再手動往下找。

## v2.13.0

- **路線地圖深色模式**：切換到深色模式時，地圖底圖會套用濾鏡（invert + hue-rotate + brightness + contrast）轉為深色，Leaflet 內建的縮放/圖層/版權控制項也一併換成深色配色，不會再是一塊刺眼的白色。
- **地形圖圖層**：路線地圖新增圖層切換（標準地圖／地形圖，地形圖用 OpenTopoMap），越野跑、超馬、障礙賽預設會顯示地形圖，其他運動種類預設標準地圖。
- **獎牌牆檢視**：行事曆左上角新增一個切換按鈕，可以切換成「相片卡片」網格檢視，只顯示已完賽且有封面照片的賽事，依日期新到舊排序，個人最佳會標註 PB 徽章，點卡片可直接開啟該場賽事。目前沒有符合條件的賽事時會顯示提示，鼓勵完賽後幫賽事加張照片。
- **切換賽事的淡入動畫**：點行事曆或清單切換賽事時，右側詳情頁改成 150ms 淡入效果，視覺上比原本瞬間替換內容更柔和（有開啟「減少動態效果」系統設定的使用者會停用動畫）。

## v2.12.0

- **新增範例資料**：右上角「+ 新增賽事」下方新增「範例資料」連結，一鍵建立兩場示範賽事——一場路跑（報名中狀態，示範抽籤、住宿交通、目標配速、裝備清單等賽前欄位）、一場越野賽（已完賽狀態，示範成績儀表板、分段配速、海拔圖、路線地圖、賽後心得等欄位），幫助剛開始使用的人快速了解每個欄位大概填什麼。名稱都加上「範例｜」前綴避免跟真實賽事搞混，用一般的刪除功能就可以移除。重複點擊不會一直疊加新的範例。

## v2.11.0

- **新增歷年完賽長條圖**：生涯數據列旁邊新增一個小型長條圖，顯示每年完賽場次，跟既有的統計數字並排顯示。
- **新增路線軌跡地圖**：匯入 GPX／TCX／FIT 檔案時，除了原本的距離、爬升、心率，現在也會保留一份精簡過的座標軌跡（最多 400 點，避免資料量暴增），在「地理與路線」區塊顯示成實際地圖上的路線（用 Leaflet.js + OpenStreetMap，只有賽事真的有座標資料時才會載入地圖套件，沒有座標的賽事完全不受影響、不會拖慢載入速度）。
- 修正：地圖資料存在但賽事狀態是「已完賽」時，路線區塊原本預設是收合的，地圖會因為容器還沒顯示、量不到正確尺寸而跑版；現在只要有座標資料，這個區塊就會自動展開。
- 順手修正一個沿用很久的翻譯漏洞：海拔變化圖的標題文字沒有接上多語系字典，一直顯示中文。

## v2.10.0

- 空狀態視覺優化：CP／補給站、住宿、交通、隨行人員、裝備清單、補給時程、分段配速、多媒體連結、鞋款管理、資料復原（已刪除賽事／同步覆蓋紀錄）——這些原本只有一行灰字「尚未新增」的地方，都加上對應主題的低飽和度小圖示（旗子、房子、汽車、人像、裝備袋、瓶罐、碼表、連結、鞋子、垃圾桶、同步箭頭），風格跟首頁空狀態的運動圖示一致。
- 過程中修了三個圖示：交通原本畫成像電腦螢幕，改成看得出是車子；裝備清單原本畫兩次都變成掛鎖（一個圈圈疊在方塊上，怎麼調整內部細節看起來都還是像鎖），最後改成側背包/裝備袋的寬扁造型才跳脫這個問題；鞋子純線條在小尺寸下糊成一團，改成實心剪影才看得出鞋型。

## v2.9.0

依你提出的三點 UX 建議實作：

- **手機版新增賽事懸浮按鈕**：畫面 <640px 時，右下角新增一個「+」懸浮按鈕（疊在使用說明小人上方，不會互相遮擋），改用單手更好點擊；原本頂部的按鈕在手機版隱藏，避免重複。測試時抓到一個真正的 bug：一開始按鈕點了完全沒反應，因為對應的事件監聽器只綁在賽事詳情區塊裡，懸浮按鈕在那個區塊外面所以收不到——已修正，改綁在全域層級。
- **自動儲存視覺回饋**：標題列新增小指示器，欄位修改後會顯示「儲存中…」（一個會脈動的小圓點），存檔完成後變成「已自動儲存 ✓」，2.5 秒後淡出。
- **長表單快速導覽**：詳情頁新增一排可吸頂的錨點按鈕（基本／路線／氣象／預算／裝備／賽後），點擊直接跳到對應區塊並自動展開（如果原本是收合的），不用再手動捲動翻找。吸頂位置會自動跟著頂部導覽列的實際高度走（包含手機捲動時導覽列縮小的狀態），不會疊在一起。

## v2.8.1

- 小修正：「清除全部資料」原本沒有一併清掉同步覆蓋紀錄，賽事都清空後那些紀錄其實已經失去意義；裝備範本、鞋款資料維持不動（這兩個是可重複使用的參考資料，不是賽事紀錄本身，清除賽事不應該連帶清掉）。

## v2.8.0

- **修正：「匯出 JSON」備份其實沒有對應的還原方式**——之前只做了下載，沒有做「匯入」，等於備份檔案存下來後其實沒有路可以用。新增「匯入 JSON」，可以把之前匯出的檔案讀回來；如果匯入的賽事跟現有資料有衝突（同一筆賽事兩邊版本不同），會沿用跟雲端同步一樣的處理方式：自動套用較新的版本，較舊的保留在「資料復原」，不會直接蓋掉遺失。
- 實測過完整的「匯出 → 清空資料 → 重新整理 → 匯入」流程，確認真的能把資料復原回來，不只是看程式碼覺得應該可以。
- 順手驗證了一個原本懷疑的效能問題：用你這種超馬距離會產生的大型 GPX/FIT 檔（模擬 20 小時、7.2 萬個座標點的 UTMB 等級檔案）實測匯入只要 0.6 秒，確認不是問題，沒有為此新增不必要的東西。

## v2.7.0

- **修正：雲端同步失敗完全不會通知使用者**——跟本機儲存失敗是同一類問題，只是這次是雲端那一側：網路不穩導致同步失敗時，過去只會印在 console，本機資料雖然安全，但雲端會悄悄跟本機不同步。現在改成失敗會自動重試（15 秒後），連續失敗 3 次以上才會在登入資訊旁顯示提示（避免暫時性網路小抖動就緊張兮兮跳警示），下次同步成功會自動消失。
- **修正一個找了很久才發現的既有 bug**：整個「使用 Google 登入同步」區塊（登入/登出按鈕、雲端同步狀態、推播通知按鈕）其實從來沒有真正接上多語系字典——字典裡老早就有日文英文翻譯，但畫面上一直硬編碼顯示中文，不管切到哪個語言都一樣。這次一併修正並三個語言都實測過。

## v2.6.0

這次處理的是我先前評估但還沒動手的兩個風險，另外多抓到一個更嚴重的問題：

- **修正：本機儲存失敗完全不會通知使用者**——過去如果瀏覽器儲存空間滿了，存檔失敗只會印在瀏覽器開發者工具的 console，畫面上完全沒有任何提示，等於使用者以為存好了但其實沒有。現在存檔真的失敗時，畫面最上方會出現明顯的紅色提示，直到下一次存檔成功才會消失。
- **新增：本機空間使用量預警**——資料量接近瀏覽器儲存上限（保守抓 3MB，因為 Safari 額度較小）時，會主動跳出提示建議先匯出備份，不用等到真的存不下去才知道。
- **修正：直式封面照片會被壓成一小條**——之前為了讓橫式海報不被裁切，改成完整顯示、置中留白，但這也讓直式照片（例如人像）在標題這種矮胖形狀的區塊裡被壓得很小。現在上傳時會記錄照片長寬比，偵測到直式照片會自動把標題區塊加高，讓照片有合理的顯示空間。

## v2.5.0

主動找優化點並直接實作，這次著重在既有的操作摩擦：

- **新增「複製賽事」**：詳情頁新按鈕，保留地點、路線、裝備清單、官方網站等欄位建立新賽事，適合同系列跨年賽事（例如「山羚羊越野系列賽」）；名稱裡有西元年份會自動 +1，日期、報名結果、成績、心得等會清空重新開始。複製後會自動被系列比較表抓到。
- **所有彈出視窗支援 Esc 鍵關閉**：之前 7 個彈出視窗（貼上資料、GPX 匯入、Excel 匯入、鞋款管理、配速試算、使用說明、資料復原）都只能點按鈕關閉，現在按 Esc 都能直接關閉。
- **搜尋框加上防抖動**：賽事數量多時，原本每打一個字就會整個行事曆重新渲染一次；現在改成停止輸入 150ms 後才重新整理，打字更順。

## v2.4.0

視覺優化，主動盤點後直接動手，順便修掉一個在盤點時發現的既有 bug：

- 行事曆週六、週日欄位新增淡淡底色，方便快速掃視週末賽事（深色/淺色模式都有調整）
- 「今天」格子從純外框改成外框＋淡色填底，在週末重疊時也能清楚分辨
- 生涯數據列（總完賽場次／全馬 PB／今年累計里程）加上對應小圖示，比較有儀表板感
- 修正：生涯數據列的文字之前沒有接上多語系字典，日文／英文模式下其實一直顯示中文，現在已修正並實測三語言都正確

## v2.3.0

- **刪除賽事改成垃圾桶機制**：不再直接消失，會保留 30 天，期間可在左下角「已刪除賽事」還原或永久刪除
- **多裝置同步新增安全網**：如果同一場賽事在雲端與本機兩邊都被修改過，系統會自動套用較新的版本，但不會靜默丟掉舊的——舊版本會保留在「資料復原」的同步覆蓋紀錄裡（最多 20 筆），同步當下也會跳出可點擊的提示通知你發生了什麼；還原也是可逆的，還原時會先把目前版本也存進紀錄，不會二次遺失資料

## v2.2.1

- 修正封面照片背景顯示會裁掉圖片內容的問題：改用「完整顯示、置中留白」而不是「裁切填滿」，寬版海報類圖片（例如賽事主視覺横幅）不會再被裁到看不出標題或關鍵資訊，兩側留白處維持原本的淡化效果

## v2.2.0

- 運動紀錄檔匯入新增支援 FIT 檔（Garmin 等裝置常見的二進位格式，之前只支援 GPX/TCX），純瀏覽器端解析
- 新增賽事的頁面「建立賽事」按鈕下方新增兩個按鈕：匯入運動紀錄檔（自動帶入距離/爬升/心率等數據）、匯入賽事主視覺（自動處理成封面照片），建立賽事時一併套用
- 新增「賽事官方網站」欄位，新增賽事頁面可直接貼連結，之後也能在基本資訊區塊編輯

## v2.1.1

- 封面照片改版：不再強制裁成小正方形縮圖（會裁掉海報/照片上的重要資訊），改成上傳時保留完整構圖等比縮圖，顯示時變成整個標題區塊的淡化背景底圖——淡化程度會跟著深色/淺色模式自動調整，文字永遠維持清楚可讀
- 行事曆格子、手機清單上的小縮圖不受影響，一樣是小方塊縮圖

## v2.1.0

- 新增賽事封面照片：詳情頁大字標題下方可上傳照片，自動置中裁切成正方形（480×480、JPEG 壓縮），純瀏覽器端處理不會上傳到任何伺服器
- 封面照片會顯示在：詳情頁標題旁的縮圖、行事曆格子裡的小圖示、手機版清單檢視
- 沒有上傳照片的賽事完全不受影響，畫面照舊

## v2.0.1

- 使用說明按鈕換成手繪風格的跑步小人插畫（去背處理），旁邊新增會浮動的提示小標籤「有問題找我！」，跟著切換語言

## v2.0.0

- **新增多語系支援**：日文、英文全站翻譯，右上角新增語言切換選單（繁體中文／日本語／English），選擇會記住在瀏覽器裡
- 翻譯涵蓋：介面文字、所有賽事欄位標籤、下拉選單選項、行事曆、7 個 modal（新增賽事、GPX 匯入、Excel 匯入、貼上資料、鞋款管理、配速試算、使用說明）、成績分享圖與配速手環的 Canvas 文字、所有錯誤訊息
- 使用者自行輸入的資料（賽事名稱、地點、心得筆記等）**不會**被翻譯，維持原文
- 新增 Noto Sans JP 字體，日文模式下自動切換
- 架構設計：翻譯字典查不到對應詞時會自動退回繁體中文，不會開天窗；新增第四種語言只需要擴充字典，不用改動任何邏輯
- 新增 `glossary/glossary-zh-ja.md`、`glossary/glossary-zh-en.md`——直接從程式碼裡的翻譯字典自動整理產生的中日／中英名詞對照表，字典異動後重新產生即可，不用手動維護兩份

## v1.3.0

- 新增 App 內建使用說明：右下角路跑小人圖示，點開直接看操作指南，不用離開網站找 USAGE.md
- 版本號改為單一來源（JS 常數），畫面顯示與內建說明的版本字樣會自動同步，避免兩處各自寫死不一致

## v1.2.0

- 新增真正的背景推播通知（Firebase Cloud Messaging + Cloud Functions 排程），賽事倒數一個月／一週會直接推送到手機，不需要打開網站或行事曆 App。需要額外部署 Cloud Function、升級 Blaze 方案，詳見 [USAGE.md](./USAGE.md#5a-推播通知選用需要額外部署)
- 新增 `firebase-messaging-sw.js`（背景通知用的 service worker，需與 `index.html` 放在同一層）
- 新增 `functions/`（Cloud Functions 原始碼）與 `firestore.rules`（含 fcmTokens 存取規則）

## v1.1.0

- 新增行事曆提醒匯出（`.ics`）：單場賽事或全部即將到來的賽事，內建「倒數一個月」「倒數一週」兩個提醒，匯入手機原生行事曆即可用手機通知提醒——取代純網站無法在背景推播通知的限制
- 篩選 UI 改為兩層式（全部／即將到來／歷史紀錄 + 展開後才顯示細分狀態），避免賽事數量多時篩選按鈕過於擁擠

## v1.0.0

第一個完整版本，涵蓋以下功能：

**核心與介面**
- 行事曆主畫面（年/月導覽），手機（≤640px）自動切換為賽事清單檢視
- 雙層篩選：全部／即將到來／歷史紀錄，點選後展開對應細分狀態，並顯示即時筆數
- 賽事名稱／城市搜尋
- 生涯數據總覽（累積 5 場以上顯示：總完賽場次、全馬 PB、當年累計里程）
- 深色模式（跟隨系統或手動切換，記住選擇）
- 手機版標題列捲動時自動縮小
- 跑者／越野跑／鐵人三項自訂圖樣，依賽事運動種類自動顯示；PWA 自訂圖示

**賽事欄位**
- 基本資訊與時間、抽籤機率記錄
- 賽事系列（跨年比較 PB 趨勢與中籤率）
- 地理與路線（含海拔變化圖、CP 明細、ITRA/UTMB 點數欄位）
- 氣象（歷年預測／當日實際）
- 預算與行程規劃（費用、住宿、交通、隨行人員）
- 裝備清單（範本系統，內建路跑／越野／三鐵標配三份預設範本）
- 補給時程規劃（逐點記錄計畫，賽後比對實際）
- 訓練計畫、賽前目標（A/B/C）
- 配速試算與手環產生器
- 賽後成績儀表板（大字時間、配速、名次、心率、爬升、PB 金色標記）
- 前後半程掉速分析
- 比賽鞋款與里程追蹤
- 分段配速、多媒體連結、賽後心得筆記
- 一鍵產生賽事成績分享圖（PNG 下載）

**資料匯入**
- Excel 匯入（賽事規劃表，含工作表選擇與逐筆確認）
- GPX / TCX 運動紀錄檔匯入（自動抓距離、爬升、時間、心率、分段配速、海拔剖面）
- 貼上 JSON 資料快速填入（供 AI 讀取賽事網站／簡章／證書後使用）

**同步與備份**
- Firebase（Google 登入 + Firestore）選用雲端同步，本機儲存為主、雲端為疊加層，登入時自動合併資料
- 匯出 JSON 備份、清除全部資料

**技術基礎**
- 純前端單一 `index.html`，部署於 GitHub Pages
- 響應式版面（含 CSS Grid 欄寬修正，避免長標題撐版）
- 無障礙：表單欄位皆有對應 `label` / `id`

# 版本紀錄

版本號顯示在網站右下角。每次功能異動都會在這裡加一筆，並同步更新 [USAGE.md](./USAGE.md) 與 [README.md](./README.md) 對應段落。

> 這份檔案只保留 v3.0.0 之後的版本。**v1.0.0 – v2.99.0 的紀錄已封存在 [CHANGELOG-archive.md](./CHANGELOG-archive.md)**——原本累積到 307KB，單一檔案繼續長下去會難以閱讀也難以在 GitHub 上瀏覽。

## v3.75.0

- **頂端列新增「小／中／大」字級切換**，在深淺色切換的左邊，預設是「中」。三個字本身大小遞增，一眼看得出是在調字級。
  - **對手機使用者來說這是唯一能把字放大的方式**：viewport 設了 `user-scalable=no`（v3.33 刻意保留的手機縮放鎖定），雙指縮放是關掉的。
  - 實作：樣式表裡 231 個 30px 以下的 `font-size` 全部改成 `calc(Npx*var(--fs,1))`，小／中／大分別是 ×0.9／×1／×1.15（例如 13px → 11.7／13／15）。只縮放文字，不動版面間距與圖片。
  - **30px 以上的大數字不縮放**（完賽時間、倒數天數等 14 處）。那些本來就很大，放大 15% 在手機上會把「2:59:34」撐出螢幕，而且讀不清楚的從來不是它們。
  - **控制項自己的字級不跟著縮放**，否則切到「大」連按鈕都變大，頂端列在手機上會被擠爆。
  - 選擇存在 `font-scale-v1`，**在第一次繪製前就套用**（跟主題同一段早期腳本），重新整理不會先閃一下「中」再跳。切回「中」會清掉屬性與儲存值（中就是預設值）。
  - 分享圖、生涯回顧圖、雷達圖這些 canvas 圖片不受影響——它們的字級寫在繪圖程式裡，輸出尺寸固定。
- **回饋按鈕左邊加一句「說說你的想法」**。刻意避開「問題」兩個字：跟下面的「有問題找我！」擺在一起會分不清要按哪顆（說明鈕是使用教學、回饋鈕才是回報與許願）。外觀跟說明鈕的文字一致，但**刻意不做上下浮動**——兩句疊在一起都在動會很吵。文字本身也點得到，點下去等同按回饋鈕。
- **測試方式**：`mobile` 群組新增 3 項：三種字級下文字實際算出 11.7／13／14.95px；大數字在小與大之間字級不變；手機在「大」時沒有水平溢出。`feedback` 群組新增 5 項：控制項在深淺色切換左邊、依序是小中大、預設按下「中」；切換後有存、按下狀態同步、切回中會清乾淨；控制項自己不縮放；說明文字在按鈕左邊、垂直置中、沒有動畫；點文字會開表單。**反例驗證**：拿掉「大」那行縮放規則，字級測試立刻變紅。全站回歸 275/275 通過。

## v3.74.0

- **新增「意見回饋」浮動按鈕**，疊在「有問題找我」正上方，連到 Google 表單。
  - **圖樣**：對話框＋一顆橘點。橘點就是 Logo 折線末端那顆點（`#B8451F`），跟 App 的視覺語彙一致。另外評估過「對話框＋愛心」（會跟分享圖上的按讚意思打架）與「對話框＋鉛筆」（縮到 44px 細節會糊），三個版本的比較圖另附 `feedback-icon-options.png`。
  - **位置**：跟說明鈕**水平中心完全對齊**（右邊距由中心點回推），間距 12px。刻意比說明鈕小一號（桌機 46 vs 60、手機 42 vs 52）——說明仍然是主要入口。**手機上多一顆「＋新增賽事」**在說明鈕上方，所以回饋鈕在手機會自動疊到它上面，三顆互不重疊。
  - **Google 表單「預先填入」**：按下去時自動把 **App 版本、裝置（手機／電腦、系統、瀏覽器、是否為已安裝的 App）、當下在哪個畫面** 帶進表單，收到回饋不用再回問「你用哪一版、哪台裝置」。**只帶這三樣，絕不帶任何賽事資料**——回饋表單是公開收件匣，使用者按下去之前沒機會檢查會送出什麼。有獨立測試確認賽事名稱、號碼布、成績都不會出現在網址裡。
  - 設定方式寫在 `FEEDBACK_FORM_URL`／`FEEDBACK_PREFILL` 兩個常數旁的註解。entry ID 寫 `entry.123` 或只寫 `123` 都可以，會自動補前綴；沒設定的欄位就不帶；`usp=pp_url` 是 Google 表單預先填入的開關，程式會自動加。
  - **還沒設定表單網址時不是死按鈕**：按下去會顯示「回饋表單準備中，敬請期待」。
  - 另外評估過「App 內建回饋視窗直接寫進 Firestore」，體驗較好，但要在 `firestore.rules` 開一個任何人都能寫的集合、自己處理垃圾訊息——以目前的使用規模不值得，Google 表單的防濫用由 Google 處理。
- **測試方式**：新增 `feedback` 群組（第 17 個）4 項，用替換常數的方式模擬「已設定表單」：桌機位置在說明鈕正上方、置中、較小且點得到；預先填入網址含 `usp=pp_url` 與三個欄位（含自動補 `entry.` 前綴）；**網址裡不含賽事名稱、號碼布、成績**；以 `_blank` + `noopener` 開新分頁。`mobile` 群組新增 1 項：手機上回饋鈕在「＋新增賽事」上方、三顆不重疊且置中。`drawers` 群組新增 1 項：未設定時按下去有提示、不開任何視窗。全站回歸 267/267 通過。

## v3.73.0

- **歷年平均的取樣從「單一天」改成「同月同日 ±3 天」**。原本每一年只取比賽那一天的日均值，5 年就只有 5 個樣本——同一個日期在不同年份之間跳動很大，某一年剛好寒流就會把整個平均拉走。改成每年取前後 3 天，5 年 × 7 天 = **35 筆樣本**，穩定得多；Open-Meteo 本來就支援日期區間，**查詢次數完全沒有增加**（還是 5 次）。
  - 跨年、跨月自動進位：2027-01-02 的比賽會查 2025-12-30～2026-01-05，依此類推。
  - 2/29 的處理不變：非閏年整年跳過，不拿 2/28 來湊。實測 2028-02-29 的比賽，往前五年只有 2024 是閏年，所以只送出一次查詢（區間 2024-02-26～03-03）。
- **畫面上標示這是幾年、幾筆資料算出來的**：「歷年平均・依 5 年 × 前後 3 天，共 35 筆」。某幾年查不到時樣本會靜靜地變少（未來賽事的「去年」可能還沒發生，尤其容易），不寫出來的話，兩年平均跟五年平均在畫面上長得一模一樣。舊的快取沒有樣本數欄位時，退回顯示「依 N 年同日資料」。
- 取樣範圍抽成 `CLIMATE_AVG_YEARS`／`CLIMATE_AVG_WINDOW` 兩個常數，之後要調整年數或天數只改一個地方。
- **測試方式**：`drawers` 群組新增 3 項。攔截 `fetch` 檢查實際送出的五個日期區間（含跨年進位）；三種樣本狀態（5 年 35 筆／2 年 14 筆／舊快取無樣本數）都顯示正確的說明文字；閏日賽事只送出一次查詢且區間正確。全站回歸 261/261 通過。

## v3.72.0

- **抓到即時預報之後欄位還是空的——修掉**。查證結果：回填只做了「歷史天氣」與「海象」兩種，**即時預報完全沒有回填**（`applyLiveForecastAutofill` 根本不存在）。而且即時預報原本只抓溫度、降雨機率、陣風三個變數，就算要回填也沒有風速與風向可以填。
  - 即時預報改為一併抓取體感溫度、相對濕度、風速、風向，並新增回填：**降雨機率、風速、風向**。實測 51% 的降雨機率與東南風都正確填入。
  - **刻意不回填「實際體感溫度」與「當日濕度」**。那兩個是賽後才知道的實際值，由歷史天氣負責。所有回填都只填空欄位、不覆蓋既有值——如果讓預報先把它們佔住，比完之後歷史天氣的回填就再也補不進真正的實測值，畫面上會永遠停在賽前的預測，而且看不出那是預測。這條有獨立測試：預報回填後那兩個欄位必須還是空的，接著歷史天氣要補得進去。
- **「歷年平均氣溫／濕度」對未來賽事也會抓了**。原本觸發條件綁著 `raceIsHistorical()`，未來賽事永遠是空的——但歷年平均是「這個地點、這個日期、過去幾十年的平均」，跟比賽有沒有結束完全無關，**賽前想知道「這個時節大概幾度」才是最需要它的時候**。
- 舊版抓的預報快取（沒有風速風向欄位）不會出錯，降雨機率照樣補得上；重新整理時也會補跑一次回填。
- **測試方式**：`drawers` 群組新增 4 項。預報回填三個欄位的值正確；實際值欄位維持空白且賽後歷史天氣補得進去；不覆蓋手動值、重複執行不重複改、舊快取不爆；歷年平均的觸發條件不再含 `raceIsHistorical`。全站回歸 258/258 通過。

## v3.71.0

- **「當日氣象」一片空白的問題**。查證後是兩件事疊在一起，都不是抓取功能本身壞掉：
  - **自動抓取需要 GPS 軌跡**（要座標才查得到那天那個地點的天氣）。沒有匯入 GPX／FIT 的賽事不會觸發自動查詢——但**畫面上完全沒說**，使用者只看到一張空的「氣象資料」卡，看起來就像功能壞了。
  - **「抓取即時預報」按鈕只存在於備戰釘選面板**，賽事詳情頁的氣象區塊根本沒有這個入口。所以未來賽事在詳情頁上永遠沒有手動抓取的辦法。
- 新增 `raceDayWeatherBlockHtml()`，把五種狀態全部講清楚、該給入口的就給入口：
  - 未來 7 天內 ＋ 有軌跡 → **顯示「抓取即時預報」按鈕**（詳情頁現在也有了）
  - 未來 7 天內 ＋ 沒軌跡 → 「匯入 GPX/FIT 軌跡後即可抓取即時預報」
  - 比賽還在 7 天以外 → 「比賽前 7 天才會提供即時預報」
  - 已完賽 ＋ 有軌跡 → 「正在查詢賽事當天的歷史天氣…」（原本就有）
  - 已完賽 ＋ 沒軌跡 → **「這場沒有 GPS 軌跡，無法自動查詢當天的歷史天氣；可以手動填寫下方欄位」**
  - 抓取失敗的錯誤訊息也會顯示在這裡，不再只出現在備戰面板
- 這又是同一類問題：**功能因為前提不成立而不執行時，要說出原因**（v3.38 照片略過、v3.61 分享選項、v3.70 預算警示都是同一條）。
- **測試方式**：`drawers` 群組新增 3 項。五種狀態各自產出正確的說明文字；詳情頁的氣象區塊裡真的有 `fetch-live-weather` 按鈕且綁對賽事 id；沒有軌跡的歷史賽事不可以什麼都不說。全站回歸 254/254 通過。

## v3.70.0

- **「預算與行程」摘要卡從「有幾筆」改成「是什麼」**，五個方向一次做完：
  1. **內容取代數量**：原本是「住宿 4／交通 4／同行者 1」——你早就知道有幾筆，想知道的是哪一間、幾點出發。改成顯示最近一筆住宿的名稱與日期、去程第一段交通的工具／時間／地點、同行者姓名，其餘用「+N」帶過。交通排序是「去程優先、時間早的在前」，因為這張卡最常在出發前看。
  2. **補上沒顯示的欄位**：付款狀態（未繳費／已繳清／退款中）與晶片押金，兩個原本都沒出現在卡片上。付款狀態尤其重要——賽前最怕忘記繳費。
  3. **金額改為總和**：`JPY 51,500（報名 10,500＋住宿 27,000＋交通 14,000）`。**押金不算進總花費**（那筆會退）。為此在交通清單新增 `cost` 欄位（3c）——住宿本來就有，交通沒有的話「這場總共花多少」永遠算不出來。
  4. **賽前警示**，只收能明確判定的四種：報名費未付、住宿沒有涵蓋比賽當天（入住 ≤ 比賽日 < 退房）、去程交通晚於起跑時間、海外賽事尚未安排交通。**資料不全就不下判斷**（住宿沒填日期不報）、**國內賽事不檢查交通**（當天開車來回會一直誤報），海外與否用既有的 `isOverseasRace()`（依你資料裡最常出現的國家推算主場）。
  5. **卡片支援第二行**：`dashCardHtml()` 新增 `sub2` 與 `warnText`，資訊量翻倍不用擠在一行。
- **警示只染警示那一段**。第一版整個第二行都轉成警示色，結果飯店名稱、班機時間看起來也像出了問題——讀不出重點在哪。改成只有 `⚠ …` 那一段是橘色。這是看截圖才發現的。
- **測試方式**：`drawers` 群組新增 6 項。金額總和 51,500 且分項與押金都正確顯示；第二行含飯店名、班機時間、同行者姓名且不含「住宿 2」這種數量寫法；四種警示各自成立、齊全的資料不報警示；警示色只包住警示文字、不包住行程內容；空白賽事完全不報警示；交通欄位確實多了 `cost` 且新建的項目預設為 null。全站回歸 251/251 通過。

## v3.69.0

- **「征戰年數」改成算年資（跨幾年），不再是「有比賽的年份數」**。使用者第一場是 2012 年、現在 2025 年還在跑，卻顯示 10——原本是 `new Set(年份).size`，中間沒比賽的年份不會被算進去。數字本身沒錯，但「征戰年數」這四個字讀起來就是年資，兩者對不上。改成第一場到最後一場跨了幾年（含頭尾），同一份資料現在顯示 14。
  - 「有比賽的年份數」沒有丟掉，改存成 `activeYearCount`，其他統計還用得到。
  - **畫面上加了區間說明**（滑鼠移上去）：「2012－2025，其中 10 年有比賽」。只看到一個 14 還是會想問為什麼，把兩個數字一起講出來就不用猜。
  - 年資用「最後一場」而不是「今年」當結尾：如果 2020 年之後就沒再比賽，征戰年數應該停在那裡，不會隨著時間繼續長。
- **測試方式**：`share` 群組新增 3 項。2012 開始、中間空四年、到 2025 的資料要算出 14 而不是 10，且 `activeYearCount` 是 10；只有一年時是 1、完全沒有完賽紀錄時是 0；畫面上那一格的數值是 14 且 tooltip 含起訖年份與實際比賽年數。全站回歸 245/245 通過。

## v3.68.0

- **修掉「在新增賽事表單上貼賽事名稱＋日期沒反應」**，順便修掉一個更嚴重、只是還沒被回報的問題。
  - 原因是 v3.64.0 加的那道否決：「貼上的文字提到清單中已存在的賽事，就不是新增」。立意是避免建立重複賽事，但名稱比對是**子字串**比對——清單裡有「臺北馬拉松」，貼「**2026** 臺北馬拉松」就會中，於是**每年同一場賽事的新一屆都會被擋掉**。使用者回報的是在新增表單上沒反應，但同一個否決在任何地方都會擋掉隔年那一屆。
  - 兩個放行條件：
    - **使用者已經自己打開新增表單**：他明確表達要建立一場新賽事，這時候還擋著、什麼都不填，就是單純的沒反應。
    - **日期不同就是不同屆**：只有名稱對得上**而且日期也一樣**，才算真的重複、才否決。
  - 「同名又同一天」仍然不會跳新增表單，重複防護沒有被拿掉。
- **測試方式**：`paste` 群組新增 3 項。清單有 2025 那屆時，在新增表單上貼 2026 那屆要填進表單；沒開表單時貼 2026 那屆要開出新增表單並帶入；貼「同名同一天」則不可以跳新增表單。**反例驗證**：把邏輯還原成 v3.64.0 的無條件否決，前兩項立刻變紅、第三項仍然通過——正好證明這次修的是那道否決過寬的部分，沒有把重複防護一起拿掉。全站回歸 242/242 通過。

## v3.67.0

- **修掉「點了下載生涯回顧圖完全沒反應」**。視窗其實**有打開**（`hidden` 是 false、預覽也產好了），但它用的是一般彈窗的 `z-index:50`，而生涯回顧本身是 `z-index:210` 的全螢幕圖層——整個被蓋在底下，畫面上看起來就是按了沒反應。把它拉到 240。
- **順手修掉第二個「看得到但按不到」**：生涯回顧圖是直式長條（2160×4560），照原比例塞進視窗會把下載按鈕推到捲軸外面。預覽區限高 46vh 並自己捲動，按鈕永遠留在視窗底部。桌機與手機都實測過按鈕在可視範圍內、而且點擊落點確實是按鈕本身。
- **這兩個都是 v3.66.0 當下沒驗到的**：我驗了「視窗有沒有打開」「預覽尺寸對不對」，但沒驗「使用者看不看得到」「按不按得到」。功能正確不等於使用者用得到。
- **測試方式**：`share` 群組新增 2 項。視窗的 z-index 必須大於生涯回顧圖層，而且畫面正中央的元素要落在視窗內（真的疊在上面，不是只有數字比較大）；下載按鈕必須在可視範圍內，且該座標的 `elementFromPoint` 命中的就是按鈕本身。**反例驗證**：拿掉 z-index 那行，第一項立刻變紅。全站回歸 239/239 通過。

## v3.66.0

- **生涯回顧新增「下載生涯回顧圖」**，可以勾選要放哪些區塊：全馬 PB、生涯數據、軌跡畫廊、戰靴排行、能量補給、QR code。沿用賽事分享圖那一整套做法（canvas 直接畫、預覽用的就是正式產圖函式、走 `shareOrDownloadImage`，手機進系統分享面板、桌機直接下載）。
  - **高畫質**：版面用 1080 的邏輯寬度排，實際輸出 **2160px 寬**（`ctx.scale(2,2)`），字與線條都是兩倍取樣，放大不會糊。高度依內容算出來，實測資料齊全時是 2160×4560。
  - **高度是量出來的，不是寫死的**。生涯卡的內容量落差很大（有沒有軌跡、鞋款幾雙、有沒有補給），寫死高度要嘛下面一大片空白、要嘛塞不下。所以同一段排版程式跑兩趟：第一趟只累加 y 不畫東西、算出總高，第二趟用真正的尺寸重畫。兩趟共用同一份程式碼，才不會量到一個高度、畫出另一個。
  - 資料不足的區塊**灰掉並標原因**（沒有匯入過 GPS 軌跡／還沒有綁定鞋款的完賽紀錄／沒有已補給的紀錄），沿用 v3.61.0 的原則。勾選狀態存在 `career-share-prefs-v1`，下次打開沿用。
- **實際看圖才抓到的 bug**：PB 時間原本用 `secToHMSDenoised()`，那個函式回傳的是 **HTML**（把冒號包進 `<span>` 做淡化），畫到 canvas 上變成 `</span>59<span cl` 這串標籤原文。改用 `secToHMS()`。**單元測試不會抓到這種錯**——canvas 有畫出東西、尺寸也正確，只有真的把圖產出來看才看得見。
- **測試方式**：`share` 群組新增 5 項。輸出寬度必須是 2160；六個區塊的勾選都要真的改變輸出高度（關掉軌跡／關掉英雄卡／只留數據，高度依序遞減）；**攔截 `fillText` 檢查畫上去的每一段文字都不含角括號**、且 PB 時間確實是 `2:59:34`（這項有反例驗證：改回 HTML 版立刻變紅）；資料不足時區塊灰掉且列出原因、只剩數據與 QR 可勾；勾選狀態有存進 localStorage。全站回歸 237/237 通過。

## v3.65.0

- **成績標籤的同義詞大幅擴大**，中英日三種寫法都吃得下：
  - **大會時間**：大會成績／大會時間／大會紀錄／槍聲時間／公告成績／總時間／Gun Time／Official Time／Gross Time／グロスタイム
  - **晶片時間**：個人成績／個人時間／晶片成績／晶片時間／淨時間／净时间／實跑時間／Net Time／Chip Time／ネットタイム
  - **總排名**：總排名／總名次／綜合排名／全場排名／總成績排名／Overall Ranking／Rank／Place／総合順位
  - **分組名次**：分組排名／組別排名／分齡排名／組內排名／Division Rank／Age Group／Category／種目別順位
  - **號碼布**：再加上參賽編號／選手證號／報名編號／Race Number／Runner No.
  - **順便多抓兩個既有欄位**：前半程／後半程（前半・後半・First/Second Half）。
- **「性別排名」另外開了獨立欄位，沒有對應到分組名次**。使用者原本希望把兩者視為同義詞，但這會存到錯的數字——他自己的成績頁上，性別排名是 259/2251、分組排名是 57/368，是兩個不同的名次，共用一個欄位會互相覆蓋。所以新增 `results.genderRank`／`results.genderParticipants` 兩個欄位（賽後區段、三語標籤齊全），成績儀表板也會顯示。
- **實作時抓到一個會誤判的寫法**：總排名的同義詞如果收了裸的「排名」兩個字，因為「分組排名」「性別排名」都含這兩個字、而比對又是非錨定搜尋，結果是總排名把分組或性別的數字吃走（實測「分組排名 12/150」會讓總排名也變成 12）。所以只收明確講「總」「綜合」「全場」的寫法。
- 分類器的標籤偵測同步擴大，否則會出現「欄位抓得到但判不成成績頁」這種上下不一致。
- **測試方式**：`paste` 群組新增 5 項。中英日三種格式的同義詞逐一比對；性別排名與分組名次在同一份文字裡各自存到正確欄位；只有分組排名或只有性別排名時總排名必須留空（防止裸「排名」誤中）；前後半程與號碼布同義詞；性別排名要出現在成績儀表板上。全站回歸 232/232 通過。

## v3.64.0

- **「賽事名稱＋完賽時間」現在給過了**，而且順便修掉一個實測才發現的 bug。
  - 使用者問「只抓到賽事名稱和完賽時間，其他都沒有，給過嗎？」——實測答案是**不給過**（2 分）。而且測的時候發現另一件事：**「名稱＋日期＋時間」會跳出「新增賽事」表單，即使那場賽事早就存在**——等於鼓勵你建立一場重複的賽事。
  - 兩個問題同一個解法：**貼上的文字裡出現清單中已存在的賽事名稱，就是很強的證據**——這段內容是「那一場的資料」，不是一場新賽事。
    - `classifyResultText()`：名稱對得上時加 3 分，讓「名稱＋時間」這種最精簡的貼法過得了門檻。
    - `classifyNewRaceText()`：名稱對得上時直接回 false，不再跳新增表單。
  - 名稱比對會正規化空白與全形括號，並容許「2024 Panasonic 台北城市路跑賽」對上「Panasonic 台北城市路跑賽」（官網複製常常多一個年份）。**名稱短於 4 個字不比對**，避免「路跑」「馬拉松」這種泛稱亂中。
  - **散文優先，這是加分之後才冒出來的 regression**：一篇「提到賽事名稱又寫了完賽時間」的心得會同時滿足這個條件，加了分就被判成成績、整段文字反而沒地方去（實測心得 8 分卻被 5 分的成績搶走）。所以心得那條成立時就不給這個加分。
  - 「只有一個時間」「提到沒建立過的賽事」仍然不動作——這兩種太曖昧，寧可沒反應。
- **測試方式**：`paste` 群組新增 2 項。七種內容的分流矩陣一次鎖住（名稱＋時間→成績、帶年份的名稱→成績、名稱＋日期＋時間→成績而非新增、沒見過的賽事＋日期→新增、提到賽名的心得→心得、只有時間→無、沒建立過的賽事＋時間→無）；名稱比對忽略短名稱、長名稱要對得上。全站回歸 227/227 通過。

## v3.63.0

- **沒有排名區塊的成績頁也抓得到了**。使用者問「是不是名稱、大會成績、晶片成績、總排名一個都不能漏才會偵測」——答案是不需要全部，但**當時的計分實際上等於要求要有排名**：只有兩個成績時間、沒有排名的頁面拿到 4 分，門檻 5 分，又是差一分，結果同樣是貼上去完全沒反應。這是計分校準的問題，跟 v3.58 那次是同一種失敗模式。
  - 修正：**同時抓到「大會成績」與「個人成績」兩個獨立的完賽時間時加 2 分**。沒有別種內容會並列兩個完賽時間，這個結構本身就是成績頁的特徵。實測那份只有兩個時間的頁面從 4 分變 6 分，通過門檻，三個欄位（大會時間、晶片時間、號碼布）都正確填入。
  - **沒有放寬到會搶走心得**：含「大會成績 3:25:17」的心得仍然是 4 分、仍然走心得那條路；單一個時間也仍然不夠。這兩種情況都加了測試鎖住。
  - 沒有排名就不填排名欄位，不會為了湊數硬塞。
- **測試方式**：`paste` 群組新增 2 項。無排名的成績頁：判定成立、大會時間 4410 秒、晶片時間 4331 秒、號碼布 004514、排名欄位維持空的；放寬後心得（含成績時間與不含的兩種）仍然走心得、單一時間仍然不算成績頁。全站回歸 225/225 通過。

## v3.62.0

- **貼上成績頁時一併抓「號碼布編號」**。
  - **不能看到 4 位數以上就當號碼布**：成績頁裡到處都是長數字（總參賽人數 3000、分組人數 3680、賽事年份 2024），所以分成兩段判斷：
    - **有標籤最可信**（號碼布：／參賽號碼／選手編號／Bib No.／ゼッケン／番号）。這條放寬成英數，`A1234`、`R-045` 這種帶字母或連字號的號碼布很常見。
    - **沒標籤時只認「整行就是一串數字」**，而且要排除已經被排名／人數用掉的數字，再排除看起來像年份的四位數。**有前導零（003150、010685）是很強的訊號**，那種不受年份規則限制——沒有人會把年份寫成 02024。
  - 三位數以下一律不算。
  - 兩個實作坑：`Bib No. R-045` 的標籤後面還跟著「No.」，正則沒把句點吃掉就會把 `No` 當成號碼布——所以加了「抓到的值一定要含數字」這道檢查；另外號碼布欄位在「基本資訊」區段、成績在「賽後」區段，預覽視窗的標籤查詢原本只看賽後區段，會直接顯示 `bibNumber` 這個原始欄位名，改成跨區段查。
- **測試方式**：`paste` 群組新增 5 項。兩份真實成績頁樣本的號碼布都抓對；有標籤時吃得下 `A1234`／`R-045`／日文 `ゼッケン 7821`；人數 3680、年份 2024、三位數 123 都不會被誤認；前導零的 `02024` 仍然判為號碼布；預覽視窗顯示「號碼布編號」而不是 `bibNumber`，且確認後真的寫進資料。全站回歸 223/223 通過。

## v3.61.0

- **分享圖的軌跡／戰靴／補給沒有不見，是那場賽事沒有那些資料**——但畫面上看不出差別，所以修掉這個體驗問題。
  - 查證結果：`shareAvailableOptions()` 與繪圖邏輯都正常，資料齊全的賽事四個選項都在、金色軌跡與戰靴補給都畫得出來（實測確認）。使用者回報的那場是 2022 年的舊賽事，沒綁鞋款、沒有補給紀錄、也沒匯入 GPX。
  - **問題在於「選項消失」跟「功能壞掉」長得一模一樣**。使用者只會看到以前有的東西不見了，不會知道是因為這場缺資料——會來問就是證據。跟 v3.38 照片靜默略過是同一類問題：**說出原因比默默不做重要**。
  - 改成資料不足時**灰掉並標出原因**，不再整個隱藏：「沒有心率資料」「這場沒綁定鞋款」「沒有已補給的項目」「沒有 GPX 軌跡」。非跑步類的戰靴會說「這個運動種類不顯示戰靴」，跟「沒綁鞋款」是兩件不同的事，訊息要分開。
  - 灰掉的選項是 `disabled`，勾不動，也不會影響已經存下來的偏好設定。
- **測試方式**：`share` 群組新增 3 項。只有成績的賽事：四個選項仍在畫面上、全部 disabled、文字含正確原因、可勾的只剩 QR；資料齊全的賽事：沒有任何灰掉的選項、五個都可勾；三鐵顯示的是運動種類的原因而不是「沒綁鞋款」。全站回歸 218/218 通過。

## v3.60.0

- **成績儀表板加上「距離」**，放在第一格——它是讀其他數字的前提（配速、名次都要先知道距離才有意義）。
  - **多項運動與游泳也照樣顯示**。配速對它們是關掉的（三鐵把游泳、騎車、跑步除在一起會算出 2'49"/km 這種假數字），但**距離本身沒有被混算的問題**，113 公里就是 113 公里。
  - **數字精度**：21.0975（半馬的標準距離）要完整顯示，不能四捨五入成 21.098；10 公里也不要變成 10.0000。取到小數第 4 位再用 `parseFloat` 去掉尾零，兩種情況一次滿足，順便擋掉 `23.400000000000002` 這種浮點尾數雜訊。第一版寫成 `toFixed(3)`，半馬直接被進位成 21.098——是實測四種距離時抓到的。
  - 沒填距離的賽事不會出現這一格。
- **測試方式**：`drawers` 群組新增 4 項。距離排在第一格且值正確；四種距離的格式（21.0975／42.195／10／浮點雜訊）逐一比對；三鐵有距離但沒有配速、游泳兩者都有；沒填距離時這一格不存在。全站回歸 215/215 通過。

## v3.59.0

- **生涯回顧的兩個標籤改名**：「生涯總里程」→「生涯完賽總里程」、「生涯累計爬升」→「生涯累計完賽爬升」。三語同步（日：生涯完走総距離／生涯完走累積標高；英：Lifetime finished distance／Lifetime finished elevation）。
- **改名前先查證過數字本身**：兩個值都是 `completed.reduce(...)`，本來就只累加 `status==='completed'` 的賽事，報名中、考慮中、DNS、DNF 都不算——所以新標籤是準確的描述，不是把一個混合的數字硬貼上「完賽」兩個字。順手用一場距離 999 公里的「已報名」賽事驗證確實沒被算進去。
- 「珠峰高度」徽章的說明文字一併改成「生涯累計完賽爬升達到 8,848 公尺」，指的是同一個數字，措辭要一致。
- **測試方式**：實際打開生涯回顧比對畫面文字，新標籤都在、舊標籤完全不存在（用 `(?!完)` 的負向比對確保「生涯累計爬升」不會被「生涯累計完賽爬升」誤判成還在）。全站回歸 211/211 通過。

## v3.58.0

- **貼上成績查詢頁的內容，自動填入成績欄位**。使用者回報「建立完賽事後，把成績資訊貼到空白處沒有任何反應」——查證後確認**這種內容從來沒有被支援過**，不是壞掉，是漏掉。
  - **原因**：成績頁是「表格」不是「散文」——短行、大量數字、幾乎沒有完整句子。心得那條計分制要的是長度、句子數、第一人稱，實測這種內容只拿到 4 分，門檻是 5 分，永遠差一點。差一點的結果就是完全沒反應，而且不會有任何提示。
  - 新增專屬的判斷與抽取：大會成績（Gun Time）、個人成績（Chip Time）、總排名、總參賽人數、分組名次、分組人數，六個欄位一次到位。**「287/3000」這種「名次／總人數」的寫法會自動拆成兩個欄位**。
  - **標籤與數值被排版拆到不同行也對得上**：成績頁常常是「總排名」一行、「Overall Ranking」一行、「287/3000」又一行。比對允許跨行，但限制中間只能隔少量字元，避免抓到下一個區塊的數字。
  - 兩個時間標籤都抓不到時，退一步用文字裡第一個 `hh:mm:ss` 當晶片時間。
  - **排在「新增賽事」之前判斷**：成績頁通常也有賽事名稱與日期，會被新增賽事那條先吃掉，變成「明明已經有這場了卻跳出新增表單」。有成績時間與排名這種更具體的證據時，它就不是一場新賽事而是一份成績。但清單是空的時候讓給新增賽事（沒有賽事可以填）。
  - 欄位預設全勾（成績頁是結構化的，抓到的數字就是這場的成績），會覆蓋現有值的標紅提示。
- **測試方式**：`paste` 群組新增 6 項，直接用使用者回報的那份真實成績頁文字驗證：六個欄位的值逐一比對（1:51:53／1:51:36／287／3000／57／368）；跨行標籤比對成立；已經有該場賽事時走成績而不是新增賽事；確認後六個欄位真的寫進資料；沒有成績時間就不算成績頁；只有名稱＋日期時仍然走新增賽事。全站回歸 211/211 通過。

## v3.57.0

- **行事曆的賽事名稱放大**：13px → 15px（手機 12px → 13px），縮圖與圓點跟著放大，行距拉開。
  - **同時改成折兩行才截斷**。單純放大字級其實跟目的相反——字變大之後單行塞得下的字更少，「陽明山越野賽」會變成「陽明山越…」。格子高度放得下兩行，折行比放大更能讓你一眼讀到完整名稱。實測同一天三場賽事仍然不會撐出格子。
- **行事曆格子依運動別上底色**（照你指定的色系）：路跑金色、越野跑叢林綠、超馬銀色、二鐵粉色、三鐵神秘紫、自行車酒紅、游泳水藍、障礙賽鋼鐵灰。深色與淺色主題各有一組色值，深色底的透明度拉高（同樣的 alpha 疊在深色上幾乎看不出來）。
  - **底色刻意做得很淡**（透明度 0.15～0.22）：格子裡還有日期數字與賽事名稱要讀，底色一濃就全部都不好讀。運動別的實色仍然用在左側色條與圓點上。
  - 實色（`--sport-*`）與底圖色（`--sport-bg-*`）分成兩組變數，**不用 `color-mix()` 從實色算**——那個要 Safari 16.2 以上才支援，這個 App 要在 iPhone 上跑，不值得為了少寫幾行冒這個險。
  - 選取狀態的深色底要蓋過運動別底圖：底圖是行內樣式帶進來的 CSS 變數，很容易贏過 class 規則，所以 `.cal-chip.selected` 的 background 加了 `!important`。這項有獨立測試。
- **一併校準了運動別配色**：原本路跑是暗紅、二鐵是土黃、自行車是灰藍，跟你要的色系差距較大。新的色值同時套用到全站所有用到運動別顏色的地方（圓點、圖示、統計圖表），視覺語彙才一致。
- **測試方式**：`drawers` 群組新增 4 項。八種運動各自帶到對應的 `--sport-bg-*` 變數、實際算出來的背景色至少 7 種互不相同且都不是透明；字級 ≥15px 且確實設了兩行截斷；選取狀態的背景等於 `--ink`（底圖沒有蓋過去）；同一天三場賽事的最後一張卡片不超出格子。全站回歸 205/205 通過。

## v3.56.0

- **貼上「賽事名稱＋日期」→ 直接開新增賽事表單並填好**。跟其他幾種貼上不同，這一種的目的地不是某一場賽事的欄位，而是新增表單本身。
  - 抓名稱、日期、距離、官網，並用既有的 `guessSportTypeFromNameAndDistance()` 順便猜運動種類（「陽明山越野賽」會自動選越野跑）。
  - **停在表單、不直接建立**：名稱與日期是猜的，讓你看過一眼再按「建立賽事」，跟其他幾種貼上一樣是「先預覽再落地」。
  - **必須同時抓得到名稱與日期才攔**。只有日期（行事曆貼一行）或只有名稱（隨手打的字）都不夠——那樣只會變成一直跳視窗。訂房、車票、心得各自成立時一律讓路，不互搶。
  - **不受「必須已經有賽事」的限制**（其他幾種貼上有這個前提，因為要填進某一場）。清單還是空的時候正是最可能貼賽事資訊進來的時機——這個 bug 是實際測試時才發現的，第一版把判斷放在那道守門之後，導致空清單時完全沒反應。
  - 三個抽取上的坑，都鎖進測試：報名頁同時有「報名日期」與「比賽日期」而且報名日期排在前面，**有標籤的比賽日期要優先**；名稱尾巴常黏著「10 公里」「半程馬拉松」要切掉，但**不能把「萬金石馬拉松」本體的「馬拉松」吃掉**（只切結尾）；`(?:公里|km)\b` 的 `\b` 在中文字後面不成立，會整個比對失敗。
- **新增賽事表單改成兩欄版面**（照你給的配置）：賽事名稱｜比賽日期／官方網站｜賽事種類／狀態｜距離。窄螢幕（≤640px）自動回落成單欄。
- **新增「距離」欄位**，附常見距離的快捷清單（5K／10K／半程馬拉松／超半馬 30K／全程馬拉松／50K／100K），可以直接選也可以自己打。
- **日期早於今天 → 狀態自動判定「已完賽」**。只在你沒有自己動過狀態下拉時才套用（還停在預設值），否則你明明選了「未起跑 DNS」卻被改掉會很莫名其妙。詳情頁那邊本來就有同樣的判斷，這次補上新增表單。
- **測試方式**：`paste` 群組新增 9 項。名稱／日期／距離／官網四項抽取正確；標籤日期贏過報名日期；名稱尾巴雜訊切乾淨且不誤傷；只有日期或只有名稱不攔；訂房／車票／心得都不會被搶走；空清單時照樣運作；過去日期建立後真的存成 `completed` 且距離有進去；未來日期維持「考慮中」；表單有距離欄位與五個以上的距離快捷。全站回歸 201/201 通過。

## v3.55.0

- **填完縣市，國家自動帶入**。接在既有的「無感智慧預設」機制上（本來就在做「填名稱 → 猜系列／運動別」這類推論），所以自動填入的高亮提示、不覆蓋使用者已填值這些行為全部沿用，沒有另造一套。
  - **兩層策略，順序不能反**：先比對明確地名清單，比不到才用後綴規則。後綴規則是主力——不可能把全世界的鄉鎮都列進來，但各國行政區用字剛好分得很開：臺灣用「縣／鄉／鎮」、日本用「県／町／村／区」、韓國用「도／시／군／구」。**日文的「区」跟中文的「區」是不同的字**，這點本身就是很可靠的判別依據。
  - 順序之所以重要：「福井縣若狹」是用臺灣字寫的日本地名，後綴規則會判成臺灣，但地名清單先比到「福井」「若狹」就正確判成日本。
  - 臺灣涵蓋 22 縣市（含臺／台兩種寫法），鄉鎮靠後綴規則吃下去（「宜蘭縣礁溪鄉」「花蓮縣秀林鄉」都對）。日本涵蓋 47 都道府県加主要城市（神戸、那覇、軽井沢、妙高、若狭⋯）。另收韓國、港澳、東南亞、中國、歐美澳紐等常見賽事目的地，中英日文寫法都認。
  - **沿用你自己的寫法**：你既有資料寫「台灣」就填「台灣」，寫「臺灣」就填「臺灣」，寫「Japan」就填「Japan」——先翻既有賽事找你用過的寫法，找不到才用介面語言的預設。同一份資料混用兩種寫法不只難看，還會讓依國家分組的統計拆成兩堆。
  - **只在國家欄位空著時填**，不覆蓋你自己打的值；也只有改縣市欄位會觸發。認不出來的地名回 null，不硬猜。
- **測試方式**：`data` 群組新增 7 項。臺灣 8 種寫法與日本 7 種寫法全部判對；地名清單優先於後綴規則（「福井縣若狹」→ 日本）；11 個國際城市（首爾／서울／Chamonix／Boston／Sydney／北京市／廣東省⋯）判對；空字串、null、未知地名回 null；沿用使用者既有的臺／台與 Japan 寫法；已填值不覆蓋、非縣市欄位不觸發；**實際在表單的縣市欄位輸入「福井県」，國家欄位真的出現「日本」**。全站回歸 192/192 通過。

## v3.54.0

- **修掉一個正在發生的雲端同步失敗**（`FirebaseError: Request payload size exceeds the limit: 11534336 bytes`）。這是在測試 IG 功能時，從使用者 console 截圖裡意外發現的——**不是新功能造成的，是既有的 bug**，而且症狀很惡劣：`replaceAll()` 把所有賽事塞進同一個 `writeBatch` 一次 commit，封面照一多就撐破 Firestore 的單次請求上限（11MB），然後**整批失敗、一筆都沒寫上去**。本機資料沒事，但雲端一直停在舊版本，使用者要換裝置才會發現。
  - 改成分批 commit：每批上限抓 5MB（遠低於 11MB，留餘裕給協定開銷）與 400 個操作（Firestore 上限 500，留餘裕）。刪除操作沒有內容、只受操作數限制，單獨成批。
  - **單一賽事自己就超過文件上限（1MB）時跳過它、其餘照常同步**，並跳出提示告訴使用者是哪幾場、為什麼（多半是封面照或軌跡點過多）。原本這種情況會讓整批一起失敗——一場壞掉拖垮全部，是比少同步一場更糟的結果。
  - 位元組估算用 `JSON.stringify().length`：base64 封面是 ASCII，長度即位元組；中文會被低估，所以批次預算抓得比實際上限保守很多。
- **IG 貼文內文倒回 App：實測後放棄**。Meta 在 2026/6/15 取消了 oEmbed 的 token 要求，實測也確認**沒有被 CORS 擋**，但回傳的 HTML 內文長度只有 27 個字元、內容是 `View this post on Instagram`——**caption 根本不在回傳內容裡**（真正的內文是 IG 的 embed script 之後才去抓的）。`thumbnail_url` 也已於 2025/11/3 從 oEmbed 移除，封面照同樣拿不到。這條路能拿到的東西，v3.53 的「存成連結」已經全部涵蓋；要把內文倒回來，複製那段文字貼上（v3.49 的心得判別）反而更快。
- **測試方式**：`sync` 群組新增 5 項。位元組預算（8 場 × 900KB = 7.2MB）要切成多批且每批都在預算內；1000 場要依操作數上限切且總數不漏；單場超過文件上限時被挑出來、其餘仍然進批次；空輸入與 null 都安全；呼叫端在有賽事被跳過時一定要跳出含賽事名稱的提示。**反例驗證**：把邏輯還原成舊的「全部塞一批」，前三項立刻變紅。全站回歸 185/185 通過。

## v3.53.0

- **貼上純網址時存成這場賽事的連結**。在這之前，貼一個網址是**完全沒反應**的——分類器判成 `url` 就直接放行，既不會攔也不會提示。但「複製賽事官網／自己的 Strava 紀錄／IG 貼文，想收進這場賽事」是很常見的動作，這是一個真實存在的體驗缺口。
  - 確認視窗可挑賽事、改類型、加備註，存進「多媒體與連結」。**只存網址本身，不去讀取網頁內容**——跨網域抓網頁會被 CORS 擋，而且需要後端；與其做一個半殘的預覽，不如把能誠實做到的部分做好，視窗上也明講這件事。
  - 依網域猜類型：Instagram／Facebook／Threads → 個人相簿，Strava／Garmin Connect → GPX 軌跡，網址以 `.pdf` 結尾 → 賽事簡章 PDF，其餘一律「官方網站」（貼賽事網址是最常見的情境，而且類型在視窗裡本來就能改，猜錯的代價只是多點一下）。
  - **判斷條件刻意很嚴**：整段文字必須就是一個 `http(s)` 網址、中間不能有空白。夾在句子裡的網址不算——那通常是心得或別人的分享，攔下來會很吵。也因為條件嚴，它排在分流最前面也不會跟住宿／交通／心得搶（那三種都是整段文字，不可能只有一個網址）。
  - 網址一律過既有的 `safeUrl()`，`javascript:`、`data:` 這類 scheme 進不來。這場已經有一模一樣的連結時會提醒（但不阻止）。
- **修掉一個佔位符沒被置換的 bug**：連結視窗的說明文字原本寫成 `{s}`，但 `tf()` 只認 `{n}`，畫面上會直接顯示「要把這個 {s} 連結存到…」。**這是實際截圖看畫面時才發現的**——單元測試全過、語法檢查也全過，只有真的把畫面叫出來看才看得到。三語都修了，並新增一項守門測試：逐一觸發四種貼上視窗，介面文字裡不得殘留 `{x}` 形式的佔位符（原文預覽區不算，那是使用者貼進來的內容）。反例驗證過會紅。
- **測試方式**：`paste` 群組新增 7 項。網域對應到正確類型、認不出的網域退回官網；夾在句子裡的網址／`javascript:`／`data:`／純文字都不攔；貼上後開的是連結視窗且預設選中目前賽事、類型正確；改類型與備註後儲存進 `mediaLinks` 且既有連結不動；重複連結會提醒；心得與訂房信不會被網址那條路搶走；四種視窗都沒有殘留佔位符。全站回歸 180/180 通過。

## v3.52.0

- **加固貼上住宿／交通判別，處理「網頁選字複製」的雜訊**。上兩版的判斷規則是拿訂房確認信、訂票信這種乾淨、排版好的文字驗證的；但使用者更常見的操作是在訂房網頁或訂票網頁上直接選取一段文字複製，這種內容會夾雜網站介面——麵包屑導覽、星等評論、「立即預訂」「查看地圖」這類按鈕字樣，以及「平均每晚 NT$3,200 起」這種搜尋結果參考價。拿真實的 Agoda／高鐵訂票網站樣式測試後，找到四個會抓錯的地方，逐一修掉：
  - **飯店名稱抓到麵包屑**：像「首頁 › 台灣 › 宜蘭 › 礁溪」這種導覽列排在確認信第一行的位置，原本「找不到標籤就用第一行」的退路會把它當成飯店名。現在找名稱前先濾掉麵包屑、星等評論、按鈕字樣這幾類雜訊行。
  - **「酒店」這個常見命名沒被辨識**：原本的關鍵字只有「飯店／旅館／民宿／旅店」，中國、港澳、不少台灣精品旅宿慣用的「酒店」不在裡面——名稱抓不到不是雜訊的問題，是關鍵字本身就不夠。這次補上酒店、渡假村、度假村、會館、山莊。
  - **費用抓到「平均每晚」的參考價，不是真正的總金額**：訂房網頁常常在確認資訊之前就先列出「平均每晚 NT$3,200 起」，原本的費用規則抓文字裡第一個看起來像價錢的數字，會把這個參考價當成總費用。現在優先找「總金額／合計／實付」這類明確標籤，只有找不到時才退回寬鬆比對，而且會先把含「每晚／起／低至」字樣的行整行拿掉。
  - **交通時間跨行時抓不到、地名被夾在中間的時間打斷**：訂票網頁常把日期和車次時刻拆成兩行（「去程 2026-12-19」另起一行才是「08:31 台北 → 09:56 左營」），原本比對日期＋時間只在同一行內找，跨行就抓不到，退化成只有日期沒有時間。「08:31 台北 → 09:56 左營」這種時間夾在站名中間的寫法，也會讓起訖地名的箭頭比對誤抓成「台北 → 09:56」。現在時間比對可以跨一行，抓起訖地名前會先把時間字樣拿掉。
  - 訂位代號原本只套用到它所在的那一段（去程或回程其中一段），現在整份文字找一次、兩段都套用——一組訂位代號本來就是管整趟行程的。
  - 交通同樣加了雜訊行過濾（「更多班次」「查看座位表」這類介面字樣），避免「更多車站資訊」被誤認成一個地名。
  - **沒有放寬判斷門檻**：加了不少關鍵字與退路，但額外驗證過純雜訊（麵包屑＋評論＋按鈕字樣，沒有真正的訂房內容）跟只是隨口提到「高鐵站」「酒店」的閒聊句子，都確認仍然不會被誤判成住宿或交通。
  - IG 貼文截圖、訂房／訂票網頁截圖這類圖片內容需要 OCR 才能辨識文字，跟這個 App 單檔、離線、最少依賴的原則衝突（需要引入數 MB 的 OCR 函式庫，或串接需要金鑰與連網的雲端服務），這次沒有做；圖片貼上維持現有行為（設成封面照）。
- **測試方式**：`paste` 群組新增 7 項，用實測的 Agoda 與台灣高鐵網站文字樣式驗證：麵包屑不會變成飯店名且「酒店」抓得到；費用是總金額 8400 不是參考價 3200；星等評論與按鈕字樣不影響住宿判斷；日期時刻跨行時出發時間正確帶出小時分鐘；時間夾在站名中間時起訖地名正確、沒有被時間打斷；訂位代號在只出現一次的情況下兩段行程都套用得到；「更多班次」這類介面字樣不會被當成地名。全站回歸 173/173 通過（16 個群組分批驗證，各批次全數通過）。

## v3.51.0

- **貼上交通票券自動填入**。機票、高鐵、台鐵、新幹線、客運、捷運、船票的確認信或訂票頁面內容，在空白處貼上就會抓成交通紀錄。
  - **交通工具選項從 5 種補齊到 10 種**：飛機、高鐵、火車／鐵路、捷運／地鐵、客運／巴士、接駁車、船／渡輪、計程車／叫車、自駕、其他。原本火車、客運、捷運、船都只能選「其他」，自動判別沒有東西可以對；既有資料的值（hsr／shuttle／self_drive／flight／other）維持有效。三語同步。
  - **一張確認信常常同時有去程與回程**：以「去程／回程／Outbound／Return／往路／復路」切段，一段一筆；第一個標記之前的抬頭（訂位代號等）併進每一段。沒有標記就整段一筆，**方向依日期跟賽事日比**——比賽前是去程、之後是回程；換賽事時會重新猜。
  - 抓的欄位：方向、交通工具、出發時間、出發地（對到「接駁點位置」）、備註（起訖、班次號、座位、抵達時間、訂位代號）。方向與工具是猜的，所以**視窗裡直接是可以改的下拉與輸入框**，不是只能勾不勾。
  - **判斷是計分制**：工具字眼 +2、訂位代號 +2、座位／車廂 +1、出發／抵達 +1、班次號 +1、日期＋箭頭 +1，≥4 分才算；**有入住／退房就扣 3 分**——訂房信裡的「機場接送」不能把它變成交通票。交通與住宿都過門檻時取分數高者。
  - 三個實作坑，都在測試裡鎖住：「車次」高鐵跟台鐵都在用，段落只看到車次會判成火車，抬頭寫高鐵／新幹線才是高鐵；訂位代號 ABC123 會被「兩個字母＋數字」的班機號規則吃成 BC123，先把代號抽掉再找班次；「自強號 123 車次」的班次號在「車次」前面，且 `\s` 會跨行把下一行的年份 2026 當成車次，改用只吃空格的 `[ \t]`。
  - 只有日期沒有時間時出發時間留空，跟住宿一樣不猜。一律新增、不改既有交通紀錄。
- **測試方式**：`paste` 群組加 6 項。七種票券樣本全部判為交通且工具各自正確（flight／hsr／train／bus／ferry／hsr／metro）；機票的班機號、座位、抵達、代號逐一比對且代號不被當成班機號；高鐵來回切成兩段、方向與時間正確、車次不被判成火車；無標記時方向依賽事日期反轉；訂房信不會變成交通票、機票不會變成住宿；實際貼上兩段、把第二段改成火車後確認，三筆交通中既有的在第一筆、後兩筆內容正確。全站回歸 166/166 通過。

## v3.50.0

- **貼上住宿資訊自動填入**。在空白處貼上訂房確認信或訂房網站複製下來的內容，會跳出預覽視窗，確認後在這場賽事**新增一筆住宿**。
  - 抓得到的欄位：飯店名稱、地址、入住時間、退房時間、費用、訂房狀態。中文（入住／退房／總金額／已付款）與英文（Check-in／Check-out／Total／Booking confirmed）格式都實測過。
  - **判斷條件比心得嚴格**：訂房資料是結構化的，可以要求具體證據——「入住」與「退房」同時出現加 3 分、訂房編號加 2 分，分數 ≥4 才算。像「山中民宿／2026-09-05 ~ 09-06／已預訂」這種極簡筆記靠關鍵字湊不到門檻，所以另外加一條「住宿字眼 + 完整日期」加 1 分。實測：提到「賽前一晚住在飯店」的完賽心得得 3 分，不會被住宿搶走，仍然走心得那條路。
  - **住宿先判、心得後判**：訂房確認信也常常夠長夠像散文，順序反過來會被心得先吃掉。
  - **欄位預設全勾**（跟心得相反）。訂房信裡的日期就是這次的日期，不像心得裡的數字可能是去年的、別人的。每一項仍可取消勾選。
  - **只有日期沒有時間時，時間欄位留空**，不補一個 00:00——猜一個時間會變成看起來像真的、其實是編的資料。視窗上會說明這件事。
  - **一律新增、不改既有住宿**：判斷錯了最多多一筆可以刪掉的紀錄，不會把已經填好的訂房資料蓋掉。
  - 飯店名稱會跳過標題行：「民宿訂房」這種含住宿字眼的標題不會被當成飯店名，取的是下一行的「山中民宿」。
- **測試方式**：`paste` 群組加 6 項。中文／英文訂房信的六個欄位逐一比對；標題行不被當成飯店名；只有日期時 `checkIn` 不含時間部分；分流正確（訂房信→住宿、心得→心得、提到飯店的心得→心得）；實際貼上後既有住宿保持在第一筆、新的那筆在第二筆且金額正確。全站回歸 160/160 通過。

## v3.49.0

- **在空白處貼上一段文字，自動判斷是不是賽後心得**，是的話開預覽視窗讓你挑填進哪一場、哪個欄位。沿用既有的全域貼上路徑（原本只處理圖片與 JSON，非 JSON 的文字直接放行）。
  - **判斷用計分制，不是單一條件**。任何一條規則單獨用都會誤判：只看長度會把程式碼當心得，只看關鍵字會把賽事官網簡介當心得。長度、賽事詞彙、句子數、第一人稱各自加分，分數 ≥5 且長度 ≥40 才算。XML／GPX、純網址、逗號分隔的表格、看起來像程式碼的內容一律先擋掉。實測：真心得 7 分通過，官網簡介 3 分不通過（沒有第一人稱、句子少）。
  - **焦點防護沿用**：游標在輸入框、文字框或任何可編輯區域時完全不攔截——在「檢討筆記」裡貼東西不會被自己的功能吃掉。這條有獨立測試。
  - 預覽視窗可挑賽事（預設是目前開著的那場）、挑欄位（檢討筆記／路線心得／撞牆期紀錄／優點／缺點）；欄位已有內容時預設「接在後面」，也可以選覆蓋，並顯示現有內容前 120 字。
  - **順便抽出成績數字，但一律預設不勾**。心得裡的數字經常是去年的、別人的，或只是目標值（「想破 3:30」）。每一項都附上原文片段讓你自己判斷，會覆蓋現有值的還會標紅。
  - **距離要有明確標記才算**：「30 公里之後開始撞牆」是里程碑不是賽事距離，必須是「全程 42.195 公里」這種寫法才會列出來。
- **測試方式**：新增 `paste` 群組 8 項。分類器對真心得通過、對五種雜訊（短句、網址、程式碼、CSV、GPX）全部不通過；在 textarea 裡貼上不會被攔截；空白處貼上會開視窗且預設選中目前賽事、數字全部未勾；抽出的時間等於 10771 秒、名次 128 且都有原文片段；直接按「填入」只寫文字、不動成績；切換到另一場、勾選時間後填入，原有內容保留在前面且時間有寫進去；里程碑數字不會被當成距離；欄位標籤跨區段解析（距離在路線區段，不能顯示成 `route.distanceKm`）。全站回歸 154/154 通過。

## v3.48.0

- **每場賽事一個公開連結（2a）**。完賽賽事的分享視窗新增「公開連結」區塊：建立、複製、撤銷。
  - **公開範圍（依你的決定）**：成績（時間／PB／排名）、路線 GPS 軌跡、照片、戰靴、已補給的補給品、基本資訊（名稱／日期／城市／運動別）。**白名單制**——快照只放明列的欄位，預算、筆記、裝備清單、訓練計畫這些「沒列出來的」自動不會進去，之後新增任何欄位也一樣。測試用「私人標記字串不得出現在快照 JSON」驗證。
  - 快照寫到 Firestore `/public/{隨機id}`，內容是一整包 JSON 字串——讀取端不用逐欄位解 Firestore 的型別包裝。**撤銷就刪掉文件，連結立即失效**。軌跡降採樣到 400 點、座標取 5 位小數；整包預算 900KB（Firestore 單文件上限 1MB），超過先砍照片、再砍 OG 圖、最後砍軌跡。
  - **`?s=<id>` 唯讀頁**：完全獨立於 App——不讀本機資料、不初始化月曆、看的人不需要帳號。所有文字經過 escapeHtml（快照是別人寫的資料，一律當不可信輸入；測試用帶 onerror 的惡意名稱驗證）。軌跡畫成 SVG，不載入 Leaflet。讀取優先走 SDK，SDK 失敗退 REST。
  - 「複製文案」的網址在有公開連結時自動換成那一條——點得進去、看得到內容，比首頁有意義。
  - **需要改 `firestore.rules`**（區塊在 `functions-share/README-DEPLOY.md`）：`/public/{id}` 開放讀取、只有登入且 owner 相符者可寫可刪。
- **OG 預覽（2b，`functions-share/`）**。社群爬蟲不執行 JS，GitHub Pages 的 `?s=` 連結貼到 Threads／FB／LINE 只會顯示同一張預設縮圖。這支 Cloud Function 在 `/s/{id}` 回傳那一場的 og:title／og:description／og:image（發佈時算好的方形分享圖，直接從快照的 base64 吐出 JPEG，不需要 Storage、不需要圖床，快照刪掉圖就跟著失效），真人點開自動轉到 App 唯讀頁。
  - **不部署也能用公開連結**（`?s=` 直連照常），只是社群預覽是通用的。部署後把服務網址填進 `index.html` 的 `OG_SHARE_BASE`。步驟在 `functions-share/README-DEPLOY.md`。
  - HTML 產生邏輯有本機單元測試（`node test.js`）：og 標籤齊全、惡意名稱被跳脫、無圖時不放 og:image。
- **測試方式**：新增兩個群組共 9 項。`publink`：快照白名單（結構＋文字標記雙重驗證；學到一課——軌跡座標取五位小數後，任何數字串都可能出現在 25.00999 裡，數字不能當洩漏標記）、40 張高噪點照片下仍守住 900KB 預算、發佈寫入／撤銷刪除／`publicShareId` 正確維護、文案網址切換。`pubview`：唯讀頁渲染成績且沒有任何可編輯欄位、惡意名稱不執行、軌跡 SVG 有畫、本機資料未被載入、失效連結顯示說明。全站回歸 146/146 通過。

## v3.47.0

- **分享圖加上 QR code（1a）**。IG 的圖片不能點連結，看到照片的人除非掃 QR，否則沒有任何方式找到這個網站。
  - **自己寫了一個最小 QR 產生器**（位元組模式、EC level M、版本 1–6），不引入外部套件——這個 App 是單檔、離線可用、外部套件要鎖版號加 SRI，為了一個角落的方塊拉一包 40KB 的函式庫不划算。驗證方式：把產生的矩陣跟 OpenCV 內建編碼器的輸出逐格比對（除了規格上無意義的 remainder bits 外完全一致），再用 OpenCV 的解碼器實際掃出網址。
  - **畫在淺色圓角底板上**，不是直接疊在深色背景。QR 必須「深色模組配淺色底」才掃得出來，反白的只有部分手機認得。四周留 4 個模組的靜默區，規格要求，少了掃不到。
  - **尺寸 240px（方形）／250px（限動）**，位置在方形的右上角（那塊本來就空著，不用跟軌跡、補給文字搶位置）與限動的右下角。240px 是掃得到的下限——1080 的圖在手機上顯示約 390pt 寬，240px 的 QR 只剩 87pt，再小就得把手機湊到螢幕前才掃得出來。
  - 網址優先用當下的部署網址（換網域不用改程式），`file://` 開啟時退回正式站台。
  - 可在分享設定視窗勾選開關，預設開。
- **「複製文案」按鈕（1b）**：賽事名、成績、前三項數據、hashtag、網址一次複製，貼到 IG 說明欄即可。只取前三項數據是因為 IG 說明欄第二行之後會被「⋯更多」收起來，五項全塞等於後面沒人看得到。`navigator.clipboard` 需要 https，`file://` 或舊瀏覽器退回 textarea + execCommand。
- **分享面板帶上文案（1c）**：`navigator.share({files,title,text})`。IG 只吃圖、會忽略文字，但 LINE、Threads、Messenger 會一起帶過去，連結在那些地方是可以點的。
- **測試方式**：新增 8 項，其中五項是**真的用解碼器掃**——產生分享圖 → 取出 canvas 像素 → OpenCV 解碼 → 比對網址；方形與限動各驗一次，再各縮到 640px（IG 實際送出的解析度區間）確認還掃得到；關掉 QR 開關後圖上不能有任何可解碼的 QR。另外驗文案含賽事名／時間／hashtag／網址且不超過 6 行、複製函式回報成功。全站回歸 137/137 通過。

## v3.46.0

- **手機開彈窗就白畫面、不斷重整——真正的原因找到了，跟 Service Worker 無關**（`sw.js` 移除後仍然發生，而且「同步診斷」一點就重現）。
  - 開任何彈窗時，`body.overlay-open #main-content` 會套 `filter:blur(4px)` 做空間景深（v3.13.0）。`filter` 會把整個 `#main-content` 連同底下所有東西——整個月曆、上百張 base64 封面、獎牌牆、圖表——一次點陣化成**單一 GPU 圖層**再做模糊。頁面越長、照片越多，這張貼圖就越大；iOS Safari 對這種超大圖層沒有分塊處理，記憶體一超過就把整個分頁殺掉、自動重新載入。使用者看到的就是「開一個彈窗 → 白畫面 → 不斷重整 → 『重複發生問題』」。最近照片一多，剛好跨過門檻。
  - **模糊改成只給桌機**（`@media (hover:hover) and (pointer:fine)`）。`transform:scale(0.96)` 與遮罩變暗保留——transform 沒有這個問題（Safari 會分塊），所以手機上景深效果還在，只是不模糊。
  - 「尤其是同步診斷」的原因：它是少數從主畫面（月曆＋生涯數據＋獎牌牆，`#main-content` 最高的時候）打開的彈窗。
- v3.45.0 的救援閘門與 SW 修正保留：它們處理的是另一類問題，而且這次「連續三次啟動失敗自動清除」的機制沒有誤判——因為問題不在啟動，在開彈窗之後。
- **測試方式**：手機群組新增一項——觸控模擬下加上 `overlay-open` 後，`#main-content` 的 `filter` 必須是 `none`、`transform` 不能是 `none`。全站回歸 129/129 通過。

## v3.45.0

- **手機一直自動重整、白畫面**。查到兩個獨立問題，都跟 v3.31.0 的離線 Service Worker 有關：
  - **導覽請求的逾時寫法不安全**。原本把 `{signal: controller.signal}` 當第二個參數傳給一個 `mode==='navigate'` 的 Request，各家瀏覽器對這個組合的處理不一致（有的直接丟 TypeError）。一旦丟錯，每次導覽都會掉進 catch、永遠只吃快取——使用者再也拿不到新版，而快取裡萬一是壞掉或抓到一半的頁面，就變成每次開都白畫面。改用 `Promise.race` 做逾時，不碰 Request 本身；退回快取時同時在背景抓一份新的，下一次就會是最新版。`cache.put` 失敗也不再讓整個導覽失敗。
  - **沒有任何脫困的方法**。頁面開不起來時，Service Worker 還是會繼續把壞掉的快取端出來，而清除它需要 JS 跑起來——這正是當下做不到的事。
- **新增救援閘門**，是一支獨立的小腳本、放在主程式之前，主程式整個掛掉它還是會跑：
  - **`?nosw=1`**：註銷所有 Service Worker、清掉所有快取，然後回到乾淨網址。這個分頁接下來都不再註冊（存 `sessionStorage`，關掉分頁就恢復），不然導回去又馬上把離線快取裝回來，等於什麼都沒解決。
  - **連續三次「開始載入但沒能啟動完成」自動做同樣的清除**，並停用這一次的註冊，讓頁面先活下來。計數在 `init()` 成功結束時歸零，所以連按幾次重新整理不會被誤判——第一版用「20 秒內載入超過 3 次」，測試時立刻誤殺正常的連續重整，改掉了。
  - 清除後會跳出提示說明發生什麼事、資料都還在。
  - **清除順序：先註銷、再刪快取**。反過來的話，還活著的 worker 會在刪完之後因為攔到請求又把快取建回來（實測會留下 `race-log-*`）。同時對還在控制頁面的 worker 送 `HARD_RESET`，讓它自己刪快取再註銷。
- **測試方式**：offline 群組加 7 項——正常重整不被誤判；連續啟動失敗會停用註冊、註銷 worker、跳出說明、而且頁面本身仍然可用；`?nosw=1` 會回到乾淨網址、這個分頁保持停用、worker 歸零。全站回歸 128/128 通過。
- **無法直接驗證 iOS Safari**（這個環境裝不了 WebKit），以上是對「所有可能造成這個現象的路徑」一起補強，不是已確認的單一根因。

## v3.44.0

- **三鐵賽事把全程距離灌進鞋子里程——修掉**。綁了跑步段穿的鞋，226 公里全部算到那雙鞋頭上，含 3.8 公里游泳與 180 公里騎車。鞋子的壽命條、退役提醒、年度回顧的鞋款里程全部被灌水，一場 226 就吃掉 600 公里壽命的三分之一。
  - 新增 `shoeDistanceOfRace(race)`，所有跟鞋子有關的里程都改走它：有分項資料（`legs`）時只加總 `sport==='running'` 的段落；單項跑步賽事維持全程距離。
  - **多項賽事沒有分項資料時回 0，不是全程距離**。少算可以之後補上，多算會讓人以為鞋子該退役了——後者的代價大得多。
  - 同一個錯誤也在**戰靴平均配速**上：226 公里的總時間除以總距離，得到的不是這雙鞋的配速。改成用跑步段的時間與距離；沒有分項資料的多項賽事直接跳過那一場，不拿假配速污染平均值。
  - 受影響的四個地方一次改齊：鞋子總里程、衰退交叉比對的累積里程、年度回顧的鞋款統計、戰靴平均配速。
- **測試方式**：新增三項。226 三鐵（含游泳／轉換區／騎車／跑步四段）加一場全馬，鞋子里程必須是 84.39 而不是 268.195；沒有分項資料的 113 三鐵加進去之後總里程不變；平均配速等於「跑步段配速」與「全馬配速」的平均。全站回歸 121/121 通過。
- **已經記錄的三鐵賽事會自動修正**：里程是每次即時算出來的，不是存起來的累計值，重新整理就會看到正確數字。只是沒有分項資料的三鐵會變成 0——把 FIT 檔匯入就會抓到分項。

## v3.43.0

- **「裝備、補給與戰略（賽前準備）」與「預算與行程規劃」對調**，裝備排在預算前面。區段順序現在是：基本資訊與時間／路線與氣象／裝備補給與戰略／預算與行程規劃／賽後紀錄與個人數據。
- 區段導覽列跟著換成 基本／路線氣象／**裝備／預算**／賽後——導覽列是區段的索引，順序跟畫面不一致會讓人以為自己點錯了。
- **測試方式**：新增一項，同時鎖住兩件事——DOM 裡五個區段的 id 順序正確，而且導覽列的 `data-target` 順序跟它完全一致（以後再調整順序，只改其中一邊就會紅）。全站回歸 118/118 通過。

## v3.42.0

- **「地理與路線」與「氣象」合併成一個區段「路線與氣象」**，底下分成兩個子標題：**官方路線**、**當日氣象**。兩者本來就是同一件事的兩面——路線決定你要跑什麼地形，氣象決定那天跑起來是什麼感覺，各自佔一個收合區段只是多一次點擊。詳情頁從六個區段變五個。
- 區段導覽列跟著少一格：基本／**路線氣象**／預算／裝備／賽後。
- 順手修掉合併後才浮現的間距問題：子區塊裡最後一個元素的 margin 會穿出沒有下內距的 `.subsection`，再疊到區段的下內距上，間距變成一倍半。多加一條 `details.section>.subsection:last-child>*:last-child{margin-bottom:0}`。
- **測試方式**：新增一項——`section-weather` 不存在、合併後的區段底下兩個子標題依序是「官方路線」「當日氣象」、導覽列剩五格且沒有指向 `section-weather` 的項目。v3.41.0 那項間距檢查在合併後一度變紅（28px），加上巢狀歸零規則後回到 18px。全站回歸 117/117 通過。

## v3.41.0

- **區段裡最後一張卡片緊貼下緣——修掉**。`details.section` 的內距是 `0 20px`，沒有下內距，最後一張卡片的 `margin-bottom` 會直接從容器底部穿出去（margin collapsing）。結果是左右各有 20px、下面卻是 0，「時間與報名」「CP／補給站明細」「預算與行程」這些每個區段的最後一張卡都被壓在邊線上。
  - 補上 18px 下內距擋住穿透，同時把最後一個子元素自己的下間距歸零（`>*:last-child{margin-bottom:0}`、`>.subsection:last-child{padding-bottom:0}`）——不然 18px 內距加上原本的 18px margin 會變成兩倍空白，反而鬆散。
  - 四個區段實測都是下緣 18px、左右 20px。
- **測試方式**：新增一項——逐一展開區段，量最後一個子元素到區段下緣的距離，必須存在（≥12px）且不超過左內距（不能變成兩倍空白）。反例（拿掉下內距）確認會紅。全站回歸 116/116 通過。

## v3.40.0

- **地圖上的照片卡片超出地圖、資訊被切掉——修掉**。這是 v3.37.0 的副作用：縮圖轉正之後直立照真的變成直立的，而 popup 圖片原本只限寬度（`max-width:180px; width:100%`），3:4 的直立照高度就變成 240px，加上時間／海拔／心率配速三行，整張卡比 280px 的地圖還高。
  - 改成以高度為主要限制：`max-height:150px`、`width:auto`、`object-fit:contain`。橫幅照片照樣用滿 180px 寬，不會被拉扁。
  - 順手收掉 Leaflet 預設的寬內距（左右各 20px），在 220px 寬的卡片裡那等於少掉五分之一的可用寬度。
- **測試方式**：新增一項——拿 600×800 的直立圖產生 popup，量實際渲染高度：整張卡 ≤240px、圖片 ≤150px 且高大於寬、三行數據都在。反例（改回只限寬度的舊 CSS）確認會紅。全站回歸 115/115 通過。

## v3.39.0

- **表格檢視依年份收合**。生涯上百場全部攤開，找一場舊比賽要捲很久。改成年份標題列（點一下展開／收合），標題上直接看該年摘要：場次、完賽數、合計里程。
  - **預設只展開今年**；資料裡沒有今年就展開最新的一年。
  - **被選中的賽事所在年份強制展開**：從別的檢視切過來時，選中的列藏在收起的年份裡會讓人以為它不見了。
  - 展開狀態記在記憶體、同一次瀏覽有效。刻意不存 localStorage——年份清單隨資料變，存起來只會累積一堆已經不存在的年份。
  - 沒填日期的賽事歸在「未填日期」一組。長按快速動作、點列開詳情頁的行為不變。
- **測試方式**：新增三項。2025／2026／2027 三年的資料下預設只有 2026 展開、可見列數正確；2025 標題顯示「1 場・完賽 1・42.2 km」、點開多出該列、把 2026 收回後只剩 2025 的列；選中 2027 的賽事重畫後該年自動展開且選中列可見。全站回歸 114/114 通過。

## v3.38.0

- **有些照片完全加不進去——三個各自獨立的原因，全部處理**：
  - **檔案選擇器只放行 JPEG**（`accept="image/jpeg,image/jpg"`）。iPhone 的 HEIC、PNG、WebP 在選擇器裡直接是灰的、根本選不到。改成 `image/*,.heic,.heif`；全域拖曳的副檔名白名單同樣補上 heic／heif／webp／tif，並且改成「副檔名在清單內**或** MIME 是 image/*」都收。exifr 本身早就讀得懂 HEIC，被擋住的一直是選擇器。
  - **沒有 EXIF 拍攝時間的照片被靜默略過**。拖 10 張進來只出現 6 張，畫面上不會有任何訊息。現在逐檔記錄原因並列在照片區塊下方（檔名＋為什麼），按「知道了」清掉。最常見的兩種：截圖；經過 LINE／Messenger 轉傳或編輯軟體重存、EXIF 被清掉的照片。另外 `DateTimeOriginal` 找不到時會再試 `CreateDate`／`DateTimeDigitized`，多救回一些。
  - **沒有內嵌縮圖的照片只顯示通用圖示**。HEIC 與編輯過重存的 JPEG 常常沒有內嵌縮圖。現在退一步自己解碼原圖縮一張（`createImageBitmap` 帶 `resizeWidth:320`，在解碼階段就縮小，不會把全尺寸點陣圖留在記憶體；`imageOrientation:'from-image'` 讓瀏覽器轉正）。這違反「不載入整張原圖」的原則，所以**只在沒有內嵌縮圖時才走**。HEIC 只有 Safari 解得開，Chrome 會失敗——失敗就回通用圖示，照片本身照樣加得進來。
  - 照片說明文字改寫，講清楚支援哪些格式、以及什麼情況會加不進來。
- **測試方式**：新增四項。沒有拍攝時間的檔案不會進 `geoPhotos`、會進略過清單且標成 `noTime`；略過清單在畫面上列出檔名、按「知道了」會消失；沒有內嵌縮圖時退回自己解碼，產出的縮圖寬度正好 320；選擇器的 accept 含 heic 與 image/*。全站回歸 111/111 通過。

## v3.37.0

- **空間記憶迴廊的照片縮圖倒著、躺著——修掉**。內嵌縮圖存的是感光元件的原始方向，本身沒有方向標籤；直立拍的照片，縮圖躺著或倒著是常態（你那張是 180°）。以前把縮圖原封不動拿來用，現在用主圖的 EXIF `Orientation` 轉正再存。
  - 八種方向碼全部處理（旋轉四種＋鏡像四種），5～8 直式的畫布寬高對調。
  - 解碼用 `createImageBitmap(blob,{imageOrientation:'none'})`：萬一縮圖自己也帶方向標籤，瀏覽器在 drawImage 時會再轉一次，兩次就轉過頭。舊瀏覽器沒有這個 API 時退回 `<img>`。
  - exifr 可能給數字也可能給翻譯過的字串（'Rotate 90 CW'），兩種都收；認不出來一律不轉——轉錯比不轉更糟。
  - **已經加進去的照片不會自動修正**（縮圖是從原始檔抽的，原始檔沒存下來），移除後重新拖進來即可。
- **測試方式**：新增兩項。做一張左上角有紅塊的縮圖，1／3／6／8 四種方向轉完後紅塊在正確的角、寬高對調正確；方向碼以數字或字串給都能解析、垃圾值回 1。全站回歸 107/107 通過。

## v3.36.0

- **雷達圖的網格線在深色模式下看不見——修掉**。這是 v3.35.0 的副作用：以前雷達圖沒有隨主題重畫，深色模式下看到的網格其實是淺色模式的 `--rule`（#D8DAD3，淺灰）印在深底上，反而很清楚；v3.35.0 改成正確重畫後，網格拿到的是深色模式的 `--rule`（#2A3038），跟底色只差一階，幾乎消失。
  - `--rule` 是「分隔線」的顏色，本來就設計成低調；拿來畫圖表網格在深色模式下就是不夠。改用次字色配透明度：外圈 0.75、內圈 0.42、放射軸線 0.36。兩個主題都看得到框架，又不會壓過金色的資料多邊形；讀圖時先看到範圍、再看到刻度。
- **測試方式**：新增一項像素檢查——深色模式下 canvas 裡灰階、中等亮度的像素數要超過門檻；把網格改回 `--rule` 的反例確認會紅。全站回歸 105/105 通過。

## v3.35.0

- **雷達圖切到深色模式後文字變得幾乎看不見**——修掉。兩個原因疊在一起：
  - **Canvas 不會跟著 CSS 變數換色**。雷達圖是原生 canvas，畫的那一刻讀什麼顏色就是什麼顏色；切主題後 SVG 與 CSS 都自動變了，只有它還是淺色模式的深灰字印在深色底上。現在 `applyTheme()` 會把還在畫面上的雷達圖用新的變數值重畫一次（直接畫完成狀態，不重跑生長動畫——切個主題不該像重新載入頁面）。
  - **軸名與數值原本都用次字色**（`--ink-soft`），深色模式下數值那行本來就偏淡。改成軸名用主字色、數值用次字色，兩個主題都更清楚；數值字級 10px → 11px。
- **測試方式**：新增一項像素檢查——淺色模式畫完後，切到深色，canvas 頂部（「距離」標籤所在）的亮像素數必須明顯增加。反例驗證過：拿掉切主題重畫，這項會紅。全站回歸 104/104 通過。

## v3.34.0

- **區段導覽列（基本／路線／氣象／預算／裝備／賽後）移到成績儀表板上方**，緊接在 header 的「在 Google 日曆開啟」之後。導覽列是 sticky 的，要在使用者開始捲動前就先進到視野，才知道有這排可以跳；壓在儀表板下面時第一屏被儀表板佔滿，導覽列要捲過去才看得到。頁面順序現在是 header → 導覽列 → 儀表板 → 各區段。
- **測試方式**：順序那項改成驗四段相對位置（header → 導覽列 → 儀表板 → 第一個區段）。全站回歸 103/103 通過。

## v3.33.0

- **桌機的 Ctrl/⌘＋滾輪縮放解除封鎖**。原本為了「跟手機行為一致」連桌機也攔掉，是判斷錯誤：那是瀏覽器層級的縮放、等同無障礙功能，使用者要看大一點是他的權利。手機鎖的是「像原生 App」的觸控體驗（雙指縮放、連點兩下），兩者不是同一件事。手機的鎖定原樣保留。
- **測試方式**：手機群組新增一項——帶 `ctrlKey`／`metaKey` 的 wheel 事件都不能被 `preventDefault`。全站回歸 103/103 通過。

## v3.32.0

- **氣候與表現散佈圖改用全部路跑歷史賽事推算**。「依 6 場推算」不是資料少，是三道篩選把大部分賽事擋在外面：只收路跑、只收 10K／半馬／全馬三個經典距離、要有體感溫度。第二道是主因——5K、15K、30K、超馬路跑落在區間外就整場丟掉。
  - **效率基準從「三個分類各自的 PB」改成「個人距離曲線」**（Riegel：T = a × D^k）。k 從你各距離的最佳成績做 log-log 迴歸擬合，夾在 1.0～1.25 之間；不同距離的成績少於兩種時用一般跑者的 1.06。截距取「包絡」：曲線壓到最緊的那一場之下，所有賽事效率 ≤100%、剛好一場 100%，語意跟原本的「PB 是 100%」一致。只用前緣點（每 0.5 km 一桶取最佳）擬合，不然同距離跑差的場次會把曲線往上拉。
  - **沒有體感溫度的賽事退回歷史氣候平均溫**，畫成空心點，推算文字註明「其中 N 場用氣候平均溫」。估計值就是估計值，圖上要看得出來。
  - **越野做成預設關的開關**。開了會用 EPP 的爬升折損係數（每 1% 坡度慢幾 %）以整場平均坡度校正回平地等價，畫成三角；係數資料不夠時註明暫不納入。粗，但跟係數本身的定義一致；不校正的話越野點會全部躺在最下面把 R² 拖爛。
  - **甜蜜點只看效率 ≥98%**，不再把「各距離最佳」都算進去——一個人跑過六種距離，六場都是該距離最佳，但相對曲線可能只有 96%；甜蜜點要的是「那天狀態真的好」，不是「那個距離只跑過一次」。金點仍標各距離最佳，但 tooltip 一律寫出效率，不再寫「PB」讓人以為是 100%。
  - **EPP（環境折損預測）沒有跟著放寬**：它的係數要拿去預測完賽時間，寧可少幾場也不混進估計值，維持三個經典距離、只用實測溫度、不含越野。
  - 縱軸標題改成「配速衰退（相對個人距離曲線）」；圖例改兩欄兩列。
- **測試方式**：新增 `climate` 群組 9 項。合成資料（5K～全馬六種距離＋三場越野）下：舊規則 4 場、新規則 7 場，5K／30K 進來、沒溫度的仍排除；k 擬合在合理範圍、六個距離、包絡讓所有效率 ≤100 且剛好一場 100；只有一種距離時用預設 k；兩場氣候平均溫被標成估計值且畫成空心；越野預設關、開了三場進來且校正後效率高於未校正；開關實際重畫出三角並更新文字；爬升係數不足時越野排除且顯示提示；甜蜜點數量小於「各距離最佳」數量；EPP 資料層維持舊規則。全站回歸 102/102 通過。

## v3.31.0

- **離線 Service Worker（新檔案 `sw.js`，必須跟 index.html 同層部署）**。山上沒訊號時網站打得開、疲勞模式能用。資料本來就全在本機，缺的只是頁面本身——一個 index.html 加兩個 CDN 套件，所以 SW 只快取這幾樣，Firebase、天氣 API 一律直接放行。
  - **快取版本綁在註冊 URL 的 `?v=APP_VERSION`**：版號一變就是新的 SW、新的快取，舊快取在 activate 時刪掉。不用另外維護版本常數，也不會出現「index.html 更新了、快取還是舊的」。
  - **頁面走網路優先、3.5 秒逾時退回快取**。快取優先會讓每次都拿到舊版、更新要靠換 SW 才生效；網路優先有訊號時永遠最新，沒訊號時 3.5 秒內出畫面。CDN 套件版號鎖死＋SRI，內容不會變，快取優先。
  - **CDN 預快取一律用 `mode:'cors'` 取**：index.html 用 SRI 驗證，快取回應若是 opaque，SRI 判定失敗、套件整個不載入。sw.js 裡的兩個 URL 必須跟 index.html 的 `<script src>` 一字不差，測試有鎖。
  - **新版不自動接管**：install 完等使用者按「立即更新」才 `skipWaiting`，避免正在填表單時頁面突然重新載入。首次安裝的 `clients.claim()` 也會觸發 controllerchange，那時不重新載入。
  - 離線／恢復連線各跳一則 toast。`file://` 與非 https 來源不註冊，本機開檔案測試不會報錯。
- **加入主畫面提示**：第三次開啟才問，按「稍後」30 天內不再問，已在主畫面裡開的完全不顯示。Android／桌機 Chrome 走 `beforeinstallprompt`；iOS Safari 沒有這個事件，只能用文字教「分享 → 加入主畫面」。
- **儲存空間 ≥80% 主動提示**，附「匯出 JSON」「清理封面」「知道了（7 天）」。同步診斷裡的數字變紅是被動的，要使用者自己點進去看。啟動時查一次、之後每次存檔最多 5 分鐘查一次。
  - **清理封面**：列出有封面的賽事、依佔用大小排序（base64 長度 ×0.75 就是位元組，不用解碼）、逐張移除。移除是兩段式確認、5 秒自動解除——封面沒有垃圾桶，刪了就沒了。
  - 儲存警示比安裝提示優先：空間快滿是會掉資料的事，安裝只是方便。同時只顯示一條提示。
- **測試方式**：新增 `offline` 群組 10 項，**自己起本機 http 伺服器跑**（SW 不能在 file:// 註冊）：sw.js 預快取清單與 index.html 的 CDN URL 一字不差；註冊 URL 帶版號；SW 進入 active；**瀏覽器切成離線後重新載入，頁面仍然活著、版本號正確**；疲勞模式的函式存在；安裝提示第二次不出現、第三次出現；稍後會存 30 天；儲存 85% 出現警示、40% 不出現；清理封面大的排前面、第一下變「確定移除？」、第二下才真的移除。另外以反例驗證過：註銷 SW 再離線，重新載入確實失敗——這項測試不是靠 HTTP 快取通過的。全站回歸 93/93 通過。

## v3.30.0

- **完賽賽事的成績儀表板搬到 header 正下方**（區段導覽列之前）。打開一場已完賽的賽事，第一眼要看到的是成績，不是「基本資訊與時間」；原本要往下捲、點開「賽後紀錄與個人數據」才看得到大字時間。
  - 儀表板本來就只在已完賽且有成績時才產生，未完賽的頁面這裡什麼都沒有，不用另外寫判斷。
  - 從賽後區段移出而不是複製——同一個數字出現兩次是 v3.25.0 那輪清 header 時才處理掉的問題，不能再犯。
- **沒填的抽屜卡片淡化、收成一行**；填了的維持原樣。13 張卡片裡通常有一半是空的，讓它們跟填了的一樣重，使用者掃過去分不出哪裡有內容、哪裡只是標題。
  - 空的：透明度 0.58、虛線框、標題與「尚未填寫」並排、高度約剩一半；hover／focus 回到全亮，不然找不到要點哪裡。
  - 「空不空」由每張卡片自己判斷（它知道什麼叫有填），不從 sub 文字反推——那會綁死翻譯字串。
- **測試方式**：新增四項。有城市與距離的賽事：基本資料與路線卡不是空的、目標／心得／裝備卡是空的；空卡片實際高度小於填了的；完賽賽事的儀表板落在 header 之後、導覽列之前，全頁只有一個，賽後區段裡沒有；未完賽賽事沒有儀表板。全站回歸 83/83 通過。

## v3.29.0

- **指令面板的手機入口**：搜尋框第一個字打 `>` 或 `#`，搜尋框把字交給指令面板接手。指令面板原本只有 Cmd/Ctrl+K 能開，手機沒有實體鍵盤等於沒有入口——動態回顧輪播、生涯回顧這些收在裡面的功能，手機上從 v3.25.0 起就碰不到。
- **長按賽事卡片彈出快速動作**（行事曆格子、清單列、獎牌牆卡片、表格列）：開啟／複製／產生分享圖／刪除。
  - 只吃 touch 的 pointer 事件——桌機有右鍵與 hover，長按是手機的手勢。
  - 長按觸發後瀏覽器補送的 click 在捕獲階段擋掉，否則放開手指會順便把詳情頁打開。Android 的系統長按選單（contextmenu）也擋掉，iOS 的文字選取用 `-webkit-touch-callout:none` 關掉。
  - 刪除走詳情頁同一條路：軟刪除＋復原 toast。沒有再加一層確認——安全網本來就是垃圾桶，多一層確認是把同一個保護做兩次。
- **詳情頁左右滑切換區段**：往左下一區、往右上一區，導覽列 chip 跟著反白。
  - 「目前在哪一區」不另外存狀態，每次用「頂端最接近視窗上緣的區段」算——使用者用手指捲動時，存起來的狀態會跟畫面脫節。
  - 手指落在自己會橫向捲動的元素上（分段表、裝備看板、地圖、輸入框）時整個不啟動，那些本來就要吃橫向手勢，搶走會讓它們不能用。判斷橫向／縱向只在剛開始移動時做一次，跟清單滑動同一套邏輯。
- **時間欄位即時驗證**（交接文件裡的待辦）。
  - **修掉一個真正的資料損失**：`hmsToSec` 看不懂就回 null，而 change 事件會把 null 直接存進去——打錯一次，原本的成績就被清成空白，畫面上什麼都不說。現在看不懂的字串留在欄位裡標紅，不寫進資料。
  - 解析放寬：`3.44.25`、`3 44 25`、`3h44m25s`、`3時44分25秒`、`4'15"`、全形冒號都收。手機上打冒號要切符號鍵盤，打句點不用——不收句點等於逼使用者多按三次。但「寬」不等於「猜」：任何一段不是數字仍然回 null。
  - 歧義寫法即時顯示解讀結果（`44:25` → 「= 0:44:25」），讓人在離開欄位前就看到。
  - 驗證掛在 `input` 事件不是 `change`：change 是離開欄位才觸發，那時注意力已經到下一格，錯誤要回頭找。抽屜不在 `#detail` 底下，兩個容器都要掛。
  - 依關鍵決策沒做分段輸入框。
- **匯入預覽差異全欄位化**：總爬升、平均心率、最大心率、步頻、分段數都標「會取代現有的 ⋯」，原本只標距離與時間。規則只有一條：檔案有值、現有也有值、不一樣，才標——檔案沒有的欄位本來就不覆蓋，標了反而讓人以為會被清空。步頻預覽顯示的是 ×2 之後的值，跟套用結果對得上。
- **測試方式**：新增 11 項。手機群組：搜尋框打 `>` 開面板且字帶過去；長按 550ms 開快速動作、放開不進詳情頁；快速動作的複製與刪除實際生效（刪除是軟刪除）；短按 150ms 不觸發；左滑切到下一區；斜向下滑（縱向為主）不切。資料群組：解析器 12 種寫法；打錯不清掉原值且標紅；`44:25` 顯示「= 0:44:25」並存成 2665 秒；預覽七格全標；沒差異時一個都不標。全站回歸 79/79 通過。

## v3.28.0

- **分享圖改成先設定再輸出**：格式（方形 1:1／限時動態 9:16）、要放的內容（心率／戰靴／補給／軌跡）、即時預覽，最後才分享或下載。
  - **限時動態版 1080×1920**：上下各留 250px 安全區給 IG 的帳號列與回覆列。直式的問題不是寬度是高度——五格數據改三欄排（306px 欄寬放得下最長的「42.195 公里」）、戰靴與補給並排成一列各佔半寬，這樣中段才留得出 400px 以上給軌跡；照方形的排法直接拉長，軌跡會被擠到只剩 158px 高，等於畫不出來。
  - **勾選只列這場真的有的項目**：沒有心率就沒有心率開關。一顆永遠沒作用的開關比沒有開關更誤導。
  - **預覽用正式的產圖函式畫**，不另外維護一套「預覽版」排版——那種東西永遠會跟正式輸出對不上。
  - **方形版新增「平均心率」**（原本沒有），但方形只有一排四格的位置：五格都有時（配速／距離／爬升／心率／名次）會放不下名次。想要名次就關掉心率，或改用限動版。
  - 格式與勾選記在 `localStorage`，下一場沿用。
- **觸控裝置改走系統分享面板（Web Share API）**，桌機維持下載。
  - **為什麼要分**：手機上 `<a download>` 不是「存到相簿」——iOS Safari 會把 PNG 開成新分頁，使用者得自己長按存圖再去 IG 貼；分享面板一步就到限時動態。桌機反過來，分享面板選項少又慢，下載才是大家預期的。
  - **判斷條件是「觸控裝置」（`pointer: coarse`），不是「有沒有 API」**：桌機 Chrome 也有 `navigator.share`，但那不是桌機使用者想要的。
  - 分享面板按取消是 `AbortError`，那不是失敗——不跳錯誤、也不偷偷下載一份。其他錯誤才退回下載。
  - 徽章成就圖、年度回顧卡（社群用途）一起走這套；配速手環、賽前作戰卡、應援指南（列印用途）維持下載。
- **產圖程式重構成「資料層＋版面層」**：`shareStatsBadges()`／`shareDetailRows()` 算內容，方形與限動兩套版面吃同一份。運動別規則只寫一次，不會再發生「方形修好了、限動還在顯示泳渡爬升」。
- **測試方式**：新增兩個群組共 10 項。`share`：視窗打開有兩種格式與四個勾選、預覽是 PNG；切限動後預覽變 1080×1920 且設定被記住；關掉軌跡後下半段找不到金色像素；沒資料的賽事沒有任何勾選；桌機就算有 `navigator.share` 也走下載。`share_touch`（觸控模擬）：`pointer: coarse` 成立；優先分享面板；取消回傳 cancelled 且不下載；分享失敗退回下載；視窗按鈕文字是「分享」。全站回歸 68/68 通過。

## v3.27.0

- **分享圖下半部補上戰靴、補給與 GPX 軌跡**。原本那裡是一整片空白，佔掉畫面將近三分之一卻什麼都沒說。
  - **戰靴**只在跑步相關賽事顯示。自行車、游泳本來就不綁鞋款，硬是留一個空欄位只會讓人以為資料填錯。
  - **補給優先用實際吃掉的**——補給時程裡勾過「賽後：已補給」的項目，同名合併數量。完全沒勾時才退回補給設定裡的計畫數量，**並且標示成「補給（計畫）」**：把計畫當成實際寫在分享圖上是在說謊，但整塊空著也沒必要。一行放不下時用「+N」交代還有幾項，不截斷成「咖啡因…」那種既難看又讀不出總數的形式。
  - **軌跡是三層疊加的金光**，背景透明（不鋪卡片、不畫外框，直接浮在底色上）。單一 `shadowBlur` 做不出發光，只會像描了一圈模糊邊；疊加時用 `globalCompositeOperation='lighter'`，深色底上相加才會愈靠中心愈亮。亮芯與起終點圓點改回正常疊加模式——用 lighter 畫亮色核心會過曝成一團白。
  - **沒有軌跡的賽事**不留空欄位：文字會自動佔滿整行寬度。
- **順手修掉分享圖的運動別錯誤**（詳情頁 v3.10 就修好了，分享圖沿用舊算法一直沒動）：游泳改用每 100m 配速、不顯示總爬升，多項運動不顯示全場平均配速。你回報的那張泳渡分享圖上的「89'25"/km」與「總爬升 531 公尺」就是這個。**分享圖比詳情頁更該修**——它是會被截圖轉傳出去的畫面。
- **測試方式**：新增四項。`share_image_respects_swim_units`（泳渡分享圖只能出現 /100m、不能出現 /km 與 531）、`share_image_shows_shoe_and_consumed_fuel`（鞋名與已補給項目要出現、沒勾的項目不能出現）、`share_image_track_glows_gold`（下三分之一要找得到紅綠明顯高於藍的亮像素——只驗「有沒有畫線」的話，畫成白色或綠色都會通過）、`share_image_survives_missing_track`。斷言用的是攔截 `fillText` 看「畫了什麼字」而不是比對像素，否則字型與抗鋸齒的差異會讓測試壞得比程式還頻繁。已用「軌跡改成白色」與「補給不看 consumed」兩個反例驗證過這些檢查真的會紅。全站回歸 58/58 通過。

## v3.26.0

- **獎牌牆的縮圖從 320px 提高到 480px**（壓縮品質 0.72 → 0.80）。
  - **原本糊掉不是壓縮的問題，是解析度不夠**：獎牌牆的卡片實測桌機 245 CSS px、手機 170 CSS px，乘上 Retina 的 2～3 倍像素密度，實際需要 490～510 像素。320px 只有需求的六成，等於把一張小圖放大來看。
  - **為什麼停在 480 不繼續往上**：760px 原圖本身才 80 KB，縮圖若做到 640px 要 69 KB——存了第二份幾乎一樣大的檔案，那不如直接用原圖；而直接用原圖實測會讓獎牌牆渲染從 75ms 變成 152ms（60 場、沙盒環境），因為 base64 是直接串進 innerHTML 的，字串本身就是成本。480px 剛好打平需求。
  - **代價講明白**：每張縮圖約 20KB → 48KB（實測用的是高雜訊合成圖，真實照片會更小），60 場的縮圖總量約 1.2MB → 2.8MB；獎牌牆首屏渲染 75ms → 100ms（沙盒）。這是你要的畫質換來的，不是免費的。
  - **舊的 320px 縮圖會在背景自動重產**。判斷方式是實際解碼出來的寬高，不另外存版本欄位（多一個欄位要一路改到 schema 與雲端文件）；原圖本來就小於 480px 的不重產，否則每次啟動都會白做一輪還觸發一次存檔與雲端同步。重產完成會存檔一次。
- **切換到獎牌牆後自動捲到牆的位置**。牆維持原本的版面順序（仍在生涯數據與榮譽櫃下方），沒有上移。
  - **理由是回饋發生在看不到的地方**：按下切換鈕後，畫面上半部完全沒有變化，牆在一個半螢幕之外，唯一的變化是按鈕自己變深色——而那正好是使用者眼睛所在之處以外的細節，看起來像沒反應。
  - 捲動位置扣掉 `--topbar-height`，否則會被 sticky 頂欄蓋住；系統開「減少動態效果」時直接跳過去不做平滑捲動。
- **測試方式**：縮圖那項的斷言改成讀 `COVER_THUMB_PX` 常數而不是寫死 320，之後再調解析度不用跟著改測試。新增三項——`undersized_thumbs_regenerate_once_only`（舊 320 縮圖會被重產成 480、原圖過小的不重產、第二輪不再觸發存檔）、`photo_wall_scrolls_into_view_on_toggle`、`photo_wall_stays_below_trophy_cabinet`（確認只是捲過去，沒有把牆搬到榮譽櫃上方）。另外實際截圖比對 320 與 480 的畫面差異。全站回歸 54/54 通過。

## v3.25.0

- **行事曆導覽列的「{年份} 年度回顧」按鈕移除**，動態回顧輪播（Story Mode）的入口改到指令面板（Cmd/Ctrl + K → `>` → 年度回顧相片輪播）。
  - **移除的理由是那一排的節奏被打斷**：導覽列從「‹ 年月 ›」「今天」接著兩顆等寬的圖示切換鈕，本來是一條愈往右愈緊湊的線；最後再掛一顆帶年份的長文字按鈕，寬度突然又拉開，而且它跟左邊那些「切換檢視」性質完全不同，被擺在一起只是因為那裡有空位。
  - **功能本身沒有刪掉**，只是換個收法——跟「生涯回顧」用的是同一套：平常不佔版面，需要時從指令面板叫出來。沒有任何「已完賽且有封面照片」的賽事時，這個指令不會出現在清單裡（判斷條件跟原本按鈕的顯示條件相同）。
  - **代價講明白**：指令面板只能用鍵盤開啟，所以這個功能現在在手機上沒有入口。如果要留手機入口，得另外找地方放（例如生涯數據區塊或資料管理選單），那是另一輪的事。
  - 日文與英文的按鈕文字鍵（`ui.storyModeButton`）已無人使用，換成指令面板的標籤。
  - **測試方式**：確認按鈕確實不存在於 DOM、導覽列截圖乾淨；指令面板輸入 `>回顧` 會同時列出「生涯回顧」與「年度回顧相片輪播」；實際執行後輪播全螢幕正常開啟。全站回歸 51/51 通過。

## v3.24.0

- **匯入運動紀錄檔改成一律覆蓋，包括成績時間**。這推翻了 v3.22.0 保留下來的規則（「已有時間就不覆蓋」），依使用者要求：檔案就是最終來源。
  - **舊規則的實際代價**：那是程式替使用者做的決定。一旦時間欄位先被填進錯的值，再怎麼匯入都蓋不掉，只能手動改；而畫面會同時混到舊的時間與新的距離，配速自然算錯——v3.22.0 那次回報的 8'56"/100m 就是這樣來的。當時只修可見性沒動規則，現在連規則一起改。
  - **保護機制改放在套用之前**：匯入預覽會把每一格「會變成什麼、原本是什麼」攤開來看——時間那一列現在也會用警示色標「會取代現有的 3:44:25」。不想覆蓋就按取消，決定權留在看得到差異的人手上，而不是由程式偷偷保留。
  - **「資料來源」欄位同樣改成覆蓋**：匯入之後這場的數據確實來自檔案，留著舊標註（例如手動輸入）會對不上。
  - **檔案沒有的欄位不會被清空**：「一律覆蓋」指的是有值才蓋，不是拿空白把手填的資料洗掉。只帶距離的 GPX 不會把既有的心率、時間清成空的。
  - **貼上 JSON 快速填入的行為本來就是覆蓋**，這次沒有變動；Excel 匯入走的是另一條路徑，也沒有動到。
  - **測試方式**：回歸測試新增兩項——`activity_import_overwrites_existing_time`（既有 3:44:25 會被檔案的 1:33:46 取代、距離同步更新、資料來源改寫）與 `activity_import_keeps_fields_absent_from_file`（檔案缺的欄位維持原值不被清空）。另外實際渲染匯入預覽確認時間那列的警示標示正確。三種語言的提示文字都改了，不再說「已有官方成績的欄位不會被覆蓋」。全站回歸 51/51 通過。

## v3.23.0

- **兩個外部套件（xlsx、idb-keyval）補上 SRI 完整性驗證，並鎖定確切版本**。這兩支 script 是全站唯一從外部載入、而且能直接執行任意程式碼的東西；CDN 若被入侵或在傳輸途中被換包，瀏覽器原本會照單全收地執行。加上 `integrity` 之後，檔案內容只要跟雜湊對不上就會被擋下來不執行。
  - **同時把 `idb-keyval@6` 改成 `idb-keyval@6.3.0`**。這一點比 SRI 本身更要緊：`@6` 是浮動範圍，jsDelivr 會解析到當下最新的 6.x。光是 v6 系列裡，`dist/umd.js` 的內容就已經換過三輪（6.2.3/6.2.4 一種、6.2.5/6.2.6 一種、6.3.0 又一種）。**SRI 配浮動版本等於埋定時炸彈**——上游哪天發新版，雜湊立刻對不上，套件被整個擋掉。要鎖完整性就必須先鎖版本。鎖定的 6.3.0 正是先前 `@6` 實際載入的版本，行為沒有改變。
  - **xlsx 從 cdnjs 換到 jsDelivr 的 npm 路徑**（`cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js`）。理由是**雜湊必須驗得出來才敢用**：jsDelivr 的 npm 路徑是逐位元組轉送 npm 套件原檔，雜湊可以直接從 npm 官方 tarball 算出來並比對；cdnjs 則是自行重新打包，沒辦法在本地確認算出來的值跟它實際送出的位元組一致。填一個沒驗證過的雜湊比不填更糟——不是「多一層保護」，是「Excel 匯入直接壞掉」。換過去之後兩支 script 同源，也順手加了 `preconnect`。
  - **失敗時的行為沒有變，本來就已經處理好**：idb-keyval 載不到會自動退回 `localStorage`；xlsx 載不到則會在按下匯入時跳出明確訊息，不會靜默失敗。
  - **測試方式**：回歸測試新增兩項（`external_scripts_have_sri`、`external_scripts_pinned_to_exact_version`），斷言所有外部 script 都帶 `sha384-` 雜湊與 `crossorigin="anonymous"`（少了後者瀏覽器拿不到內容，也就無從比對），且路徑上帶完整三段版號。並且用「把其中一支還原成無 SRI 的浮動版本」當反例驗證過這兩項真的會紅，不是永遠綠燈。全站回歸 49/49 通過。

## v3.22.0

- **匯入預覽會明確標示哪些欄位會被保留、哪些會被取代**。使用者回報匯入 FIT 後畫面顯示的成績跟檔案裡的不一樣：檔案是 2.51 公里／1:33:46，畫面卻是 3:44:25、配速 8'56"/100m。
  - **原因不是錯誤，是兩個欄位的覆蓋規則不一致**：時間有「已有值就不覆蓋」的保護（避免用手錶時間蓋掉官方晶片成績），而距離沒有、會直接被檔案蓋掉。結果就是畫面同時混到舊的時間與新的距離，除出來的配速自然是錯的（13465 秒 ÷ 25.1 = 536 秒，正好等於顯示的 8'56"/100m）。
  - 舊版的對話框只在上方用一句「已有官方成績的欄位不會被覆蓋」概括帶過，看不出這一次具體是哪一格、現有的值是多少。現在每一列都會標出來：時間下方顯示「保留現有 3:44:25，不會被覆蓋」（淡色），距離下方顯示「會取代現有的 3.3 公里」（警示色）。套用之前就看得到差異，不用套用後才發現數字對不上。
  - **刻意沒有改覆蓋規則本身**：時間不覆蓋是對的——官方晶片成績本來就比手錶按下停止的時間權威。距離會覆蓋也有道理——檔案量到的實際距離通常比手填的概略值準。真正的問題只是使用者看不見這個差異，所以修的是可見性，不是行為。
  - 三種語言都加上了對應文字。

## v3.21.1

- **新增賽事畫面的匯入按鈕改名並加上說明**：「匯入運動紀錄檔」改成「匯入完賽紀錄檔／賽道 GPX 檔」，下方加一行小字「支援 Garmin / COROS / Strava 等匯出的檔案，可吃 FIT、GPX、TCX」。
  - 新名稱把兩種實際用途都講出來了——匯入自己跑完的紀錄，或是匯入主辦單位公告的賽道 GPX（賽前規劃用）。原本的「運動紀錄檔」只涵蓋前者，看不出後者也能用。
  - 說明文字跟按鈕之間留了間距：貼太近會看起來像按鈕的一部分，也會跟下面的「匯入賽事主視覺」擠在一起分不出層次。
  - 三種語言都更新。賽後區塊那顆同樣功能的按鈕本來就已經帶著說明文字，這次沒有動。

## v3.21.0

- **路線數據抽屜重新設計**：13 個欄位拆成三組卡片、單位移進輸入框、路面比例加上堆疊長條。表單元件的改動是做在共用的欄位產生器上，所以整個 App 的抽屜都一起受惠。
  - **分組（基礎數據／賽道材質／賽事資訊與說明）**：13 個欄位平鋪在一起時，使用者得自己在腦中分辨「這是講高度的、那是講路面的」；分組之後每一塊的意義是自明的，也知道還差哪一區沒填。
  - **單位從標題移進輸入框右緣**，並換成國際符號（公里→km、公尺→m）。標題裡掛「(公尺)」會讓一整欄的標題長短不一、視線沒辦法對齊；而單位其實是「這格要填什麼格式」的提示，貼在輸入框上比放在標題上更接近它的用途。符號也比中文省空間、跟數字用的等寬字更搭。
  - **補充說明改放輸入框下方**：「ITRA / UTMB 點數（官方公告值，非自動計算）」的括號內容會把標題撐成兩行、擠掉真正的欄位名稱，現在標題只剩「ITRA / UTMB 點數」，說明用小字放在輸入框下面。
  - **對焦狀態加上光暈**：原本只有邊框換色，在深色底上對比不夠、不容易看出焦點在哪一格，現在多一圈淡光暈。
  - **路面比例堆疊長條**：柏油／土路／階梯三個數字本來就該加起來接近 100，但分開填的時候很難察覺自己填成了 125；畫成一條就一眼看得出來。**總和不是 100 時只標示出來、不自動修正**——那是使用者的資料，不是我們的。
  - **兩項建議沒有照做，理由如下**：
    - **配色**：原始建議指定亮橘 `#FF6B00` 或青藍 `#00F0FF` 當高亮色。這個 App 已經建立了一套顏色語彙——金色代表「PB／達成」、綠色代表「進行中的焦點」、橘紅代表「警示」——再加一個高彩度色會讓原本的語意失效，而且跟整體的土色系調性衝突。對焦光暈改用既有的主色。至於建議的背景／輸入框層級，這個 App 的深色主題本來就已經是分層的（背景 `#0D1013`、輸入框 `#1E232A`、半透明邊框），標籤與輸入框間距 6px、欄位間距 16px 也正好落在建議的範圍內，所以這部分不需要改。
    - **時間分段輸入框**：建議把「全程關門時間」改成 `HH:MM:SS` 三格分段輸入。沒有做——這個 App 的時間欄位（目標時間、關門時間、分段時間、各種成績）共用同一套解析邏輯，改成分段輸入會牽動全部；而且三格分段在手機上反而更難用（三個小點擊目標、還要處理跨格跳轉與退格），現有的單欄位已經接受多種寫法並自動正規化。如果你之後遇到實際填錯格式的情況，再針對驗證提示來改會更有效。
  - **測試方式**：確認三組卡片與 13 個欄位完整呈現、單位以 km／m／%% 顯示且不再出現在標題裡、ITRA 標題已清乾淨且說明在下方、堆疊長條比例正確（85/12/3）、總和 100 時不顯示警示而 125 時正確警示、欄位編輯仍正確自動儲存。另外確認共用欄位產生器的改動沒有影響其他抽屜（基本資料抽屜的 11 個欄位照常渲染）。全站回歸通過。

## v3.20.0

- **鞋款選擇移到「匯入運動紀錄檔」下方**。原本「比賽鞋款／本場穿著鞋款」收在「成績與數據」抽屜的最底下，要先點開抽屜、再滑到最後才看得到。
  - **理由是動線**：匯入完運動紀錄檔之後，接著要做的就是記下這場穿了哪雙鞋，兩件事屬於同一個流程，放在一起才順。
  - 自行車與游泳賽事照舊不顯示鞋款欄位（那兩項不記錄跑鞋）。
  - **測試方式**：確認鞋款區塊確實出現在匯入按鈕之後、不再出現在抽屜裡、下拉選擇鞋款仍然正確存檔、「管理鞋款」按鈕仍然打得開管理視窗、自行車賽事仍然不顯示這個區塊。全站回歸通過。

## v3.19.1

- **鐵人三項的兩次轉換改成標示 T1／T2**。原本兩列都寫「轉換區」，沒辦法一眼分辨在看哪一段——T1 是游泳上岸換自行車、T2 是下車換跑步，兩者的意義與檢討方向完全不同。
  - 編號在顯示時依照轉換區出現的順序算，不存進資料裡：分段本身只記錄「這是一次轉換」，第幾次是由它在整場賽事中的位置決定的，存起來反而會在分段被重新匯入時不同步。
  - 三種語言都適用（T1 轉換區／T1 トランジション／T1 Transition）。
  - **順帶確認了一件先前懸而未決的事**：使用者這次提供的畫面是真實 FIT 檔匯入的結果——游泳 1:43:38、T1 28:10、自行車 6:43:04、T2 18:01、跑步 5:25:39，五個分段全部正確解析。v3.11.0 的多項運動分段功能在實機檔案上確實可用，先前只有合成檔案的驗證結果到此得到證實。

## v3.19.0

- **游泳賽事改用游泳的單位與欄位**。使用者回報一場泳渡顯示「配速 89'25"/km、平均步頻 23 spm」，兩個數字都沒有意義。
  - **配速改成每 100 公尺**：游泳講的強度單位是「分'秒"/100m」，不是每公里。一場 2.5 公里的泳渡用 min/km 算出來是 89'25"/km——技術上沒錯，但沒有任何泳者這樣讀。賽後儀表板與分段配速表都改了。
  - **拿掉「平均步頻」**：錶在游泳時記的是划頻（每分鐘划手次數），跟跑步的步頻完全是兩回事，掛上「步頻」這個標籤只會誤導；而且這個欄位還附了一顆「用震動體驗節奏」的按鈕，對游泳毫無意義。
  - **順帶拿掉「總爬升」**：同一張截圖裡還有「總爬升 531 公尺」——開放水域的 GPS 高度本來就跳得很厲害，累加起來就會得到這種數字。水面是平的，這個欄位對游泳沒有意義，顯示出來只會讓人以為資料壞了。使用者沒有特別提到這一項，但它跟前兩項是同一類問題，一併處理；雷達圖的「爬升」維度也同步改成「無資料」，不然會出現「儀表板不顯示、雷達圖卻畫出一大塊爬升」的矛盾。
  - **分段表的 GAP 對游泳沒有意義**：坡度校正配速（GAP）是拿海拔變化去校正的，水裡沒有海拔，所以游泳的分段只顯示單一數字，表頭也不再寫「GAP · 實際」。
  - **共用格式化函式**：新增 `formatSwimPace()`，賽後儀表板、分段表、鐵人三項的游泳分段共用同一份。刻意跟 `formatPace()` 分開而不是加參數——兩者的輸入單位不同（每公里秒數 vs 每 100 公尺秒數），合在一起很容易在呼叫端傳錯。原本鐵人三項分段各自寫了一份相同的算式，一併收斂。
  - **測試方式**：確認游泳賽事顯示每 100m 配速、完全沒有步頻與爬升欄位、分段表也是每 100m 且沒有 GAP 提示；同時確認路跑賽事完全不受影響（照舊顯示每公里配速與步頻）。鐵人三項的分項成績（游泳每 100m、自行車 km/h、跑步每公里）重新驗證仍然正確。

## v3.18.0

- **修正跑步賽事的平均步頻少一半**：使用者回報一場越野賽顯示「86 spm」，而跑者實際的步頻應該落在 160～190。
  - **原因**：錶在 FIT 檔裡記的 `cadence` 欄位，跑步時是「單腳每分鐘踏步數」（跟自行車共用同一個欄位、同樣是 RPM 的語意），所以一場正常的跑步會記成 80～95。跑者口中的步頻是雙腳合計，剛好是兩倍。原本的程式把這個原始值直接當成 spm 顯示。
  - **不是看到跑步就乘二，有數值範圍的防呆**：有些錶（或先經過 Strava 之類平台轉檔的檔案）已經直接寫成雙腳合計，那種再乘二會變成 340 這種明顯荒謬的數字。只有落在單腳合理區間（30～110）的才換算。
  - **上限取 110 的理由**：單腳 110 等於雙腳 220 spm，已經超過菁英選手的水準、實際不會出現；而「總步頻 111 以上」是真實存在的數值，留在區間外才不會被誤乘。**但 110 以下仍有一段模糊地帶**——走路很多的超級馬拉松，雙腳合計可能就落在 100～110，這種情況光看數字無法分辨，會被誤乘。這個限制記錄在此，不假裝已經完全解決。
  - **鐵人三項按分段各自判斷**：跑步段換算、自行車段的踏頻（RPM）與游泳段的划頻都原封不動——分段本來就帶有自己的運動類型，正好可以區分。
  - **舊資料自動補正**：啟動時一次補正既有的賽事。因為換算有數值範圍限制，已經正確的資料不會被動到，**重複執行也不會再乘一次**（86→172，而 172 已超出區間），所以補正可以安全地重跑，不需要額外的「已處理」標記。
  - **測試方式**：逐一驗證路跑／越野／超馬會換算、已是雙腳合計的值不動、自行車／游泳／三鐵／二鐵／其他一律不動、null 不會出錯、區間邊界（30、110 內換算；29、111 外不換算）正確、同一筆資料重複執行結果不變。端到端測試涵蓋匯入路徑與舊資料補正，並確認三鐵的三個分段只有跑步段被換算。

## v3.17.2

- **修正 Android 上長按沒有震動的問題**。使用者回報在 Android 上感受不到震動。先確認程式有沒有真的呼叫——實測確認三段震動（10ms、30ms、解鎖時的雙重心跳）都正確發出，所以問題不在這一側，而是瀏覽器把呼叫擋掉了。
  - **根本原因是 Chrome 的限制，不是程式錯誤**：Chrome 要求震動必須有「黏性使用者啟用」（使用者得先在這個頁面上點過東西），而且 Chrome **明確不把 touchstart 算成使用者手勢**。所以「網頁載入後第一個動作就是長按 PB 數字」會被靜默擋下——`navigator.vibrate()` 回傳 `false`、完全不震，主控台只留一行 Intervention 訊息。若在長按之前已經點過畫面上任何東西，震動就會正常。
  - **修法是讓程式偵測失敗並自動降級**：所有震動改走統一的 `tryVibrate()`，檢查回傳值——被擋掉或裝置根本沒有這個 API 時，自動掛上 `no-haptics`，改用加強版的視覺回饋（更亮的光暈、金色描邊、底部進度條）。不管是 iOS（完全不支援）還是 Android（被擋掉），使用者都會得到看得出三個階段的回饋，而不是對著沒反應的畫面猜。震動真的成功時則會把標記移除，維持原本較克制的視覺。
  - 全站 12 個震動呼叫點（長按蓄力、滑動完賽、步頻體驗、徽章解鎖等）都改走同一個包裝。
  - **順帶修正版本紀錄的重複編號**：上一輪的表格高度調整誤用了已經存在的 v3.17.0，已改為 v3.17.1 並調整順序，這一筆為 v3.17.2。
  - **測試方式**：模擬三種情境各測一次——Android 但被 Chrome 擋下（回傳 false）、Android 正常震動、iOS 完全沒有 API。確認被擋下與沒有 API 時都會自動切換成加強視覺、解鎖功能照常運作；正常震動時三段模式都正確發出、不會誤掛降級標記。

## v3.17.1

- **表格改頭換面：分段表從 1.7 個螢幕縮到 0.45 個，並拿掉「試算表感」**。
  - **先量再改**：實際在手機尺寸量過，一列其實只有 34px、內距 6px，並不肥——真正的問題是**數量**。42 公里的賽事有 42 列，加起來 1,448px，整整 1.7 個手機螢幕全是表格，使用者得一路滑過去才看得到下面的內容。所以問題不在「格子大」，在「表太長」。
  - **改成固定高度內部捲動**：分段表限制在 380px（約 11 列），要看完整的就在表格內部滑，不必把整頁推長。表頭本來就是 sticky，捲動時欄位名稱不會消失。用 `max-height` 而不是寫死高度，分段少的賽事不會留一塊空白。捲動區底部加一道漸層提示「下面還有」，不然固定高度會讓人誤以為到底了。
  - **標題必須留在捲動區外面**：第一版把標題一起包進捲動區，結果一滑標題就跟著跑掉，使用者會失去「我正在看哪張表」的定位。改成捲動區只包表格本體。
  - **拿掉斑馬紋**：一列淺一列深是「這是一張試算表」最強烈的視覺訊號。全站六張表格（賽事清單、系列賽比較、鞋款分析、EPP 分段、匯入預覽、同步診斷）的斑馬底色全部移除，改成滑過該列才淡淡反白。
  - **用 5 公里的節奏取代斑馬紋的視線引導**：分段表每跨過一個 5K 整數就畫一條稍微明顯的分隔線。這比「一列淺一列深」更有意義——跑者本來就是以 5K 為單位在讀配速的。
  - **表頭改成標籤感**：字級再壓小、字距拉開、改成大寫字母，跟內容的數字拉開層次，讓視覺重心回到資料本身（配速數字與強度長條），而不是格線。
  - **測試方式**：確認捲動區高度固定在 380px、內部確實可以捲動、標題留在捲動區外；最重要的是確認**三維度游標聯動沒有壞**——海拔剖面圖、地圖標記與分段表列的互相高亮，那個功能的實作會在渲染時快取分段表的每一列，動到這張表的結構有風險，實測正反兩個方向都正常。全站回歸通過。

## v3.17.0

- **全站表格重新設計：拿掉 Excel 感、密度提高三倍**。使用者回報表格「長得像 Excel、呆板單調、每個格子都很大、一直往下滑」。查證後最嚴重的是分段配速表：每一列高達 130px（配速三行＋心率兩行），一場全馬 42 個分段要滑 5,500px。
  - **分段配速表重做**：配速壓成一行——GAP 粗體、實際配速淡色、中間用「·」隔開，不再每一列都寫「(GAP)」「(實際配速)」（那兩個標籤是整張表最長的字串、也是每列被撐到三行的主因），改成在表頭寫一次「GAP · 實際」。心率改成數字＋區間標籤並排，拿掉柱狀圖示（區間標籤本身就是不靠顏色的文字提示，色盲使用者一樣看得懂）。相對強度從「整格鋪滿深綠底」改成一條 3px 的細線——資訊完全一樣（越長越快），但不再壓在文字底下、不再讓整張表又重又花。**實測：42 個分段從約 5,500px 降到 1,449px，每列 34px，每個配速都是單行。**
  - **全站表格共用的質感層**：賽事清單、系列比較、鞋款分析、EPP 分段、同步診斷、匯入預覽六張表統一改成——分隔線淡到只剩暗示、偶數列鋪一層極淡底色當視線引導、表頭字級壓小拉開字距、去掉直線與每格外框。不動任何欄位寬度或內容，純粹把「格子感」拿掉。表頭黏在頂端，捲動時不會消失。
  - **海拔圖 ↔ 分段表的游標聯動確認沒有受影響**：聯動機制在渲染時快取每一列的 DOM 節點，重寫列的結構有可能讓它抓不到——實測正向（滑海拔圖高亮分段列）與反向（滑分段列顯示海拔提示）都正常。
  - **深色模式各自截圖確認**：心率區間顏色 Z3 綠、Z4 金、Z5 紅在深底上依然清楚，斑馬底色與分隔線用 color-mix 從主題變數推導，兩種模式自動對應。

## v3.16.0

- **封面圖縮圖分離，手機端清單與獎牌牆速度提升 2.5 倍**。上傳封面時額外產生一張 320px 的縮圖存在 `coverThumb`，月曆格子、清單、獎牌牆、焦點預覽這四個一次要畫幾十張圖的地方全部改用縮圖；詳情頁大圖、動態回顧、焦點編輯器這三個一次只畫一張的地方維持 760px 原圖。
  - **卡頓的根本原因**：封面是 base64 字串直接存在賽事物件裡，100 場賽事的封面加起來 8～9 MB 常駐記憶體，每次重畫月曆或獎牌牆都要重新解碼一百張 760px 的圖。在 4 倍 CPU 減速模擬中階手機的環境下實測：獎牌牆 503ms、搜尋每次觸發 483ms。
  - **實測改善**：獎牌牆 503 → 203ms、搜尋 483 → 196ms。縮圖約 17 KB，是原圖的四分之一。
  - **關於預估與實測的落差**：我原本預估能到 8 倍，實際是 2.5 倍。原因是測試環境用的是合成的假 base64（瀏覽器解碼會失敗、不會真的耗時），所以量到的主要是「組 100 張卡片的 HTML 字串」的成本，而不是圖片解碼的成本。**真實照片的解碼成本會比這個大很多，實機上的改善幅度應該高於 2.5 倍**——但我沒有實機數據，所以版本紀錄裡只寫我實際量到的數字。
  - **舊資料自動補產生**：功能推出前存的賽事只有大圖沒縮圖，啟動後會在背景逐一補產生——一次做一張、每張之間讓出主執行緒，不會讓開啟 App 的瞬間卡住；全部做完才存檔一次。在補完之前，小圖會退回用大圖顯示，畫面永遠不會因為缺縮圖而空白。
  - **320px 而不是更小**：獎牌牆的卡片在桌機上接近 300px 寬，Retina 螢幕再小就會糊；月曆格子那種 24px 的地方用 320px 也只是多花一點點，不值得為它再多維護一個尺寸。
  - **縮圖也會跟著雲端同步**：17 KB 一份，換裝置登入不用重新產生。移除封面時大圖與縮圖一起清除；複製賽事時兩者一起清空。
  - **測試方式**：用真實的 1200×800 JPEG 走完整上傳流程，確認產出 760px 大圖與 320px 縮圖各一張、縮圖確實落在 320px；確認獎牌牆用縮圖、詳情頁用大圖；確認舊資料的背景補產生會正確產出縮圖並存檔；確認缺縮圖時退回大圖、移除封面時兩者都清掉。上一版修好的三個 XSS 路徑重新驗證仍然擋住（縮圖同樣經過 safeUrl）。全站回歸通過。

## v3.15.1

- **改為非開源專案**：授權從 PolyForm Noncommercial 改為「版權所有，保留一切權利」。原始碼、文件與素材未經書面同意不得重製、修改、散布或再授權；網站本身仍開放作為個人賽事紀錄工具使用，但這個使用許可不包含對原始碼的任何權利。
  - 更新的位置：`LICENSE` 全文改寫（中英雙語）、README 授權段落與「部署方式」（原本是寫給第三方 Fork 架站的教學，改成作者自己的部署備忘）、網站說明面板裡的授權文字、功能介紹頁三處 meta 描述與一張「免費・開源・無廣告」的特色卡片（改為「免費・無廣告・不需要帳號」）、網站地圖頁的頁面描述。
  - **刻意沒改的**：README 與 USAGE 裡提到「開源套件 idb-keyval」、以及 `3-開源資料來源.md`——那些描述的是這個專案**使用**的第三方套件與公開資料來源，是事實陳述，跟這個專案本身的授權無關。
  - **重要提醒：授權條款只是法律層面的宣告。** 如果 GitHub repo 是公開的，不管 LICENSE 怎麼寫，任何人都看得到完整原始碼。真正做到「不開源」需要把 repo 設為私有——但 GitHub Pages 從私有 repo 部署需要付費方案（GitHub Pro 或 Team）。這件事需要作者自行決定。

## v3.15.0

- **資安檢視：修正三個確認可被利用的 XSS 漏洞**。這次是針對整個程式碼做靜態審查加上實際攻擊測試，不是只靠肉眼看——每一個都先寫出攻擊載荷、確認真的會執行，修完再確認同一個載荷變成無害的文字。
  - **威脅模型先講清楚**：Firestore 規則限制每個人只能讀寫自己的資料，所以別人**無法**直接把惡意內容塞進你的帳號。這三個漏洞的實際攻擊路徑是「別人給你一份 JSON 備份檔叫你匯入」——匯入時檔案裡的欄位會原封不動進到資料庫，之後畫面渲染到那筆資料時就會執行對方的程式碼。對一個個人工具來說這不是最高風險的類別，但確實存在，而且修法很單純。
  - **漏洞一：鞋款壽命警示的 innerHTML 沒有跳脫**。鞋款名稱直接串進 `<strong>${alertMsg}</strong>`，名稱裡放 `<img onerror=…>` 就會執行。全站 95 處 `escapeHtml` 呼叫，這是唯一漏掉的一處。
  - **漏洞二：`javascript:` 網址**。多媒體連結的 URL 有經過 `escapeHtml`，但那只能防止跳出屬性，擋不住 `javascript:alert(1)` 這種本身就是可執行內容的 scheme——點連結就執行。
  - **漏洞三：封面圖片的屬性跳脫**。`coverImage` 欄位有 8 處直接串進 `src="…"` 與 `style="background-image:url(…)"` 完全沒跳脫，因為正常使用下它一定是程式從上傳的檔案產生的 base64，使用者打不出來——但匯入 JSON 就可以塞 `x" onerror="…`。
  - **統一的修法**：新增 `safeUrl()`，只放行 `http(s)://`、`data:image/{png,jpeg,gif,webp,avif};base64,` 與站內相對路徑，其餘一律換成空字串，並套用到所有使用者可控的 href 與 src。正常的網址、正常的照片 base64 完全不受影響；測試涵蓋大小寫混合的 `JaVaScRiPt:`、`data:text/html`、`data:image/svg+xml`（SVG 可以內嵌 script）、`vbscript:` 與屬性跳脫，全部擋下。
  - **審查過程中確認安全的部分**：賽事名稱、地點、心得、裝備、補給等所有文字欄位都有正確跳脫（拿 `<img onerror>` 當賽事名稱實測不會執行）；`officialUrl` 只出現在 `<input type="url">` 裡、從不渲染成可點的連結；沒有 `eval`、`new Function`、`document.write`。
  - **未修、但應該知道的兩項**：（1）兩個 CDN 載入的第三方套件（xlsx、idb-keyval）沒有 Subresource Integrity 雜湊——CDN 被入侵時攻擊者可以替換腳本。我沒有加，因為這個沙盒環境連不到 CDN、拿不到真實的檔案雜湊，**憑空寫一個錯的雜湊會讓套件完全載不進來**，比沒有 SRI 更糟。建議自己到 srihash.org 產生後補上。（2）沒有 Content-Security-Policy——但這個 App 的 JavaScript 全部是行內的，任何有意義的 CSP 都必須開 `'unsafe-inline'`，防護效果所剩無幾；真正要做需要把程式碼拆到獨立 .js 檔，是一個架構層級的改動。
  - **Firestore 規則的一個設計決定**：`analytics/featureCounts` 開放給未登入者寫入，目的是統計所有訪客的功能點擊。這代表任何人都可以把那份文件的數字寫成任意值——它只是使用行為統計、不含個資，被竄改的後果只是統計失真，可接受。但如果哪天要拿這份數字做決策，要記得它不可信。

## v3.14.2

- **修正 v3.14.0 把圖示外部化時產生的路徑錯誤**：`manifest.webmanifest` 裡的圖示路徑寫成 `icons/icon-192.png`，但這份 manifest 本身就放在 `icons/` 資料夾裡，而 **manifest 裡的路徑是相對於 manifest 自己的位置解析的，不是相對於網頁**——實際會被解讀成 `icons/icons/icon-192.png`，檔案根本不存在。結果是網站圖示正常（那是 index.html 直接引用的），但「加入主畫面」時會抓不到圖示。
  - 路徑改成不帶資料夾前綴的檔名，並且用本機 HTTP 伺服器實際跑過一次驗證：manifest 能正確解析、四個圖示項目全部回傳 200。
  - **這個錯誤在 file:// 的測試環境下看不出來**（瀏覽器會擋住對 manifest 的 fetch），必須用真正的 HTTP 服務才測得到——上一版就是因為只驗證了「index.html 引用的圖示有載入」而漏掉這一項。

## v3.14.1

- **清掉無用的翻譯鍵、補上非同步動作的錯誤回饋**。兩項都是上一輪全面檢視時列出、但不在當時執行範圍內的整理。
  - **39 個「無用」翻譯鍵，實際只有 17 個真的能刪**：檢查時發現其中 22 個是**用字串串接動態組出來的**——分項成績的運動類型（`'ui.leg_'+leg.sport`）、徽章分類（`'ui.trophyCat_'+category`）、星期標籤（`'ui.weekday.'+i`）、運動類別標題，還有範例資料按鈕會在兩個鍵之間切換的 `ui.clearSampleData`。這些鍵在原始碼裡永遠不會以完整字串出現，**單純用「有沒有被引用」去搜尋一定會誤判成無用，刪掉就會讓畫面顯示出原始鍵名**。只刪掉確認過在日文與英文各定義一次、完全沒有任何引用的 17 個，刪完在三種語言下逐一驗證動態組出來的標籤都還能正確翻譯。
  - **16 個缺少錯誤處理的非同步函式，沒有一律加上 try/catch**：其中 `saveJson` 與 `loadJson` **必須繼續往外拋出例外**——它們的呼叫端（`persist` 等）正是靠接到例外才會跳出「儲存空間可能已滿」的警示，在函式內部把錯誤吞掉，反而會讓上一版剛做好的警示永遠不觸發。另外 `parseActivityFile` 的呼叫端本來就有完整的錯誤處理。
  - **改成在「沒有人接手錯誤」的呼叫點包起來**：分享圖、配速手環、疲勞模式、拖放匯入這四個進入點，失敗時原本只會變成未處理的 Promise 拒絕——主控台有訊息，但使用者按了按鈕卻什麼都沒發生，無從判斷是還在跑還是壞了。現在會跳出明確的提示。
  - **再加一道全域防線**：逐一包住的只是已知的進入點，難保沒有漏網的，所以全域攔截未處理的 Promise 拒絕，至少讓使用者知道「剛剛那個動作沒成功」。
  - **測試方式**：最重要的一項是確認上一版的儲存空間警示**沒有因為這次改動而失效**（模擬寫入失敗，確認警示仍然跳出）；另外確認失敗的動作會跳提示、成功的動作不會多跳東西；翻譯鍵刪除後在繁中、日文、英文三種語言下確認動態組出的標籤（分項運動、徽章分類、星期）都還能正確顯示，沒有任何一個漏出原始鍵名。

## v3.14.0

- **全面檢視後的四項優化**：儲存空間警示、圖示外部化、無障礙標籤、版本紀錄封存。這一版沒有新功能，全部都是把既有的東西做得更穩。
  - **儲存寫入失敗現在一定會通知**：查證發現只有「賽事」的儲存失敗會跳警示橫幅，鞋款、裝備範本、個人資料、徽章、補給品資料庫、同步覆蓋紀錄這六種**失敗時只在主控台印一行訊息，畫面上完全沒有提示**——使用者會以為存好了，實際上沒有。六個都補上警示。封面照片是以 base64 存進瀏覽器的，照片一多就會逼近空間上限，這不是假想的風險。
  - **同步診斷新增儲存空間用量**：顯示「已使用 X MB / 約 Y MB」，超過 80% 會轉成警示色。等到寫入失敗才發現已經太晚，這讓問題可以被提前看到。瀏覽器不支援用量查詢時整行不顯示，不放空欄位讓人困惑。
  - **把所有內嵌圖示改成外部檔案**：`index.html` 裡有 115,036 個字元是 base64 圖片（PWA 圖示四種尺寸、Apple 觸控圖示、說明按鈕的頭像），佔整個檔案的 12.6%，而且每次開啟網頁都要全部下載，即使瀏覽器只用得到其中一個尺寸。改成 `icons/` 底下的獨立檔案並加上 `manifest.webmanifest` 之後，**index.html 從 1024 KB 降到 913 KB**，圖示也能被瀏覽器單獨快取。相同的圖片位元組會自動去重複，不會存兩份。
  - **補上圖示按鈕的無障礙名稱**：全站 157 個按鈕裡，絕大多數本來就有可見文字（那本身就是無障礙名稱），真正有問題的是只有符號的五顆——刪除列的 ×、步頻體驗的 👋、鞋款編輯的 ✎。它們原本只有 `title`，而 `title` 在部分螢幕閱讀器上並不可靠，現在都補上 `aria-label`。
  - **版本紀錄封存**：CHANGELOG.md 已經累積到 307 KB、162 個版本，單一檔案繼續長下去難以閱讀、在 GitHub 上也難以瀏覽。v1.0.0 – v2.99.0 移到 CHANGELOG-archive.md，主檔只留 v3.x，**從 307 KB 降到 45 KB**。
  - **需要注意的部署變更**：圖示現在是獨立檔案，**部署時 `icons/` 這個資料夾必須跟 index.html 一起上傳**，否則圖示與「加入主畫面」會失效。這是這一版唯一需要留意的事。
  - **測試方式**：模擬六種儲存各自失敗，確認六個都會跳出警示；確認儲存用量會正確顯示；確認外部化後的圖示與 manifest 都能正確載入、說明按鈕的頭像有成功顯示；確認剩餘的未標記按鈕只有一個純裝飾用的箭頭（旁邊本來就有文字）。全站回歸測試通過。

## v3.13.0

- **iOS 風格的空間景深**：打開彈出視窗、抽屜面板或指令控制台時，主內容會往後縮一點並失焦（縮放 0.96、模糊 4px、圓角 12px），用 Apple 系統介面那條帶物理重量感的貝茲曲線過場，視覺焦點完全交給前景。
  - **套用對象刻意選 `#main-content`，不是 `#app` 或 `body`**：`filter` 與 `transform` 會讓元素變成 `position:fixed` 子孫的「包含區塊」——固定定位的東西會從「相對視窗固定」變成「相對這個容器固定」，位置整個跑掉。動手前先確認了這個 App 的兩顆懸浮按鈕（使用說明、新增賽事）都在 `</main>` 之後、`#main-content` 之外，標題列與快速導覽列是 sticky 也都在 main 之外，所以套在 `#main-content` 上完全不會影響它們。測試特地在效果作用中量測懸浮按鈕的座標，確認位移為零。
  - **結構本來就已經到位**：原始構想提到「必須有一個包含所有主內容的容器，以及獨立在外的彈出層容器」——這個 App 的 `<main id="main-content">` 與十幾個彈出層本來就是這樣分開的，不需要重組 DOM。
  - **用 MutationObserver 監看彈出層的 hidden 屬性，而不是逐一去每個開關函式裡加一行**：這個 App 的彈出層有十幾個，開關的進入點散在按鈕、指令控制台、鍵盤快捷鍵、拖放等很多地方，逐一掛鉤只要漏掉一個，就會出現「視窗關了但背景還糊著」這種很難查的殘留狀態。監看 DOM 實際狀態就不會有漏網之魚，也自然支援多層疊加（關掉上層時只要還有其他層開著，背景就維持退後狀態）。
  - **全螢幕不透明的彈出層刻意不列入**（動態回顧輪播、生涯回顧主卡、疲勞模式）：它們本來就完全蓋住畫面，底下再做模糊只是白做工。
  - **尊重「減少動態效果」**：開啟該設定時不做位移與模糊，改成單純把主內容淡化到 45%。完全不做任何區別的話，使用者會失去「主畫面現在不是操作對象」這個重要提示，所以保留語意、只換表現方式。
  - **測試方式**：驗證彈出視窗、抽屜、指令控制台三種都會正確觸發與解除；兩層疊加時關掉其中一層不會提早解除、全部關閉才清除；全螢幕彈出層確實被排除；最重要的是確認懸浮按鈕在效果作用期間位置完全沒有偏移（這是這個做法最容易踩的坑）。另外確認「減少動態效果」模式下 transform 與 filter 都是 none、改用透明度呈現。

## v3.12.0

- **賽事能力雷達圖**：賽後成績新增一張五邊形雷達圖，把距離、爬升、氣溫嚴苛度、高心率負荷、配速穩定度五個維度正規化到 0~1 畫成多邊形，一眼看出這場賽事的「形狀」。越野賽會往爬升與氣溫膨脹，路跑馬拉松則往穩定度與心率突起。用原生 Canvas 繪製，沒有引入任何圖表套件。
  - **原始構想是六維，實際做成五維**：被拿掉的是「配速表現（相對 PB 的比例）」。這個 App 只存全馬一項 PB，拿它去除越野 43K 或 113 鐵人的完賽時間會得出完全沒有意義的數字。寧可少一個維度，也不要放一個看起來精確、底下卻是硬湊的數值。
  - **高心率負荷改用「分段中落在 Z4/Z5 的比例」**：原始構想寫的是 Z4/Z5 的時間佔比，但這個 App 沒有存「在各心率區間各待了多久」的資料，只有整場平均心率與每個分段的平均心率。用分段當取樣單位是現有資料能做到的最接近版本，畫面上也直接顯示「7/20 段」這種原始分子分母，讓人看得出這個數字是怎麼來的，不會誤以為是精確的時間統計。
  - **缺資料的維度回傳 null，不是 0**：0 在雷達圖上看起來像「這場很輕鬆」，跟「沒有這筆資料」是完全不同的兩件事。少於三個維度有資料時整張圖不顯示（畫出來只會誤導），有缺的維度則在圖下方用一行小字說明哪幾項沒有資料。
  - **順手修掉原始構想程式碼裡的一個錯誤**：背景網格那段把 `y` 座標寫成了 `const x`，同一個區塊裡重複宣告同名常數會直接拋出例外，整張圖畫不出來。
  - **其他實作細節**：依裝置像素比放大畫布解析度（不然高解析度螢幕上線條會糊）；顏色從 CSS 變數讀取，深色與淺色模式都正確；生長動畫用 ease-out 曲線在 1 秒內從中心展開，系統開啟「減少動態效果」時直接畫完成狀態；重畫詳情頁時舊的動畫迴圈會自動停止，不會殘留。
  - **測試方式**：五個維度的正規化逐一驗證——50 公里對應 0.5、150 公里正確封頂在 1、爬升 1500 公尺對應 0.5、氣溫 15°C 為 0 且 5°C 與 25°C 對稱皆為 0.5、35°C 封頂、四個分段中兩段在 Z4/Z5 得 0.5、配速完全一致得 1、配速劇烈變動得 0。另外確認沒有資料的維度回傳 null 而非 0、資料太少時整張圖不渲染。繪圖層實際讀取畫布像素確認不是空白（畫了五萬多個像素）、解析度有依裝置像素比放大。深色與淺色模式各自截圖確認。

## v3.11.1

- **匯入 FIT 檔時回報分段偵測結果**。使用者回報升級後「什麼都沒有變」，但從截圖看得出來全場平均配速還在顯示——而多項運動的情況下那個數字應該要被隱藏，代表分項資料是空的。問題在於**分項成績只會在匯入當下產生，已經存在的賽事不會自動補上**，而這件事在畫面上完全沒有任何提示。
  - **現在匯入 FIT 檔一定會跳出結果**：偵測到分段時顯示「已偵測到 N 段分項成績」，沒有偵測到時顯示「這個檔案沒有多項運動分段資料（找到 N 筆 session），只會顯示整場成績」。有了這個回饋，分項成績沒出現時就能立刻分辨是哪一種情況——完全沒有跳訊息代表用的還是舊版、跳了「沒有分段資料」代表檔案本身沒有這種結構、跳了「已偵測到 N 段」但畫面沒變才是真的有問題。
  - **順帶查證了實作是否符合規格**：Garmin 官方文件明確說明轉換區會被寫成 sport 為 transition 的獨立 session，而且建議的做法就是「依 session 的起始時間與總時長去切分 record 訊息」——跟這一版的實作一致。session 訊息的欄位編號（起始時間、運動類型、總時長、總距離）也對照過規格確認無誤。
  - **GPX／TCX 匯入不會誤跳這個訊息**：偵測結果只在 FIT 解析路徑產生，其他格式不會有這個欄位。
  - **測試方式**：分別用合成的多項運動檔（三筆 session）與單項運動檔（一筆 session）驗證——前者正確產生三段分項並顯示「已偵測到 3 段」，後者不產生分項並顯示「沒有多項運動分段資料（找到 1 筆 session）」。

## v3.11.0

- **鐵人三項／二鐵分項成績**：匯入多項運動的 FIT 檔之後，賽後成績會依游泳／T1／自行車／T2／跑步分段顯示，每一段各自有距離、時間、心率、步頻，並且用該項運動自己的慣用單位表示強度。
  - **起因是一張截圖**：使用者的鐵人三項賽事顯示「平均配速 3'52"/km、平均步頻 44 spm」，而分段表裡前幾公里是 25'/km、後面變成 2'44"/km。那不是資料有誤，是把游泳、騎車、跑步的時間與距離全部混在一起平均出來的結果——數字本身毫無意義。
  - **分段來源是手錶寫的，不是猜的**：多項運動的 FIT 檔會為每一段賽程各寫一筆 session 訊息，裡面帶有運動類型、起始時間、總時長、總距離。原本的解析器只讀了 record（每秒的 GPS／心率／步頻取樣點）這一種訊息，session 整個被丟掉——資訊其實一直在檔案裡。這一版補上 session 的解析。用配速去猜「這段應該是游泳還是騎車」的做法刻意沒有採用：遇到爬坡慢騎或轉換區小跑就會判斷錯誤。
  - **轉換區（T1／T2）也保留成獨立一段**：FIT 規格裡轉換本身就是一種運動類型（sport=3），所以 T1／T2 各花多久是直接讀出來的，不用從時間差回推。
  - **每一段用自己的單位**：游泳顯示每 100 公尺配速、自行車顯示時速、跑步顯示每公里配速。全部統一成 min/km 技術上沒錯，但實際上沒有人這樣讀游泳或騎車的強度——這正是原本畫面看起來荒謬的原因之一。轉換區沒有配速可言，只顯示時間。
  - **多項運動不再顯示全場平均配速、平均心率、平均步頻**：那三個數字在鐵人三項的情境下都是把不同運動混在一起算出來的，留著只會誤導。單項賽事完全不受影響，照舊顯示。
  - **每一段的心率與步頻是從實際取樣點自己算的**，不是直接讀 session 裡的平均值欄位——不是每支錶都會寫那些欄位。取樣點依時間戳分配到對應的分段區間（距離沒辦法用，轉換區會歸零、重疊）。區間取「含起點、不含終點」：分段是連續銜接的，兩端都含的話正好落在交界的取樣點會同時算進兩段，讓交界附近的平均值互相污染——這個問題是在合成測試檔驗證時，發現游泳段心率算出 128 而不是預期的 130 才抓到的。
  - **測試方式**：手工組出一個符合 FIT 二進位規格的合成多項運動檔案（三筆 session：游泳／轉換／自行車，加上 40 個取樣點），驗證三筆 session 都正確解析出運動類型、時長、距離，且每一段的心率與步頻完全對得上該段時間窗內的取樣值（游泳 130／30、轉換 110／0、自行車 150／85，沒有互相污染）。配速單位分別驗證為 1'35"/100m、null（轉換區）、36.0 km/h。UI 層確認單項賽事完全不顯示分項區塊、儀表板照舊；多項賽事正確顯示五段、轉換區樣式淡化、窄螢幕版面不會折行折得難看。
  - **尚待實機驗證**：這次是用合成檔案測的，我手上沒有真實的多項運動 FIT 檔。**請用你的澄清湖或 113 的原始檔重新匯入一次確認**——如果分段沒出來或數字不對，把情況跟我說，很可能是某些錶廠寫 session 的方式跟規格預設不同（例如把每段寫成多筆 lap 而不是 session）。
  - **只有 FIT 有這個資訊**：GPX 與 TCX 格式不帶多項運動的分段資料，用那兩種格式匯入的賽事不會有分項成績。

## v3.10.1

- **「同步覆蓋紀錄」也加上一鍵移除全部**，跟上一版的「清空全部」同一套兩段式確認：第一次點只換成「確定移除？再按一次」，5 秒內沒再按就自動解除，關閉畫面或改去點某一筆的「還原此版本」也會解除。
  - **兩顆按鈕的確認狀態各自獨立**：先按了上面的「清空全部」進入待確認，再去按下面的「移除全部」，不會因為共用狀態而誤觸；反之亦然。這一點特別寫了測試確認，因為兩段式確認最容易出的錯就是狀態共用。
  - **只清覆蓋紀錄，不動賽事**：移除之後賽事本身維持目前那個較新的版本，垃圾桶裡的、現役的都原封不動。測試驗證了清空四筆覆蓋紀錄之後，三筆現役與兩筆垃圾桶賽事完全沒變。
  - **要留意的是這個動作的實質意義**：移除覆蓋紀錄不會傷到現在的資料，但被覆蓋的那些舊版本就再也還原不回來了，所以一樣配了確認步驟，而不是點一下就清掉。
  - **測試方式**：驗證第一次點擊不會真的移除、第二次才生效、清空後按鈕自動消失、清空結果重新整理後依然保持（確認有寫進儲存空間）。三種語言的按鈕文字都比對過。

## v3.10.0

- **資料復原畫面新增「清空全部」**，垃圾桶裡的賽事可以一次永久刪除，不用一筆一筆點。
  - **沿用這個 App 既有的「按兩次才生效」模式**（跟「刪除範本」那顆按鈕一樣），不跳系統對話框。第一次點只把按鈕文字換成「確定清空？再按一次」，**5 秒內沒有再按就自動解除**——避免使用者按了第一次之後去做別的事，回來時按鈕還停在「已解除保險」的狀態，一碰就全部清光。
  - **只刪垃圾桶裡的**：條件是「有刪除標記」的賽事，沒刪除的一律不動。測試建了三筆現役＋四筆垃圾桶的賽事，清空後三筆現役完好無損。
  - **測試過程抓到一個真的會造成誤刪的狀況**：按下第一次「清空全部」之後、還沒確認之前，如果改去點某一筆的「還原」，按鈕會停在已解除保險的狀態——接下來只要再點一次「清空全部」就會立刻全部刪掉，少了一層保護。現在還原任何一筆都會一併解除確認狀態，因為使用者改去還原就代表意圖已經變了。關閉畫面時也會解除。
  - **順手修掉手機版的名稱被擠成一個字的問題**：這一列原本是「名稱＋刪除日期＋還原＋永久刪除」全部擠在同一行，名稱雖然設了彈性寬度，但其餘三個元素都不換行，在手機上名稱會被壓到只剩一個字加省略號（截圖裡看到的「T‥」「瘋‥」「第‥」就是這樣來的），完全看不出是哪一場賽事——而這正是決定要不要還原時唯一的判斷依據。現在窄螢幕改成上下兩段：名稱獨佔一行完整顯示，日期與兩顆按鈕排在下一行。

## v3.9.0

- **個人資料、裝備範本、徽章解鎖紀錄納入雲端同步**。上一版把「這三種其實沒同步」誠實標示出來，這一版把它們真的補上；現在六種資料全部都會跟著 Google 帳號走，換裝置登入就能拿回來。
  - **存在既有的 `meta/globalLists` 文件裡，不另開路徑**：安全性規則本來就涵蓋 `users/{uid}/meta/{docId}`，不用動規則；三者資料量都很小；而且一次寫入全部到齊，不會出現「範本寫成功、徽章寫失敗」這種各自半套的狀態。
  - **合併規則一律「本機優先」，永遠不覆蓋你在這台裝置上輸入的東西**：
    - 裝備範本比照鞋款與補給品，依 id 聯集，本機已有的那筆原封不動。
    - 徽章解鎖兩邊都有同一枚時，保留**比較早**的解鎖時間——徽章記錄的是「什麼時候第一次達成」，在另一台裝置上重新判定出來的較晚時間不該蓋掉真正達成的那一刻；同時保留本機的「已讀」狀態，不會讓看過的徽章又跳一次開箱動畫。
    - 個人資料是單一份設定、沒辦法像清單那樣聯集，所以做成**逐欄位補空**：本機這個欄位是空的才從雲端拿，已經填過的一律保留。換新裝置（整份空白）時等同於把雲端那份完整帶過來，而在已經有資料的裝置上登入，則絕對不會把手動輸入的體重或心率換成別台的舊值。
  - **三種資料在本機變動時也會推上雲端**：沿用既有那套防連續觸發的延遲推送機制，不是每改一個欄位就發一次請求。
  - **修掉上一版自己寫錯的一個計數**：同步診斷顯示「個人資料填了幾欄」時，我把心率欄位當成平鋪在最外層（`restingHr` / `maxHr`），但實際結構是巢狀在 `hr.running` / `hr.cycling` / `hr.swimming` 底下的三組。舊寫法會少算所有心率欄位。現在抽成一個共用的計數函式，同步診斷跟合併判斷共用同一份，不會再出現兩邊對結構的認知不一致。
  - **測試方式**：合併邏輯分五種情境各自驗證——全新裝置（本機全空）能完整拉回雲端的五種資料；本機已有值時體重與最大心率**確實沒被雲端的不同數值覆蓋**，而空著的身高與安靜心率正確從雲端補上；範本依 id 聯集且同 id 保留本機版本；徽章正確保留較早的解鎖時間、較晚的不覆蓋、新的補進來、已讀狀態保留；傳入 null、空物件、型別完全錯誤的雲端資料都不會讓程式崩潰。另外做了完整的跨裝置往返測試：裝置 A 填好資料推上去，裝置 B 清空本機後拉回來，確認體重、心率、範本名稱、徽章、鞋款全部正確還原。同步診斷的六列也確認都顯示實際雲端筆數。

## v3.8.0

- **同步診斷改成完整的「本機 vs 雲端」對照表**，涵蓋全部六種資料，換裝置登入後可以一眼確認到底哪些帶過來了、哪些沒有。
  - **查證過程中發現一件必須誠實說明的事：有三種資料其實從來沒有同步到雲端**。這個 App 實際推上 Firestore 的只有賽事紀錄（`users/{uid}/races/`）、鞋款與補給品資料庫（`meta/globalLists`）三種；**個人資料（體重、安靜心率、最大心率、心率區間）、裝備範本、徽章解鎖紀錄這三種只存在本機**，程式裡完全沒有對應的雲端寫入。舊版診斷畫面只顯示鞋款與補給品兩個數字，看起來一切正常，反而會讓人誤以為登入之後所有東西都會跟著過來。
  - **所以這張表刻意把「不同步」標出來，而不是假裝都有**：六種資料各自列出本機筆數，會同步的顯示雲端筆數，不會同步的直接標示「不同步」，下方再用一句話說明這些項目換裝置不會自動帶過來、要用「資料管理 → 匯出 JSON」手動搬移。
  - **兩邊數字對不上的那一列會用警示色標出來**——這正是打開這個畫面最想知道的事，不用自己逐行比對。數字相同的列維持一般樣式，不會製造多餘的視覺雜訊。
  - **賽事紀錄的雲端筆數是這次新查的**：原本只查 `meta/globalLists`（鞋款＋補給品），現在同時查 races 集合，兩邊平行發出請求。本機筆數只計算未刪除的，垃圾桶裡的數量另外用小字附註，避免「本機 13 筆、雲端 12 筆」這種看起來像同步失敗、其實只是垃圾桶沒算進去的誤會。
  - **測試方式**：建立六種資料各若干筆，確認表格六列數字都正確、三個「不同步」標示都在、垃圾桶附註正確顯示。接著模擬一個「雲端數字跟本機不一致」的查詢結果，確認賽事與鞋款那兩列被標成警示色、數量相同的補給品那列維持正常、三個不同步項目不會被誤判成不一致。三種語言的表頭與項目名稱都比對過。
  - **下一步（尚未做）**：把個人資料、裝備範本、徽章解鎖也納入雲端同步。那會動到 Firestore 的資料結構與登入時的合併邏輯（要處理「兩台裝置各自有一份、登入時該用哪一份」），屬於獨立一輪的工作，這次先把現況誠實呈現出來。

## v3.7.0

- **「範例資料」變成一個切換：再按一次就清除**。原本已經匯入範例資料之後再按，只會跳到第一場範例賽事；現在會把五筆範例賽事一次清掉，按鈕文字也跟著在「範例資料／清除範例資料」之間切換，不用靠記憶去猜現在按下去會發生什麼事。
  - **絕對不會動到自己的紀錄**：清除時只刪掉 id 落在一份固定清單（五個 `example-` 開頭的固定字串）裡的賽事。使用者自己建立的賽事 id 是隨機產生的，永遠不可能落在這份清單裡，所以不存在誤刪的可能。測試特地建了三筆自己的賽事、外加一筆已經丟進垃圾桶的，跑完三輪「匯入→清除→再匯入→再清除」之後全部完好無損，垃圾桶裡那筆也還在垃圾桶裡。
  - **範例資料用真刪除，不進垃圾桶**：其他賽事刪除都是軟刪除（保留 30 天可復原），但範例資料是隨時可以一鍵再匯入的示範內容，把五筆範例丟進「資料復原」只會把真正需要救回的賽事洗掉。
  - **順手修掉一個潛在的重複資料問題**：判斷「目前有沒有範例資料」原本會排除已經軟刪除的那些，所以如果使用者自己把範例賽事刪掉（進垃圾桶）之後再按一次按鈕，會再匯入一組 id 完全相同的範例，資料裡就出現重複 id。現在清除會連垃圾桶裡的範例一起清掉，重新匯入時不會撞號。
  - **按鈕文字的多語言處理有個容易踩到的細節**：這個 App 的翻譯機制會在第一次執行時，把元素當下的繁體中文原文快取起來當作 fallback。切換按鈕的翻譯 key 時如果沒有同步更新那份快取，中文介面就會顯示上一個狀態留下來的舊字串。三種語言都實測過切換前後的文字，以及來回切語言之後不會殘留。
  - **測試方式**：驗證按鈕文字在匯入前後正確切換、清除後只有範例消失、自己的賽事（含垃圾桶裡的）完全不受影響、清除後再匯入不會產生重複 id、被軟刪除的範例也會一併清掉。另外重新整理頁面兩次，確認清除是真的寫進儲存空間、按鈕文字也會依照實際狀態正確顯示。

## v3.6.1

- **英文品牌名稱從「Race Day」改為「My Race」**。改動的範圍刻意收斂，只動真正當作品牌標示在用的地方：README 標題、功能介紹頁與網站地圖頁右上角的品牌標示、以及匯出行事曆檔（.ics）裡內嵌的產生者識別字串。
  - **兩處看起來像、其實不是品牌的沒有動**：英文介面裡的 `Actual Weather on Race Day`（比賽當天的實際天氣）跟 `Crew Guide: Race-Day Outfit`（比賽當天的穿著），這兩個 race day 是「比賽當天」的一般英文用法，不是產品名稱，改掉會變成語意錯誤的句子。
  - **網址沒有動，這需要你自己決定**：`https://ai-sub3.github.io/Race-Day/` 這個網址在十幾個地方出現（README、功能介紹頁的 og:url 與各個連結、網站地圖），但它是由 GitHub 儲存庫名稱決定的。如果不同步改儲存庫名稱就把這些字串改掉，所有連結都會失效；而改儲存庫名稱會讓現有的書籤、已經分享出去的連結全部失效，所以這件事沒有代為決定，維持原樣。
  - **App 本身的顯示名稱不受影響**：這個 App 在畫面上、瀏覽器分頁標題、加入主畫面之後顯示的名稱一直都是「賽事紀錄」，英文介面用的是「Race Log」，從來沒有用過「Race Day」當作介面名稱——「Race Day」只存在於儲存庫／品牌標示層級。
  - **測試方式**：確認三處品牌標示都已更新、兩處一般英文用法維持原樣、匯出的 .ics 檔內容正確帶有新的識別字串且檔案結構仍然合法。三個 HTML 檔案的標籤結構、全站回歸測試都重新跑過。

## v3.6.0

- **標題列整理：帳號與次要動作收進頭像選單**。原本這一列平鋪了八個東西——深色模式、語言下拉、一行「支援：繁體中文・日本語・English」說明、頭像、使用者名稱、同步診斷、登出、建立個人資訊、範例資料——在手機上會擠成三到四行，佔掉整個第一屏。
  - **刪掉那行語言說明**：下拉選單本身打開就看得到三個語言選項，旁邊再寫一次「支援：繁體中文・日本語・English」是純粹的重複，而且它是整列裡最長的一段文字，是換行的主因之一。
  - **帳號相關的六項收進頭像選單**：點右上角頭像展開，裡面依序是使用者名稱、啟用推播通知、同步診斷、登出，分隔線之後是建立個人資訊、範例資料。頭像本身就是「這是你的帳號」最直覺的提示，不需要再配文字說明；沒登入時顯示一個通用人形圖示。
  - **沿用既有的選單機制，沒有另外造一套**：這個 App 的「資料管理」下拉本來就是一套通用的 `data-menu-trigger` 選單（含點外面關閉、Esc 關閉、項目點擊後自動收起），帳號選單直接掛上同一套，不用重寫開關與定位邏輯。
  - **留在外面的只有三樣**：深色模式切換、語言下拉、「+ 新增賽事」。前兩個是隨時可能要調的全域設定，第三個是這個 App 最主要的動作（手機版本來就已經改用右下角的懸浮按鈕，標題列這顆只在桌機顯示）。標題列的元素從五組降到四個。
  - **測試方式**：確認選單能正常開合、`aria-expanded` 正確、移進選單的「範例資料」與「建立個人資訊」按下去依然有作用（這是最需要驗證的部分——它們從平鋪的按鈕變成選單項目，如果事件綁定只認原本的位置就會失效），以及選單在執行動作後會自動收起。深色與淺色模式、登入與未登入四種狀態都截圖確認過。回歸測試中有一項失敗，追下去確認是測試腳本自己過時了——它直接點擊「範例資料」按鈕，而那顆現在收在選單裡、預設不可見，腳本改成先開選單再點就通過了。

## v3.5.0

- **鎖定畫面縮放，手機上不再跟著手指放大縮小或跑版**。使用者回報手機上整個區塊會隨手指移動而放大縮小、常常跑版，希望像原生 App 那樣畫面固定。查證後確認這是兩個獨立原因疊在一起，必須分別處理：
  - **原因一：iOS 的「對焦自動放大」。** 只要輸入框算出來的字級小於 16px，點進去的瞬間 iOS 就會把整頁放大——而且**離開欄位之後不會自己縮回來**，畫面就一直停在放大裁切的狀態。這個 App 的輸入框字級是 13px 到 15px，全部都踩到這條線：一般欄位 15px、鞋款編輯 14px、補給品資料庫每一列只有 13px。這一輪把觸控裝置上的輸入框字級統一提到 16px（用 `hover:none` 判斷，桌機維持原本較精緻的字級，不會整個變胖）。
  - **原因二：主動捏合縮放沒有被鎖住。** viewport 的 `user-scalable=no` 只有 Android 會遵守——**iOS 從 10 開始就刻意忽略它**（為了無障礙），所以只加 viewport 設定在 iPhone 上完全沒用，必須用 JS 攔截 WebKit 專屬的 gesture 事件才會真的鎖住。兩邊都做，各自負責自己的平台；另外也擋掉連點兩下放大，以及桌機的 Ctrl／⌘＋滾輪縮放，讓行為一致。
  - **順手補上防跑版的兩道保險**：整頁禁止橫向捲動（任何一個算錯寬度的元素都會讓整頁可以左右拖動，視覺焦點就跟著跑掉；寬內容如表格、圖表、軌跡畫廊本來就各自包在可橫向捲動的容器裡，不受影響），以及圖片與 SVG 一律不超出容器寬度。
  - **這是一個明確的取捨**：鎖住縮放會讓使用者無法用捏合放大看細節，不符合無障礙的一般建議。這是使用者針對自己的個人工具明確要求的決定，記錄在此。文字本身的大小仍然可以透過系統的字級設定調整。
  - **測試方式**：驗證了捏合手勢、連點兩下、Ctrl＋滾輪都被正確攔截，而一般捲動、單次點擊、間隔較長的兩次點擊都不受影響；所有輸入框在觸控裝置上都確認達到 16px。最重要的是確認**既有的觸控手勢沒有被誤傷**——整頁的 `touch-action` 設成只允許垂直捲動之後，橫向的手勢會不會失效是這次最大的風險：實測月曆左右滑動切換月份、賽事列左右滑動完賽／刪除、Bottom Sheet 下拉關閉、海拔圖拖曳游標、軌跡畫廊橫向捲動、長按解鎖生涯回顧全部正常（因為這些元素各自宣告了自己的 `touch-action`，會覆蓋整頁的預設值）。最後也確認了字級提高之後，版面最緊湊的補給品資料庫沒有溢出或破版。
  - **注意**：iOS 的自動放大行為無法在桌機瀏覽器的手機模擬模式重現，這是真實 WebKit-on-iOS 的行為，所以這次的修正需要在實機上確認。

## v3.4.1

- **摘要列表的分隔符號從「・」改成「/」**：全站所有「用一行把幾個數值串起來」的顯示都改用斜線分隔（例如「能量膠 8/  鹽錠 4/  1500 ml」「A 7:00:00/  B 7:30:00」）。涵蓋 13 張摘要卡片、鞋款管理的里程與規格摘要、生涯回顧主卡的軌跡資訊／戰靴排行／補給統計、動態回顧輪播的天氣標示、年度回顧長圖的鞋款資訊、補給品資料庫的營養摘要。
  - **兩處刻意沒改**：AI 報告提示詞產生器裡的兩段（天氣、計畫補給）用的也是同一個符號，但那是要送給語言模型的文字、不是畫面上給人看的，改它只會動到提示詞內容、對使用者看到的東西沒有任何影響。
  - **日文介面裡當作標點的「・」也保留**：日文用這個符號連接並列詞是正常的標點用法（例如「距離・標高・タイム」「装備・補給・戦略」），那些不是清單分隔符號，跟著一起換會讓日文讀起來很奇怪。實際改的只有「確實是把幾個數值串成一列」的那些。
  - **順手修掉英文版多出來的空格**：英文原本用的是前後帶空格的中點（`{a} items · {b} packed`），直接換成斜線會變成「2 stations /  1 with cutoffs」，斜線前面多一個空格。已經改成跟中日文一致的「2 stations/  1 with cutoffs」。
  - **測試方式**：一次建立一場填滿所有欄位的賽事，把六個區塊全部展開，一次抓出 13 張摘要卡片的實際文字確認全部轉換完成、沒有任何一張殘留舊符號；鞋款管理與生涯回顧主卡的三個區塊也分別確認。三種語言各自驗證過。

## v3.4.0

- **「裝備、補給與戰略」區塊剩下的三塊也收進抽屜**：補給設定（能量膠、鹽錠、水袋容量、補給備註）、訓練計畫與減量期（計畫起訖日、減量期開始日、週跑量目標、關鍵課表備註、外部連結、訓練備註）、賽前目標（A／B／C 三段目標時間與配速策略）。至此整個賽事詳情頁的可編輯欄位全部改成「摘要卡片＋點開編輯」。
  - **拆成三張而不是一張**：這 11 個欄位加上三段目標，性質差很多——補給是賽前要帶什麼、訓練計畫是幾個月的期程、賽前目標是當天要跑多快。塞進同一個抽屜只是把長頁面換成長面板，跟前幾輪把 22 個基本欄位拆成兩張、19 個賽後欄位拆成兩張是同一個理由。
  - **摘要各自挑最有代表性的資訊**：補給顯示能量膠與鹽錠數量、水袋容量；訓練計畫顯示計畫起訖日、減量期開始日、週跑量目標；賽前目標直接把已填的目標時間列出來（「A 3:00:00・B 3:10:00」），不用點開就知道自己設了什麼。
  - **賽前目標的欄位走的是另一套渲染路徑**：其他區塊的欄位都是由共用的欄位產生器產出、id 是 `f-` 開頭；賽前目標是自己組的 HTML、用 `data-path` 標記綁定。兩者都靠同一個事件委派處理自動儲存，所以搬進抽屜同樣能運作——但測試必須分別驗證，不能因為前幾輪的欄位都正常就假設這個也一定正常。
  - **留在頁面上的**：環境折損預測（EPP）的說明與圖表、配速試算按鈕、以及前幾輪已經卡片化的裝備清單與補給時程規劃。
  - **測試方式**：確認 11 個欄位完整分配到兩個抽屜、沒有遺漏或重複（4＋7、零重疊）；三個抽屜的編輯都正確自動儲存（含賽前目標那套不同的綁定方式），關閉後摘要卡片數字跟著更新；EPP、配速試算按鈕、裝備清單卡片都確認還留在頁面上。前幾輪的其他抽屜卡片、長按解鎖、徽章系統都重新跑過回歸測試。

## v3.3.1

- **沒有震動馬達的裝置，長按蓄力的視覺回饋加強**：使用者在 iPhone 上長按 PB 數字解鎖生涯回顧時，回報「沒有感受到震動」。查證後確認這不是程式錯誤——**iOS 上所有瀏覽器都不支援 Vibration API**（它們共用 WebKit，Safari 對這個 API 的官方立場是反對），所以 `navigator.vibrate` 在 iPhone 上根本不存在；換 Chrome 或 Firefox 也一樣。程式碼裡 17 個震動呼叫點全部都有 `if(navigator.vibrate)` 防護，所以功能本身正常運作、解鎖也成功，只是少了觸覺那一半。
  - **問題在於：在 iPhone 上，原本設計的三階段蓄力只剩下很細微的視覺差異**，使用者會覺得「按下去好像沒反應」，而且沒辦法知道這是預期行為還是壞掉。
  - **解法是補強視覺，不是加說明文字**：偵測到裝置沒有 Vibration API 時（啟動時判斷一次，掛一個 class），把既有兩個階段的光暈加強——第一階段光暈更亮更大，第二階段再加一圈金色描邊讓臨界點明確——並在卡片底部加一條 1.5 秒填滿的進度條，把「還要按多久」直接畫出來。有震動馬達的裝置完全不受影響，維持原本較克制的視覺，因為觸覺已經把階段感講清楚了。
  - **這也是這個專案一路以來的一個盲點修正**：iOS 不支援震動這件事，我在前面好幾輪的程式碼註解跟版本紀錄裡都寫過，但從來沒有反映到使用者實際看得到的畫面上——寫在註解裡的「已知限制」，對使用者來說等於不存在。
  - **測試方式**：模擬兩種裝置各測一次。沒有震動 API 的情況：確認 class 正確掛上、兩個階段的光暈確實一階比一階強（第二階段多了內描邊）、進度條會隨按壓推進、中途放開會重置、1.5 秒後仍正常解鎖。有震動 API 的情況：確認 class 不會掛上、不會出現進度條與額外描邊、解鎖行為不變。另外把三個階段各自截圖比對，確認光靠眼睛就分得出來。

## v3.3.0

- **新增「裝備與補給」徽章類別，8 枚新徽章**：百里戰靴（單雙鞋累計 500 公里）、光榮除役（讓一雙鞋跑到壽命正式退役）、輪替大師（完賽紀錄中用過 3 雙以上不同鞋款）、補給紀實者（5 場賽事確實記錄實際補給）、碳水引擎（單場實際補給碳水達 200 公克）、咖啡因飛行員（單場咖啡因達 200 毫克）、珠峰高度（生涯累計爬升 8,848 公尺）、年年不缺席（連續 3 個年份都有完賽紀錄）。徽章總數從 95 增加到 103。
  - **只做「資料算得出來」的那一批，沒有做需要行為追蹤的那些**：前面幾輪提案累積了三十幾個徽章構想，其中一大類（連續 5 次完全用鍵盤操作、在某個元素上停留 5 秒不動、1 秒內極速點擊復原、滑鼠畫圓超過 5 圈之類）需要一套目前不存在的「使用者操作序列追蹤」機制——那是 session 級別的行為記錄，跟現有徽章「讀賽事資料做判定」是完全不同的東西，而且很難驗證正確性。這一輪先把八個從既有資料就能算出來的補上；行為追蹤那套如果要做，應該是獨立一輪、連同它自己的測試一起。
  - **查證後發現既有 95 枚徽章完全沒有涵蓋鞋款、爬升、咖啡因、連續年份這幾個面向**，所以這八枚不是在既有分類裡硬塞，而是補上真正的空白，另開一個類別。
  - **順手修掉一個會讓新類別完全不顯示的問題**：榮譽櫃的類別清單原本是寫死的四個字串陣列，新類別的徽章雖然定義好了、判定也會跑，但在畫面上完全不會出現，而且不會報任何錯——這種「安靜地少東西」最難發現。改成從徽章定義本身推導出類別清單，以後再加類別不用記得回來改這裡。
  - **補給類徽章的判定跟上一輪的生涯統計同一套規則**：只計入「賽後：已補給」有勾選的項目、數量留空以 1 份計、營養素只算有連到補給品資料庫的項目。兩邊用不同標準的話，使用者會看到「生涯統計說吃了 200g 碳水，但碳水引擎徽章沒解鎖」這種對不起來的狀況。
  - **測試方式**：八枚徽章逐一驗證「剛好差一點不解鎖」與「跨過門檻就解鎖」兩種狀態——例如百里戰靴在累計 484 公里時確認未解鎖、補到 526 公里才解鎖；補給紀實者在第 4 場確認未解鎖、第 5 場才解鎖。另外確認空資料時八枚全部都不會誤解鎖、進度值都落在 0~100 之間、解鎖時附帶的「觸發賽事」指向真實存在的賽事。UI 層確認新類別確實出現在榮譽櫃、103 枚徽章全部渲染得出來、新徽章的成就圖下載按鈕正常。前幾輪的生涯回顧主卡、軌跡畫廊、補給統計、抽屜卡片都重新跑過回歸測試。

## v3.2.0

- **生涯回顧主卡新增「能量補給」統計**：把生涯吃掉的補給品加總起來——總份數、碳水／鈉／咖啡因總量，以及依消耗份數排序的補給品排行。至此提案裡原本擱置的兩項（軌跡畫廊、補給統計）都補完了。
  - **只計入「已補給」打勾的項目，不是所有排進計畫的**：補給時程本來就是賽前規劃用的，每一列有一個「賽後：已補給」的勾選框。這張卡片講的是「你生涯實際吃掉了多少」，把計畫當成實際消耗會高估。測試特地放了一筆「規劃了 5 份但沒勾已補給」的資料，確認它不會被算進去。
  - **數量沒填的以 1 份計**：使用者把項目排進時程、又勾了已補給，代表確實吃了，只是懶得填數量；當成 0 份反而更不準。
  - **營養素的累加規則跟既有的單場統計完全一致**：只有連到補給品資料庫、且資料庫有填該欄位的項目才會累加碳水／鈉／咖啡因。純手打的自訂項目（例如「自備飯糰」）仍然會出現在消耗排行裡、也計入總份數，只是不貢獻營養素數字——因為根本不知道它的成分。這樣兩邊算出來的數字才對得起來。
  - **開發過程中發現一段 CSS 沒有真的寫進檔案**：這次的樣式跟翻譯原本合併在同一次編輯裡，翻譯那段因為前一輪已經改過同一行而比對失敗、整個編輯中止，但樣式那部分已經先寫進去了——實際結果是樣式遺失、翻譯也沒加到。第一次截圖時看到版面完全沒有套用（數字跟標籤全部直向堆疊、漸層底也不見了）才發現。這也是為什麼每次都要真的把畫面截出來看：程式跑得過、測試也全過，不代表使用者看到的東西是對的。
  - **測試方式**：用手算驗證過的資料驗證加總邏輯——碳水 174g、鈉 210mg、咖啡因 200mg、總份數 9，四個數字都跟手算吻合。另外確認未勾選已補給的項目、未完賽賽事的補給都正確排除，數量留空的正確以 1 份計，純手打項目正確列入排行但不影響營養素總量。空資料的情況測過兩種：完全沒有補給紀錄、以及只有規劃但沒有任何一筆勾選已補給，兩種都正確整段隱藏而不是顯示一堆 0。上一輪的軌跡畫廊、長按解鎖、以及前幾輪的抽屜卡片、獎牌牆 3D 傾斜、磁性吸附都重新跑過回歸測試。

## v3.1.0

- **生涯回顧主卡新增「軌跡畫廊」**：把有 GPS 軌跡的完賽紀錄，轉成黑底上的霓虹發光線條，橫向捲動瀏覽。PB 場次用金色並標上 PB 徽章，其餘用螢光綠；每條軌跡的起點是綠點、終點是橘點。
  - **投影算法沿用既有的分享圖邏輯，不是另外發明一套**：這個 App 的分享圖本來就會把 GPS 軌跡畫在 canvas 上（`drawRouteTrackOnCanvas`），裡面已經有一套等距長方投影＋用中心緯度餘弦修正經度的做法。軌跡畫廊用的是同一套算法——但**沒有硬把兩者抽成共用函式**：一個輸出 canvas 繪圖指令、一個產生 SVG 路徑字串，輸出型態完全不同，強行共用只會多一層轉接。共用的是算法，不是程式碼本身。
  - **緯度修正不是可有可無的細節**：少了餘弦修正，同樣經緯度跨度的軌跡在高緯度會被橫向拉長，環湖賽的圓形路線會變成橢圓。測試特地用一個「經緯度跨度相同的正方形迴圈」驗證：在北緯 25 度算出來的寬高比是 0.907，跟 cos(25°)=0.9063 吻合，代表形狀是對的。
  - **點數上限 300 點**：長距離賽事的軌跡動輒上萬個點，全部塞進 SVG 路徑只會讓 DOM 字串肥大，視覺上完全看不出差異。等距抽樣到 300 點以內，另外強制保留最後一個點——不然抽樣的餘數會讓軌跡末端少一截，終點標記的位置也會跟著跑掉。
  - **發光效果用 SVG 濾鏡，不是疊好幾層半透明線條**：後者要畫三四次同樣的路徑才有光暈感，點數多的時候成本明顯。另外每張卡片的濾鏡 id 都帶上賽事 id，避免同一頁多張卡片的濾鏡互相蓋掉（SVG 的 id 是全域的，這是很容易踩到的坑）。
  - **橫向捲動用原生的 scroll-snap**，不自己寫 touch 事件處理——原生捲動本來就帶有邊緣阻尼與慣性，手感比自己算位移好，也不會跟外層的垂直捲動打架。
  - **收錄範圍**：只收有 GPS 軌跡的完賽紀錄（沒有軌跡就畫不出形狀，直接排除；未完賽、未出賽也不收），依「PB ＞ 海外 ＞ 日期新到舊」排序，上限 12 張——這張卡是生涯精華，不是全部賽事的清單。
  - **測試方式**：投影算法單獨測過形狀正確性、置中與留白、起終點標記、閉合迴圈的起終點重合、五千點軌跡的抽樣上限與終點保留。邊界情況測了單點、空陣列、null、以及所有點都相同（跨度為零、會除以零）的軌跡，都不會崩潰或產生 NaN。畫廊整合測過篩選正確（排除無軌跡與未完賽）、排序正確（PB 排第一）、濾鏡 id 不重複、橫向捲動與 snap 生效。上一輪的長按解鎖、取消路徑、以及前幾輪的抽屜卡片、獎牌牆 3D 傾斜、磁性吸附都重新跑過回歸測試。

## v3.0.0

- **生涯回顧主卡（Hall of Fame）＋長按 PB 數字解鎖**：在生涯總覽的「全馬 PB」卡片上長按 1.5 秒，經過三階段的蓄力回饋後解鎖一張全螢幕的生涯回顧；也可以用 `Cmd/Ctrl+K` 輸入 `> wrap`／`生涯` 直接開啟。
  - **這次提案的範圍比前幾輪大得多，做了明確取捨**：長按解鎖機制、英雄 PB 卡、生涯數據網格、戰靴排行這一輪做完；提案裡的「GPX 軌跡霓虹畫廊」與「補給品消耗統計」沒有做——前者要把經緯度轉成 SVG 路徑再做橫向畫廊，後者需要一套目前不存在的補給品用量彙總邏輯，兩個各自都是獨立一輪的份量，硬塞進來只會做得潦草。
  - **所有數字都是從實際資料即時算出來的，沒有任何寫死**。提案裡點名了具體的賽事與數字（大阪馬 2:59:31、Maurten 消耗 15 包之類），但那些一旦寫死，資料一變這張卡就開始說謊。實作上全部改成從 `state.races` 與鞋款資料推導，包括英雄卡上「締造 PB 的是哪一場、哪一天」也是反查出來的。
  - **查證後發現兩件事跟原本的假設不同**：其一，「彩帶動畫」這件事在前幾輪一直被我當成「有外部套件相依、待解決」而擱置，這次查證才發現這個專案早就有一套純 CSS 的彩帶實作（`confettiHtml()`），根本不需要引入任何套件——解鎖瞬間的彩帶直接沿用它。其二，這個 App 已經有一個「動態回顧輪播」的全螢幕年度回顧，提案的主卡跟它概念相近但範圍不同（一個是單一年度的照片輪播，一個是生涯尺度的數據總結），確認兩者不衝突、各自保留。
  - **長按的三階段蓄力**：0.5 秒微震＋微光、1.0 秒中震＋高亮與細微抖動、1.5 秒解鎖時兩下心跳重擊。抖動幅度刻意只做 1px，是「感覺得到引擎在震」而不是「畫面壞掉在跳」。放大值必須寫進抖動的 keyframes 裡——animation 的 transform 會蓋掉同元素上的 transform 宣告，分開寫的話放大效果會在抖動開始的瞬間消失。
  - **取消路徑是這個功能最需要嚴謹的地方**：手指提早放開、或按住後滑走超過 10px，都要立刻清掉所有計時器；只清一部分的話，放開之後還會有殘留的震動或樣式突然跳出來。10px 這個容許值讓「按住時手指自然的微小晃動」不會誤取消，但真的想捲動頁面時會正確放棄蓄力。
  - **測試方式**：完整走完 1.5 秒的三個階段，逐一確認每一階的樣式類別與震動都在對的時間點觸發、解鎖時的震動樣式是兩下心跳、解鎖後蓄力樣式全部清乾淨。取消路徑分三種情境測：提早放開不解鎖、放開後不會有殘留震動、滑走超過門檻會取消、小幅晃動不會誤取消仍能正常解鎖。另外確認 `Cmd+K` 用英文 `wrap` 跟中文「生涯」都搜得到、完全沒有完賽紀錄時這個指令會自動隱藏、Esc 會優先關閉這張卡（它疊在其他彈窗之上）。三語言的文字都比對過。前幾輪的抽屜卡片、獎牌牆 3D 傾斜、懸浮按鈕磁性吸附、三維度游標聯動都重新跑過回歸測試。

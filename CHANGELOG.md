# 版本紀錄

版本號顯示在網站右下角。這份紀錄從 v1.0.0（第一個完整可用版本）開始；之後每次功能異動都會在這裡加一筆，並同步更新 [USAGE.md](./USAGE.md) 與 [README.md](./README.md) 對應段落。

## v2.40.3

- **修正同系列配速疊圖在分段紀錄很多筆、但只有少數幾筆有填「平均配速」欄位時幾乎完全空白的問題**：上一版（v2.40.2）修的是「分段紀錄本來就集中在賽事某一段」，但這次回報的萬金石馬拉松全馬情境不一樣：很多人記分段習慣是每公里記一次「累積距離＋累積時間」（例如 Garmin 自動分圈匯出），不會另外手動算、填「這一段的平均配速」欄位——圖表原本只認 avgPaceSecPerKm 這一個欄位，這種紀錄方式下大部分分段都會被當成「沒有配速資料」整批濾掉，只剩極少數真的有手動填配速的分段留在圖上，變成只在畫面邊緣擠出一小段看起來像壞掉的線，跟上一版修的問題長得很像、但成因不同。
  - 這次改成：有填 avgPaceSecPerKm 就直接用；沒填的話，只要這筆跟距離排序後緊鄰的前一筆都有累積距離跟累積時間，就直接用兩者的差算出這一段配速，不需要使用者自己手動換算填入；連第一筆分段也一併處理（假設賽事從距離 0、時間 0 開始）。用「42 筆分段都有距離跟累積時間、但只有最後兩筆手動填了配速」這個貼近萬金石馬拉松回報情境的測試資料重現過問題，確認修正後圖表正確顯示完整的 1~42 公里配速曲線。
  - 針對這次回報裡提出的「乾脆固定 X 軸 0~100 公里、Y 軸 1:00~20:00/km」：沒有採用這個做法，因為固定這麼寬的配速範圍，全馬正常配速（通常落在 4~7 分速之間）的實際變化幅度會被壓縮成一條幾乎看不出差異的平線，反而讓圖表更難用；改成修正資料判讀的根本問題後，圖表本來就會依實際資料自動縮放到看得清楚差異的範圍，不需要犧牲精細度換取「至少看得到東西」。
  - 另外針對重複距離、時間往回跳等異常資料也測過，確認會安全跳過不會讓整段計算掛掉或算出負的配速。

## v2.40.2

- **修正同系列配速疊圖在分段紀錄集中於賽事後段時幾乎完全空白的問題**：圖表 X 軸（距離）下限原本寫死是 0（代表「起點」），如果兩場賽事的分段紀錄本來就只集中在賽事的某一段（例如只補了最後幾公里的配速資料，沒有從頭開始記錄），寫死的 0 會硬生生撐出一大段完全沒有資料的空白區域，實際的配速曲線反而被擠壓成畫面最右邊一小條看起來像「壞掉」的線——這正是回報的畫面呈現的樣子。改成用兩場賽事分段資料裡實際出現的最小距離當下限（跟海拔剖面圖用同一種做法），圖表就會確實對應到資料涵蓋的範圍，不會再硬塞一段不存在的空白。用回報畫面同樣的情境（分段只從 38km 開始到 42km）重現過問題，確認修正後圖表正確撐滿版面；也重跑了前一版測過的既有情境（分段從頭開始記錄、兩場賽事距離不同），確認畫面沒有變化，這次的修正沒有影響到原本就正常的案例。

## v2.40.1

- **修正住宿／交通區塊的日期時間欄位被裁切看不到時間的問題**：入住時間、退房時間、發車/出發時間這幾個欄位，瀏覽器原生會同時顯示日期、上午/下午、時:分跟一個選擇器圖示，需要的寬度比一般文字輸入框多不少；但這幾個欄位所在的重複列表（住宿、交通）原本跟其他一般欄位共用同一個最小寬度（130px），欄位一多、排在同一列時常常被擠到只剩 130px，時:分的部分就會被裁掉看不見。這次針對這種日期時間欄位額外給一個比較寬的最小寬度（200px），其他欄位不受影響。用跟原始回報畫面接近的版面寬度測過，確認入住/退房時間現在能完整顯示「09/26/2026, 04:00 PM」這樣的完整日期時間，不會再被裁切。
  - 過程中另外發現一個跟這次回報無關、範圍更小的既有問題：手機寬度下「裝備、補給與戰略」整個區塊本身有大約 22px 的水平溢出（不需要任何資料就會出現，不是這次修改造成的），這次沒有一併處理，先記錄下來，之後有需要再處理。

## v2.40.0

- **同系列賽事配速疊圖（Ghost Comparison）**：五個功能需求中的最後一個，全部完成（即時天氣、賽前作戰卡、海拔剖面圖地圖聯動、單一徽章分享卡、本次的配速疊圖）。本場賽事跟同一個「賽事系列」的其他賽事都有分段配速資料時，賽後紀錄區塊會自動出現一張疊圖：本場一條實線、比較對象一條虛線，本場較快的區間上色成綠色、較慢的區間上色成紅色。同系列如果有 2 場以上可以比較，圖表上方會出現下拉選單切換要跟哪一年比，預設選最近的一場。
  - 兩場賽事的分段點距離不需要完全對齊（例如一場每 5km 記錄一次、另一場每 1km 一次），用線性內插對齊到共同的距離刻度上再比較色塊；用一組模擬「前半場超前、後半場撞牆」的測試資料驗證過，確認前後兩段能正確分別上色成綠、紅，不會混成一片。
  - 兩場賽事總距離不同時（例如系列賽某年路線微調），色塊只會畫在兩者距離重疊的範圍內，超出的部分維持單純畫線、不強行上色比較；截圖驗證過不會有版面跑掉或色塊亂畫的情況。
  - 沒有系列名稱、系列裡只有這一場、或比較對象沒有分段配速資料時，這個區塊不會出現，不會是空白或報錯。

## v2.39.0

- **單一徽章分享卡**：五個功能需求中的第 5 個（前 4 個已在先前版本完成：即時天氣、賽前作戰卡、海拔剖面圖地圖聯動；同系列配速疊圖／Ghost Comparison 目前還沒動工，是這五個裡面複雜度最高的一個，會在下一輪繼續）。開箱動畫新增「下載成就圖」按鈕（跟「收下徽章」並排，下載不會關掉動畫），榮譽櫃裡每個已解鎖的徽章下方也有同樣的連結，可以事後回頭下載任何一個已解鎖徽章的圖片。產生 9:16 直式圖片，徽章圖示（原本開箱動畫用的 emoji，這次確認在 canvas 裡能正確以全彩渲染，不是缺字方框）、名稱、達成條件、達成方式（沿用既有的「達成於：某場賽事（日期）」邏輯，沒有對應單一賽事的徽章正確顯示達成日期），配上呼應開箱動畫金色慶祝感的視覺，適合直接發布到社群限時動態。
- 順手在 USAGE.md 補上整個徽章系統（榮譽櫃、開箱動畫）的說明——這個功能經過好幾輪陸續擴充，但從來沒有寫進使用說明過，這次一併補齊。

## v2.38.0

- **海拔剖面圖與地圖游標聯動**：五個功能需求中的第 3 個（第 1、2 個已完成）。滑鼠移到海拔變化圖上時，即時顯示該點的累積里程、海拔、瞬時坡度，如果賽事同時有匯入 GPS 軌跡，路線地圖上會同步出現一個游標標記跟著移動；手機上用手指觸控滑動也支援。CP 站點會在圖上標出虛線位置，並用箭頭標示這個 CP 是落在爬升後還是下坡後。
  - 這個功能最核心的技術問題：GPS 軌跡（`trackPoints`，只有經緯度）跟海拔剖面資料（`elevationProfile`，距離＋海拔）是分開匯入、各自獨立降採樣的兩份資料，彼此沒有共同索引可以對應。解法是用 haversine 公式重新算出軌跡點自己的累積距離，再依比例去對應海拔圖游標的位置，而不是假設兩個陣列的點數或間距一致。
  - 開發過程中抓到並修正了兩個實測後才發現的問題：
    1. 軌跡點的「最近點」搜尋原本無條件進位到下一個點，資料點稀疏時會抓到明顯不夠近的點；改成同時比較前後兩個點，取真正距離最近的那一個。
    2. CP 是「爬升後還是下坡後」的判斷，原本用固定公里數的窗口去抓附近的海拔點，但海拔剖面資料點的間距落差很大，窗口太小的話常常一個點都篩不到、誤判成「持平」；改成不管點的實際間距、固定抓最近點前後幾個索引，資料點數夠就一定抓得到。
    3. CP 標記本身用的顏色是深色模式的顏色，在預設的淺色主題下幾乎看不見；改成白底深色外框的樣式，不管在哪個主題、疊在海拔圖哪種顏色的背景上都維持清楚可辨識，兩個主題都截圖確認過。
  - 完全沒有海拔或路線資料的賽事不會顯示這個區塊，不會是空白或報錯。

## v2.37.0

- **賽前作戰卡 / 鎖屏桌布產生器**：這是五個功能需求中的第 2 個（第 1 個即時天氣已在上一版完成）。賽前狀態的賽事詳情頁新增「下載賽前作戰卡」按鈕，產生 9:16 直式高對比圖片（號碼布、起跑分區/時間、CP 關門時間、緊急聯絡人、補給配方），可直接設成手機鎖屏桌布，賽中不用解鎖手機就能快速確認關門點。
  - 新增兩個欄位支援這個功能：**號碼布編號**、**起跑分區 / 波次**（都在「基本」分頁、起跑時間旁邊）。
  - 圖片版面經過測試：完全沒填任何細節時顯示引導提示而不是一張空白卡片；CP 數量很多時（測過 12 個）只顯示前 8 個並標明還有幾個未顯示，不會把版面撐爆；欄位內容豐富時（測過同時有號碼布、起跑資訊、4 個 CP、緊急聯絡人、補給配方）各區塊間距與分隔線都正確，沒有重疊。
  - 過程中修正一個問題：CP 距離的括號原本不管什麼語言都用全形括號「（）」，英文版這樣不符合排版慣例，已改成依語言顯示半形或全形括號。
  - 完賽狀態的賽事不會顯示這個按鈕（賽前作戰卡只在賽前用得到）。
- 距離開賽 7 天內、有匯入 GPS 軌跡的賽事，天氣卡片仍會優先顯示即時預報，跟賽前作戰卡各自獨立、互不影響。

## v2.36.0

- **即時賽事天氣（Open-Meteo API）**：這是這次一次收到的五個功能需求中的第一個，其餘四個（賽前作戰字卡／鎖屏桌布產生器、海拔剖面圖與地圖游標聯動、同系列配速疊圖、單一徽章分享卡）都還沒開始做，會在後續分開處理，不倉促一次全部趕上。
  - 備戰釘選面板的天氣卡片，距離開賽 7 天內、且賽事已經匯入 GPX/FIT 軌跡的話，會出現「抓取即時預報」按鈕，直接在瀏覽器呼叫免費、免金鑰的 Open-Meteo API（用軌跡第一個點的座標查詢），取得當日逐時氣溫、降雨機率、陣風風速，自動挑出最接近起跑時刻的一筆顯示。
  - 抓回來的資料快取在賽事自己身上，不會每次打開面板都重新呼叫一次 API；「重新整理預報」按鈕可以手動強制更新。
  - 沒有 GPS 軌跡的賽事會顯示提示文字，不會出現抓取按鈕（沒有座標可以查）；抓取失敗（網路問題等）會顯示明確錯誤訊息，不會整個卡住或靜默失敗。
  - 這個功能本身依賴外部 API，這次開發環境本身連不到 api.open-meteo.com（限制對外連線的沙盒環境），沒辦法測試真正的網路請求成功案例；改用攔截並回傳模擬回應的方式，驗證了實際的資料解析、依起跑時刻挑選最接近時段、寫回快取、畫面顯示這幾個環節都正確無誤，網址組成也對照官方文件範例逐項核對過（`temperature_2m`、`precipitation_probability`、`wind_gusts_10m` 這幾個參數名稱都是查證過的正確值，不是憑印象猜的）。實際在你自己的瀏覽器環境應該可以正常連線，如果遇到問題請告訴我。

## v2.35.3

- **修正 Excel 匯入沒有錯誤保護的問題**：選擇檔案後的解析過程原本完全沒有 try/catch，如果 Excel 解析工具（透過 CDN 載入的外部套件）剛好沒載入成功（網路問題、被廣告攔截外掛擋到、CDN 本身出狀況等），或是選到的檔案根本不是有效的 Excel 檔，整個匯入動作會靜默失敗——畫面上什麼提示都不會有，只有瀏覽器主控台會留下一行使用者永遠不會看到的錯誤訊息。這次補上跟 GPX 匯入一樣等級的錯誤處理：解析失敗時會跳出說明視窗，並且針對「解析工具本身沒載入成功」這個特定情況給出具體建議（檢查網路、重新整理、或先停用廣告攔截外掛試試看），不是只丟一句「發生錯誤」。用「解析工具確實沒載入」這個實際情境測試過（剛好這個開發環境本身連不到那個 CDN，直接拿來當測試案例），確認會正確顯示提示，不會是靜默失敗。
- 這是接續上一版的系統性排查，這次額外檢查了：honor-retire 徽章動畫期間按鈕能不能被連續點擊（原本以為是問題，重新用不強迫的點擊方式測過後確認既有的 `pointer-events:none` 保護本身就正確擋下了，不需要額外修正）、95 個徽章判定在 200 筆賽事規模下的執行效能（21ms，沒有問題）、GPX/TCX 檔案解析對畸形輸入的容錯（確認原本就有妥善的錯誤處理）。

## v2.35.2

- **修正兩個徽章（千里老兵、千足蜈蚣）永遠不會播放開箱動畫的問題**：這兩個徽章的達成條件不是綁定單一場賽事（分別是「鞋款退役時累積里程滿壽命」與「累積使用滿 5 雙不同鞋款」），系統原本只有「使用者點進觸發徽章的那場賽事」才會補放開箱動畫，但這兩個徽章根本沒有對應的單一賽事，永遠不會被配對到，等於解鎖了也不會有任何畫面回饋，只會在榮譽櫃裡悄悄多一個沒被看到的徽章。另外「標記為退役」這個操作本身也從來沒有觸發過徽章判定，就算之後改別的地方讓判定補跑，也要等到下次完賽才會發現。這次修正：徽章判定新增一個機制，對於沒有對應單一賽事的徽章，判定出新解鎖時直接播放，不用等一個永遠不會出現的時機；同時在「退役」「標記為退役」相關的操作裡也補上判定，不再只有完賽才會觸發。用實際情境測過確認兩個徽章都能正確在對的時機播放動畫。
  - 這個 bug 是這次系統性排查找到的，過程另外測過：95 個徽章全數重新跑過判定邏輯（含空資料防呆）、事件監聽器有沒有重複註冊、徽章判定裡引用的欄位名稱與選項值逐一比對資料結構確認沒有打錯字、匯出/匯入/刪除復原流程、z-index 疊層順序——這些都沒有發現問題。

## v2.35.1

- **新增 15 個徽章**：安心羈絆、跨國連線、櫻花追跡者、迴圈地獄、志在參加、全地形制霸、游刃有餘、高速賽道、海外野獸、水岸巡航、補給站獵人、溜滑梯特攻、鏡像分段、無痛巡航。總數從 80 個增加到 95 個。
  - 「新北探旅」做了簡化：原始描述提到「包含特殊賽制（如夜間定向越野）或新店周邊」，但系統裡賽事地點只記錄到「縣市」層級，沒有「特殊賽制」這種分類欄位，也沒有細到「新店」這種行政區以下的資料，這兩個條件都沒有依據可以判定，所以只保留「新北市累積 3 場」這個能明確判定的部分。
  - 「安心羈絆」判定邏輯跟稍早的「不敗金身」同一套（連續 N 場、中途中斷就重新累計），用一組刻意在中間拿掉緊急聯絡人的測試資料驗證過，確認會正確判定為未達成而不是誤判成還連續著。

## v2.35.0

- **新增 12 個徽章**：跨海遠征、星夜迷航、親情後盾、鋸齒狂人、無氧稜線、積分入帳、跨年連鬥、不敗金身、晨星領航員、減量期大師、前線起跑、終點狙擊手。總數從 68 個增加到 80 個。
  - 「鋸齒狂人」用海拔剖面圖的資料自動偵測方向反轉次數，且落差要達到 50 公尺以上才算數，避免把資料裡的微小雜訊誤判成一個山頭；用平滑爬升的假資料測過，確認不會誤判。
  - 「不敗金身」判定的是「連續 10 場」，賽事狀態會包含 DNS／DNF（不是只看已完賽的紀錄，因為那樣就永遠不會有 DNS/DNF 出現在序列裡，條件會變得沒有意義）；用一筆特地插入的 DNF 紀錄測過，確認會正確打斷連續紀錄。
- **兩個既有徽章的門檻調整**：這次收到的清單裡「強制裝備達人」跟「碳水無底洞」用的是新的門檻數字（強制裝備 5 項、能量膠 5 包），跟兩輪前建立時的門檻（8 項／8 包）不一樣。判斷這是同一個徽章的門檻更新，不是兩個新徽章，所以直接修改原本那兩個徽章的判定條件，不是新增重複的徽章。
- **三個項目沒有新增，附上原因**：「Watts goingon」「瓦數倉鼠」這次收到的內容跟兩輪前一模一樣（瓦數倉鼠的 FTP／推重比門檻依然沒有可判定的資料來源，原因同兩輪前的說明），已經存在的不重複新增；「膝蓋終結者」的條件（階梯比例超過 15%）跟既有的「石階地獄」完全相同，只是換了名稱，所以也沒有另外新增一個條件重複的徽章。

## v2.34.3

- **手機版頂部導覽列改成完全不固定**：上一版把固定範圍縮小到只剩「月份切換＋搜尋＋篩選」，但實測後這樣還是佔掉螢幕不小的空間，尤其是有海外賽事等次要篩選標籤時會再多一行。這次手機寬度下（640px 以下）不管是行事曆列表還是賽事詳情頁，頂部導覽列整個都不固定，一律跟著內容正常捲走，需要搜尋或篩選時捲回最上面即可。桌機、平板寬度（641px 以上）維持吸頂不變。
- 過程中也修掉一個連帶發現的問題：641~860px 這個平板寬度區間，原本負責調整吸頂列左右間距的樣式規則沒有限定範圍，會在 640px 以下的手機寬度也生效，跟這次新增的「完全不固定」規則衝突。已經把該規則限定在 641~860px 之間，跟手機寬度的規則互斥，不會再有疊加的問題。

## v2.34.2

- **修正手機版行事曆列表畫面頂部固定區塊過大的問題**：隨著這幾輪陸續加入生涯數據、徽章榮譽櫃等內容，頂部導覽列（品牌列＋行事曆列＋匯入匯出列三段疊在一起）在手機上實測高度逼近 450~485px，超過一支手機螢幕的一半，不管捲到哪裡都固定佔住這麼大一塊，變成不管往下滑多少，都會有一大塊資訊「擋住」下面剛長出來的生涯數據、榮譽櫃內容。這次把固定的範圍縮小：只有「行事曆月份切換＋搜尋＋篩選」這一段維持固定（往下捲動時螢幕上方會自動再壓縮一次，隱藏副標題），品牌列跟匯入匯出列改成跟著內容正常捲走。整個捲動範圍內固定區塊的總高度從原本約 447~485px 降到約 145~313px（依捲動位置浮動，捲到底時約為螢幕高度的 37%，原本約 55%）。
  - 過程中抓到一個真正的技術問題：CSS 的 `position:sticky` 只能在「自己所屬的容器還沒整個捲出畫面」的範圍內維持固定，如果只是把某一段的樣式改成 sticky、但它還是包在原本那個三段疊起來、捲一下就整個滑走的頂部容器裡面，捲過那個容器的高度之後，這段還是會被一起帶走，看起來像是固定了、其實只是撐了一小段就失效。所以除了改樣式，也把這段的 HTML 實際搬到跟頁面內容一樣長的容器裡，才能讓它在整趟捲動過程中真的維持固定——這點在提交前已經用長頁面實際捲動測試（捲到 5000px 深）確認過不會再失效。
  - 桌機寬度、以及手機上檢視單一賽事詳情頁的行為維持不變。

## v2.34.1

- **已解鎖徽章顯示達成條件**：名稱下方改成優先顯示徽章本身的解鎖條件說明（例如「比賽當日體感溫度落在 5°C ~ 16°C 之間且完賽」），下面再用更小的字附上「達成於：某場賽事（日期）」，兩種資訊都保留。
- **四大類別各自可收折**：探索與疆域、地形與極限、速度與戰略、跨界與裝備現在各自是獨立的收折區塊，標題旁會顯示該類別自己的解鎖進度（例如「3 / 20」），不用每次都整批一起展開或收起。
- **同類型徽章排在一起**：68 個徽章在各自類別內重新排序，讓條件相近的擺在一起方便比對，例如破三殿堂緊接著破三菁英、三個連續週末的挑戰（越戰越勇／勇者中的勇者／太喜歡了吧）排在一起、三鐵潛力股到三鐵傳奇的階梯依序排列。排序透過新增的 `order` 欄位控制，沒有更動任何判定邏輯本身，68 個徽章的解鎖條件測試過一輪，確認調整順序沒有影響判定結果。

## v2.34.0

- **新增 20 個徽章**：橫跨四大類別（探索與疆域、地形與極限、速度與戰略、跨界與裝備），總數從 48 個增加到 68 個。包含前線紮營、孤狼出征、天時地利、台幣戰士、東洋征途、定向尋寶員／定向菜雞、玩泥巴、單攻王、海平面起飛、中級山神獸、高反差地貌、分組凸台、無傷減量、巡航導彈、All-in、海神波賽頓、Watts goingon、強制裝備達人、碳水無底洞。
  - 兩個項目沒有實作，原因說明：「海線越野」需要判斷賽道是否「結合海線與山徑」，系統目前沒有這種地形分類欄位，沒有依據可以判定；「瓦數倉鼠」原本兩輪前就標註過 FTP／推重比門檻無法判定（沒有 FTP 欄位），這次的說明補了「推力比 2 以上且持續 1 小時」，但這個 app 沒有記錄體重也沒有記錄逐秒功率數據，同樣無法判定，維持原本簡化版本（自行車項目有填平均功率即符合）；同時新增的「Watts goingon」條件跟這個簡化版本完全一樣，為了不做出兩個永遠同時解鎖/同時鎖住的重複徽章，只新增了「Watts goingon」，沒有把「瓦數倉鼠」的條件改寫成重複的版本。
- **已解鎖徽章顯示達成方式**：榮譽櫃裡已解鎖的徽章，名稱下方會用小字標明「達成於：某場賽事（日期）」；沒有對應到單一觸發賽事的徽章（例如需要累積使用 5 雙不同鞋款的「千足蜈蚣」），改顯示達成日期。鎖定中的徽章維持原本的剪影加解鎖條件提示，兩種狀態的文字視覺上清楚區分。

## v2.33.0

- **徽章系統（榮譽櫃）**：生涯數據列新增「榮譽櫃」，48 個徽章橫跨探索與疆域、地形與極限、速度與戰略、跨界與裝備四大類。未解鎖的徽章顯示灰色剪影加問號、解鎖條件提示文字，累積型的徽章（例如「累積完成 3 場日本賽事」）還會顯示進度條。
  - **完賽當下的開箱體驗**：賽事狀態切成「已完賽」的當下，只要剛好觸發新徽章，會立即彈出全螢幕五彩紙屑動畫，配上金色徽章卡片；點「收下徽章」後才真正收進榮譽櫃。開啟系統「減少動態效果」設定的話會跳過動畫直接顯示結果。
  - **舊資料的補放機制**：這個功能上線前就已經完賽、且早就符合某個徽章條件的賽事，不會一開網站就轟炸畫面——會先在背景安靜記錄「已解鎖但還沒看過動畫」，等你之後點進「當初觸發這個徽章的那一場賽事」，才會補放開箱動畫。
  - 依你的說明訂正了「破百俱樂部」的「半馬破百」條件為半馬完賽成績低於 100 分鐘；「越戰越勇／勇者中的勇者／太喜歡了吧」原始描述中有兩個重複的「連續三個週末」，實作成連續 2／3／4 個週末的三個遞增檔位，如果理解錯了請告訴我調整；「瓦數倉鼠」原本提到的 FTP／推重比門檻沒有明確數值、這個 app 目前也沒有 FTP 欄位，暫時只判定自行車項目是否有填平均功率數據。
  - **已知限制**：目前 48 個徽章的名稱與解鎖提示文字只有中文版本，日文／英文介面下這部分內容會維持顯示中文（其餘所有徽章系統的介面文字，例如「榮譽櫃」「徽章解鎖」「收下徽章」等，都已經正確支援三語）。

## v2.32.1

- **既有鞋款的預期壽命可以編輯了**：上一版只有新增鞋款時能設定，已經存在的鞋款沒有編輯入口；這次在每雙鞋的里程數字旁加了一個編輯（✎）按鈕，點下去會就地變成可輸入的欄位，存檔後立即反映在健康度計算上。
- **耐久度光條改成分段式 HP 長條**：從原本的平滑漸層長條，改成 5 格區塊、隨里程逐格點亮的樣式，0~50% 綠色、51~80% 黃色、超過 80% 紅色並持續閃爍提醒；旁邊的文字說明也改得更具體：「中底彈性開始衰退」、「已達退役里程，注意受傷風險」。
- **榮譽退役儀式**：鞋款累積里程達到 100% 時，原本樸素的「退役」文字連結會換成醒目的「✅ 標記為退役」按鈕，點下去會有金色「光榮退役」印章蓋上去的動畫（沿用這個 app 原本就有的蓋章視覺語言），動畫結束後才真的移到「已退役」區塊，比平時任意時候想退役一雙鞋的操作更有儀式感。開啟系統「減少動態效果」設定的話會直接顯示結果，不播放動畫。
- 這次也順手抓到一個翻譯沒對齊的地方：中文版把健康度文字說明改得更具體之後，忘記同步更新日文/英文版的翻譯字典，導致這兩個語言還停在舊的簡短版本；已經一併補上。

## v2.32.0

- **鞋款管理全面改版**：
  - 新增鞋款時可以填「預期壽命(km)」（選填，預設 600），舊資料自動補上這個欄位，無痛升級。
  - 「刪除」改成「退役」：有比賽紀錄綁定的鞋款只能退役，不能直接刪除，賽事的歷史紀錄與總里程完整保留；完全沒有綁定紀錄的鞋款才會同時提供「退役」跟「刪除」兩個選項。退役的鞋款可以再按「恢復服役」拿回來用。
  - 已退役的鞋款會跟服役中的分成兩個區塊，套用灰階＋降低透明度效果，加上金色「光榮退役」標籤。
  - 每雙鞋名稱下方新增健康度漸層長條：0~50% 綠色（`--trail`）、51~80% 平滑漸變到黃色（`--gold`）、超過 80% 轉紅色（`--flag`），旁邊搭配對應的文字狀態標籤（服役中／中底衰退／建議汰換），顏色轉換的門檻值抓到一個原本不一致的地方並修正——長條顏色原本在剛好 80% 時已經變紅，但狀態標籤要超過 80% 才切換，兩者現在統一在同一個門檻切換。
  - 「本場穿著鞋款」下拉選單預設隱藏已退役的鞋款；如果某場賽事本來就綁定了一雙後來退役的鞋，那場賽事自己的下拉選單還是會保留顯示（並標註「光榮退役」），只是不會再提供給其他新賽事選用。
  - 賽事狀態改成「已完賽」且有綁定鞋款時，背景會計算這雙鞋累積里程是否剛好跨過 80% 或 100% 的門檻，是的話立即跳出提示（沿用既有的 toast 元件），只有真的跨過門檻那一刻才會提醒，不會每次存檔都重複跳。

## v2.31.2

- **移除離線狀態提示**：拿掉頂部導覽列的「離線中，變更將於連線後自動同步」提示，連同對應的 HTML、CSS、JS（定時檢查、事件監聽、手動關閉按鈕）一併清乾淨，不是只用樣式藏起來。本機資料一律先存本機這件事本身沒有變，只是不再額外顯示這個提示。

## v2.31.1

- **修正手機版檢視賽事詳情時頂部導覽列一直佔住畫面的問題**：頂部導覽列（行事曆切換、搜尋框、篩選標籤、匯入匯出這些按鈕）原本不管在哪個畫面都會固定黏在頂端；這些控制項只有在看行事曆列表時有用，點進單一賽事詳情頁後，捲動瀏覽賽事內容時，這一大塊固定不動的區域會佔掉手機螢幕一大半空間，擋住實際想看的賽事資訊。這次改成：手機寬度下，點進賽事詳情（或新增賽事表單）後，頂部導覽列不再固定，會跟著內容正常往上捲走；區塊快速導覽列（基本／路線／氣象…那排小按鈕）改成直接貼齊螢幕頂端固定，捲動賽事內容時還是隨時點得到。回到行事曆列表畫面、或桌機寬度瀏覽時，頂部導覽列維持原本固定的行為不變。
- **修正儲存失敗提示疊在頂部導覽列上的問題**：這個提示原本固定在畫面最上方，跟頂部導覽列搶同一個位置，兩者互相干擾；改成固定在畫面最下方，不會再跟頂部導覽列衝突。
- 附註：這次錄影裡看到的紅色「本機儲存失敗」提示，版本號顯示是 v2.30.1——這正是上一版（v2.31.0）「改用 IndexedDB」要解決的問題（原本用 localStorage，78 場賽事加上封面照片容易超過瀏覽器約 5MB 的額度上限），更新到 v2.31.0 以後，這個特定的儲存失敗提示應該就不會再出現了。

## v2.31.0

- **改用 IndexedDB 儲存（原本是 localStorage）**：本機資料改存在 IndexedDB（經由開源套件 idb-keyval，CDN 免費載入），額度通常是數百 MB 到數 GB，不會再有 localStorage 那種 ~5MB 就見底的問題。CDN 連不到時（網路問題、被封鎖）自動退回原本的 localStorage，確保這個核心功能不會因為外部套件載入失敗就整個壞掉。第一次用新版開啟網站時，會自動偵測並搬移舊版留在 localStorage 裡的資料到 IndexedDB，只搬一次，不會遺失既有資料，也不會每次開啟都重跑一次搬移。因為 `persist`／`loadRaces` 這幾個核心函式先前就已經是非同步（為了支援 Claude 內建 artifact 預覽環境而設計），這次改動不需要大規模調整呼叫端程式碼；唯一原本是同步寫法的 `loadSyncOverwrites`／`persistSyncOverwrites`（同步覆蓋紀錄）已一併改成非同步。移除了原本針對 localStorage ~5MB 額度設計的「儲存空間接近上限」黃色主動預警（改用 IndexedDB 後這個假設不再成立），保留真正存檔失敗時的紅色錯誤提示。

## v2.30.1

- **加入專案授權**：新增 `LICENSE` 檔案，採用 PolyForm Noncommercial License 1.0.0（官方原文，未經改寫），個人/研究/教育/非營利等非商業用途可自由使用、修改、散布，不授權商業性使用。頂部導覽列版本號旁新增「© 2026 劉恩龍 LIU EN LUNG」版權標示；使用說明視窗底部新增「版權與授權」段落並附授權連結；README.md 新增授權段落。原本的 GNU AGPLv3 與「All Rights Reserved」都沒有採用——AGPLv3 是可以商用的（只是要求原始碼公開，不是禁止商用），跟「杜絕商業炒作」這個目標不完全吻合；「All Rights Reserved」則會連非商業的社群使用都一併擋掉，跟「與社群良性交流」互相矛盾。PolyForm Noncommercial 是專門為「非商業用途開放、商業用途禁止」這個組合設計的授權條款，比較貼近原始需求。

## v2.30.0

- **封面主色環境光暈**：有封面照片的賽事，詳情頁標題區塊後方會用已經抽取好的主色調（`coverImageAccentColor`）暈染一片大範圍模糊的柔光，沒有照片的賽事不受影響。實際截圖調過參數，原本照你給的數值（模糊 120px、透明度 0.15、範圍較大）看起來比較像一塊有軟邊的色塊而不是真正的柔光暈染，縮小範圍、提高模糊相對於色塊的比例後，邊緣過渡更自然。
- **時間降噪排版**：成績儀表板的大字完賽時間、全馬 PB 榮譽卡片、備戰釘選的配速目標時間，冒號透明度降到 0.4，數字本身保持完整對比。範圍上沒有套用到密集表格裡的小字時間欄位（系列賽事比較、分段配速表、緊湊表格檢視）——那些欄位本來就小又密集，跟其他純文字欄位混在一起，部分文字降低透明度反而增加雜訊而不是減少。
- **橫向滾動漸隱邊緣**：系列賽事比較、分段配速一覽（順便讓這個表格本身也真的能橫向捲動了，之前沒有）、多媒體相片網格，捲動容器邊緣改用 `mask-image` 做柔和淡出，兩側都會淡出（不是只有右側），不管捲到哪個位置邊緣視覺都一致。
- **獎牌牆懸浮聚焦**：滑鼠移到獎牌牆的相片卡片上，照片會有微小放大效果，底下的文字區塊背景也會稍微加深。

## v2.29.2

- **離線提示「X」按鈕徹底重做**：連續三次回報同一個「離線提示卡住、按 X 沒反應」的問題，橫跨 Chrome、Edge、iPhone 三種不同瀏覽器引擎都一樣——這種一致性代表問題不太可能是單一瀏覽器的怪癖，是先前那幾版的「聰明」自動復原邏輯本身有問題。這次整個簡化：按鈕改成直接綁定事件（不透過 document 層級的事件代理），關掉後在這次頁面瀏覽期間是絕對的，任何自動檢查（定時輪詢、瀏覽器事件、切回分頁）都不會再把它打開，除非重新整理頁面。用大量重複觸發事件（10 次 offline 事件、跨越兩輪定時輪詢、分頁切換事件）測過，確認按了就是真的關掉；也確認重新整理頁面後會照常重新判斷，不是整個功能永久失效。附註：如果離線提示本身仍然常態性出現，有可能是瀏覽器依據當下的網路環境（VPN、特殊路由等）回報的連線狀態，不完全是這個網站能單方面修正的瀏覽器限制——但不管實際連線狀態為何，這次「X」按鈕保證按了就會關掉。

## v2.29.1

- **修正離線提示的「X」按鈕按了沒反應的問題**：上一版（v2.26.1）修離線提示卡住的問題時，自己另外種下一個新 bug——只要瀏覽器觸發一次 offline 事件，就會把「使用者已經手動關掉」的狀態重置回去。問題是瀏覽器在還沒恢復連線之前，其實可能重複觸發好幾次 offline 事件，不是每一次都代表真的重新斷線一次。結果就是按 X 關掉提示，幾乎馬上被下一次 offline 事件打開，看起來像完全沒反應。這次改成只追蹤「真的從連線狀態變回離線」這種情況才會重置，已經用重複觸發 offline 事件（不夾雜 online 事件）的方式重現並確認修好；連線真的恢復、或是真的重新斷線一次，提示還是會照常運作。

## v2.29.0

- **備戰釘選英雄倒數視覺**：備戰釘選卡片裡的賽事如果有封面照片，標題區塊會改成全滿版的照片背景，疊加巨大的倒數天數數字，背景圖用 JS 依捲動位置做視差位移（沒有用 `background-attachment:fixed` 這個常見做法，因為那個在 iOS Safari 上經常失效，這個 app 有不少人會用「加入主畫面」在手機上開，所以改用可以跨瀏覽器穩定運作的位移寫法，也有做位移範圍限制避免捲到底部/頂部露出圖片邊緣空白）。裝備打包進度、配速策略、天氣預報這幾張小卡片維持在下方，功能沒有減少。沒有封面照片的賽事維持原本的樣式，不會顯示一個空的英雄區塊。有開啟系統「減少動態效果」設定的話會停用視差效果。
- **全馬 PB 榮譽卡片**：生涯數據列的「全馬 PB」從原本跟其他數字並排的小欄位，獨立成一張置中的榮譽卡片，用大字級的金色等寬字體呈現成績。

## v2.28.0

- **生涯數據數字滾動動畫**：總完賽場次、今年累計里程會用電子碼表式的滾動效果跳到最終數值；也新增了「生涯累計爬升」這個新的數據卡片。動畫只有在數值真的變動時才會播放（例如剛把某場賽事標記完賽），單純因為其他操作重新整理畫面、數字沒變的話不會重播，避免每次互動都跳動反而分心。有開啟系統「減少動態效果」設定的話會直接顯示最終數字。
- **裝備打包進度改成動態進度環**：備戰釘選卡片裡的裝備打包進度，從長條圖改成圓形進度環，開啟時會有填滿的動畫效果，中間顯示「已打包／總數」。
- **修正一個找出來的既有翻譯漏洞**：生涯數據列的場次/里程數字後面的單位文字（「場」「公里」）之前是寫死的中文，不管切到日文還是英文介面都只有數字換了、單位還是中文，變成「6 races」寫成「6 場」這種混雜。這次一併修正並加上正確的多語系單位文字。

## v2.27.0

- **毛玻璃導覽列**：頂部導覽列改成半透明背景加 `backdrop-filter: blur(12px)`，往下捲動時底下內容會隱約透出模糊的顏色，深色/淺色模式各自調過透明度與模糊程度。快速導覽列（區塊跳轉那排小按鈕）也一併套用同樣效果，避免跟頂部導覽列的視覺風格不一致。
- **漸層海拔剖面圖**：海拔變化圖的面積填色改用 SVG 遮罩做垂直漸層，從貼近稜線的地方開始淡入，往下漸漸變透明到底部完全看不見，不再是一整塊同樣深淺的色塊。之前做的「依坡度上色」（平緩綠色、陡峭紅色）維持不變，這次是疊加一層「上濃下淡」的效果在上面，兩個效果同時存在，不是二選一。

## v2.26.1

- **修正離線提示卡住的問題**：先前完全依賴瀏覽器的 `online`/`offline` 事件更新離線提示，但這兩個事件在部分瀏覽器、作業系統、網路環境組合下並不可靠，有紀錄顯示的案例是明明已經恢復連線，事件卻沒有觸發，導致提示一直卡在「離線中」。這次加了兩層修正：每 8 秒重新檢查一次目前的連線狀態（不依賴事件）、切回分頁時也會重新檢查；另外離線提示旁邊新增一個「✕」按鈕，如果提示卡住了可以直接點掉，之後只要真的偵測到連線恢復、或是又發生一次新的離線狀況，提示會自動恢復正常運作，不會被永久關掉。

## v2.26.0

- **數據表格靠右對齊**：系列賽事比較、分段配速一覽、配速試算表這三個表格的數字欄位（時間、距離、中籤率、心率）改成靠右對齊，冒號跟小數點會對齊在同一條線上，掃視比較不同列的數字時更好讀。分段配速表的「配速」欄位維持左對齊——那一欄本來就用背景長條圖顯示相對快慢，跟右對齊要解決的問題是同一件事，兩個疊在一起反而互相干擾。過程中抓到一個 CSS 優先權的問題：原本通用的靠右對齊規則跟表格自己的靠左對齊規則優先權相同，後宣告的會贏，剛好表格自己的規則排在後面，會把我要的靠右對齊蓋掉；已經改成優先權更高的寫法，不受宣告順序影響。
- **狀態標籤柔和化**：`.stamp` 狀態標籤、PB 徽章、減量期標示，從「外框+文字色」改成「10~20% 透明度背景色+文字色」，去邊框，深淺色模式都有各自調過的透明度數值（深色模式的背景色需要更高的透明度才看得清楚，不是兩個模式套用同一組數字）。原本 stamp 標籤的輕微傾斜（像蓋章）維持不變——這是既有的設計巧思，跟這次要求的邊框/背景改動是兩件事，沒有一併拿掉。

## v2.25.0

- **卡片化設計**：賽事詳情頁的六大區塊（基本、路線、氣象、預算、裝備、賽後）從原本的底線分隔，改成獨立卡片——白色（深色模式為 `--surface`）背景、8px 圓角、淡淡的陰影，卡片之間留白分開。同時把頁面底色（`--paper`）調暗一點點，讓卡片跟底色的層次更明顯。深色模式的陰影透明度另外調高（0.3 而非淺色模式的 0.05），不然陰影在深色背景上幾乎看不見。
- **表單欄位呼吸感**：欄位之間的間距加大，標籤文字顏色從 `--ink-soft` 改成更淡的 `--stone`，跟實際填寫的資料（維持原本較深的顏色）拉開對比，掃視表單時更容易分辨「這是標題」還是「這是我填的內容」。

## v2.24.0

- **柔光 Focus 狀態**：全站鍵盤 focus 樣式從實線外框改成柔和的光暈陰影（`box-shadow` 加短過場動畫），輸入框額外加上邊框顏色過場。透明度比原本建議的 0.2 略高（淺色模式 0.28、深色模式 0.35），確保在一般螢幕亮度下仍看得清楚，不會因為太淡而失去 focus 指示的實際功能（這是鍵盤操作、無障礙輔助工具都會依賴的東西，太淡等於變相拿掉這個功能）。修正一個過程中抓到的細節：一開始的寫法會讓所有 focus 狀態的元素都變成方角，圓形的懸浮按鈕、藥丸形狀的篩選標籤被 focus 到時會整個變形，已修正為讓光暈自動貼合每個元素原本的形狀。
- **行事曆「今天」實心圓底白字**：日期數字從「綠字＋外框」改成 iOS 行事曆那種實心圓底白字，視線一落到月曆上就能立刻定錨今天的位置。

## v2.23.0

- **封面照片自適應主題色**：上傳封面照片時，會在瀏覽器本機用 Canvas 分析像素（不需要外部套件、不會上傳到任何地方），取樣統計排除太暗、太亮、太灰的像素後，找出照片的主色調，套用到標題區塊的倒數進度條顏色，讓每場賽事的頁面依照片呈現不同氛圍。賽事進入「7 天內」的緊急倒數狀態時，維持既有的警示紅色，不會被主題色蓋掉（功能性警示優先於裝飾）。
- **摺疊區塊展開動畫**：賽事詳情的六大區塊（基本、路線、氣象、預算、裝備、賽後）展開時，內容會有一個輕微淡入＋位移的過場，不再是瞬間跳出。收合方向維持原生瞬間收合（這是目前瀏覽器 `<details>` 標籤的已知限制——要做到收合也平滑，需要攔截每一次點擊、延遲原生收合時機，牽動範圍太大、風險偏高，這次先做風險較低、也是使用者最常感受到的展開方向）。已加上 `prefers-reduced-motion` 判斷，開啟「減少動態效果」系統設定的使用者會停用動畫。
- **卡片懸浮與點擊回饋**：行事曆格子、手機版清單卡片、獎牌牆卡片，滑鼠移上去會有輕微上浮＋陰影，點擊時有微縮回饋，操作手感更接近原生 App。緊湊表格檢視維持原本的純色塊 hover，沒有加上浮動效果——密集資料表格習慣上不會有這種效果，加了反而干擾閱讀對齊。

## v2.22.0

- **分段配速內嵌長條圖**：賽事詳情新增一張「分段配速一覽」表格，配速欄位背景用漸層畫出長條，長度對應該分段的相對快慢（該場賽事最快的分段長條最滿），一眼就能看出哪一段掉速，不用逐行讀數字。
- **心率區間色彩標籤（Z1~Z5）**：成績儀表板的平均心率、分段配速表的心率欄位，會依「該場賽事自己填的最大心率」算出百分比，套上經典的灰／藍／綠／橘／紅五區間配色，並加上 Z1~Z5 文字標籤（不只靠顏色，避免色盲使用者看不出差異）。沒有填最大心率時維持原樣不上色。
- **參賽熱力圖**：生涯數據列新增類似 GitHub 貢獻度熱力圖的檢視，以年為列、月為欄，顏色深淺對應該月總里程，滑鼠移上去可以看到當月場次與里程明細。

## v2.21.0

- **扁平化 CSV 匯出（自動化友善）**：新增「匯出 CSV（扁平化）」按鈕，一次下載 10 份 CSV 檔——`races.csv` 主表（所有純量欄位攤平成單一資料列，例如 `route_distanceKm`、`location_country`）加上 9 份子表（`checkpoints.csv`、`splits.csv`、`equipmentChecklist.csv`、`goals.csv`、`nutritionSchedule.csv`、`accommodations.csv`、`transportation.csv`、`companions.csv`、`mediaLinks.csv`），每份子表都有 `race_id`／`race_name` 欄位對回主表，設計成關聯式結構，方便直接用 SQL JOIN、Python `pandas.merge`、Excel VBA 逐表讀取。已刪除（垃圾桶）的賽事不會出現在匯出結果。GPS 座標與海拔剖面點位資料量大且非分析用途，不列入匯出（需要完整資料仍可用既有的 JSON 匯出）。CSV 開頭加了 UTF-8 BOM，確保 Excel 開啟中文/日文內容不會變亂碼；欄位名稱固定使用英文，不會因為介面語言切換而改變，避免自動化腳本因此失效。

## v2.20.0

- **累積爬升趨勢圖**：生涯數據列新增第二個圖表，以每季為單位畫出總爬升量的趨勢線，至少要有 2 個季度的已完賽資料才會顯示（只有 1 個點畫不出趨勢）。
- **緊湊表格檢視**：行事曆區塊新增第二個檢視切換按鈕，可以把賽事清單轉換成類似 Excel 的純文字密集表格——日期、賽事名稱、狀態、距離、總爬升、完賽時間，數字欄位靠右對齊方便跨年比對歷屆表現；表格會跟著目前的篩選條件走，點列可直接開啟該場賽事。

## v2.19.0

- **備戰釘選（Focus Mode）**：賽前狀態的賽事若在 14 天內開賽，主畫面最上方會自動獨立顯示一張卡片，直接展開裝備打包進度（幾件已打包／總數）、目標配速策略、天氣預報，不用在清單裡找。同時開賽的賽事有好幾場時，只會顯示最近的一場。可以「暫時隱藏」（重新整理頁面後會再出現，不是永久關掉）。
- **減量期狀態標示**：賽前訓練計畫設定的「減量期開始日」一旦跨入，會在備戰釘選卡片與賽事詳情頁自動亮起「進入減量期」標示，不用自己算日期。

## v2.18.0

- **多媒體相片網格**：賽後紀錄的「多媒體與連結」下方新增一條橫向捲動的縮圖網格。連結本身直接指向圖片檔（.jpg/.png/.gif/.webp/.avif）時會顯示真正的縮圖；其他類型的連結（相簿首頁、社群貼文、PDF 等）因為瀏覽器基於安全性限制無法跨網域讀取對方網頁內容取得預覽圖，改顯示對應類型的圖示卡片（相簿用相機圖示、獎牌用獎盃圖示、文件類用文件圖示），一樣可以點擊開啟；圖片連結萬一載入失敗，也會自動退回顯示圖示卡片，不會留著一張破圖。
- **海外賽事篩選**：篩選列新增「海外賽事」標籤，會自動判斷出現次數明顯過半的國家當作主場（低於一半時不會顯示這個篩選，避免誤判），點開後可以再依國家細分（例如「日本 (3)」），把跨國遠征的賽事單獨拉出來看。

## v2.17.0

- **離線狀態提示**：瀏覽器偵測到沒有網路連線時，頂部導覽列會出現「離線中，變更將於連線後自動同步」的提示（連線恢復後自動消失）。這只是把既有事實顯示出來——本機資料本來就是優先存 localStorage，跟有沒有網路無關，離線時所有編輯照樣正常儲存；這個提示單純是讓使用者在山區收不到訊號時，能親眼看到「離線也沒關係」，不用自己猜。

## v2.16.2

- 路線軌跡圖的路線顏色從暗綠色改成深紅色搭配發光效果（外層較寬、半透明的亮紅色疊在深紅色實線下方），不管是一般地圖還是地形圖（綠色系為主）背景，深色或淺色模式下都清楚好辨識。起點/終點標記也一併調整成白色外框的亮綠色／金黃色，跟路線顏色明確區分開來。

## v2.16.1

- 運動種類新增「游泳」，主題色用水藍色（跟既有的三鐵色、一般狀態藍色都特意錯開，不會混淆）。新增專屬的游泳圖示（波浪+划手姿勢），也新增「游泳標配」裝備範本（泳鏡、泳帽、開放水域安全浮標等，泳鏡/泳帽/浮標預設必填），套用跟前一版其他運動種類一樣的自動套用邏輯；游泳賽事跟自行車一樣不會顯示「比賽鞋款」追蹤。

## v2.16.0

- **依運動種類動態調整表單**：新增賽事、或後續變更賽事的運動種類時，如果裝備清單還是空的，會自動帶入對應的標準範本（路跑／越野／三鐵／新增的自行車標配），已經自己填過內容的清單不會被覆蓋。越野跑、超馬、障礙賽會自動帶出頭燈、求生毯等強制裝備並標記必填；自行車賽事會隱藏「比賽鞋款」追蹤（改用清單裡的功率計欄位），三鐵、二鐵因為仍有跑步項目所以維持顯示。
- **海拔變化圖坡度上色**：圖表現在會依兩點間的坡度百分比逐段上色，從平緩的綠色到陡峭（≥25%）的深紅色，附上小圖例，不用細看數字也能一眼找出「魔王陡坡」在哪裡。
- **封面照片焦點調整**：新增「調整焦點」功能，可以在完整照片上點選或拖曳決定裁切基準點（存成百分比座標），套用到行事曆格子、清單、獎牌牆這些會把照片裁成正方形的小縮圖，避免專業相機拍的照片主角被裁掉。詳情頁標題背景本身是完整顯示不裁切，所以焦點只影響小縮圖，不影響標題背景。

## v2.15.1

- **手機清單卡片補上地形資訊**：手機版月曆清單的賽事卡片，名稱下方新增一行淡色小字顯示距離與總爬升（例如「25K · +900m」），沒有這些資料的賽事則不顯示該行，不用點進詳情頁也能一眼掌握賽事硬度。

## v2.15.0

- **運動類型專屬主題色**：8 種運動種類（路跑、越野跑、超馬、二鐵、三鐵、自行車、障礙賽、其他）各自有一個專屬的低飽和度顏色，套用在賽事詳情頁的運動圖示、標題區塊下緣的裝飾線（沒有封面照片時），以及行事曆格子／清單裡沒有封面照片賽事的小色點，讓掃視行事曆或切換賽事時能更快辨識出是哪種運動。
- 顏色刻意跟既有的狀態色系（綠色＝已報名／已完賽、藍色＝考慮中／抽籤中、橘紅＝DNS／DNF）錯開色相，避免和賽事狀態的顏色語意混淆；深色模式下也各自有對應的較亮版本。

## v2.14.0

- **清單拖曳排序**：裝備清單、補給時程規劃、分段配速這三個區塊，每筆資料左側新增拖曳把手，滑鼠可以直接拖曳調整順序。考量到原生拖放無法用鍵盤操作、觸控裝置支援度也不一致，同時提供「上移／下移」按鈕作為鍵盤與觸控都能用的替代方案（第一筆的上移、最後一筆的下移會自動停用）。
- **完賽自動導覽**：把賽事狀態從「已報名／抽籤中」等賽前狀態切換成「已完賽」時，畫面會自動展開並平滑捲動到「賽後紀錄與個人數據」區塊，不用再手動往下找。

## v2.13.0

- **路線地圖深色模式**：切換到深色模式時，地圖底圖會套用濾鏡（invert + hue-rotate + brightness + contrast）轉為深色，Leaflet 內建的縮放/圖層/版權控制項也一併換成深色配色，不會再是一塊刺眼的白色。
- **地形圖圖層**：路線地圖新增圖層切換（標準地圖／地形圖，地形圖用 OpenTopoMap），越野跑、超馬、障礙賽預設會顯示地形圖，其他運動種類預設標準地圖。
- **獎牌牆檢視**：行事曆左上角新增一個切換按鈕，可以切換成「相片卡片」網格檢視，只顯示已完賽且有封面照片的賽事，依日期新到舊排序，個人最佳會標註 PB 徽章，點卡片可直接開啟該場賽事。目前沒有符合條件的賽事時會顯示提示，鼓勵完賽後幫賽事加張照片。
- **切換賽事的淡入動畫**：點行事曆或清單切換賽事時，右側詳情頁改成 150ms 淡入效果，視覺上比原本瞬間替換內容更柔和（有開啟「減少動態效果」系統設定的使用者會停用動畫）。

## v2.12.0

- **新增範例資料**：右上角「+ 新增賽事」下方新增「範例資料」連結，一鍵建立兩場示範賽事——一場路跑（報名中狀態，示範抽籤、住宿交通、目標配速、裝備清單等賽前欄位）、一場越野賽（已完賽狀態，示範成績儀表板、分段配速、海拔圖、路線地圖、賽後心得等欄位），幫助剛開始使用的人快速了解每個欄位大概填什麼。名稱都加上「範例｜」前綴避免跟真實賽事搞混，用一般的刪除功能就可以移除。重複點擊不會一直疊加新的範例。

## v2.11.0

- **新增歷年完賽長條圖**：生涯數據列旁邊新增一個小型長條圖，顯示每年完賽場次，跟既有的統計數字並排顯示。
- **新增路線軌跡地圖**：匯入 GPX／TCX／FIT 檔案時，除了原本的距離、爬升、心率，現在也會保留一份精簡過的座標軌跡（最多 400 點，避免資料量暴增），在「地理與路線」區塊顯示成實際地圖上的路線（用 Leaflet.js + OpenStreetMap，只有賽事真的有座標資料時才會載入地圖套件，沒有座標的賽事完全不受影響、不會拖慢載入速度）。
- 修正：地圖資料存在但賽事狀態是「已完賽」時，路線區塊原本預設是收合的，地圖會因為容器還沒顯示、量不到正確尺寸而跑版；現在只要有座標資料，這個區塊就會自動展開。
- 順手修正一個沿用很久的翻譯漏洞：海拔變化圖的標題文字沒有接上多語系字典，一直顯示中文。

## v2.10.0

- 空狀態視覺優化：CP／補給站、住宿、交通、隨行人員、裝備清單、補給時程、分段配速、多媒體連結、鞋款管理、資料復原（已刪除賽事／同步覆蓋紀錄）——這些原本只有一行灰字「尚未新增」的地方，都加上對應主題的低飽和度小圖示（旗子、房子、汽車、人像、裝備袋、瓶罐、碼表、連結、鞋子、垃圾桶、同步箭頭），風格跟首頁空狀態的運動圖示一致。
- 過程中修了三個圖示：交通原本畫成像電腦螢幕，改成看得出是車子；裝備清單原本畫兩次都變成掛鎖（一個圈圈疊在方塊上，怎麼調整內部細節看起來都還是像鎖），最後改成側背包/裝備袋的寬扁造型才跳脫這個問題；鞋子純線條在小尺寸下糊成一團，改成實心剪影才看得出鞋型。

## v2.9.0

依你提出的三點 UX 建議實作：

- **手機版新增賽事懸浮按鈕**：畫面 <640px 時，右下角新增一個「+」懸浮按鈕（疊在使用說明小人上方，不會互相遮擋），改用單手更好點擊；原本頂部的按鈕在手機版隱藏，避免重複。測試時抓到一個真正的 bug：一開始按鈕點了完全沒反應，因為對應的事件監聽器只綁在賽事詳情區塊裡，懸浮按鈕在那個區塊外面所以收不到——已修正，改綁在全域層級。
- **自動儲存視覺回饋**：標題列新增小指示器，欄位修改後會顯示「儲存中…」（一個會脈動的小圓點），存檔完成後變成「已自動儲存 ✓」，2.5 秒後淡出。
- **長表單快速導覽**：詳情頁新增一排可吸頂的錨點按鈕（基本／路線／氣象／預算／裝備／賽後），點擊直接跳到對應區塊並自動展開（如果原本是收合的），不用再手動捲動翻找。吸頂位置會自動跟著頂部導覽列的實際高度走（包含手機捲動時導覽列縮小的狀態），不會疊在一起。

## v2.8.1

- 小修正：「清除全部資料」原本沒有一併清掉同步覆蓋紀錄，賽事都清空後那些紀錄其實已經失去意義；裝備範本、鞋款資料維持不動（這兩個是可重複使用的參考資料，不是賽事紀錄本身，清除賽事不應該連帶清掉）。

## v2.8.0

- **修正：「匯出 JSON」備份其實沒有對應的還原方式**——之前只做了下載，沒有做「匯入」，等於備份檔案存下來後其實沒有路可以用。新增「匯入 JSON」，可以把之前匯出的檔案讀回來；如果匯入的賽事跟現有資料有衝突（同一筆賽事兩邊版本不同），會沿用跟雲端同步一樣的處理方式：自動套用較新的版本，較舊的保留在「資料復原」，不會直接蓋掉遺失。
- 實測過完整的「匯出 → 清空資料 → 重新整理 → 匯入」流程，確認真的能把資料復原回來，不只是看程式碼覺得應該可以。
- 順手驗證了一個原本懷疑的效能問題：用你這種超馬距離會產生的大型 GPX/FIT 檔（模擬 20 小時、7.2 萬個座標點的 UTMB 等級檔案）實測匯入只要 0.6 秒，確認不是問題，沒有為此新增不必要的東西。

## v2.7.0

- **修正：雲端同步失敗完全不會通知使用者**——跟本機儲存失敗是同一類問題，只是這次是雲端那一側：網路不穩導致同步失敗時，過去只會印在 console，本機資料雖然安全，但雲端會悄悄跟本機不同步。現在改成失敗會自動重試（15 秒後），連續失敗 3 次以上才會在登入資訊旁顯示提示（避免暫時性網路小抖動就緊張兮兮跳警示），下次同步成功會自動消失。
- **修正一個找了很久才發現的既有 bug**：整個「使用 Google 登入同步」區塊（登入/登出按鈕、雲端同步狀態、推播通知按鈕）其實從來沒有真正接上多語系字典——字典裡老早就有日文英文翻譯，但畫面上一直硬編碼顯示中文，不管切到哪個語言都一樣。這次一併修正並三個語言都實測過。

## v2.6.0

這次處理的是我先前評估但還沒動手的兩個風險，另外多抓到一個更嚴重的問題：

- **修正：本機儲存失敗完全不會通知使用者**——過去如果瀏覽器儲存空間滿了，存檔失敗只會印在瀏覽器開發者工具的 console，畫面上完全沒有任何提示，等於使用者以為存好了但其實沒有。現在存檔真的失敗時，畫面最上方會出現明顯的紅色提示，直到下一次存檔成功才會消失。
- **新增：本機空間使用量預警**——資料量接近瀏覽器儲存上限（保守抓 3MB，因為 Safari 額度較小）時，會主動跳出提示建議先匯出備份，不用等到真的存不下去才知道。
- **修正：直式封面照片會被壓成一小條**——之前為了讓橫式海報不被裁切，改成完整顯示、置中留白，但這也讓直式照片（例如人像）在標題這種矮胖形狀的區塊裡被壓得很小。現在上傳時會記錄照片長寬比，偵測到直式照片會自動把標題區塊加高，讓照片有合理的顯示空間。

## v2.5.0

主動找優化點並直接實作，這次著重在既有的操作摩擦：

- **新增「複製賽事」**：詳情頁新按鈕，保留地點、路線、裝備清單、官方網站等欄位建立新賽事，適合同系列跨年賽事（例如「山羚羊越野系列賽」）；名稱裡有西元年份會自動 +1，日期、報名結果、成績、心得等會清空重新開始。複製後會自動被系列比較表抓到。
- **所有彈出視窗支援 Esc 鍵關閉**：之前 7 個彈出視窗（貼上資料、GPX 匯入、Excel 匯入、鞋款管理、配速試算、使用說明、資料復原）都只能點按鈕關閉，現在按 Esc 都能直接關閉。
- **搜尋框加上防抖動**：賽事數量多時，原本每打一個字就會整個行事曆重新渲染一次；現在改成停止輸入 150ms 後才重新整理，打字更順。

## v2.4.0

視覺優化，主動盤點後直接動手，順便修掉一個在盤點時發現的既有 bug：

- 行事曆週六、週日欄位新增淡淡底色，方便快速掃視週末賽事（深色/淺色模式都有調整）
- 「今天」格子從純外框改成外框＋淡色填底，在週末重疊時也能清楚分辨
- 生涯數據列（總完賽場次／全馬 PB／今年累計里程）加上對應小圖示，比較有儀表板感
- 修正：生涯數據列的文字之前沒有接上多語系字典，日文／英文模式下其實一直顯示中文，現在已修正並實測三語言都正確

## v2.3.0

- **刪除賽事改成垃圾桶機制**：不再直接消失，會保留 30 天，期間可在左下角「已刪除賽事」還原或永久刪除
- **多裝置同步新增安全網**：如果同一場賽事在雲端與本機兩邊都被修改過，系統會自動套用較新的版本，但不會靜默丟掉舊的——舊版本會保留在「資料復原」的同步覆蓋紀錄裡（最多 20 筆），同步當下也會跳出可點擊的提示通知你發生了什麼；還原也是可逆的，還原時會先把目前版本也存進紀錄，不會二次遺失資料

## v2.2.1

- 修正封面照片背景顯示會裁掉圖片內容的問題：改用「完整顯示、置中留白」而不是「裁切填滿」，寬版海報類圖片（例如賽事主視覺横幅）不會再被裁到看不出標題或關鍵資訊，兩側留白處維持原本的淡化效果

## v2.2.0

- 運動紀錄檔匯入新增支援 FIT 檔（Garmin 等裝置常見的二進位格式，之前只支援 GPX/TCX），純瀏覽器端解析
- 新增賽事的頁面「建立賽事」按鈕下方新增兩個按鈕：匯入運動紀錄檔（自動帶入距離/爬升/心率等數據）、匯入賽事主視覺（自動處理成封面照片），建立賽事時一併套用
- 新增「賽事官方網站」欄位，新增賽事頁面可直接貼連結，之後也能在基本資訊區塊編輯

## v2.1.1

- 封面照片改版：不再強制裁成小正方形縮圖（會裁掉海報/照片上的重要資訊），改成上傳時保留完整構圖等比縮圖，顯示時變成整個標題區塊的淡化背景底圖——淡化程度會跟著深色/淺色模式自動調整，文字永遠維持清楚可讀
- 行事曆格子、手機清單上的小縮圖不受影響，一樣是小方塊縮圖

## v2.1.0

- 新增賽事封面照片：詳情頁大字標題下方可上傳照片，自動置中裁切成正方形（480×480、JPEG 壓縮），純瀏覽器端處理不會上傳到任何伺服器
- 封面照片會顯示在：詳情頁標題旁的縮圖、行事曆格子裡的小圖示、手機版清單檢視
- 沒有上傳照片的賽事完全不受影響，畫面照舊

## v2.0.1

- 使用說明按鈕換成手繪風格的跑步小人插畫（去背處理），旁邊新增會浮動的提示小標籤「有問題找我！」，跟著切換語言

## v2.0.0

- **新增多語系支援**：日文、英文全站翻譯，右上角新增語言切換選單（繁體中文／日本語／English），選擇會記住在瀏覽器裡
- 翻譯涵蓋：介面文字、所有賽事欄位標籤、下拉選單選項、行事曆、7 個 modal（新增賽事、GPX 匯入、Excel 匯入、貼上資料、鞋款管理、配速試算、使用說明）、成績分享圖與配速手環的 Canvas 文字、所有錯誤訊息
- 使用者自行輸入的資料（賽事名稱、地點、心得筆記等）**不會**被翻譯，維持原文
- 新增 Noto Sans JP 字體，日文模式下自動切換
- 架構設計：翻譯字典查不到對應詞時會自動退回繁體中文，不會開天窗；新增第四種語言只需要擴充字典，不用改動任何邏輯
- 新增 `glossary/glossary-zh-ja.md`、`glossary/glossary-zh-en.md`——直接從程式碼裡的翻譯字典自動整理產生的中日／中英名詞對照表，字典異動後重新產生即可，不用手動維護兩份

## v1.3.0

- 新增 App 內建使用說明：右下角路跑小人圖示，點開直接看操作指南，不用離開網站找 USAGE.md
- 版本號改為單一來源（JS 常數），畫面顯示與內建說明的版本字樣會自動同步，避免兩處各自寫死不一致

## v1.2.0

- 新增真正的背景推播通知（Firebase Cloud Messaging + Cloud Functions 排程），賽事倒數一個月／一週會直接推送到手機，不需要打開網站或行事曆 App。需要額外部署 Cloud Function、升級 Blaze 方案，詳見 [USAGE.md](./USAGE.md#5a-推播通知選用需要額外部署)
- 新增 `firebase-messaging-sw.js`（背景通知用的 service worker，需與 `index.html` 放在同一層）
- 新增 `functions/`（Cloud Functions 原始碼）與 `firestore.rules`（含 fcmTokens 存取規則）

## v1.1.0

- 新增行事曆提醒匯出（`.ics`）：單場賽事或全部即將到來的賽事，內建「倒數一個月」「倒數一週」兩個提醒，匯入手機原生行事曆即可用手機通知提醒——取代純網站無法在背景推播通知的限制
- 篩選 UI 改為兩層式（全部／即將到來／歷史紀錄 + 展開後才顯示細分狀態），避免賽事數量多時篩選按鈕過於擁擠

## v1.0.0

第一個完整版本，涵蓋以下功能：

**核心與介面**
- 行事曆主畫面（年/月導覽），手機（≤640px）自動切換為賽事清單檢視
- 雙層篩選：全部／即將到來／歷史紀錄，點選後展開對應細分狀態，並顯示即時筆數
- 賽事名稱／城市搜尋
- 生涯數據總覽（累積 5 場以上顯示：總完賽場次、全馬 PB、當年累計里程）
- 深色模式（跟隨系統或手動切換，記住選擇）
- 手機版標題列捲動時自動縮小
- 跑者／越野跑／鐵人三項自訂圖樣，依賽事運動種類自動顯示；PWA 自訂圖示

**賽事欄位**
- 基本資訊與時間、抽籤機率記錄
- 賽事系列（跨年比較 PB 趨勢與中籤率）
- 地理與路線（含海拔變化圖、CP 明細、ITRA/UTMB 點數欄位）
- 氣象（歷年預測／當日實際）
- 預算與行程規劃（費用、住宿、交通、隨行人員）
- 裝備清單（範本系統，內建路跑／越野／三鐵標配三份預設範本）
- 補給時程規劃（逐點記錄計畫，賽後比對實際）
- 訓練計畫、賽前目標（A/B/C）
- 配速試算與手環產生器
- 賽後成績儀表板（大字時間、配速、名次、心率、爬升、PB 金色標記）
- 前後半程掉速分析
- 比賽鞋款與里程追蹤
- 分段配速、多媒體連結、賽後心得筆記
- 一鍵產生賽事成績分享圖（PNG 下載）

**資料匯入**
- Excel 匯入（賽事規劃表，含工作表選擇與逐筆確認）
- GPX / TCX 運動紀錄檔匯入（自動抓距離、爬升、時間、心率、分段配速、海拔剖面）
- 貼上 JSON 資料快速填入（供 AI 讀取賽事網站／簡章／證書後使用）

**同步與備份**
- Firebase（Google 登入 + Firestore）選用雲端同步，本機儲存為主、雲端為疊加層，登入時自動合併資料
- 匯出 JSON 備份、清除全部資料

**技術基礎**
- 純前端單一 `index.html`，部署於 GitHub Pages
- 響應式版面（含 CSS Grid 欄寬修正，避免長標題撐版）
- 無障礙：表單欄位皆有對應 `label` / `id`

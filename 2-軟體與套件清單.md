# 賽事紀錄 App — 軟體與套件清單

整理日期：2026-09-18
本文件盤點「賽事紀錄」專案目前實際用到的每一份軟體／套件，依「主網頁 App」「Firebase 背景推播服務」「Firebase Cloud Functions（Node.js）」三個部分分類，每一項都附上確切的版本號（直接從程式碼裡的載入路徑或設定檔核對，不是憑印象寫的）跟用途。

---

## 一、主網頁 App（`index.html`，純前端、無建置流程）

整個網站是**單一一份 HTML 檔案**，核心邏輯用原生 JavaScript（Vanilla JS）撰寫，沒有使用 React／Vue／Svelte 等前端框架，沒有 build 工具鏈（webpack/vite 等），可以直接雙擊開啟或部署到 GitHub Pages。以下是額外載入的外部套件：

| 套件 | 版本 | 載入方式 | 用途 |
|---|---|---|---|
| **SheetJS (xlsx)** | 0.18.5 | `<script>` 標籤，來自 cdnjs.cloudflare.com | 匯入／匯出 Excel（.xlsx）檔案，用在批次匯入賽事資料、匯出扁平化 CSV 等功能 |
| **idb-keyval** | 6.x（`@6`，跟隨 6.x 系列最新版） | `<script>` 標籤，來自 cdn.jsdelivr.net | 包裝瀏覽器原生 IndexedDB API，作為本地資料的主要儲存機制（CDN 連不到時會自動退回 localStorage） |
| **Leaflet** | 1.9.4 | 延遲載入（只有畫面上真的需要顯示地圖時才動態插入 `<script>`／`<link>` 標籤），來自 unpkg.com | 路線軌跡互動地圖（標準地圖／地形圖兩種底圖切換） |
| **Google Fonts** | 不適用（Google 代管，非版本化套件） | `<link>` 標籤，來自 fonts.googleapis.com / fonts.gstatic.com | 字型：Noto Sans TC（繁體中文）、Noto Sans JP（日文）、IBM Plex Mono（數字／等寬字型） |
| **Firebase JS SDK（Modular）** | 10.13.2 | ES module `import`，來自 www.gstatic.com/firebasejs | 雲端同步（選用功能）：`firebase-app.js`（核心）、`firebase-auth.js`（Google 登入）、`firebase-firestore.js`（資料庫）、`firebase-messaging.js`（推播權杖註冊） |

**地圖圖磚來源**（不是套件，是 Leaflet 呼叫的外部圖磚服務，詳見第三份文件「開源資料」）：
- 標準底圖：OpenStreetMap 圖磚（`{s}.tile.openstreetmap.org`）
- 地形底圖：OpenTopoMap 圖磚（`{s}.tile.opentopomap.org`）

---

## 二、Firebase 背景推播服務（`firebase-messaging-sw.js`）

這是瀏覽器的 Service Worker 檔案，獨立於主網頁執行，負責處理「App 沒開著」時收到的背景推播通知。

| 套件 | 版本 | 載入方式 | 用途 |
|---|---|---|---|
| **Firebase JS SDK（Compat）** | 10.13.2 | `importScripts()`，來自 www.gstatic.com/firebasejs | `firebase-app-compat.js`（核心）、`firebase-messaging-compat.js`（背景推播處理），跟主網頁用的是同一個 SDK 版本，但用的是「Compat」相容包裝而非 Modular 版本（Service Worker 環境的慣例寫法） |

---

## 三、Firebase Cloud Functions（`functions/`，Node.js 後端）

這是專案裡唯一有「後端伺服器」概念的部分，負責每天定時掃描哪些賽事「剛好倒數 30 天」或「剛好倒數 7 天」，對該賽事擁有者已註冊的裝置發送推播通知。跟主網頁不同，這裡走正規的 npm 套件管理（有 `package.json`）。

| 項目 | 版本 | 說明 |
|---|---|---|
| **Node.js（執行環境）** | 20 | 在 `package.json` 的 `engines.node` 欄位指定，Firebase 部署時會用這個版本的 Node 執行環境 |
| **firebase-admin** | ^12.0.0 | npm 套件，用來初始化 Firebase 專案、存取 Firestore 資料庫（`admin.firestore()`） |
| **firebase-functions** | ^5.0.0 | npm 套件，提供 `onSchedule`（定時觸發函式）等 Cloud Functions v2 API |

（`^` 開頭代表「相容於這個主版本內的最新次版本」，是 npm 慣用的版本鎖定寫法，不是寫死單一版本號；實際部署時的確切版本取決於部署當下 npm 解析到的最新相容版本。）

---

## 四、雲端資料庫與驗證服務

嚴格來說不是「軟體套件」，但屬於這個專案依賴的外部服務，一併記錄：

| 服務 | 說明 |
|---|---|
| **Firebase Authentication** | Google 帳號登入，用來識別「這是哪個使用者」，讓雲端同步知道要讀寫哪一份資料 |
| **Cloud Firestore** | NoSQL 文件資料庫，儲存使用者的賽事資料（雲端同步功能，選用，未啟用時 app 完全靠本機 IndexedDB／localStorage 運作） |
| **Firebase Cloud Messaging (FCM)** | 推播通知服務，搭配上面的 Service Worker 使用 |

專案代號：`race-day-6da0b`（Firebase 專案 ID，可在 `firebase-messaging-sw.js` 裡的 `firebaseConfig` 看到）。

---

## 五、授權條款

專案本身採用 **PolyForm Noncommercial License 1.0.0**（非商業使用授權），著作權標示為「劉恩龍 LIU EN LUNG」，詳見 `LICENSE` 檔案。這不是套件依賴，但既然是「目前用到的軟體」清單的一部分，一併記錄專案自身的授權方式。

---

## 小結：這個專案「刻意不用」的東西

為了讓這份清單更完整，也記錄幾個這個專案**明確沒有採用**的常見選項，避免未來誤以為漏列：

- 沒有前端框架（React／Vue／Angular／Svelte 皆無）
- 沒有 CSS 框架（Tailwind／Bootstrap 皆無，樣式全部手寫 CSS）
- 沒有打包工具（webpack／vite／rollup／esbuild 皆無）
- 沒有圖表函式庫（Chart.js／D3.js／Plotly 皆無——app 本身的圖表全部用手寫 SVG／Canvas 繪製；Plotly 只出現在「下載 Python 分析套件」產生的 Jupyter Notebook 範本程式碼裡，那是給使用者在自己的 Python 環境執行的，不是網頁本身載入的套件）
- 沒有狀態管理套件（Redux／Zustand／Pinia 皆無，狀態管理是手寫的全域物件）
- 沒有自己的後端資料庫伺服器（唯一的「後端」是上面第三節的 Firebase Cloud Functions，且僅限於推播排程這一項用途；一般的資料讀寫走純前端的 IndexedDB／localStorage，或選用性質的 Firestore）

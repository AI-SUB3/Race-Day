# 賽事紀錄 App — 運動、生理科學計算方法總覽

整理日期：2026-09-18
本文件盤點「賽事紀錄」（Race-Day）這個 app 裡，所有跟運動科學／生理學相關的計算邏輯。每一條都標明：這個方法是不是有公開發表的原始出處（如果有，附上文獻）、實際的計算式、本專案的程式碼怎麼實作、學界對這個方法可靠性的共識、以及目前業界（哪些知名 App／運動員或教練／國家）的使用狀況。

**特別提醒**：以下方法明確分成兩類——

- **既有、已發表的科學方法**（GAP、Karvonen、正／負分段配速、最小平方法、皮爾森相關係數、K-Means）：這些都有可查證的原始文獻與業界採用紀錄，下面逐項附上檢索到的來源。
- **本專案自行設計的方法**（EPP 環境折損模型、氣候相對效率分析）：這些是這次開發過程中，針對這個 app 的資料量身打造的統計方法，**不是任何已發表、已受同儕審查、或已被其他 App／運動員採用的公開方法**。文件裡會明確標示「本專案自創」，可靠性欄位也會如實寫「未經外部驗證」，不會假裝這些是什麼業界標準。

---

## 1. GAP（Grade Adjusted Pace，坡度調整配速）

**原始來源**：學術基礎來自 Alberto E. Minetti 等人的兩篇代謝成本研究——Minetti et al. (1994, walking) 與 Minetti et al. (2002, "Energy cost of walking and running at extreme uphill and downhill slopes", *Journal of Applied Physiology*)，把受試者放在可調角度的跑步機上（坡度 -45% 到 +45%），直接量測耗氧量算出「每公斤體重、每公尺水平位移」的代謝成本曲線。Strava 官方文件也提到這個概念另外受到 C.T.M. Davies 早期研究的啟發。

**計算式（Minetti 2002 原始多項式）**：
```
C(i) = 155.4·i⁵ − 30.4·i⁴ − 43.3·i³ + 46.3·i² + 19.5·i + 3.6
```
其中 `i` 是坡度（小數表示，例如 0.1 代表 10%），`C(i)` 是每公斤每公尺的能量成本（J/kg/m），以 `C(0)=3.6` 當平路基準去換算等價配速。

**本專案運算邏輯**（`gapPaceMultiplier()`、`splitGradePct()`、`splitGapSecPerKm()`）：**沒有直接套用上面的 5 次多項式**，而是用一個簡化過的分段模型——

- 上坡（坡度 ≥0）：`multiplier = 1 + 坡度% × 0.0375`（即每 1% 坡度增加 3.75% 配速成本，線性）
- 下坡：用一個頂點在 -6%、效益上限 14% 的拋物線模型（下坡越陡，一開始配速受益越大，但過了 -6% 之後受益開始遞減、甚至反轉變成額外負擔）
- 結果會夾在 [0.75, 3.0] 倍之間，避免 GPS 雜訊算出離譜坡度時整個 GAP 數字失真
- GAP 是即時算出來顯示的衍生值，不存進資料本身，使用者事後修改「淨爬升」欄位時會自動重算

**跟原始文獻的落差**：文獻普遍指出下坡代謝成本最低點落在 -10%～-20% 之間（不同研究對確切數字略有分歧，但都比 -6% 更陡），本專案為了讓計算保持簡單、可即時執行，選了比研究建議更保守的 -6% 當頂點，這代表本專案的下坡配速修正幅度，會比嚴格套用 Minetti 曲線的實作更小。這是刻意簡化，不是計算錯誤，但使用者若拿這個數字跟 Strava／Garmin 逐一比對，會出現合理範圍內的差異。

**可靠性**：Minetti 的代謝成本曲線是這個領域被引用次數最多的基礎研究之一，經過同儕審查、發表於 *Journal of Applied Physiology*，是目前所有主流 GAP 實作的共同科學根據。但要注意：（1）GAP 只反映能量代謝成本，不反映下坡對股四頭肌造成的離心收縮肌肉損傷，也不反映路面技術難度；（2）各家 App 的實際公式都是「受 Minetti 啟發」而非逐字套用原始多項式，彼此之間常有分段線性近似、平滑窗口、坡度上限等差異，同一段軌跡在不同 App 上算出的 GAP 數字本來就不會完全一致。

**目前泛用性**：Strava（付費 Premium 功能）、Garmin Connect、COROS、TrainingPeaks 等主流平台都有各自版本的 GAP 計算。法語系國家稱為 VAP（Vitesse Ajustée selon la Pente），概念相同。這是全球endurance跑步／越野跑社群廣泛使用的分析工具，沒有特別集中在哪個國家，Minetti 本人任職於義大利米蘭大學。

---

## 2. Karvonen 儲備心率法（Heart Rate Reserve Method）

**原始來源**：Karvonen MJ, Kentala E, Mustala O. (1957), *"The Effects of Training on Heart Rate: A Longitudinal Study"*, *Annales Medicinae Experimentalis et Biologiae Fenniae*, 35(3), 307-315。作者 Martti Karvonen 是芬蘭赫爾辛基大學的生理學家，這是心率訓練領域被引用近 70 年的奠基性研究。

**計算式**：
```
目標心率 = 安靜心率 + (最大心率 − 安靜心率) × 強度百分比
```
其中「最大心率 − 安靜心率」稱為「儲備心率」（Heart Rate Reserve, HRR）。

**本專案運算邏輯**（`karvonenZones()`、`karvonenZoneForHr()`）：完全依照原始公式，強度切點固定在 [0, 0.6, 0.7, 0.8, 0.9, 1.0] 六個百分比（對應 5 個訓練強度區間：輕鬆恢復、有氧耐力、節奏跑、乳酸閾值、VO2 Max），依此切出每一區的心率上下限。不同運動種類可以分開設定各自的安靜心率／最大心率，賽後分段配速表會自動依賽事的運動種類套用對應區間。

**可靠性**：研究證實 %HRR（儲備心率百分比）跟 %VO2 Reserve（儲備攝氧量百分比）幾乎呈 1:1 對應（迴歸斜率 1.03，Swain & Leutholtz, 1997），比單純「%最大心率」這種更簡化的算法更能反映實際代謝強度，美國運動醫學會（ACSM）的運動處方指南也建議使用這個方法。限制在於：這個公式本身的準確度，高度依賴輸入的「最大心率」跟「安靜心率」量測得準不準——如果最大心率是用年齡公式（例如 220-年齡）粗估，而不是實測，本身就帶有約 10-12 bpm 的誤差（Tanaka et al., 2001），這個誤差會直接帶進 Karvonen 算出來的區間。

**目前泛用性**：全球運動生理學教材的標準教學內容，超過半世紀持續被教練、運動生理實驗室、心臟復健臨床場域採用，是「業界標準」等級的方法，不特別集中在哪個國家或哪個運動品牌——幾乎所有支援心率區間功能的運動手錶／App（Garmin、Polar、COROS、TrainingPeaks 等）都提供以儲備心率法為基礎的區間選項。

---

## 3. 正／負分段配速分析（Negative / Positive Split）

**原始來源**：不是單一公式或單一發明人，而是運動科學界對「配速策略」的描述性分類架構，目前研究界普遍引用 Abbiss & Laursen 的整理性回顧論文，建立了「平均分段（even）、正分段（positive）、負分段（negative）、變動分段（variable）」這套沿用至今的分類框架。

**計算式（本專案定義）**：
```
分段掉速百分比 = (後半程秒數 − 前半程秒數) / 前半程秒數 × 100%
```
數值為負代表「負分段」（後半程比前半程快），數值為正代表「正分段」（後半程比前半程慢）。

**本專案運算邏輯**（`computeSplitPercent()`）：直接用使用者手動填寫的「前半程秒數」「後半程秒數」兩個欄位代入上面的公式，正值超過 5% 標記為「掉速明顯」，負值標記為「後半程加速」。這是最簡單的二分法（只切前後兩半），沒有做更細緻的逐公里變動分析。

**可靠性**：這是運動科學界持續在辯論、尚未有定論的領域，不像 GAP 或 Karvonen 那樣是「已解決」的問題——多數研究支持負分段策略有生理學上的優勢（肝醣節省、體溫調節、減緩心血管漂移），但也有研究指出，在有配速員（pacer）陪跑的菁英馬拉松場景下，微幅正分段反而可能是更有效率的策略（例如 Kipchoge 的破 2 小時嘗試）。歷史資料也顯示，菁英馬拉松跑者的配速策略隨時代改變：1967-1988 年的世界紀錄保持者多半是正分段（前段快、25K 後掉速），1988-2018 年之後則趨向更平均的分段。換句話說，「負分段一定比較好」目前並沒有壓倒性的研究共識，比較準確的說法是「負分段是被廣泛採用、但效果因人因賽制而異的策略之一」。

**目前泛用性**：幾乎所有主流跑步 App（Strava、Garmin Connect、Nike Run Club 等）的賽事分析頁面都會顯示前後半程配速比較。Eliud Kipchoge 等菁英馬拉松選手的破紀錄賽事，常被拿來當「負分段」策略的範例引用，是教練界普遍會拿來討論配速規劃的概念，沒有特定國家集中使用。

---

## 4. 海拔爬升雜訊過濾（Elevation Gain Noise Thresholding）

**原始來源**：不是單一學術方法，而是 GPS／氣壓式高度計資料處理的業界慣用技術——因為原始高度資料（尤其是純 GPS 來源、沒有氣壓高度計的裝置）本身有 ±10~20 公尺等級的雜訊，直接加總每個取樣點之間的正值高度差，總爬升量會被雜訊嚴重灌水，因此需要設定一個「至少要連續爬升超過 N 公尺才算數」的門檻，先把雜訊濾掉。

**計算式（本專案的峰谷門檻法）**：從第一個點開始追蹤目前的「谷底」與「山峰」，海拔持續上升就更新山峰；一旦從山峰回落超過門檻值，就把「山峰 − 谷底」計入總爬升，並把目前點重設為新的谷底／山峰起點；軌跡結束時，如果峰值仍高於最後一個谷底，也計入這最後一段。

**本專案運算邏輯**（`computeElevationGain()`）：門檻預設 **2 公尺**（`threshold=threshold||2`），套用在賽事軌跡匯入（GPX/TCX/FIT）跟海拔剖面圖的爬升量計算上。

**跟業界做法的比對**：查證到 Strava 官方說明文件明確寫出他們的門檻是「有氣壓式高度計資料的活動用 2 公尺，沒有氣壓資料、純靠 GPS 反查高度資料庫的活動用 10 公尺」——本專案目前統一固定用 2 公尺，沒有依資料來源（有無氣壓高度計）動態調整門檻，對於純 GPS 來源（沒有氣壓計）的軌跡，這代表本專案算出來的總爬升量，可能會比 Strava 對同一份純 GPS 軌跡算出來的數字更高（因為用了比較寬鬆的門檻）。這是本文件盤點時發現、值得記錄下來的已知落差，不是使用者操作錯誤。

**可靠性**：這種峰谷門檻法是業界公認合理、但沒有「唯一正確門檻值」的做法——不同平台（Strava、Garmin、TrainingPeaks）門檻值本來就不完全一致，同一份原始軌跡檔案上傳到不同平台，總爬升量常有到「兩倍」量級的落差是已知現象（來自使用者社群討論串的實測經驗）。

**目前泛用性**：所有主流運動記錄平台（Strava、Garmin Connect、COROS、TrainingPeaks、Komoot、Ride with GPS）都內建某種形式的爬升雜訊過濾，是這個領域的業界標準做法，只是具體門檻與平滑演算法各家不公開細節、也不互相一致。

---

## 5. 最小平方法線性迴歸（Least Squares Regression）與 R²

**原始來源**：一般認為由法國數學家 Adrien-Marie Legendre 於 1805 年首次發表完整方法，德國數學家 Carl Friedrich Gauss 宣稱自己從 1795 年就已經在用（1809 年發表），兩人之間有著名的優先權爭議。這個方法後來被 Francis Galton（1886，迴歸概念的提出者）跟他的學生 Karl Pearson 進一步發展，奠定了現代迴歸分析與相關係數的數學基礎。R²（判定係數）是從同一套最小平方法框架衍生出來的配套統計量，用來量化一條迴歸線解釋了多少變異。

**計算式**：
```
斜率 slope = (nΣxy − ΣxΣy) / (nΣx² − (Σx)²)
截距 intercept = (Σy − slope×Σx) / n
R² = 1 − (Σ(y − ŷ)² / Σ(y − ȳ)²)
```

**本專案運算邏輯**（`linearRegression()`）：標準最小平方法公式的直接實作，用在①氣候與表現散佈圖（體感溫度 vs 配速衰退百分比）、②EPP 模型的高溫折損係數、③EPP 模型的爬升折損係數，三處都會一併算出 R² 附在結果旁邊，資料點數 n<2 或 x 值全部相同（分母為 0）時回傳 null，不會硬算出一個沒意義的斜率。

**可靠性**：最小平方法本身是統計學裡最基礎、最成熟、應用最廣泛的估計方法之一，數學上完全成立，沒有爭議。但要注意：這個方法對「離群值」（outlier）很敏感，樣本數少的時候，單一一筆異常資料就可能大幅拉歪算出來的斜率——這也是本專案在 EPP 模型裡，特別要求最低樣本數、並且把 R² 顯示給使用者看的原因（方法本身沒問題，問題在於業餘跑者的比賽紀錄筆數通常不足以支撐穩定的迴歸結果）。

**目前泛用性**：這是跨領域（不限運動科學）的通用統計工具，從經濟學、生物統計到機器學習都在用，沒有特定的運動品牌或國家代表性可言。

---

## 6. K-Means 分群與皮爾森相關係數（Jupyter Notebook 分析範本）

這兩個方法**不是在網頁 App 本身裡執行**，而是出現在「下載 Python 分析套件（.ipynb）」這個功能產生的 Jupyter Notebook 範本程式碼裡（透過 scikit-learn 的 `KMeans`、scipy 的 `pearsonr`），給使用者下載後在自己的 Python 環境執行。

**K-Means 原始來源**：概念最早可追溯到 Hugo Steinhaus（1956，波蘭），「k-means」這個名稱由 James MacQueen 在 1967 年首次使用，目前最普及的標準演算法版本則是 Stuart Lloyd 在貝爾實驗室於 1957 年提出（但遲至 1982 年才正式發表論文）。

**皮爾森相關係數原始來源**：由 Karl Pearson 於 1900 年前後正式發展，奠基在 Francis Galton 之前的迴歸研究之上。

**本專案運算邏輯**：Notebook 裡的 Module A 用 K-Means（`sklearn.cluster.KMeans`，先用 `StandardScaler` 標準化再分群）把「鞋底厚度」「足跟差」「平均步頻」三個維度分成幾群；Module C 用 `scipy.stats.pearsonr` 算「減量天數」「週跑量目標」「完賽時間」兩兩之間的相關係數。兩者都是標準函式庫呼叫，沒有自行改寫演算法。

**可靠性**：兩個都是機器學習／統計學裡極為成熟、被驗證過無數次的標準方法，可靠性不是問題；真正的限制在於**資料量**——業餘跑者能累積的比賽紀錄筆數通常是個位數到十幾筆，K-Means 分群或皮爾森相關係數在這種小樣本下算出來的結果，統計上的參考價值有限（這也是為什麼 Notebook 範本裡，兩段程式碼一開始都會先檢查資料筆數夠不夠，不夠就只印出友善提示、不強行畫圖）。

**目前泛用性**：K-Means 是資料科學／機器學習教育與實務裡最常被教授的分群演算法之一，皮爾森相關係數是幾乎所有統計學入門課程都會教的基礎工具，兩者都跨足所有使用 Python／R 做資料分析的領域，不是運動科學專屬、也沒有特定國家代表性。

---

## 7. EPP 環境折損與等價配速預測模型（本專案自創，非既有科學方法）

**⚠️ 重要聲明**：這不是任何已發表、已受學界審查、或已被其他 App／運動員採用的公開方法。這是這次開發過程中，**專門為這個 app 的資料結構跟使用情境設計的統計模型**，除了套用最小平方法這個通用數學工具之外，具體的係數萃取邏輯（例如「只在同一場賽事自己的分段之間比較坡度效應」）是本專案原創的設計決定，市面上沒有其他運動品牌或研究單位在用這一套方法，也沒有經過任何形式的外部驗證或同儕審查。

**本專案運算邏輯**：
- **高溫折損係數（α）**：沿用第 5 項的氣候散佈圖迴歸（體感溫度 vs. 相對同距離分類 PB 的配速衰退百分比），只取體感溫度 ≥15°C 的賽事資料，斜率轉換成「每升高 1°C 配速衰退幾 %」。
- **爬升折損係數（β）**：把每一場有分段配速紀錄的賽事，各自算出「這場自己的平均配速」，再把每一段配速換算成「相對這場自己平均配速的比例」，拿這個比例對該段坡度做迴歸——刻意不是拿不同賽事的總配速互相比較，避免把「賽事距離不同」造成的配速差異，錯誤歸因成坡度造成的差異。
- **基準配速**：從全馬／半馬／10K 三個經典距離分類裡，選一個使用者有資料的類別（優先全馬），取該分類目前的 PB 配速當基準。
- 兩個係數都設有最低樣本數門檻（高溫折損至少 4 場、爬升折損至少 2 場賽事＋6 個分段），沒達到門檻就顯示「資料還不夠」，不會硬算一個不可信的係數；達到門檻時也會同步顯示 R²。
- 折損瀑布圖把「基準時間」「爬升折損」「高溫折損」（已完賽的話還有「其他因素」）疊加起來，算出「AI 預測完賽時間」，但**這個預測值不會自動寫進賽前目標**，需要使用者自己按按鈕才會採用。

**可靠性**：明確標示為「未經外部驗證」。這個模型建立在兩個已知的統計限制上，使用時要有心理準備：（1）業餘跑者的比賽紀錄樣本數通常只有個位數到十幾筆，用最小平方法算出來的係數容易被單一一場「狀態特別差」的比賽帶偏；（2）模型假設「高溫效應」跟「坡度效應」可以線性疊加、彼此獨立，但實際上兩者可能有交互作用（例如又熱又陡的賽段，掉速可能不是兩個效應簡單相加），這個交互作用目前沒有納入模型。這正是為什麼系統刻意不讓這個預測值自動變成賽前目標的原因——如果這組自創的係數剛好估計錯誤，又被直接當成比賽當天的配速計畫執行，後果會比單純沒有這個功能更嚴重。

**目前泛用性**：無。這是本專案獨有的功能，沒有其他 App 或運動品牌採用同一套方法，也不是任何已發表研究的實作。

---

## 8. 鞋款磨損前後段配速比較（本專案自創，非既有科學方法）

**⚠️ 重要聲明**：同第 7 項，這是本專案針對「鞋款管理」功能自行設計的簡單比較邏輯，不是任何已發表的鞋具科學研究方法。

**本專案運算邏輯**（`computeShoeDegradation()`）：把穿同一雙鞋跑過的所有已完賽賽事，依照「穿這雙鞋累積的里程」分成「里程數低於這雙鞋預期壽命一半」跟「超過一半」兩組，分別算出這兩組的平均正／負分段百分比（第 3 項的指標），做簡單對照，看鞋子里程數增加後，後半程掉速的幅度有沒有變得更明顯。

**可靠性**：這只是一個描述性的分組平均比較，不是統計檢定（沒有算顯著性），樣本數通常也很少（一雙鞋能累積到的比賽場次有限），結果僅供參考，不宜過度解讀成「這雙鞋確實磨損導致表現下降」的因果結論——後半程掉速也可能來自賽事本身難度不同、體能狀況不同等其他因素。

**目前泛用性**：無，本專案獨有功能。市面上跑鞋壽命追蹤功能（例如 Strava 的裝備里程追蹤）通常只做累積里程統計，沒有查到其他主流 App 做這種「磨損前後配速對照」的分析。

# 使用說明（賽事紀錄 App）

版本：v2.2.1　　最後更新：隨程式碼版本同步更新（見 [CHANGELOG.md](./CHANGELOG.md)）

這份文件說明每個功能怎麼用。專案整體介紹、部署方式請看 [README.md](./README.md)。

---

## 目錄
0. [App 內建使用說明](#0-app-內建使用說明)
1. [主畫面：行事曆與篩選](#1-主畫面行事曆與篩選)
2. [新增與管理賽事](#2-新增與管理賽事)
3. [賽事詳情欄位說明](#3-賽事詳情欄位說明)
4. [資料匯入](#4-資料匯入)
5. [雲端同步（選用）](#5-雲端同步選用)
6. [深色模式](#6-深色模式)
7. [資料備份、刪除與復原](#7-資料備份刪除與復原)
8. [手機使用與加入主畫面](#8-手機使用與加入主畫面)
9. [疑難排解](#9-疑難排解)

---

## 0. App 內建使用說明

右下角有一個路跑小人圖示，點下去會直接彈出這份說明的精簡版，不用離開網站找這份檔案。內建版本涵蓋日常操作；雲端同步／推播通知的完整部署步驟仍以這份 USAGE.md 為準。

## 0a. 多語系

右上角語言選單可切換繁體中文／日本語／English，選擇會記住在瀏覽器裡。介面文字、欄位標籤、下拉選單、彈出視窗、錯誤訊息、分享圖與配速手環上的文字都會跟著切換；**你自己輸入的賽事名稱、地點、心得筆記等資料不會被翻譯**，永遠維持你輸入時的原文。中日、中英名詞對照表在 [`glossary/`](./glossary/) 資料夾。

## 0b. 全域指令控制台（v2.84.0 起）

按 `Cmd+K`（Mac）或 `Ctrl+K`（Windows/Linux）隨時呼叫出一個置中的搜尋框，不用碰滑鼠就能跳到任何一場賽事，或執行幾個最常用的操作。再按一次 `Cmd/Ctrl+K` 或按 `Esc` 關閉；方向鍵 `↑`／`↓` 選取結果、`Enter` 執行。三種輸入語法：

- **沒有前綴**：全文檢索賽事名稱／別名／城市，比對邏輯跟主畫面搜尋框完全一樣，選一筆結果按 Enter 直接跳到那場賽事的詳情頁。
- **`>` 開頭**：執行全域動作，目前收錄切換深色模式、新增賽事、管理鞋款、補給品資料庫、建立個人資訊、匯出 JSON、匯出 CSV（扁平化）、範例資料——已登入雲端同步時還會多一項「同步診斷」。這個 App 功能非常多，這裡刻意只收錄最常用的幾個，不是每個功能都塞進來。
- **`#` 開頭**：快速篩選，直接列出符合條件的賽事，選一筆按 Enter 跳過去。輸入單獨一個 `#` 會先列出目前支援的篩選關鍵字（`pb` 個人最佳、`dnf` 未完賽、`dns` 未起跑、`upcoming` 即將到來），點一下會把完整語法（例如 `#pb `）填進輸入框、直接看到符合的賽事，不用自己記語法。

## 1. 主畫面：行事曆與篩選

- 上方 `‹ 年 月 ›` 可切換月份，「今天」按鈕跳回當月。
- 手機（寬度 640px 以下）自動改成「本月賽事清單」，只列出有賽事的日期，方便點按；電腦/平板維持月曆格狀檢視。
- **滑動手勢**（v2.80.0 起，觸控裝置適用）：手機清單裡的每一張賽事卡片，可以向右滑動快速標記「已完賽」（背景會露出綠色勾勾），或向左滑動快速刪除（背景會露出紅色垃圾桶）。滑動距離要超過一定門檻，放開手指才會真的觸發——沒滑到門檻就放手，卡片會滑順地彈回原位，不會誤觸。滑到門檻之後放手是直接生效的動作：向右完賽（跟詳情頁改狀態下拉選單是同一套底層邏輯，包含無感智慧預設、裝備範本自動套用、鞋款里程門檻提醒都會一併觸發）、向左刪除（v2.88.0 起滑到底立刻刪除，不用再多一次「再次點擊以確認」——真正的安全機制是刪除本身走的軟刪除＋5 秒復原提示，詳見下面「刪除單一賽事」段落）都是一滑就定案，不是先進入某種「待確認」的中間狀態。支援觸覺震動回饋（滑動剛好超過門檻的瞬間會有一次短促震動，完賽／刪除觸發時會有一組稍長的震動）的裝置會有額外的手感回饋；**iOS Safari 完全不支援瀏覽器的震動 API，在 iPhone 上滑動一樣能正常完賽／刪除，只是不會有震動感**，這是 iOS 本身的限制，不是這個功能故障。滑動跟一般點擊開啟賽事詳情頁互不衝突，往下捲動清單也不會被誤判成滑動。
- **月曆邊緣滑動切換月份**（v2.87.0 起，觸控裝置適用）：在月曆標題列（「‹ 年 月 ›」那一排按鈕所在的地方）左右滑動，就能切換上一個／下一個月，效果跟點 ‹/› 按鈕一樣。這個手勢刻意只放在標題列，沒有放在下面整塊賽事清單上——手機版清單本身每一列都已經是「滑動完賽／刪除」的熱區（上面那條），兩種滑動如果疊在同一塊區域會分不清使用者到底想做哪一件事；標題列本身不受賽事列佔用，滑起來不會跟既有手勢打架。
- **彈出視窗改成手機版 Bottom Sheet、可下拉關閉**（v2.87.0 起）：手機（寬度 640px 以下）打開任何一個彈出視窗（鞋款管理、配速試算、匯入精靈等等），畫面會改成從螢幕底部滑出、貼齊左右下邊緣，最上方有一顆小把手——手指按著把手往下拉超過一定距離放開，視窗就會關閉（支援震動回饋的裝置會有一次短震動），拉不夠遠放開則會彈回原位。這個手勢只認把手那一小塊區域，視窗內容本身（例如捲動很長的鞋款清單）往下捲動不會被誤判成要關閉。桌機（寬度超過 640px）維持原本置中彈出的樣子，不會出現這個手勢，也不需要。
- **篩選**分兩層：
  - 第一層（全部／即將到來／歷史紀錄）永遠顯示，且各自帶有目前符合的賽事數量。
  - 點「即將到來」或「歷史紀錄」後，下方會展開對應的細分狀態（已報名／抽籤中／考慮中，或已完賽／DNS／DNF）可以再篩選；回到「全部」則收合，避免畫面一次塞太多按鈕。
  - 如果賽事的「國家」欄位有填，且明顯有一個國家佔多數賽事（超過一半），會多一個「海外賽事」標籤，點開後可以再依國家細分（例如「日本」），方便把跨國遠征的賽事單獨拉出來看；沒有明顯主場國家時不會顯示這個標籤，避免猜錯。
- 搜尋框可用賽事名稱或城市關鍵字過濾。
- 「今天」按鈕右邊的方格圖示可以切換成「獎牌牆」檢視：以相片卡片呈現所有已完賽且有封面照片的賽事，依日期新到舊排列，個人最佳會標上 PB 徽章，點卡片直接開啟該場賽事；再點一次圖示切回月曆。
- 累積 5 場賽事以上，月曆上方會出現「生涯數據」：全馬 PB 會獨立成一張置中的大字級金色榮譽卡片；下方是總完賽場次、當年累計里程、生涯累計爬升，數字會有滾動跳動的動畫效果（只有數值真的變動時才會播放），旁邊附一個歷年完賽場次的小長條圖，下方還有一個每季總爬升量的趨勢線圖（至少要有 2 個季度的資料才會出現）。再下面是**氣候與表現散佈圖**：把路跑（越野賽因為爬升雜訊太大不列入）已完賽賽事，依 10K／半馬／全馬三個經典距離分別找出自己的 PB 配速當 100% 效率基準，橫軸是那場賽事的體感溫度、縱軸是相對於同距離 PB 的配速衰退百分比，同一張圖上就能看出「跑得比較慢」到底是因為那天比較熱、還是純粹狀態不好——距離不同的賽事因為都換算成「相對自己 PB 差多少 %」，可以直接放在同一張圖比較。圖上會用淺綠色色塊標出「最佳作戰溫度帶」（依你 PB 或效率 98% 以上的賽事溫度範圍抓出來的，色塊上方直接標「甜蜜點」字樣），紅色虛線是線性迴歸算出來的趨勢線，兩個軸都有標題（橫軸「體感溫度」、縱軸「配速衰退（相對自己 PB）」），右上角有圖例說明金色點是 PB、藍色點是其他賽事；把滑鼠移到任一個點上，會顯示那場賽事的名稱、體感溫度與衰退百分比。下方文字會直接告訴你「氣溫每升高 1°C，配速平均衰退約幾個百分點」，同時附上 R²（這條迴歸線解釋了多少變異）跟採用的賽事數——R² 偏低（低於 0.4）時，趨勢線會改用更淡、更稀疏的虛線呈現，下方也會多一行提醒「這條線僅供參考，不是穩定的規律」，避免資料點太少、太分散時，這條線看起來還是一樣篤定。至少要有 4 場符合條件的賽事（路跑、已完賽、經典距離、有填體感溫度）才會出現這張圖，資料不足時就先不顯示，不會硬畫一張只有兩三個點、看不出趨勢的圖。再下面是類似 GitHub 貢獻度的參賽熱力圖（年為列、月為欄，顏色深淺對應當月總里程，滑鼠移上去看場次與里程明細）。
- **榮譽櫃（徽章系統）**：生涯數據最下方是「榮譽櫃」，橫跨探索與疆域、地形與極限、速度與戰略、跨界與裝備四大類，總共近百個徽章，四大類別跟底下每個類別都可以各自收折。未解鎖的徽章顯示灰色剪影加問號跟解鎖條件提示，累積型的徽章（例如「累積完成 3 場日本賽事」）還會顯示進度條；已解鎖的徽章名稱下方會標明實際達成的條件，以及「達成於：某場賽事（日期）」（沒有對應單一觸發賽事的徽章則顯示達成日期）。
  - **快速篩選**：榮譽櫃上方有「全部／已解鎖／進行中／未達成」四個篩選鈕。「進行中」專門用來找「只差一步」的目標——只顯示已經有進度、但還沒解鎖的徽章，不分類別、直接依進度百分比從高到低攤平排序（真正最接近解鎖的徽章不會被埋在某個類別區塊裡），每張卡片會標一個小小的類別標籤方便辨識原本屬於哪一類；「未達成」則是完全還沒起步（進度 0%）的徽章，一樣按類別分區顯示。切換篩選只會換掉榮譽櫃裡面的內容，收合／展開狀態不會被重置。
- **年度回顧長圖卡**：生涯數據裡「歷年完賽場次」長條圖下方，會有一個下拉選單列出每個有完賽紀錄的年份（預設選最新的一年），選好年份後按旁邊的「下載」，就會直接下載一張那個年度的直式回顧圖（1080×1920，適合發到社群限時動態），不用另外開視窗設定。內容包含：該年度完賽場次、總里程、總爬升、破 PB 次數（依每場賽事「成績與分析」裡有沒有勾選「這是我的 PB」統計，不是系統自動判斷）、新解鎖徽章數（依徽章實際解鎖日期落在那個年度計算）、當年穿最多場次的鞋款、以及當年氣溫紀錄最低跟最高的那兩場賽事（要那場賽事的「實際體感溫度」欄位有值才會列入，不論是歷史天氣自動回填或自己手動填寫的都算）。鞋款、氣候紀錄這兩個區塊，只有那個年度真的有對應資料時才會出現，沒有鞋款紀錄或沒有任何一場賽事填過氣溫，就整段不顯示，不會出現空白或「—」的欄位。下方剩餘的空白處，會鋪一排「今年跑過的路線」——當年每一場有存軌跡點（GPX/TCX/FIT 匯入才會有）的完賽賽事，各自畫一條去背、不含地圖底圖的極簡路線形狀，依比賽日期排列成 3 欄的拼貼，最多顯示 9 場，超過的話下面會註明「+N 場」。畫面空間不夠（例如鞋款、氣候紀錄兩段都出現，把版面吃掉大半）或當年完全沒有存過軌跡點資料時，這段就不會出現，不會硬擠成一團看不出形狀的線。
- **動態回顧輪播（Story Mode）**：行事曆上方導覽列（「今天」按鈕右邊那排圖示按鈕旁）會出現「{年份} 年度回顧」按鈕——只有在「有完賽、而且有上傳封面照片」的賽事存在時才會顯示，抓的是最近一個有符合條件賽事的年份；完全沒有符合條件的賽事就不會顯示這個按鈕。點下去會以全螢幕、類似 IG 限時動態的方式播放：開頭一張年份標題頁，接著依日期先後、一張一張播放那個年度每場有封面照片的完賽賽事，畫面下方顯示賽事名稱、日期、距離，如果那場賽事有填「實際體感溫度」或「當日氣候狀況」，也會一併顯示在同一張畫面上（沒有天氣資料的賽事就不顯示這一行）。每張畫面上方有進度條，5 秒自動播放下一張；也可以點畫面左半邊回上一張、右半邊到下一張，或用鍵盤左右鍵、Esc 關閉。播完最後一張會直接關閉，不會循環播放回開頭。
- **裝備清單改用抽屜面板編輯**（v2.91.0 起）：賽前準備區塊裡的裝備清單，不再直接把整份表單攤在頁面上，改成一張摘要卡片（顯示「N 項・已打包 M」），點一下才滑出面板來編輯。桌機版從畫面右側滑入側邊欄（左邊的內容不會被遮住，可以一邊對照一邊編輯）；手機版從底部滑上來，頂端有一條小灰橫條，跟這個 App 其他彈窗在手機上的樣式一致。關閉方式有四種：右上角 ✕、點面板外的背景、按 Esc、以及手機的系統返回鍵（返回鍵會先收起面板，不會直接離開網站）。關閉後摘要卡片上的數字會立刻反映剛剛的修改。面板裡的所有操作跟原本完全一樣——編輯欄位一樣即時自動儲存、新增／刪除項目、套用與另存範本、切換佈署（Kanban）檢視、拖曳排序都照舊，不是簡化版。**這是目前唯一改用這種模式的區塊**，其他五個區塊維持原本直接展開的形式。
- **裝備重量與推重比**：裝備清單每個項目新增兩個選填欄位——「重量（克）」與「消耗品」勾選框。「消耗品」是指水、能量膠這類賽事中會被吃喝掉、越用越輕的東西，跟「分類」欄位（頭燈、服裝這種裝備種類）是分開的兩件事，需要自己勾選，系統不會自動用分類去猜。只要清單裡有任何一項填了重量，裝備清單上方就會出現一個小儀表板：甜甜圈圖顯示基礎重量（不含消耗品）跟消耗品重量的比例與總重量；如果不是每一項都填了重量，會註明「僅計算已填寫重量的 X/Y 項裝備」，不會把沒填的項目當成 0 克算，避免總重量失真。如果已經在「個人資訊」填過體重，儀表板下方還會多顯示「裝備佔體重比例」跟「總負重（人＋裝備）」，賽道起伏大的越野賽事這個數字對評估負重很有參考價值；沒填體重的話這兩行就不會出現。範本系統（套用範本、另存為範本）也認得這兩個新欄位，重量跟消耗品標記會一起存進範本、下次套用範本到新賽事時一併帶入，不用每場賽事重新輸入同一件裝備的重量。
  - **空狀態快速套用標配範本**（v2.83.0 起）：裝備清單是空的時候，如果這場賽事的運動種類剛好有對應的內建標配範本（路跑、越野、三鐵、自行車、游泳都有），「尚未新增」提示下方會多一個一鍵套用的按鈕，直接顯示要套用的範本名稱。這種情況通常不會太常遇到——新增賽事的當下，系統就已經依運動種類自動套用過一次標配範本了，會走到清單真的是空的這裡，通常是運動種類選了「其他」（沒有對應的內建範本，這個按鈕不會出現），或者是自己把清單全部清空了、想快速拿回標配範本重新開始。這個按鈕套用的是內建範本，跟上面「套用範本」下拉選單（放的是自己存的範本）是兩套不同的東西，各自獨立。
  - **空狀態的微光呼吸背景**（v2.90.0 起）：全站所有「尚未新增」「目前沒有…」這類空白區塊（裝備清單、CP 明細、補給時程、資料復原、獎牌牆等等，共 7 處以上），背景都會有一層極淡、幾乎看不出色相的冷灰微光，以 9 秒一輪的速度緩慢對流、明暗呼吸——目的是讓空白區塊看起來像「準備好等你填入資料」的待機狀態，而不是「這裡是不是壞了」的死白。顏色刻意壓得非常淡，不會比真正有資料的地方還搶眼。
  - **開箱動畫**：賽事狀態切成「已完賽」的當下，只要剛好觸發新徽章，會立即彈出全螢幕五彩紙屑動畫配上金色徽章卡片；有些徽章的達成條件不是綁定單一場賽事（例如鞋款退役、累積使用滿 5 雙鞋），這類徽章一樣會在達成的當下立即開箱，不用等一個不會出現的時機。這個功能上線前就已經達成的舊資料，會在你之後點進「當初觸發那個徽章的賽事」時才補放動畫，不會一開網站就被轟炸。開啟系統「減少動態效果」設定的話會跳過動畫直接顯示結果。
  - **下載成就圖**：開箱動畫上「下載成就圖」按鈕，或榮譽櫃裡任何一個已解鎖徽章下方的同名連結，都可以下載一張 9:16 直式圖片（徽章圖示、名稱、達成條件、達成方式），適合直接發布到社群限時動態；開箱動畫按下載不會關掉動畫本身，可以下載完再按「收下徽章」。如果這個徽章有對應到單一觸發賽事，圖片下方會補上那場賽事的關鍵數據（距離、總爬升、完賽時間、平均配速，視資料是否齊全顯示）；如果那場賽事也有匯入 GPS 軌跡，再下面會畫出路線軌跡（去背，只留線條本身，不含地圖底圖，起點綠色圓點、終點紅色圓點）。沒有對應賽事、或該賽事沒有軌跡資料時，對應的區塊就不會出現，不會是空白區塊。
- **備戰釘選**：賽前狀態的賽事若在 14 天內開賽，主畫面最上方會自動獨立顯示一張卡片，展開裝備打包進度（圓形進度環，開啟時有填滿動畫）、目標配速策略、天氣預報，減量期開始日一到也會自動標示。這場賽事如果有封面照片，卡片上方會變成全滿版照片背景搭配巨大倒數天數的「英雄」樣式，捲動頁面時背景圖會有輕微視差位移效果；**沒有照片時（v2.90.0 起）會改用一層緩慢流動的漸層背景**——金色與森林綠兩塊高度模糊的光暈以將近 20 秒一輪的速度來回漂移、交融，讓沒有照片的備戰卡片也有「正在蓄力」的氛圍，不是一片空白的底色。同時有好幾場賽事符合條件時只顯示最近的一場，可以「暫時隱藏」（重新整理頁面後會再出現）。
  - **即時天氣預報**：距離開賽 7 天內、且已經匯入 GPX/FIT 軌跡（用軌跡第一個點的座標查詢）的賽事，天氣預報卡片會出現「抓取即時預報」按鈕，透過免費、免金鑰的 Open-Meteo API 直接在瀏覽器抓取當日逐時氣溫、降雨機率與陣風風速，自動挑出最接近起跑時刻的一筆顯示。抓回來的資料會存在賽事本身，不會每次打開都重新抓，按「重新整理預報」可以手動更新。沒有匯入 GPS 軌跡的賽事會改顯示提示，不會出現抓取按鈕；網路連線有問題時會顯示錯誤訊息，不會整個卡住。
- **歷史賽事自動回填當天實際天氣**：日期已經過去、且已經匯入 GPX/FIT 軌跡的賽事，打開該場賽事詳情頁時，「氣象」分頁會自動在背景查詢當天實際發生過的天氣（氣溫、降雨量、陣風風速、風速、風向，一樣挑最接近起跑時刻的一筆），不需要按任何按鈕。這跟上面即時天氣預報用的不是同一個 Open-Meteo 端點——預報端點回傳的是「未來會不會下雨」的機率，歷史查詢用的是「歷史氣象回溯資料庫」端點，回傳的是當天實際量測／回算出來的降雨量，兩者性質不同、數字不能直接比較。抓到之後永久存在賽事本身、不會過期，不用重新整理；沒有 GPS 軌跡的歷史賽事不會觸發查詢。這個查詢只會在「打開這場賽事詳情頁」的當下觸發一次，同一次瀏覽期間同一場賽事最多只會嘗試一次（不管成功或失敗），不會因為查詢失敗就不斷自動重試而對外部服務發出大量請求；重新整理頁面後會再給一次機會。
  - **順便自動填欄位**：查到資料後，如果下方「實際體感溫度」「當日濕度」「風速」「風向」這幾個手動欄位還是空的，會直接用查到的數字幫忙填上（一樣挑最接近起跑時刻的那一筆，風向會轉成「東北風」這種中文慣用說法）；當天資料如果查到有降雨量（大於 0），「降雨機率」會直接填 100%、「當日氣候狀況」直接選「雨」——這是已經發生過的事，用「有下雨／沒下雨」二選一直接對應機率的兩個極端；已經填過的欄位一律不會被覆蓋，尊重使用者自己輸入或修改過的值。
  - **歷年同日平均氣溫／濕度**：跟上面「當天實際天氣」是分開的另一個背景查詢——用同一個地點、同一個月日，往前五年（不含賽事當年，當年的實際值已經有前面那張卡片）分別查一次歷史資料再取平均，填進「歷年平均氣溫」「歷年平均濕度」這兩個欄位。Open-Meteo 沒有「同一天橫跨多年」這種查詢方式，只能拆成 5 次個別查詢、平行送出；某些年份查詢失敗不影響其他年份，只要至少查到 1 年的資料就會顯示平均值，不用 5 年都成功。賽事日期若剛好是閏年 2/29，非閏年的年份會直接跳過那一年（不會拿 2/28 去湊），平均只算真的有這一天的那幾個年份。
  - **海象紀錄（三鐵／游泳專用）**：運動種類是三鐵或游泳、賽事日期已過、且已經匯入 GPX/FIT 軌跡時，「氣象」分頁會額外自動在背景查一次當天的海象資料（浪高、湧浪週期、海水溫度），資料來源是 Open-Meteo 的「海洋氣象」資料庫，跟上面天氣用的端點是分開的。路跑、自行車這類沒有開放水域游泳段的賽事不會觸發這個查詢。海洋氣象模型只在實際海面上的座標網格才有資料，如果軌跡起點的座標落在陸地網格上（例如量到的第一個 GPS 點還在岸上、尚未下水），會查不到資料，這是模型本身的限制，不是查詢設定錯誤；查無資料時就跟其他背景查詢一樣安靜放棄，不會顯示錯誤訊息。海水溫度低於 18°C、且完賽時間超過 2 小時，這段區塊下方會額外出現一個「❄️ 高失溫風險環境完賽」的提示。
  - **開放水域 GPS 降噪（三鐵／游泳專用）**：路線軌跡地圖下方，三鐵或游泳賽事只要有匯入含時間戳記的 GPS 軌跡（（重新）匯入 GPX/TCX/FIT 之後才有），就會出現「套用開放水域 GPS 降噪」按鈕。開放水域游泳時，手錶每次划手手腕沒入水中就會斷一次 GPS 訊號，浮出水面才重新定位，單點誤差常見到 10 公尺以上，直接加總距離容易嚴重虛高。按下按鈕會先跑一次卡爾曼濾波（等速度運動模型，把每個點的位置當「量測」、拿運動模型當「預測」，兩者依不確定性加權平均），把降噪前後的總距離攤開來讓你比較，不會直接套用；確認要套用時，可以選「只更新地圖軌跡」或「連同賽事的距離欄位一起更新成降噪後的數字」，選哪個都不影響地圖軌跡本身一定會更新成平滑後的版本。降噪能大幅壓低雜訊，但沒辦法完全還原真實路徑，套用後的距離仍然是估計值，不是精確值；套用過一次後這個按鈕會消失、改顯示「已套用」，避免重複套用。
- 「今天」按鈕右邊除了獎牌牆圖示，還有一個表格圖示可以切換成「緊湊表格檢視」：類似 Excel 的純文字密集表格，欄位是日期、賽事名稱、狀態、距離、總爬升、完賽時間，跟著目前篩選條件走，適合跨年比對歷屆體能表現，點列可直接開啟該場賽事。

## 2. 新增與管理賽事

- 點右上角「+ 新增賽事」，填賽事名稱、運動種類、狀態、比賽日期後建立。建立時若有日期，行事曆會自動跳到該月份。
- 「+ 新增賽事」下方的「範例資料」可以一鍵建立五場示範賽事（都集中在當年度 9-12 月、狀態涵蓋已報名／抽籤中／考慮中，方便直接體驗賽前規劃相關的功能），把幾乎每個欄位都填好範例內容，適合第一次使用時參考每個欄位大概怎麼填；其中越野賽跟鐵人三項的裝備清單特別示範了佈署檢視（Kanban）——越野賽有一項裝備先放在 CP 轉換袋，鐵人三項則示範了 T1／T2 轉換區。範例名稱都有「範例｜」前綴，用一般的刪除功能就可以移除；已經有範例存在時再按一次不會重複建立。
- 新增賽事頁面可直接貼上「賽事官方網站」連結；也可以先按「匯入運動紀錄檔」或「匯入賽事主視覺」，選好檔案後畫面上會顯示已選取的檔名或縮圖，按下「建立賽事」時才會一併套用到新賽事（距離、爬升、心率等數據，以及封面照片）。
- 點月曆或清單上的賽事即可開啟詳情頁進行編輯，欄位修改會即時自動儲存，不需要按「儲存」；標題列會短暫顯示「儲存中…」「已自動儲存 ✓」提示，不用擔心改了沒存到。
- 詳情頁標題下方有一排快速導覽按鈕（基本／路線／氣象／預算／裝備／賽後），點擊可以直接跳到對應區塊並自動展開，不用手動捲動翻找。
- 詳情頁右上角「刪除賽事」需要連按兩次確認，避免手滑誤刪。
- 「複製賽事」：保留地點、路線、裝備清單、官方網站等欄位建立一場新賽事，適合同系列跨年賽事；名稱裡如果有西元年份會自動 +1，日期／報名結果／成績／心得會清空讓你重新填寫。
- **無感智慧預設**（v2.78.0 起）：詳情頁編輯基本資料時，系統會順手推論幾個「大概率接下來也要填」的欄位，減少手動點選次數，被自動帶入的欄位會有短暫的綠色高亮動畫提示——每一項都只在對應欄位「還沒被你動過」時才會生效，一旦你自己選過、填過，之後就算觸發條件的欄位又改了，也不會再被蓋掉：
  - 填「賽事名稱」時，如果「賽事系列」還是空的，會自動剝掉名稱裡的西元年、民國年、「第X屆」（阿拉伯數字或中文數字都認得），剩下的部分當系列名稱猜測值填進去；如果名稱整段都是年份／屆數、剝完什麼都不剩，就不會亂填。
  - 填「賽事名稱」或「總距離」時，如果「主要運動種類」還是預設的「路跑」，會依名稱關鍵字（越野／Trail／UTMB → 越野跑；泳渡／游泳 → 游泳；三鐵／鐵人三項 → 三鐵；自行車／單車 → 自行車等）自動切換；名稱沒有對應關鍵字、但距離超過 50 公里，會切換到「超級馬拉松」。名稱關鍵字判斷優先於距離——距離只能分辨是不是超馬距離，沒辦法分辨路跑跟越野這種質地不同的運動。
  - 改「比賽日期」時，如果日期改到今天之前、狀態還是「考慮中」，會自動切換成「已完賽」；勾選「本次是否抽中」、狀態還是「考慮中」或「抽籤中」，會自動切換成「已報名」。
  - 運動種類如果因為上述推論被連帶改掉，原本「換運動種類自動帶入對應裝備範本」的機制（見下方賽事詳情欄位說明）也會一併觸發，不會因為這次改動不是使用者直接點選運動種類欄位就被跳過。

## 3. 賽事詳情欄位說明

詳情頁依賽事目前狀態，自動展開跟現在階段相關的區塊（例如已完賽的賽事會自動收起預算規劃、展開賽後紀錄），「基本資訊與時間」則固定展開。

- **生命週期軌道**（v2.79.0 起）：標題正下方有一條可以直接點擊切換的進度軌道，把「考慮中→抽籤中→已報名→已完賽」這個一般賽事會經歷的四個階段畫成連續的節點——已經過的節點是綠色打勾、目前所在的節點是金色並帶有輕微的脈動效果、還沒到的節點是淺灰色。四個節點任何時候都可以直接點擊切換，不用先點開「基本資訊」區塊找下拉選單；下方另外有「標記未出賽 (DNS)」「標記未完賽 (DNF)」兩個小連結——這兩個不是接在後面的第五、第六個階段（賽事已經到了比賽日，DNS／DNF 是這個時間點的另一種結果，不是「完賽之後」才會發生的事），畫面上會把最後一個節點換成橘紅色的驚嘆號樣式來表示；已經是 DNS／DNF 的時候，直接點回前面任何一個節點就能改回正常狀態，不需要另外找「復原」按鈕。這條軌道跟「基本資訊」區塊裡原本的狀態下拉選單是同一份資料、雙向同步——不管從哪邊改，另一邊都會跟著更新，兩種操作方式都可以用，選單也還在（含 DNS、DNF 選項，跟軌道上的兩個連結是同一組選項，只是兩個不同的操作入口）。

- **封面照片**：標題下方「+ 新增封面照片」可上傳一張照片，會保留完整構圖等比縮小壓縮，純瀏覽器處理不會上傳到任何伺服器。上傳後會變成標題整個區塊的淡化背景底圖（淡化程度跟著深色/淺色模式自動調整，文字維持清楚可讀）；直式照片會自動把標題區塊加高，避免被壓成一小條。同一張照片也會以小縮圖顯示在行事曆格子、手機版清單、獎牌牆檢視裡——這些小縮圖會把照片裁成正方形，如果主角（例如臉部）沒有正好在正中央，可以按「調整焦點」在完整照片上點選或拖曳決定裁切基準點，標題背景本身完整顯示不裁切、不受焦點設定影響。上傳時也會自動分析照片主色調，套用到倒數進度條顏色，讓每場賽事的頁面有專屬氛圍；賽事進入 7 天內的緊急倒數時會維持警示紅色，不受主色調影響。
  - **環境光渲染**（v2.89.0 起）：打開一場有封面照片的賽事詳情頁，從這張照片抓出的主色調不只套用在倒數進度條，還會以極淡的比例（約 4%）暈染整個頁面的背景色，畫面最上方也會出現一層呼應這個顏色的柔光，往下捲動時逐漸淡去——效果類似 Spotify、Apple Music 這類影音 App 打開一張專輯時「周遭氛圍也跟著換色」的沉浸感，離開這場賽事、切到別的賽事或回到月曆時會平順地淡回原本乾淨的底色（淡入淡出用 0.8 秒的過渡動畫，不是瞬間切換）。混色比例刻意壓得很低，不管照片抓出來的主色調多鮮豔（甚至是螢光黃這種極端亮色），文字跟背景的對比度都還是遠遠超過無障礙閱讀的基本標準，不會因為換了氛圍就變得看不清楚內容。這個效果仰賴瀏覽器對 CSS 混色函式的支援，不支援的舊版瀏覽器會直接跳過這個效果、維持原本純粹的底色，不影響其他功能正常使用。
- **基本資訊與時間**：名稱、別名、**賽事系列**（同一場賽事每年填一樣的名稱，如「臺北馬拉松」，系統會自動比對同系列的歷屆成績，顯示在詳情頁最上方的比較表）、狀態、運動種類、時間、抽籤資訊。
- **地理與路線**：距離、爬升、地形、**海拔變化圖**（有 CP 明細或透過 GPX/TCX/FIT 匯入時自動畫出，會依坡度上色，綠色平緩、紅色陡峭，一眼看出最硬的爬坡在哪；滑鼠移到圖上會顯示游標，即時看到該點的累積里程、海拔、瞬時坡度，如果賽事也有匯入 GPS 軌跡，路線地圖上會同步出現一個標記跟著移動，下方「分段配速一覽」表格裡對應的那一列也會同步亮起——三邊互相聯動，從任一邊移動滑鼠都會同步另外兩邊，不是只有海拔圖能發動；游標經過的坡度跨過陡坡門檻（±8%，不管是進入還是離開）的那個瞬間，支援網頁震動的裝置會震動一下，模擬滑過一道隱形阻力牆的物理感（iOS Safari 目前完全不支援網頁震動，這台裝置上聯動一樣正常運作，只是不會有震動感）；圖上的白色虛線標出 CP 站點位置，旁邊的小箭頭標示這個 CP 是落在爬升後（⌃/↗）還是下坡後（⌄/↘））、**路線軌跡地圖**（透過 GPX/TCX/FIT 匯入且檔案內有座標資料時才會出現，需要網路連線載入地圖圖磚；右上角可切換標準地圖／地形圖，越野跑、超馬、障礙賽預設顯示地形圖；深色模式下地圖底圖也會自動轉為深色）、CP / 補給站明細、ITRA/UTMB 點數（官方公告值，需自行查詢填入，非自動計算）。
  - **空間記憶迴廊（照片時空對位）**：路線軌跡地圖下方，可以把賽事當天拍的照片拖曳或選擇進來，系統會自動讀取每張照片的 EXIF 拍攝時間（只讀檔案的中繼資料跟內嵌縮圖，不會把整張原始照片載入網頁，不管單張檔案多大都不影響效能），跟這場賽事的 GPS 軌跡做時間對位，算出拍下那一刻的座標、海拔、心率、局部配速，變成地圖上一個圓形縮圖圖釘，點下去會彈出照片跟這些數據。上方有一個「相機時間與 GPS 時間偏差校正」滑桿（±120 秒），因為相機的時鐘常常跟 GPS 裝置差個幾秒到幾分鐘，拖動滑桿會即時重新對位所有已加入的照片，不用重新選檔案。拍攝時間如果早於軌跡起點或晚於終點，代表這張照片可能不是這場賽事拍的（或裝置時鐘沒對好），會標記為「未對位」、不會亂猜一個座標顯示出來；沒有 EXIF 拍攝時間的照片（例如截圖）會直接略過，不會中斷其他照片的處理。
    - **這個功能需要的是「帶時間戳記」的 GPS 軌跡（`route.timedTrackPoints`），這個欄位是這個功能上線（v2.68.0）之後才開始存的**；在那之前匯入的 GPX／TCX／FIT，只存了經緯度、沒存時間戳記，沒辦法拿來對位照片，而且這件事沒辦法回頭補——時間戳記本身沒有存在任何地方，唯一還留著的只有電腦裡的原始檔案。這種情況下這個區塊不會整段消失，而是顯示一段說明，告訴你要重新匯入原始檔案才會補上這份資料；完全沒有匯入過 GPS 軌跡的賽事（例如手動輸入），則維持原本整段不顯示，不會出現不相關的提示。重新匯入之後，這個功能跟開放水域賽事適用的 GPS 降噪（兩者用的是同一份資料）會一起變得可用。
    - **這個功能只對「（重新）匯入 GPX/TCX/FIT 之後」的賽事有效**：既有的路線軌跡地圖只存經緯度、沒有存拍攝時間對位需要的時間戳記，如果這場賽事的軌跡是很早之前匯入的，需要重新匯入一次原始檔案，這個功能才會出現。只支援 JPEG 格式的照片（EXIF 內嵌縮圖是 JPEG 格式的規範限制，RAW 檔案目前不支援）。
- **氣象**：歷年平均氣候（賽前參考）與比賽當日實際天氣（賽後填寫）。
- **預算與行程規劃**：報名費用、住宿、交通、隨行人員。
- **裝備、補給與戰略（賽前準備）**：目標時間（A/B/C）、**配速試算與手環產生器**（見下方說明）、裝備清單（可套用「路跑標配／越野標配／三鐵標配／自行車標配／游泳標配」五份內建範本，或自訂範本重複使用；新增賽事或變更運動種類時，若清單還是空的會自動套用對應範本，越野跑/超馬/障礙賽會自動把頭燈、求生毯等標記為必填，已有內容的清單不會被覆蓋）、補給時程規劃（逐點記錄預計補給，賽後可勾選「已補給」跟計畫比對）、訓練計畫。裝備清單與補給時程這兩個清單，每筆資料可以拖曳左側把手調整順序，或用「上移／下移」按鈕（適合觸控裝置與鍵盤操作）。自行車、游泳賽事會隱藏「比賽鞋款」追蹤，三鐵／二鐵因為仍有跑步項目維持顯示。
  - **環境折損預測（EPP）**：「賽前目標」下方會出現這個區塊，先從自己過去的完賽紀錄統計出兩個專屬係數——「高溫折損」（同一個經典距離分類裡，拿自己 PB 當基準，看體感溫度每升高 1°C 配速平均衰退幾 %，至少要有 4 場體感溫度 ≥15°C 的路跑賽事才會顯示）、「爬升折損」（只在同一場賽事「自己的分段」之間比較坡度跟配速，不會拿距離不同的賽事互相比較，至少要有 2 場、合計 6 個有效分段的分段配速紀錄才會顯示）。兩個係數旁邊都會附上 R²（這條迴歸線解釋了多少變異）跟採用的樣本數——業餘跑者的比賽紀錄通常沒幾筆，硬算出來的係數很容易被單一一場特別好或特別差的比賽帶偏，R² 低就代表這個數字不太可信，不是只看係數本身漂不漂亮。兩個係數都準備好、且這場賽事有填距離時，會顯示一張折損瀑布圖：基準時間（用你最長距離分類的 PB 配速當基準）開始，往上疊爬升折損、往上疊高溫折損，已完賽的話最後還會有一段「其他因素」（實際成績跟前面三段加總的差距，可能是技術地形、體能狀態、補給節奏等模型沒算進去的因素），最後一根柱子是預測（或實際）完賽時間。這個預測時間**不會自動寫進賽前目標**——瀑布圖下方有獨立的「採用為 A／B／C 目標」按鈕，要不要拿這個估計值當目標，是自己主動的選擇，不是系統幫忙決定的；模型算出來的數字如果剛好是錯的，直接照著在比賽當天執行，後果比模型不準本身嚴重得多。
    - **逐公里預測**：這場賽事有海拔剖面資料（匯入 GPX/TCX/FIT，或手動填了海拔變化圖）時，預測按鈕下方會出現「顯示逐公里預測」，展開後是一張表格，把賽事切成固定長度的區段（預設每 1 公里，超過 15 公里的長距離賽事自動放寬到每 3 公里，避免表格長到失去意義），列出每一段的淨爬升（下坡會顯示負值）、這一段的預測配速、預估抵達時刻。這比上面瀑布圖用的「全場總爬升攤平成一個平均坡度」精細很多——爬升折損跟高溫折損都會依照賽道實際的起伏分段計算，下坡段因為係數是從你自己真實的上坡變慢、下坡變快資料迴歸出來的，預測配速可以合理地比基準配速更快（不會被硬夾在「最多只會變慢」）；如果這場賽事有填起跑時間、且賽事日期在 7 天內抓到了逐時天氣預報，每一段的體感溫度也會依照「模擬跑到這一段時的預估時鐘時間」動態抓對應時段的預報值，而不是整場都套同一個溫度——這樣清晨出發、中午還在賽道上的長距離賽事，後段抓到的預測溫度會比前段更高，更貼近實際情況。沒有海拔剖面資料時看不到這個表格，只能用瀑布圖那個較粗略的全場平均估計，區塊下方會說明原因。
  - **補給品資料庫**：補給時程規劃標題右邊「補給品資料庫」按鈕，可以建立常用補給品（例如某款能量膠）的碳水、鈉、咖啡因含量，這份清單是全域共用的，不綁在單一賽事底下，建一次、每場賽事都能重複選用。每一筆補給時程規劃可以（選填）從這份資料庫選一項，選了會自動帶入品名，只需要再填「數量」——填好數量後，畫面上方會自動加總這場賽事目前已連結資料庫項目的總碳水、總鈉（單位 g／mg），沒有連結資料庫、純手動輸入文字的項目（例如「香蕉」）不會計入加總，畫面上會註明還有幾項沒被算進去。刪除某個資料庫項目不會連動刪除已經用過它的補給時程規劃列，那一列會變成「未連結資料庫」，不會出錯或憑空消失。
  - **裝備佈署視圖（Kanban）**：裝備清單標題右邊有一個切換檢視的圖示按鈕，切換成佈署檢視後，畫面會變成依「放置位置」分類的看板——隨身攜帶、起點寄物袋、終點寄物袋是固定選項；三鐵／二鐵賽事另外多出 T1（游轉騎）、T2（騎轉跑）兩個轉換區；「路線」分頁的 CP 明細裡只要有勾選「可寄放裝備」，就會自動多一欄「轉換袋・那個 CP 的名稱」——這些欄位都是跟著賽事本身的設定即時產生，不需要另外手動維護一份清單。每張裝備卡片可以直接拖曳到想要的欄位裡，也可以用卡片上自帶的下拉選單「移動到…」達到一樣的效果（觸控裝置或不方便拖曳時的替代做法）；卡片上的核取方塊可以直接勾選「已打包」，不用切回清單檢視。每一欄最上面會顯示這一欄自己的打包進度（例如「2/3」）跟一條小進度條，一眼就看得出「這個寄物袋到底還缺什麼」。裝備如果一開始還沒指定位置，會先落在「尚未分類」那一欄；CP 站被刪除、或賽事種類改成非三鐵導致 T1/T2 選項消失時，原本擺在那個位置的裝備也會自動歸到「尚未分類」，不會憑空消失不見。切回清單檢視隨時可以看到完整欄位（分類、重量、消耗品、強制裝備等），佈署檢視是額外的排列方式，不是取代清單檢視。

## 疲勞模式（賽事進行中用）

「更多操作」選單裡有「開啟疲勞模式（賽中用）」。這是設計給賽事**進行中**用的全螢幕簡化介面：長距離賽事後段雙手可能濕滑沾泥、認知能力因為極度疲勞而下降，這時候任何需要精準點擊的操作都是負擔，所以疲勞模式捨棄所有按鈕跟精細操作，改成「整個畫面隨便哪裡、往左或往右大動作滑」：

- **向右滑動＝標記抵達目前的 CP 站**，畫面會自動切換顯示下一個 CP 站的資訊，同時（如果瀏覽器支援）語音唸出下一站的名稱跟距離。
- **向左滑動＝標記下一項補給計畫為「已補給」**（跟補給時程規劃裡的「已補給」勾選欄位是同一個資料，畫面上會直接反映）。
- 每次滑動操作都會有短震動回饋（手機瀏覽器支援的情況下），讓你不用盯著螢幕確認「有沒有滑到」。
- 畫面背景是純黑色，文字是大字級金色，除了省電（OLED 螢幕純黑不發光）之外，也是為了在體力耗盡、視覺處理能力下降時還能一眼看懂。
- 開啟疲勞模式時會嘗試鎖定螢幕常亮（瀏覽器支援的情況下），不用一直解鎖手機才能看數據。
- 滑錯了可以按左上角「復原上一步」，會把剛剛那個動作（CP 抵達或補給勾選）復原，只記得最近一次操作。
- 「結束疲勞模式」按鈕在右上角，隨時可以退出回到一般畫面。
- **心率區間對照**：上方兩個大按鈕之一，按下去會切換成心率區間對照卡，列出這場賽事對應運動種類的儲備心率區間（Z1~Z5，優先用「個人資訊」裡設定好的安靜心率／最大心率算出來的個人化區間，沒設定時退回用這場賽事填的最大心率粗估）。**這張卡片顯示的是賽前就算好的參考區間，不是即時心率讀數**——App 沒有跟心率錶做藍牙連線，讀不到你手錶上當下的心率數字，這張卡片是給你拿手錶顯示的 bpm 數字來對照用的。每個區間除了顏色，還會搭配一個「柱狀強度」圖示（1~5 根直條，區間數字等於填實的柱子數量），色盲或色弱使用者光看柱子填了幾根就能分辨區間，不需要依賴顏色。這組圖示同時也套用在賽後紀錄的心率顯示、「個人資訊」裡的心率區間設定表。
- **電子紙高對比模式**：上方另一個大按鈕，切換成接近電子紙閱讀器的配色（暖白底、近黑粗體字），取代預設的純黑配金色文字。強烈陽光下，螢幕本身的自體發光常常會被環境光蓋過去、對比度反而變差，這個模式改用「高對比、反射閱讀」的邏輯設計，在戶外大太陽下會比預設的深色主題更容易辨識。這個選擇會記住（存在瀏覽器本機），下次開啟疲勞模式會直接套用上次的選擇。

**幾個誠實的技術限制，先說清楚比較不會在真正比賽時措手不及**：
- **語音播報是滑動當下才會唸，不是自動偵測 GPS 距離觸發的**。原本設計裡想做「手機自動偵測到接近 CP 站就自動語音提醒」，但瀏覽器分頁只要被切到背景或螢幕鎖定，JavaScript（包含定位監聽）幾乎一定會被系統暫停執行，這在 iPhone 的 Safari 尤其明顯；螢幕常亮鎖定也只能防止畫面自動變暗，沒辦法保證背景還在正常運作。與其做一個「常常沒反應」的自動功能，改成滑動當下才觸發——語音本身完全可靠，只是觸發時機是「你自己滑的當下」，不是自動。
- **震動回饋在 iPhone 上不會有反應**。iOS 上所有瀏覽器（包含 Safari、Chrome for iOS）底層都是用蘋果的 WebKit 引擎，而蘋果從來沒有在 WebKit 裡實作震動這個功能，這是蘋果的政策，不是這個 app 或哪個瀏覽器的 bug，短期內也不會改變。這個限制在 Android 手機上不存在。
- 螢幕常亮鎖定、震動、語音這三個功能，手機或瀏覽器不支援的話都會安靜地不生效，不會跳錯誤或卡住——核心的滑動記錄功能（標記抵達、標記補給）不受影響，任何情況下都能正常運作。

- **賽後紀錄與個人數據**：
  - 成績儀表板：完賽後自動彙整大字完賽時間、配速、名次、心率、平均步頻、爬升，PB 會有金色標記。心率如果有填「最大心率」，會依百分比自動套上灰／藍／綠／橘／紅的 Z1~Z5 心率區間色彩與文字標籤。有填平均步頻的話，旁邊會多一個 👋 小按鈕（v2.82.0 起），按下去手機會依那個步頻的節奏連續微震動 5 秒——不只是「看」到 185 這個數字，也能在掌心感受這個步頻的節奏；沒有支援網頁震動的裝置（例如 iOS Safari，目前完全不支援這個 API）按下去會直接說明原因，不會假裝在震動。
  - **PB 心跳提示**（v2.82.0 起）：晶片時間存檔時，如果這場賽事有填「賽事系列」，系統會比對同系列其他賽事裡最快的一次——如果這次剛好更快，支援震動的裝置會有一組心跳般的震動回饋，同時跳出提示，附一個「標記為個人最佳 (PB)」的按鈕。這裡系統只負責發現、通知，不會自動幫你勾選「個人最佳」這個欄位——那個欄位本來就是留給你自己決定的，要不要標記還是你說了算，按提示裡的按鈕才會真的勾選。沒有填賽事系列、或系列裡沒有其他已經填過時間的賽事，不會出現這個提示（沒有比較基準，沒辦法判斷「破了 PB」）。
  - **分段配速一覽**：有 2 筆以上分段紀錄時自動出現（分段資料來自匯入 GPX/FIT/TCX，這裡是唯讀的彙整檢視），距離欄位用累計公里數標示每一段（1K、2K、3K……），配速欄位用背景長條呈現相對快慢，心率欄位除了 Z1~Z5 色彩，還會搭配一組不依賴顏色的柱狀強度圖示（色盲或色弱使用者只看填了幾根柱子就能分辨區間），一眼掃過去就能看出哪段掉速、哪段心率飆高。匯入時會一併自動算出每一段的淨海拔變化（上坡是正、下坡是負），只要有這個數字，配速欄位就會顯示**坡度校正配速（GAP）**：粗體大字標「(GAP)」（把坡度影響換算回平地等效配速後的「真實體感配速」），下方小字灰色標「(實際配速)」。陡坡段即使實際配速很慢，GAP 通常會顯示接近平地的數字，長條圖的滿版程度也是以 GAP 為準，不會因為爬坡把數字拖慢就被誤判成「這段跑得比較鬆」。沒有海拔資料的分段，配速欄位就只顯示實際配速一行，不會硬湊 GAP。v2.58.0 以前匯入的賽事，只要當初有存海拔剖面，開啟更新後的版本時系統會自動從海拔剖面回頭補上分段缺漏的淨爬升資料，不用重新匯入原始檔案。滑鼠移到任一列上（v2.81.0 起），會同步移動上方海拔變化圖的游標、以及路線軌跡地圖上的標記到那一段的中間位置，離開這一列則三邊同時清除——跟直接在海拔圖上移動滑鼠是同一套聯動機制，只是換一個入口觸發，方向也是雙向的：從海拔圖移動一樣會回頭亮起對應的那一列。
    - GAP 是簡化版的估算，不是精確的生理耗能公式：上坡大約每 1% 坡度配速校正 3.75%；下坡在坡度 -6% 左右校正效果最大，比這個更陡的下坡因為煞車式的肌肉收縮耗能回升，GAP 校正效果會開始反向縮小，坡度極端時系統會自動設一個合理上下限，避免 GPS 高度誤差算出離譜的數字。
  - 前半程／後半程時間欄位：填了會自動算出正負分段百分比（負分段＝後段加速，正分段＝後段掉速）。
  - **檢討筆記引導範本**（v2.83.0 起）：「檢討筆記」這個開放式欄位還是空的時候，上方會出現「不知道從何寫起？套用 KPT 復盤法範本」的提示——按下去會把 Keep（這次做得好、下次要延續的）／Problem（哪裡出狀況、卡關的地方）／Try（下次想嘗試的調整）三段式範本填進去，游標會自動移到第一個要填空的地方，直接開始打字。已經寫過內容的話這個提示就不會出現，不用擔心誤按會蓋掉已經寫好的東西。
  - **匯入運動紀錄檔（GPX/TCX/FIT）**：上傳 Garmin / COROS / Strava 匯出的檔案，自動帶入距離、爬升、時間、心率、分段配速、海拔剖面（不會覆蓋已填的官方成績）。FIT 是常見的裝置原生二進位格式，也支援。
  - **比賽鞋款**：選擇本場穿的鞋款。「管理鞋款」可新增鞋款（可設定預期壽命，預設 600 公里，既有鞋款也能隨時點編輯圖示調整），累計每雙鞋的總里程（v2.86.0 起分成兩個來源相加：這個 App 記錄的比賽距離，加上自己填的訓練里程），名稱下方會顯示 5 格分段式健康度長條（綠→黃→紅，超過 80% 會閃爍提醒）跟對應文字狀態；有比賽紀錄的鞋款只能「退役」不能直接刪除（歷史紀錄完整保留），完全沒有紀錄的鞋款才能真的刪除；累積里程達 100% 時會出現「✅ 標記為退役」按鈕，點下去有金色印章動畫效果。退役鞋款會自動從其他賽事的鞋款選單隱藏（但原本綁定的那場賽事還是看得到），也可以隨時「恢復服役」。累積里程剛好跨過 80%／100% 門檻時，會在完賽當下跳出提醒。
    - **編輯里程與規格**（v2.86.0 起合併成一個表單）：點鞋款名稱旁的 ✎ 圖示，預期壽命、訓練里程、鞋底厚度、足跟差四個欄位一次展開、一次儲存，不用分開點兩個地方各編輯一半；鞋款一多的時候，畫面上只有名稱、里程總覽、規格摘要這幾行安靜地列著，編輯表單平常收起來不佔位置。
    - **訓練里程**：這個 App 本身只記錄比賽當天的距離，不追蹤日常訓練——如果你平常用 Strava、Garmin Connect 之類的運動錶 App 記錄訓練，可以把那邊看到的這雙鞋總里程，扣掉這個 App 已經算過的比賽里程，填進「訓練里程」這個欄位，讓鞋款的總里程更接近實際磨損程度。
    - **批次匯入訓練里程**（v2.86.0 起）：鞋款一多，一雙一雙手動填很花時間——用「全域貼上匯入」（Ctrl/Cmd+V）貼上一段 `[{"name":"鞋款名稱","trainingKm":123.4}, ...]` 格式的 JSON 陣列，會依名稱自動比對更新對應鞋款的訓練里程（比對是寬鬆比對，雙方字串誰包含誰都算，不要求打字完全一樣）；同一個關鍵字同時比對到兩雙以上的鞋，或完全找不到符合的鞋款，會直接跳過、算進「找不到對應」，不會硬套錯鞋——套用完會跳出「比對成功幾雙、幾筆找不到」的結果提示。
    - **鞋底厚度／足跟差**：新增鞋款時可以（選填）順便填上鞋底厚度、足跟差（單位 mm），已有的鞋款也能點「鞋底厚度／足跟差」旁的編輯圖示隨時補填或修改。這兩個欄位不影響鞋款健康度計算，純粹是規格記錄，主要用途是搭配「下載 Python 分析套件」裡的裝備矩陣分群分析。
    - **戰力分析**：鞋款清單裡只要那雙鞋有完賽紀錄，名稱旁就會出現「戰力分析」按鈕，點開會顯示：穿這雙鞋的 PB 率（幾場裡面破了幾次 PB）、平均配速，以及每一場穿這雙鞋跑過的賽事列表（日期、名稱、配速，PB 場次會標記）。如果其中有賽事填過「分段時間」的前半程／後半程秒數，還會多一段「衰退交叉比對」：用這雙鞋預期壽命的一半當分界，比較「還算新」跟「已經跑得比較舊」這兩組賽事的平均後半程掉速幅度（正分段代表後半程變慢），看得出鞋子累積里程增加後，掉速有沒有變得更明顯——資料不夠（例如兩組其中一組完全沒有賽事）時這段比對不會出現，只顯示 PB 率跟平均配速跟賽事列表。
  - **產生分享圖**：一鍵下載賽事成績卡片（PNG），可直接分享到社群。
  - **多媒體相片網格**：「多媒體與連結」清單下方會自動顯示一條橫向捲動的縮圖網格。連結直接指向圖片檔（.jpg/.png/.gif/.webp/.avif）時會顯示真正縮圖；其他連結（相簿首頁、社群貼文、PDF 等）因為瀏覽器無法讀取其他網站內容取得預覽圖，會顯示對應類型的圖示卡片，一樣可以點擊開啟。

## 4. 資料匯入

- **匯入 Excel**：讀取你自己的賽事規劃表（.xlsx），會列出所有工作表讓你勾選，解析後可逐筆確認/調整再匯入，不會自動覆蓋既有賽事。
- **貼上資料快速填入**：把賽事網站、簡章 PDF 或完賽證書內容請 Claude（或其他方式）整理成 JSON 後貼上，只會更新你貼的欄位，其餘資料不受影響。詳情頁上方「貼上資料快速填入」開啟。
- **全域貼上匯入**（v2.85.0 起）：不用先點開任何按鈕，只要當下沒有把游標點在任何輸入框、文字框裡，直接按 `Ctrl+V`（或 Mac 的 `Cmd+V`）就能貼上匯入——依剪貼簿內容自動判斷：
  - **截圖或圖片**：直接設成目前賽事的封面照，效果跟上傳封面照片一樣（沒開著任何賽事的話會提示先開一場賽事）。
  - **一整包 JSON 陣列**（例如請 AI 整理出好幾筆賽事）：整批匯入，不用先開著哪一場賽事；跟「匯入 JSON」不同的是，這裡不要求每一筆都要有 `id`——直接貼上的通常是全新賽事、本來就不會有 id，缺 id 的項目系統會自動補一個，不會因為缺這個欄位就整批被拒絕。
  - **單一 JSON 物件**：效果等同於上面的「貼上資料快速填入」，只是省了「先點按鈕、再貼進文字框、再按套用」這幾步，直接按貼上就套用到目前開著的賽事（一樣只更新你貼的欄位）；沒開著任何賽事的話會提示先開一場賽事。
  - 貼上一般文字（不是圖片、也不是能解析成 JSON 的內容）完全不受影響，維持瀏覽器原本的貼上行為，例如在檢討筆記裡貼一段心得，不會被這個功能攔截。
- **全域拖曳匯入**（v2.77.0 起）：把 GPX／TCX／FIT 軌跡檔、JSON 備份、JPG／PNG 照片從電腦檔案總管直接拖進網站畫面裡任何位置（不用先點開哪個按鈕），放開後會依副檔名自動分流到對應的匯入流程——效果等同於分別點「選擇檔案」，不是另一套簡化過的匯入邏輯：
  - **GPX／TCX／FIT**：正在看某場賽事的詳情頁時，拖進來會跳出跟原本「匯入運動紀錄檔」一樣的確認彈窗（顯示解析到的距離、爬升、時間等數據），確認後才套用，不會沒經過確認就覆寫既有資料；如果當下沒有開著任何賽事，會直接開始建立一場新賽事，把解析到的距離、爬升、心率等資料預先掛上去（狀態預設「已完賽」、比賽日期抓軌跡裡第一個時間戳記那天），只需要補填賽事名稱就能建立，不用自己先點「新增賽事」再回頭找這個檔案重新拖一次；檔案本身格式有問題的話會直接跳出錯誤提示，不會把你晾在一個半成品的建立畫面裡。
  - **JSON 備份**：不需要先開啟哪場賽事，直接依 `updatedAt` 跟既有賽事合併，效果跟點「匯入 JSON」一樣。
  - **照片（JPG／PNG）**：只拖一張，會設成目前賽事的封面照；一次拖好幾張，如果這場賽事已經有帶時間戳記的 GPS 軌跡（匯入過 GPX/TCX/FIT），會改成走「空間記憶迴廊」照片時空對位、一次匯入整批賽事照片；沒有 GPS 軌跡可以對位的話，只會取第一張設成封面照、其餘忽略，並跳出提示說明原因。
  - 拖曳提示層用毛玻璃效果顯示，會列出目前支援的檔案類型；拖進去的檔案格式不支援的話，放開後會跳出提示告知略過了幾個檔案，不會沒有任何交代。
  - 這個功能跟賽事詳情頁裡既有的「補給時程規劃排序」「裝備佈署看板」這兩個拖放功能是分開判斷的——瀏覽器本身能分辨「拖曳外部檔案」跟「拖曳頁面內部的項目」，兩者互不干擾，可以正常並存使用。

## 4a. 行事曆提醒（倒數通知）

網站本身無法在沒開啟的情況下主動推播通知（純靜態網站沒有背景伺服器可以排程）。退而求其次、但幾乎一樣有感的做法：匯出標準 `.ics` 日曆檔，交給手機原生的行事曆 App 處理提醒通知。

- **單場賽事**：詳情頁「加入行事曆提醒」，下載該場賽事的 `.ics` 檔。
- **全部即將到來的賽事**：左下角「匯出行事曆提醒」，一次匯出所有尚未發生的賽事（已完賽/DNS/DNF 或已過期的不會納入）。
- 每個 `.ics` 事件內建兩個提醒：**賽事倒數一個月**、**賽事倒數一週**，匯入 iOS/Android 行事曆後，會用手機原生的通知方式準時跳出來。
- 下載後在手機上打開該檔案，選擇加入「行事曆」App 即可（iOS 內建行事曆、Google 日曆都支援）。
- **直接在 Google 日曆開啟**：如果就是用 Google 日曆，詳情頁「加入行事曆提醒」旁邊的「在 Google 日曆開啟」按鈕（「更多操作」選單裡），或是賽事日期那一行正下方的同名捷徑連結，都會直接跳出 Google 日曆的新增活動頁面，賽事名稱、日期、地點（起終點名稱＋縣市）、賽事官網（如果有填）都先幫忙帶好，確認一下按儲存就好，不用先下載檔案再匯入。差別是這個方式沒辦法像 `.ics` 檔一樣內建「倒數一個月／一週」的提醒，活動建立後如果要設定提醒，需要自己在 Google 日曆裡另外加；比較看重內建提醒的話還是用上面的 `.ics` 匯出。

## 4b. 賽前作戰卡（鎖屏桌布）

賽前狀態（已報名／抽籤中／考慮中）的賽事詳情頁會出現「下載賽前作戰卡」按鈕，產生一張 9:16 直式高對比圖片，把號碼布編號、起跑分區/時間、CP 關門時間、緊急聯絡人、補給配方濃縮成一張卡片，可以直接設成手機鎖屏桌布，賽中不用解鎖手機就能快速確認關門點跟緊急聯絡資訊。

- 這些欄位都在賽事詳情頁的「基本」與相關分頁填寫：**號碼布編號**、**起跑分區 / 波次**（新增的兩個欄位，在「基本」分頁、起跑時間旁邊）、CP 明細（「路線」分頁）、隨行人員設為「緊急聯絡人」角色（「預算」分頁）、補給配方（「補給」分頁）。
- 完賽狀態的賽事不會顯示這個按鈕（賽前作戰卡只在賽前有用）。

## 4c'. 親友應援與補給指南圖

賽前狀態的賽事詳情頁「更多操作」選單裡，賽前作戰卡旁邊多一個「下載親友應援指南圖」按鈕。這張卡片跟賽前作戰卡是分開的兩張圖——賽前作戰卡是給跑者自己看的（關門時間、緊急聯絡人），這張是給在場邊等待的親友看的：號碼布、起跑時間、當天衣著描述（方便從人群裡認出你，新增的「應援指南：當天衣著描述」欄位在「基本」分頁、號碼布編號下方），以及每個 CP 站的**預計抵達時鐘時間**（例如「14:30」，不是「開賽後 6 小時 30 分」，親友不用自己換算）跟該站要遞的補給清單。
- 預計抵達時間需要「賽程」分頁的起跑時間、「目標時間」的 A 目標兩者都有填才能算出來（用勻速配速估算，不是套用 A 目標欄位裡另外寫的配速策略文字說明）；只填了 CP 距離、沒填起跑時間或目標時間的話，卡片仍然會列出 CP 站，只是不會有時鐘時間，並附上提示告訴你還缺什麼欄位。
- 每個 CP 站要遞的補給，是自動比對「補給時程規劃」（補給分頁）裡最接近那個 CP 距離的項目帶過來的，兩邊的距離數字不用填得一模一樣（例如 CP 在 10km、補給計畫填 10.2km，還是會正確配對在一起）。
- 卡片下方會註明時間僅供參考、實際到站時間依現場狀況調整，避免親友把預估時間當成保證時間。

## 4c. 賽事復盤 AI 提示詞

已完賽賽事的「更多操作」選單裡有「複製賽後檢討 AI 提示詞」，點下去會把這場賽事的天氣、心率區間（依「個人資訊」設定的儲備心率法區間換算）、分段配速範圍與後半程掉速比例、補給計畫與執行狀況、撞牆期與其他賽後反思筆記，組合成一段文字直接複製到剪貼簿，貼給 ChatGPT 或 Claude 之類的 AI 就能請它幫忙寫成 Instagram 貼文跟賽後檢討網誌兩種版本。每個區塊都是資料驅動的，那場賽事哪個欄位沒填，對應的段落就不會出現在 prompt 裡，不會出現要 AI 憑空編造內容的空欄位。瀏覽器如果不支援或拒絕自動寫入剪貼簿，會跳出一個文字框讓你手動全選複製。

- 如果這些欄位都還沒填，圖片會顯示提示文字，不會是一張空白卡片。

## 4c. 個人資訊（心率區間、身體數據、鞋款管理、補給品管理）

主畫面右上角、Google 登入區塊下方的「建立個人資訊」按鈕，打開一個面板，這裡的資料是「人」的資料，跟哪一場賽事無關，所有賽事共用同一份。v2.76.0 起從兩個頁籤擴充成四個：心率區間、身體數據、鞋款管理、補給品管理。

- **心率區間**分頁：採用儲備心率法（Karvonen Method）計算，公式是「安靜心率 +（最大心率－安靜心率）× 強度百分比」，比單純用「心率 ÷ 最大心率」的算法更準確（會把個人安靜心率的差異也納入考量）。跑步、騎車、游泳三種運動分開設定安靜心率與最大心率（因為同一個人在不同運動下的最大心率通常不一樣），填好兩個數字後 Z1~Z5 五個區間會自動算出來並顯示範圍。設定好之後，賽後紀錄裡「分段配速一覽」表格的心率欄位，會依那場賽事的運動種類自動套用對應的區間與顏色；還沒設定過個人資訊、或該運動類型還沒填心率資料時，會自動退回原本「心率 ÷ 這場賽事填的最大心率」的簡易估算方式，不會出錯或空白。
- **身體數據**分頁：年齡、性別、體重、身高、體脂肪、肌肉重，都是選填欄位；體重會用在推力比等相關計算。
- **鞋款管理**、**補給品管理**這兩個分頁，內容分別跟原本個別賽事「裝備」「補給」分頁裡的「管理鞋款」「補給品資料庫」按鈕打開的是同一份資料、同一套介面——多一個從個人資訊直接進去的入口，不用先點進某一場賽事才找得到。兩邊都在管理同一份清單，在任一邊新增、編輯、刪除，另一邊（不管是獨立彈窗還是個人資訊裡的分頁，只要當下是開著的）都會立刻同步顯示最新狀態，不是各自獨立的兩份清單。新增鞋款不用特別把滑鼠移過去點「新增」按鈕（v2.83.0 起）：在名稱、預期壽命、鞋底厚度、足跟差任何一個輸入框裡按下 Enter，就會直接送出，跟填一般表單的直覺一致。
- 所有分頁的資料都是輸入完、欄位失去焦點（或選單改變、或按下清單裡的動作按鈕）時就自動儲存，不用另外按儲存按鈕。

## 5. 雲端同步（選用）

- 右上角「使用 Google 登入同步」可讓資料透過 Firebase 在不同裝置間同步；不設定或不登入完全不影響本機使用。
- 需要自行申請 Firebase 專案並把設定值填入程式碼（`index.html` 中搜尋 `firebaseConfig`）。設定步驟、安全性規則、GitHub Secret Scanning 誤報說明等，請參考專案對話紀錄或 Firebase 官方文件。
- 同步邏輯：本機儲存永遠是主要資料來源；登入後每次存檔會額外（約 0.8 秒防抖）同步到 Firestore；首次登入若雲端已有資料，會依 `updatedAt` 自動合併（新的蓋舊的），不會憑空遺失資料。
- **同步範圍包含賽事資料，也包含鞋款管理、補給品資料庫這兩份全域清單**（v2.74.0 起）：鞋款、補給品資料庫存在同一份 Firestore 文件裡，同步邏輯跟賽事略有不同——這兩份清單裡的項目沒有像賽事那樣各自記錄 `updatedAt` 時間戳記，沒辦法判斷「雲端跟本機哪個比較新」，所以合併規則是依項目 id 取聯集（雲端獨有的項目補進本機、本機獨有的維持不動），同一個 id 兩邊都有但內容不同時保留本機版本。**v2.74.0 之前建立的鞋款／補給品資料庫，從來沒有被同步到雲端過**——這是先前換裝置登入後這兩份清單會消失的直接原因，不是同步失敗，是壓根沒有同步機制。修好之後，只要在任一台裝置上登入過一次（讓本機資料有機會推上雲端），之後換其他裝置登入就能正常帶回來；如果目前手上這台裝置的本機資料已經是最新、最完整的一份，建議先登入同步一次，確保雲端有這份資料的備份。
- 登入後每次同步完成，會檢查鞋款、補給品資料庫這兩個彈窗（包含獨立彈窗跟個人資訊裡的對應分頁）當下是不是開著的，開著就直接重新畫一次（v2.75.0 起，v2.76.0 起也涵蓋個人資訊裡的鞋款／補給品分頁）——如果在雲端資料還沒同步完成前就先點開了其中一個容器，這個機制能確保資料到齊後畫面會自動更新，不用手動關掉再打開。
- **同步診斷面板**（v2.75.0 起）：登入之後，登入資訊旁邊會多一個「同步診斷」按鈕，點開會顯示目前的 App 版本、登入帳號、本機鞋款／補給品資料庫的筆數，按「立即查詢雲端資料」還可以直接查詢 Firestore 裡實際存的筆數跟最後更新時間。這個面板是手機版沒有方便的瀏覽器主控台可用時的排查工具——不用借電腦、不用接資料線用遠端偵錯，直接在手機上就能確認「本機到底有幾筆」「雲端到底有幾筆」「雲端最後一次更新是什麼時候」，藉此判斷卡在推送、拉取還是畫面刷新哪一個環節。
- **啟動時「讀本機資料」跟「雲端合併」兩條流程已經確保先後順序**（v2.75.1 起）：這兩件事過去是各自獨立的非同步流程，沒有互相等待，在本機資料庫讀取速度較慢的裝置上（不同裝置、不同瀏覽器對 IndexedDB 的讀取速度不一樣），雲端合併有可能搶先在本機資料讀完之前執行，合併完成後又被姍姍來遲的本機讀取整個蓋掉——賽事、鞋款、補給品資料庫都有這個風險。現在雲端合併一定會先等本機資料讀取完成才開始，不會再有這個時序問題。
- 雲端同步失敗（例如網路不穩）時會自動每 15 秒重試，不需要手動處理；連續失敗 3 次以上才會在登入資訊旁顯示提示，這種情況下本機資料完全不受影響，只是其他裝置可能還看不到最新內容，等網路恢復、同步成功後提示會自動消失。
- 完全沒有網路連線時（例如山區收不到訊號）也不用擔心：本機資料本來就是優先存在瀏覽器裡，跟有沒有網路無關，離線時所有編輯一樣正常即時儲存；連線恢復後雲端同步會自動接續。

## 5a. 推播通知（選用，需要額外部署）

`.ics` 行事曆提醒（見上一節）不需要任何額外設定就能用；這一節是進一步的「真正推播」——不用打開網站、不用先加進行事曆，賽事倒數一個月／一週會直接推送到手機。這個功能需要一個會在背景定期執行的伺服器元件，不是單純把網站原始碼放到 GitHub Pages 就會動，需要額外部署：

**你需要做的事（我沒辦法代勞的部分）**：

1. Firebase Console → 專案設定 → 用量與帳單 → **升級到 Blaze 方案**（用量計費制，需要綁信用卡；這個功能的用量遠低於免費額度，正常情況下不會產生費用）
2. Firebase Console → 專案設定 → **Cloud Messaging** 分頁 → Web Push 憑證 → 產生金鑰組，複製那組公開金鑰
3. 打開 `index.html`，搜尋 `YOUR_VAPID_KEY`，換成剛剛複製的金鑰
4. Firestore → 規則，貼上 [`firestore.rules`](./firestore.rules) 的內容（多了 `fcmTokens` 的存取規則）並發布
5. 把 [`firebase-messaging-sw.js`](./firebase-messaging-sw.js) 上傳到 repo 根目錄，跟 `index.html`同一層（缺這個檔案，瀏覽器背景收不到通知）
6. 本機安裝 [Node.js](https://nodejs.org)（若還沒裝）與 Firebase CLI：`npm install -g firebase-tools`
7. 在電腦上開一個資料夾，放入 [`functions/index.js`](./functions/index.js) 與 [`functions/package.json`](./functions/package.json)，於該資料夾執行：
   ```
   firebase login
   firebase init functions   # 選擇你的 Firebase 專案、語言選 JavaScript，不要覆蓋掉我提供的檔案
   firebase deploy --only functions
   ```
8. 全部完成、`index.html` 也重新上傳後，回到網站用 Google 登入，右上角會出現「啟用推播通知」，點下去允許瀏覽器的通知權限請求即完成

**運作方式**：Cloud Function 每天固定時間（台灣時間早上 9:00）執行一次，檢查所有使用者的賽事，找出「距離今天剛好 30 天」或「剛好 7 天」的賽事，推播給該賽事擁有者已註冊的裝置；失效的裝置權杖（例如你解除了通知授權）會自動被清除。

## 5b. 功能使用次數統計（選用，需要 Firebase）

記錄「哪個功能被點了幾次」，用來之後分析哪些功能最常用、哪些最少用。跟賽事資料完全分開，存在 Firestore 一份共用的統計文件裡。

- **需要 Firestore 規則更新**：這個功能新增了一條規則（`analytics/featureCounts` 這個文件），需要把 [`firestore.rules`](./firestore.rules) 目前的完整內容重新貼到 Firebase Console → Firestore → 規則，並按「發布」，才會生效；沒發布規則的話，寫入次數的動作會被拒絕（不影響其他功能，只是次數不會被記錄下來）。
- **不需要登入也會記錄**：故意把寫入權限開放給所有訪客（含未登入的人），這樣才能統計到所有人的使用狀況，不只是有登入 Google 帳號的人；但讀取這份統計資料需要登入（也就是只有你自己能看到完整數字），一般訪客沒辦法直接讀出你的使用量統計。
- **怎麼查看數字**：目前還沒有在網站裡做一個統計儀表板頁面，要看數字的話，去 Firebase Console → Firestore Database → `analytics` 集合 → `featureCounts` 文件，裡面每個欄位就是一個功能代號跟對應的累積點擊次數。
- **記錄方式**：點擊當下不會馬上送出網路請求，而是先在瀏覽器裡累加，大約每 8 秒或分頁切到背景/關閉時才整批送出一次，避免使用者連續操作時對 Firestore 發送過多次寫入請求。
- **第一批涵蓋的功能**（後續會視需要擴充到更多功能）：新增賽事、複製賽事、貼上資料快速填入、切換獎牌牆／表格檢視、Excel／GPX／JSON 備份還原三種匯入、CSV／JSON 備份、單場／全部賽事的 `.ics` 行事曆匯出、在 Google 日曆開啟、產生分享圖、下載賽前作戰卡、下載徽章成就圖、開啟配速試算工具／管理鞋款／個人資訊面板、Google 登入（僅計算實際完成登入的次數，不含頁面重新整理時偵測到已登入狀態的情況）、抓取即時天氣預報。

## 6. 深色模式

- 右上角月亮/太陽圖示切換；預設跟隨系統設定，切換後會記住選擇（存在瀏覽器 `localStorage`，跟賽事資料分開儲存）。

## 7. 資料備份、刪除與復原

- 「匯出 JSON」：把目前所有賽事資料下載成一份 JSON 檔，格式對應 `race-document.schema.json`，可作備份或搬到其他地方。
- 「匯出 CSV（扁平化）」：給要接 Python／SQL／Excel VBA 做自動化的人用。一次下載 11 份 CSV：`races.csv` 主表（純量欄位攤平，例如 `route_distanceKm`、`location_country`），加上 `checkpoints.csv`／`splits.csv`／`equipmentChecklist.csv`／`goals.csv`／`nutritionSchedule.csv`／`accommodations.csv`／`transportation.csv`／`companions.csv`／`mediaLinks.csv` 九份子表，各自帶 `race_id`／`race_name` 欄位可以對回主表，方便 SQL JOIN 或 pandas merge；另外還有一份 `shoes.csv`（鞋款管理裡的全部鞋款，含鞋底厚度、足跟差），這份是全域資料、不用 race_id 對應。已刪除的賽事不會出現在匯出結果；GPS 座標與海拔剖面點位資料量大，不列入匯出（需要完整資料用 JSON 匯出）。欄位名稱固定英文，不會因介面語言切換而變動；CSV 有加 UTF-8 BOM，Excel 開啟中文/日文不會亂碼。瀏覽器可能會跳出「這個網站正在嘗試下載多個檔案」的允許提示，按允許即可。
- 「下載 Python 分析套件（.ipynb）」：緊接在「匯出 CSV」下面，一鍵下載一份可以直接在 Jupyter Notebook 打開執行的 `race-data-sandbox.ipynb`，跟上面的 CSV 是搭配使用的——先匯出 CSV，把 `races.csv`／`shoes.csv` 跟這份 .ipynb 放在同一個資料夾，由上到下依序執行每一格即可。裡面預先寫好三段分析範本：① 用 K-Means 把鞋款規格（鞋底厚度、足跟差）跟平均步頻分群的 3D 散佈圖（要先在「鞋款管理」補上鞋底厚度／足跟差才有資料可分析）；② 只看路跑賽事、依體感溫度與濕度分桶，算出相對同距離 PB 掉了幾 % 配速的互動式熱力圖；③ 減量天數、週跑量目標、完賽時間三者的皮爾森相關係數矩陣，順便列出破 PB 賽事平均提前幾天開始減量。三段範本都會先檢查資料量夠不夠，資料還太少時只會印出中文提示告訴你該去哪個分頁補資料，不會直接噴 Python 錯誤訊息卡住；圖表裡的中文標籤已經處理過字型相容性，Windows／macOS／Linux 只要系統裡有常見中文字型就能正常顯示，不會變成缺字方框。
- 「匯入 JSON」：把之前匯出的 JSON 檔讀回來，可用來還原備份或搬移資料到新裝置。如果匯入的賽事跟現有資料衝突（同一筆賽事兩邊版本不同），會自動套用較新的版本，較舊的版本保留在「資料復原」的同步覆蓋紀錄，不會直接遺失。
- 「清除全部資料」：需連按兩次確認；若有登入雲端同步，雲端資料也會一併清空，這個動作沒有復原機制，請務必先匯出備份。
- **刪除單一賽事**：詳情頁「刪除賽事」按一次就會刪除（v2.88.0 起不用再連按兩次確認），但不會真的馬上消失——會進入「垃圾桶」保留 30 天，期間可以還原，超過 30 天才會自動永久清空。刪除的當下畫面右下角會跳出一則「已刪除：{賽事名稱}」的提示，附一個「復原」按鈕，5 秒內點擊可以立刻救回來，不用特地跑去開下面說的「資料復原」畫面；5 秒一到提示會自動消失，但賽事本身還是安全留在垃圾桶裡，30 天內都能從「資料復原」找回來，不是提示消失資料就真的不見了。短時間內連續刪好幾筆的話，提示不會被新的一筆蓋掉逼你放棄前面的反悔機會——同一個 5 秒視窗內刪除的全部賽事都會被同一則提示追蹤（文字會變成「已刪除 N 場賽事」），按一次「復原」會把這個視窗內刪除的全部救回來。手機清單向左滑動刪除（見上面「滑動手勢」段落）也是同一套邏輯：滑到底立刻刪除，不用再多一次「再次點擊以確認」。
- **資料復原**：左下角「已刪除賽事」（有東西可復原時會顯示筆數）可以：
  - 還原或永久刪除已刪除的賽事。
  - 查看「同步覆蓋紀錄」：如果你在離線裝置上改了某場賽事，回到線上同步時，若雲端也有更新的版本，系統會自動套用較新的一份，但**不會直接丟掉舊的**——舊版本會保留在這裡（最多 20 筆），同步當下也會跳出提示告訴你發生了什麼、可以點擊查看。還原某個版本時，目前的版本也會自動存進紀錄，不用擔心還原後又蓋掉別的資料。
- **本機儲存機制**：資料儲存在瀏覽器的 IndexedDB（透過開源套件 idb-keyval，經由 CDN 載入），額度通常是數百 MB 到數 GB（視裝置可用空間而定），資料完全留在你自己的裝置上，不需要任何外部伺服器或費用。如果瀏覽器連不到 CDN（網路問題、被封鎖等），會自動退回舊版用的 `localStorage`（額度較小，尤其 Safari 大約只有 5MB），確保儲存功能不會因為外部套件載入失敗就整個壞掉。第一次用新版開啟網站時，如果偵測到舊版遺留在 `localStorage` 裡的資料，會自動搬移到 IndexedDB 並清掉舊的，只搬一次，不會每次開啟都重複執行；如果真的存檔失敗，畫面上方會跳出紅色提示並持續顯示，直到下一次存檔成功為止——這種情況請立刻匯出 JSON 備份。

## 8. 手機使用與加入主畫面

- 用手機瀏覽器直接開啟網站（不是透過下載預覽），選「加入主畫面」，iOS／Android 都會抓到自訂的 App 圖示（路線圖樣，呼應運動主題）。
- 加入主畫面後畫面比例、深色模式、月曆清單檢視都跟一般網頁瀏覽相同。
- 手機版右下角有一個「+」懸浮按鈕（在使用說明小人上方）可以快速新增賽事，不用捲到頁面最上方。

## 9. 疑難排解

- **登入按鈕點下去視窗一閃就消失**：多半是 Firebase 「已授權網域」沒有加上你的網站網域，到 Firebase Console → Authentication → 設定 → Authorized domains 新增。現在登入失敗時按鈕旁邊會直接顯示具體錯誤原因。
- **GitHub 顯示 Secret scanning alert，說偵測到 API Key**：這是正常現象，Firebase 的 `apiKey` 設計上就是可以公開的，真正的存取控制來自 Firestore 安全性規則，可以放心把這則警示標記為已處理。
- **手機排版跑掉**：如果你自訂了很長的賽事名稱或標題，理論上都已做過防護（欄寬固定、自動截斷），若仍發現跑版歡迎回報實際畫面。
- **舊資料遺失欄位**：程式會在讀取資料時自動幫舊格式補上新欄位預設值，正常情況下不會因為版本升級而出錯或消失資料。

# 賽事紀錄 App — 開源資料（公開資料）來源清單

整理日期：2026-09-18
本文件盤點「賽事紀錄」App 實際在執行時，會向外部發出請求、取用的公開資料來源。全部都是**免費、免金鑰（不需要申請 API Key）**的服務，符合這個 App「純前端、無自架後端資料庫」的架構限制。

---

## 1. Open-Meteo（氣象與海象資料）

Open-Meteo 是一個免費、免金鑰的開源氣象 API 服務，本身不生產原始氣象資料，而是整合各國官方氣象機構（例如德國氣象局 DWD、美國 NOAA、歐洲中期天氣預報中心 ECMWF、法國 Météo-France 等）發布的公開模式資料，重新包裝成統一格式的 API。App 裡總共呼叫了三個不同的 Open-Meteo 端點，分別對應不同用途：

### 1a. 即時天氣預報端點
- **網址**：`https://api.open-meteo.com/v1/forecast`
- **用途**：賽事開賽前 7 天內，抓取當地逐時氣溫、降雨機率、陣風風速的即時預報，顯示在「備戰釘選」卡片上
- **取用欄位**：`temperature_2m`、`precipitation_probability`、`wind_gusts_10m`

### 1b. 歷史氣象回溯資料庫端點
- **網址**：`https://archive-api.open-meteo.com/v1/archive`
- **用途**：賽事日期已經過去、且已匯入 GPS 軌跡時，自動在背景查詢當天實際發生過的天氣（不是預測值，是回算的實測／再分析資料），回填「實際體感溫度」「當日濕度」等欄位；另外也用同一個端點查詢「歷年同日平均氣溫濕度」（往前五年）
- **取用欄位**：`temperature_2m`、`apparent_temperature`、`relative_humidity_2m`、`precipitation`、`wind_gusts_10m`、`wind_speed_10m`、`wind_direction_10m`

### 1c. 海洋氣象端點
- **網址**：`https://marine-api.open-meteo.com/v1/marine`
- **用途**：三鐵／游泳賽事專用，查詢賽事當天的浪高、湧浪週期、海水溫度，用於「海象紀錄」區塊與高失溫風險提示
- **取用欄位**：`wave_height`、`swell_wave_period`、`sea_surface_temperature`
- **已知限制**：海洋氣象模式只在實際海面上的座標網格才有資料，若軌跡起點座標落在陸地網格上會查無資料（模式本身的限制，不是查詢設定錯誤）

**授權／使用條款**：Open-Meteo 對非商業用途免費、免金鑰（App 目前的用法完全符合此條件）；商業用途需要付費方案。完整條款見 `https://open-meteo.com/en/license`。

---

## 2. OpenStreetMap（開放街圖，地圖圖磚資料）

- **網址**：`https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`（標準底圖圖磚）
- **用途**：路線軌跡互動地圖的其中一種底圖選項（標準地圖模式），透過 Leaflet 套件呼叫
- **資料性質**：由全球志工協作維護的開放地理資料庫，涵蓋道路、地形、地標等地圖要素
- **授權**：**ODbL（Open Database License）**，這是開放資料授權，允許自由使用、修改、散布，前提是要標示出處（OpenStreetMap 貢獻者）並讓衍生資料庫維持同樣開放的授權條件

---

## 3. OpenTopoMap（開放地形圖，地圖圖磚資料）

- **網址**：`https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png`（地形底圖圖磚）
- **用途**：路線軌跡互動地圖的另一種底圖選項（地形模式，顯示等高線，適合越野跑／登山類賽事查看地形），同樣透過 Leaflet 套件呼叫
- **資料性質**：OpenTopoMap 本身不是原始資料來源，而是把兩份資料疊加重繪成地形圖樣式——**地圖資料**來自 OpenStreetMap（見上一項），**地形等高線／地貌暈渲**來自 NASA 的 **SRTM**（Shuttle Radar Topography Mission，2000 年太空梭雷達地形測繪任務所產生的全球數位高程模型）
- **授權**：**CC-BY-SA**（創用CC 姓名標示-相同方式分享），使用時需要同時標示「OpenStreetMap 貢獻者（ODbL）」與「OpenTopoMap（CC-BY-SA）」兩個來源

---

## 小結：完整外部請求清單

| 資料來源 | 端點／網域 | 資料類型 | 授權 | 是否需要金鑰 |
|---|---|---|---|---|
| Open-Meteo | api.open-meteo.com | 即時天氣預報 | 非商業免費 | 否 |
| Open-Meteo | archive-api.open-meteo.com | 歷史氣象回溯 | 非商業免費 | 否 |
| Open-Meteo | marine-api.open-meteo.com | 海象（浪高／水溫） | 非商業免費 | 否 |
| OpenStreetMap | tile.openstreetmap.org | 地圖圖磚 | ODbL | 否 |
| OpenTopoMap | tile.opentopomap.org | 地形圖磚（底層含 NASA SRTM 高程資料） | CC-BY-SA | 否 |

以上五個都是本專案唯一會在使用者瀏覽器裡即時發出請求的外部公開資料服務；其餘的資料（賽事本身的紀錄、裝備清單等）全部儲存在使用者自己的瀏覽器本機（IndexedDB／localStorage），或選用性質的 Firebase Firestore 雲端同步（屬於第二份文件「軟體與套件清單」裡記錄的服務，不是公開資料）。
